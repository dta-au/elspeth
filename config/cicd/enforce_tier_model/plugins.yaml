per_file_rules:
- pattern: plugins/sources/*
  rules:
  - R5
  reason: Type discrimination for schema inference and config parsing at Tier 3 boundary
    (includes get_post_call_hints isinstance checks for JSON type validation)
  expires: null
  max_hits: 7
- pattern: plugins/infrastructure/config_base.py
  rules:
  - R5
  reason: Type discrimination for config field validation and coercion
  expires: null
  max_hits: 7
- pattern: plugins/transforms/llm/*
  rules:
  - R5
  reason: Type discrimination for LLM response validation and provider dispatch (includes
    Pydantic model_validator(mode="before") isinstance(data, dict) Tier-3 input checks
    on AzureOpenAIConfig and AzureBatchConfig)
  expires: null
  max_hits: 61
- pattern: plugins/transforms/azure/*
  rules:
  - R1
  - R2
  - R4
  - R6
  - R9
  reason: Azure API responses are external data (Tier 3) - all defensive patterns
    legitimate
  expires: '2026-10-06'
- pattern: plugins/transforms/azure/*
  rules:
  - R5
  reason: Type discrimination for Azure API response validation
  expires: null
  max_hits: 14
- pattern: plugins/infrastructure/clients/dataverse.py
  rules:
  - R1
  - R5
  - R6
  reason: Dataverse OData API responses are external data (Tier 3) — isinstance/type
    checks for response validation, error classification, and retry-after parsing
  expires: '2026-10-06'
- pattern: plugins/infrastructure/clients/fingerprinting.py
  rules:
  - R1
  - R6
  reason: HMAC header fingerprinting — env var access and try/except for fingerprint
    key availability check
  expires: null
- pattern: plugins/infrastructure/clients/json_utils.py
  rules:
  - R5
  reason: Tier 3 boundary — isinstance checks for recursive NaN/Infinity detection
    in parsed JSON
  expires: null
- pattern: plugins/sinks/dataverse.py
  rules:
  - R4
  - R5
  reason: R5 offensive guard for alternate key type; R4 broad except in telemetry
    emission (must not crash audit trail — same pattern as AuditedHTTPClient._record_and_emit)
  expires: '2026-10-06'
  max_hits: 1
- pattern: plugins/sinks/chroma_sink.py
  rules:
  - R4
  reason: R4 broad except in on_complete() post-audit telemetry emission — best-effort
    operational visibility after writes+audit; must not fail completion (Tier-1 errors
    re-raise first). Same pattern as plugins/sinks/dataverse.py. (elspeth-ee69831e4c)
  expires: null
  max_hits: 1
- pattern: plugins/infrastructure/clients/llm.py
  rules:
  - R5
  reason: Deep-freeze guards, programming error detection in telemetry, and Tier 3
    content type validation
  expires: null
  max_hits: 3
- pattern: plugins/infrastructure/clients/replayer.py
  rules:
  - R5
  reason: Deep-freeze guards and Tier 1 deep_thaw type validation for recorded call
    data
  expires: null
  max_hits: 1
- pattern: plugins/infrastructure/schema_factory.py
  rules:
  - R5
  reason: Numpy floating type detection for schema inference and ndarray non-finite
    scanning
  expires: null
  max_hits: 5
- pattern: plugins/infrastructure/utils.py
  rules:
  - R5
  reason: Type discrimination for nested field path traversal
  expires: null
  max_hits: 1
- pattern: plugins/sinks/json_sink.py
  rules:
  - R5
  reason: Tier 3 JSONL file content validation
  expires: null
  max_hits: 1
- pattern: plugins/transforms/batch_stats.py
  rules:
  - R5
  reason: Float type discrimination for NaN/Infinity guard
  expires: null
  max_hits: 2
- pattern: plugins/transforms/json_explode.py
  rules:
  - R5
  reason: Contract enforcement — array field must be list
  expires: null
  max_hits: 1
- pattern: plugins/transforms/keyword_filter.py
  rules:
  - R5
  reason: Contract enforcement — non-string field rejection
  expires: null
  max_hits: 1
- pattern: plugins/transforms/safety_utils.py
  rules:
  - R5
  reason: Config discrimination and field type selection for scanning
  expires: null
  max_hits: 4
- pattern: plugins/transforms/type_coerce.py
  rules:
  - R1
  reason: Optional per-field coercion mapping — conversion_targets / target_types
    map only fields the operator explicitly requested coercion for. dict.get(field.name,
    field.field_type) with the original field type as default is identity semantics
    (unchanged fields pass through) and dict.get() on conversion_targets with explicit
    None check distinguishes configured vs. unconfigured fields — this IS the discriminator
    that controls whether type coercion runs. Neither .get() call is defensive bug-hiding.
  expires: null
  max_hits: 2
allow_hits:
- key: plugins/sources/csv_source.py:R6:CSVSource:load:fp=c55a1dfdb0de7249
  owner: john
  reason: |-
    In CSVSource.load (csv_source.py:202), the except UnicodeDecodeError catches a decode failure at open() time (BOM/header bytes for codecs like utf-16). This is a Tier-3 source boundary: the bytes are external file content whose contract we did not author, so the policy mandates quarantine, not crash. The handler records the failure via ctx.record_validation_error(schema_mode='parse'), then yields SourceRow.quarantined unless on_validation_failure=='discard', and returns. The catch is narrow (UnicodeDecodeError only), the absence is recorded as an audit fact with the file path and encoding rather than fabricated, and processing of the pipeline is preserved. Not silent data loss. The existing rationale matches the current code.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/csv_source.py:R6:CSVSource:load:fp=e7ee3490967ac0e5
  owner: john
  reason: |-
    In CSVSource.load (csv_source.py:223), the except UnicodeDecodeError wraps `yield from self._load_from_file(f, ctx)` to catch decode failures that surface during row iteration (e.g. utf-16/utf-32 truncation mid-stream). External file bytes are Tier 3; per the source boundary policy this is quarantined, not crashed. The handler records a parse-level validation error and yields SourceRow.quarantined unless discard mode, deliberately not fabricating a line number (a comment explains file-level decode errors precede CSV parsing so no meaningful lineno exists). The catch is narrow and processing stops only for this corrupt stream, with the failure recorded as an audit fact. Matches the existing rationale.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=d3cf5ffa356a45ab
  owner: john
  reason: |-
    In CSVSource._load_from_file (csv_source.py:303), the except csv.Error catches a parser error raised by csv.reader (strict=True) during the skip_rows preamble on external CSV input. This is a Tier-3 source boundary. The handler records a validation error via ctx.record_validation_error, yields SourceRow.quarantined when configured, and returns to halt rather than continue on corrupted parser state (an unmatched quote can have consumed subsequent lines) — explicitly avoiding silent data loss. The catch is narrow (csv.Error), the failure is recorded with the physical line number, and quarantine/audit are intact. Matches the source-tier validate-record-quarantine pattern in the existing rationale.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/json_source.py:R6:JSONSource:_load_jsonl:fp=1c28edea7d8baa1d
  owner: john
  reason: |-
    In JSONSource._load_jsonl (json_source.py:275), the except (json.JSONDecodeError, ValueError) catches json.loads failures on a single external JSONL line (Tier 3). The ValueError arm is specifically parse_constant=_reject_nonfinite_constant rejecting NaN/Infinity per the canonical-JSON policy — part of the same parse operation, not a generic swallow. The handler stores the raw line plus line number and routes through _record_parse_error (records validation error, yields quarantined row), then continues to the next line. Narrow catch, audited quarantine, no fabrication. Matches the existing rationale.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/transforms/rag/query.py:R5:QueryBuilder:_build_field_only:fp=8690adb65aebc738
  owner: john
  reason: |-
    In QueryBuilder._build_field_only (query.py:129), `if not isinstance(extracted, str): raise TypeError(...)` is an offensive guard, not a defensive shape filter. The `extracted` value is typed Any because row_data is dict[str, Any] and observed-mode schemas do not enforce value types; without the guard a bytes value would silently pass _validate_non_empty and corrupt the audit trail with a wrong-typed query. The code matches its inline comment exactly: it refuses to coerce and crashes loudly with a diagnostic naming the upstream plugin bug, which is the prescribed response for 'plugin returns wrong type' on Tier-2 data. R5 normally flags isinstance as defensive, but here it raises rather than swallows — the encouraged offensive form.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-08-31'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-06-02T04:52:40.846384+00:00'
  judge_model: 'claude-opus-4-7[1m]'
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.88
  judge_rationale: |-
    PRESCRIBED FORM. R5 fires on isinstance, but the code is the encouraged offensive form, not defensive suppression: `if not isinstance(extracted, str): raise TypeError(...)` with a diagnostic message naming the upstream plugin bug (query.py:129-133). The seam is genuinely shape-unguaranteed — row_data is dict[str, Any] and the rationale plainly states observed-mode schemas do not enforce value types, which the visible code does not contradict (the comment at query.py:121-128 corroborates and adds the concrete failure mode: bytes would silently pass _validate_non_empty since bytes.strip() and bool(b'x') both succeed, producing QueryResult(query=b'...') and corrupting the audit trail with a wrong-typed query). This is the Plugin-Ownership prescribed response for 'plugin returns wrong type' on Tier-2 data: raise a typed exception, do not coerce. The decorator nudge does not apply: _build_field_only takes Tier-2 pipeline data (row_data lineage), not a Tier-3 external parameter, and the suppression scope is one offensive guard, not a boundary function.
  judge_signature_version: 2
  scope_fingerprint: 6e32dd031ef9283b3598916508dc81686e4b18392e91332cd68cb45c78483f43
  judge_transport: claude_agent_sdk
  ast_path: 'body[15]/body[3]/body[0]/test/operand'
  judge_metadata_signature: 'hmac-sha256:v2:c427b7d47edd6afd539f2354991d9f6aad0ce80b8f377c4adaa2360774321021'
- key: plugins/sources/text_source.py:R6:TextSource:_validate_and_yield:fp=924793ebd8590642
  owner: john
  reason: |-
    In TextSource._validate_and_yield (text_source.py:254), `except ValidationError as exc` is a Tier-3 source boundary: external row data is validated via Pydantic model_validate, and ValidationError means the external input violated the configured schema. The narrow catch (not broad Exception) routes to ctx.record_validation_error and, unless on_validation_failure is discard, yields a quarantined SourceRow — exactly the validate-at-boundary, record-and-quarantine pattern for Tier-3 data. Not silent suppression; the failure is recorded and the row quarantined. Existing rationale accurate.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=2bcbd199162d4907
  owner: john
  reason: |-
    In CSVSource._load_from_file (csv_source.py:415), `except ExternalHeaderError as e` catches a Tier-3 fault in the source's own header bytes (normalization collision, empty/duplicate headers). The handler records via ctx.record_validation_error and, unless on_validation_failure is discard, yields a quarantined SourceRow built from the raw header line — the validate-at-boundary, record-and-quarantine pattern for malformed external input (a header collision means no rows are parseable). The catch is narrow to ExternalHeaderError; config faults like a bad field_mapping raise plain ValueError and crash as our bug. No silent suppression — the failure is recorded and quarantined.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=18e3aa5a0339497f
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. In CSVSource._load_from_file (csv_source.py:358), `except csv.Error as e` catches a parser error raised by csv.reader (constructed with strict=True at line 269) while reading the HEADER row via next_nonblank_record() -> next(reader). csv.Error is raised EXCLUSIVELY by the stdlib csv C-parser on malformed external CSV bytes (unmatched quote, bad quoting); there is no config or our-code path that produces it, so the catch is exhaustively external-origin. This is the canonical Tier-3 source boundary: the file bytes are external content whose contract we did not author. It is not a silent swallow: the handler builds a raw_row carrying audit metadata (file path, physical line number from reader.line_num, a raw_line marker) and calls ctx.record_validation_error(schema_mode='parse') UNCONDITIONALLY (writing a verr_* record into the Landscape audit trail), then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == 'discard' (explicit operator routing that suppresses only the quarantine yield, never the audit record), and then RETURNS — deliberately halting rather than continuing on a corrupted parser state (an unmatched quote can have consumed subsequent lines), explicitly avoiding silent data loss. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (its contract requires a raising-rejection test_ref); the per-line allowlist is the prescribed honest mechanism. Mirrors the already-signed skip_rows csv.Error handler at line 303.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=894a8230ab37856a
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 schema-validation quarantine, recorded not swallowed, type-narrow catch. In CSVSource._load_from_file (csv_source.py:567), `except ValidationError as e` catches the pydantic ValidationError raised by self._schema_class.model_validate(row) — the source-boundary schema validation of an external CSV row whose coerced values may be invalid (the canonical Tier-3 trust boundary: external data may not conform to the declared/inferred schema). It is not a silent swallow: it calls ctx.record_validation_error(schema_mode=self._schema_config.mode) UNCONDITIONALLY (writing a verr_* record into the Landscape audit trail), then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == 'discard'. CRITICAL DISCRIMINATION: the catch is type-narrow to pydantic ValidationError, NOT the broad ValueError. The enclosing try also wraps to_row(), ContractBuilder.process_first_row(), require_schema_contract(), and contract.validate(), but none of those raise pydantic ValidationError: to_row() is model_dump() (serialization, never validates); process_first_row() raises plain ValueError on a field_resolution collision (an explicit 'source plugin bug', i.e. ours) or on a NaN/Infinity value (Tier-1 audit-integrity rejection via normalize_type_for_contract); require_schema_contract() raises an assertion; contract.validate() RETURNS a list of violations rather than raising; and the two SourceRow constructors evaluated inside the try (SourceRow.quarantined at line 556, SourceRow.valid at line 563) are frozen-dataclass (@dataclass(frozen=True, slots=True)) constructors, not pydantic models, whose __post_init__ raises only plain ValueError on an invariant breach. A plain ValueError or AssertionError is not a ValidationError (catching the child never catches the parent, and the dataclass ValueError is an unrelated type to pydantic ValidationError), so all of these our-code / Tier-1 faults PROPAGATE and crash — exactly correct, and exactly the data-vs-config discrimination the sibling azure_blob_source path had to be FIXED to achieve (its broad except ValueError was narrowed); the CSV path gets it for free via the type-narrow catch. Verified empirically: the schema built with allow_coercion=True rejects non-finite float cells ('NaN'/'Infinity'/'inf') as ValidationError at model_validate, so a NaN cell is quarantined HERE and never reaches process_first_row — there is no crash-on-first-row path. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit handling. The method is a generator (yields rows, never raises), so @trust_boundary is infeasible (raising-test contract); the per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-08-31'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=3309035ca05ac6c6
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_csv catch narrow, external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, csv.Error on header parse and on per-row parse, and ExternalHeaderError (the field-normalization data-fault marker) on header resolution. None is a silent swallow: each builds a raw_row carrying audit metadata (raw line/preview, encoding, line/row number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — which writes a verr_* record into the Landscape audit trail (data_flow_repository.record_validation_error) — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine-routing yield, never the audit record). This is the exact CLAUDE.md source doctrine: a CSV with garbage in one row records the problem, quarantines that row/file, and keeps processing the rest. The caught types are narrow (never bare Exception); config/our-code faults are deliberately NOT caught here — the header path catches only ExternalHeaderError and lets a plain ValueError from a bad field_mapping propagate and crash (it is ours), per the inline comment. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (its contract requires a raising-rejection test_ref); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=2a6335700d1c7a23
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_csv catch narrow, external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, csv.Error on header parse and on per-row parse, and ExternalHeaderError (the field-normalization data-fault marker) on header resolution. None is a silent swallow: each builds a raw_row carrying audit metadata (raw line/preview, encoding, line/row number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — which writes a verr_* record into the Landscape audit trail (data_flow_repository.record_validation_error) — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine-routing yield, never the audit record). This is the exact CLAUDE.md source doctrine: a CSV with garbage in one row records the problem, quarantines that row/file, and keeps processing the rest. The caught types are narrow (never bare Exception); config/our-code faults are deliberately NOT caught here — the header path catches only ExternalHeaderError and lets a plain ValueError from a bad field_mapping propagate and crash (it is ours), per the inline comment. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (its contract requires a raising-rejection test_ref); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=be29fd3a24c7cb14
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_csv catch narrow, external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, csv.Error on header parse and on per-row parse, and ExternalHeaderError (the field-normalization data-fault marker) on header resolution. None is a silent swallow: each builds a raw_row carrying audit metadata (raw line/preview, encoding, line/row number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — which writes a verr_* record into the Landscape audit trail (data_flow_repository.record_validation_error) — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine-routing yield, never the audit record). This is the exact CLAUDE.md source doctrine: a CSV with garbage in one row records the problem, quarantines that row/file, and keeps processing the rest. The caught types are narrow (never bare Exception); config/our-code faults are deliberately NOT caught here — the header path catches only ExternalHeaderError and lets a plain ValueError from a bad field_mapping propagate and crash (it is ours), per the inline comment. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (its contract requires a raising-rejection test_ref); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_csv:fp=b19fe22f979c966f
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_csv catch narrow, external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, csv.Error on header parse and on per-row parse, and ExternalHeaderError (the field-normalization data-fault marker) on header resolution. None is a silent swallow: each builds a raw_row carrying audit metadata (raw line/preview, encoding, line/row number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — which writes a verr_* record into the Landscape audit trail (data_flow_repository.record_validation_error) — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine-routing yield, never the audit record). This is the exact CLAUDE.md source doctrine: a CSV with garbage in one row records the problem, quarantines that row/file, and keeps processing the rest. The caught types are narrow (never bare Exception); config/our-code faults are deliberately NOT caught here — the header path catches only ExternalHeaderError and lets a plain ValueError from a bad field_mapping propagate and crash (it is ours), per the inline comment. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (its contract requires a raising-rejection test_ref); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_jsonl:fp=bec09706ddbb5264
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_jsonl catch narrow external-origin parse faults on raw blob bytes: (json.JSONDecodeError, ValueError) on the per-line json.loads (the ValueError arm is bounded to _reject_nonfinite_constant rejecting NaN/Infinity literals — a data fault), and UnicodeDecodeError raised by the streaming TextIOWrapper on a truncated byte sequence. Neither is a silent swallow: each builds a raw_row carrying audit metadata (raw line or raw bytes hex, line number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — writing a verr_* record into the Landscape audit trail — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (explicit operator routing; suppresses only the quarantine yield, never the audit record). Per-line JSONL parse handling continues to the next line on a bad line (a line-level quarantine); the file-level UnicodeDecodeError records and returns. This is the CLAUDE.md source doctrine: a malformed JSONL line is recorded and quarantined, the rest of the file keeps processing. The caught types are narrow (never bare Exception). R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so @trust_boundary is infeasible (raising-test contract); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_jsonl:fp=354195a41d9e3bca
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_jsonl catch narrow external-origin parse faults on raw blob bytes: (json.JSONDecodeError, ValueError) on the per-line json.loads (the ValueError arm is bounded to _reject_nonfinite_constant rejecting NaN/Infinity literals — a data fault), and UnicodeDecodeError raised by the streaming TextIOWrapper on a truncated byte sequence. Neither is a silent swallow: each builds a raw_row carrying audit metadata (raw line or raw bytes hex, line number) and calls ctx.record_validation_error(...) UNCONDITIONALLY — writing a verr_* record into the Landscape audit trail — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (explicit operator routing; suppresses only the quarantine yield, never the audit record). Per-line JSONL parse handling continues to the next line on a bad line (a line-level quarantine); the file-level UnicodeDecodeError records and returns. This is the CLAUDE.md source doctrine: a malformed JSONL line is recorded and quarantined, the rest of the file keeps processing. The caught types are narrow (never bare Exception). R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so @trust_boundary is infeasible (raising-test contract); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-02'
- key: plugins/sources/dataverse.py:R6:DataverseSource:load:fp=6783ed5388156ba6
  owner: john
  reason: |-
    In DataverseSource.load (dataverse.py:596), the except ValueError wraps self._strip_odata_metadata(raw_row) where raw_row is an external Dataverse API row (Tier 3). A formatted-value/field collision raises ValueError; the handler increments quarantine_count, records via ctx.record_validation_error(schema_mode='odata_strip'), yields SourceRow.quarantined unless discard, and continues with the next row. This is the canonical source-boundary pattern: narrow typed exception, audited quarantine, no fabrication, pipeline keeps processing other rows. Matches the existing rationale.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-03'
- key: plugins/sources/dataverse.py:R6:DataverseSource:load:fp=074f0cde5af3d0f6
  owner: john
  reason: |-
    In DataverseSource.load (dataverse.py:620), the except ValueError wraps self._normalize_row_fields on a cleaned external API row (Tier 3). normalize_field_name raises ValueError when an arbitrary Dataverse field name normalizes to empty (all-special-character names); the handler quarantines the row via ctx.record_validation_error(schema_mode='field_normalization') and continues. Narrow catch, audited quarantine, no inferred/fabricated field — the absence of a usable field name is recorded, not papered over. Continuing per the configured failure policy is correct for Tier-3 user data. Matches the existing rationale.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-03'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_validate_and_yield:fp=36c675d763795770
  owner: john
  reason: |-
    In AzureBlobSource._validate_and_yield (azure_blob_source.py:1063), the except ExternalHeaderError catches Tier-3 data faults raised by _normalize_row_keys on an external JSON/JSONL row: a non-object row, an external-key normalization failure, or a row-created field-mapping collision reclassified by _resolve_json_field_names. Config/our-code faults — bad field_mapping keys/values or a normalization-algorithm bug — remain plain ValueError and are deliberately NOT caught (they crash), mirroring the CSV header path's ExternalHeaderError/ValueError split. The handler records the failure UNCONDITIONALLY via ctx.record_validation_error(schema_mode='field_normalization'), then yields SourceRow.quarantined unless on_validation_failure == 'discard', and returns. Narrow catch, audited quarantine, no fabricated field names — the unusable external key set is recorded as an audit fact, not papered over.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-03'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_validate_and_yield:fp=b34ba0b6d6f08c19
  owner: john
  reason: |-
    In AzureBlobSource._validate_and_yield (azure_blob_source.py:1140), the except ContractFieldLimitExceeded catches the contract builder's field-count cap firing during sparse-field contract inference (process_sparse_fields) on an external JSON/JSONL row. Sparse external rows can mint unbounded new keys — attacker-controllable input — and the cap is the Tier-3 defence against contract-inflation; exceeding it is a property of the external data, not of our code. The handler records the failure UNCONDITIONALLY via ctx.record_validation_error(schema_mode=self._schema_config.mode) and yields SourceRow.quarantined unless on_validation_failure == 'discard'. Narrow catch, audited quarantine; only the offending row is quarantined and the pipeline keeps processing.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-03'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_validate_and_yield:fp=7eb7d56e93a24f8d
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 schema-validation quarantine, recorded not swallowed. This except block in AzureBlobSource._validate_and_yield (azure_blob_source.py:1154) catches pydantic ValidationError from self._schema_class.model_validate(row_to_validate) — the source-boundary schema validation of an external row whose values may be invalid (the canonical Tier-3 trust boundary: external data may not conform to the declared/inferred schema). It is not a silent swallow: it calls ctx.record_validation_error(...) UNCONDITIONALLY with the validation error string and schema_mode (writing a verr_* record into the Landscape audit trail), then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (explicit operator routing that suppresses only the quarantine yield, never the audit record). This is the CLAUDE.md source doctrine: a row that fails schema validation is recorded and quarantined, the pipeline keeps processing other rows. The caught type is the narrow, expected pydantic ValidationError (never bare Exception); a wrong-type bug in our own validation wiring would surface as a different exception and crash — the in-try raise ValueError sites for missing field_resolution are code faults and propagate. R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (raising-test contract); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-03'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_json_array:fp=1e594410d1dda5e2
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_json_array catch narrow external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, and (json.JSONDecodeError, ValueError) on json.loads (the ValueError arm is bounded to _reject_nonfinite_constant rejecting NaN/Infinity literals — a data fault, since parse_constant only fires on parsed JSON tokens). Neither is a silent swallow: both route through the local _record_file_level_error helper, which builds a raw_row (container, blob_path, error) and calls ctx.record_validation_error(...) UNCONDITIONALLY — writing a verr_* record into the Landscape audit trail — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine yield, never the audit record). This is the CLAUDE.md source doctrine: malformed external JSON is recorded and quarantined at the boundary, the run is not crashed. The caught types are narrow (never bare Exception). R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (raising-test contract); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/azure_blob_source.py:R6:AzureBlobSource:_load_json_array:fp=4f8205430a175d4e
  owner: john
  reason: |-
    PRESCRIBED FORM — Tier-3 source-boundary quarantine, recorded not swallowed. These except blocks in AzureBlobSource._load_json_array catch narrow external-origin parse faults on raw blob bytes: UnicodeDecodeError on the configured-encoding decode, and (json.JSONDecodeError, ValueError) on json.loads (the ValueError arm is bounded to _reject_nonfinite_constant rejecting NaN/Infinity literals — a data fault, since parse_constant only fires on parsed JSON tokens). Neither is a silent swallow: both route through the local _record_file_level_error helper, which builds a raw_row (container, blob_path, error) and calls ctx.record_validation_error(...) UNCONDITIONALLY — writing a verr_* record into the Landscape audit trail — then conditionally yields SourceRow.quarantined(...) unless on_validation_failure == "discard" (an explicit operator routing choice that suppresses only the quarantine yield, never the audit record). This is the CLAUDE.md source doctrine: malformed external JSON is recorded and quarantined at the boundary, the run is not crashed. The caught types are narrow (never bare Exception). R6 is a syntactic detector that does not recognise record_validation_error + yield SourceRow.quarantined as explicit error handling. The method is a generator (yields rows, never raises), so the @trust_boundary decorator is infeasible (raising-test contract); per-line allowlist is the prescribed honest mechanism.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/transforms/batch_drift_compare.py:R8:BatchDriftCompare:_categorical_shifts:fp=dc863616c567763c
  owner: john
  reason: |-
    This `setdefault` is on a local accumulator constructed in the same function, not a Tier-1 audit-data read or defensive access to an invariant-bearing mapping. At `src/elspeth/plugins/transforms/batch_drift_compare.py:356` the dict is born empty as `"values_by_key: dict[ScalarBucketKey, object] = {}"`, then at line 357 it iterates only the already-collected categorical cohort values, and line 358 uses `"values_by_key.setdefault(scalar_bucket_key(value), value)"` to keep the first representative value for each type-aware scalar bucket. The surrounding path validates categorical values before this point: `_categorical_values_for` accepts only `str`, `int`, or `bool` at lines 278-289 and raises on anything else. If this site drifted into reading an existing mapping or defaulting an invariant field, the suppression would no longer apply.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:44:20.144902+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the visible code constructs `values_by_key` as a fresh local accumulator and uses `setdefault` to retain the first representative for each type-aware bucket, rather than reading or defaulting a required mapping field. The subsequent iteration over `values_by_key.values()` independently confirms that this is intentional deduplication state, so no missing-key or fixed-contract failure is concealed. The rationale’s separate validation claim is not load-bearing for this disposition, and R8 is not eligible for the trust-boundary decorator.
  judge_signature_version: 2
  scope_fingerprint: d43c30ee56babfcf81e8aab9e8e1b15f805ffb3ffc7ebb6a8433a2602ba744cb
  judge_transport: codex_cli
  ast_path: 'body[27]/body[26]/body[3]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:32da71ee0554f0d9c7c07dab9cb0f99ed31bc1e261f065f8542009728cf28e4d'
- key: plugins/sources/dataverse.py:R6:DataverseSource:load:fp=a23f99d2b5035239
  owner: john
  reason: |-
    This is a legitimate Tier-3 source-ingestion boundary, not a Tier-1 audit-data violation. In `src/elspeth/plugins/sources/dataverse.py:633-641`, `DataverseSource.load` explicitly marks Dataverse field names as external data and narrows the catch to `ExternalHeaderError`: "field names from Dataverse can be arbitrary strings" and "Catch only ExternalHeaderError (data faults: header normalizes to empty, normalization collision in the entity's own field names)." The handler at `src/elspeth/plugins/sources/dataverse.py:644-659` does not silently ignore the exception: it increments the quarantine counter, records a validation error with `schema_mode="field_normalization"`, yields `SourceRow.quarantined(...)` unless configured to discard, and then continues to the next external row. Plain `ValueError` from config faults is intentionally not caught per `src/elspeth/plugins/sources/dataverse.py:637-640`, so our config/code defects still crash.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/dataverse.py:R6:DataverseSource:load:fp=24cdc9a54bfa48f3
  owner: john
  reason: |-
    `DataverseSource.load` is processing external Dataverse payloads: `for raw_row in page.rows:` at `src/elspeth/plugins/sources/dataverse.py:609`. The flagged handler is immediately after sparse-field contract inference for that external row: `contract = self._contract_builder.process_sparse_fields(validated_row, self._field_resolution.resolution_mapping)` at lines 706-710. `ContractFieldLimitExceeded` is a prescribed Tier-3 source-boundary validation failure here, not a Tier-1 audit-data invariant: the exception is raised when a sparse external row would exceed the inferred schema field cap (`contract_builder.py:111-115`). The site records the validation failure and either emits an explicit quarantine row or discards only under the configured validation policy: `ctx.record_validation_error(...)` at lines 712-717, `yield SourceRow.quarantined(...)` at lines 718-724, then `continue` at line 725. This is not a silent swallow; it converts an untrusted source-row shape violation into the source plugin's validation/quarantine channel.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/text_source.py:R6:TextSource:load:fp=8a07bddba8538639
  owner: john
  reason: |-
    `TextSource.load` is a source-ingestion Tier-3 boundary: it opens the configured external text file at `src/elspeth/plugins/sources/text_source.py:141-143` and converts malformed external input into a source parse/quarantine result. The `UnicodeDecodeError` at line 178 is not silently ignored: it calls `_record_parse_error(...)` with `"Text parse error at line {error_line}: invalid {self._encoding} encoding ({exc})"` at lines 180-185, then `yield quarantined` at lines 186-187 when quarantine output exists. This is the prescribed source-boundary pattern for bad external file bytes, not a Tier-1 audit-data violation that must crash. Suppression is valid only while this catch remains tied to that parse-error/quarantine path.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_load_jsonl:fp=84123aeed592c85f
  owner: john
  reason: |-
    This is a source-plugin Tier-3 ingestion boundary, not Tier-1 audit data. In `JSONSource._load_jsonl`, the file bytes are external input and the handler is specifically for decode failure while reading that external file. The function documents the boundary at `src/elspeth/plugins/sources/json_source.py:245-247`: "external data (Tier 3) that fails to parse is quarantined, not crash the pipeline." The caught `UnicodeDecodeError` at `src/elspeth/plugins/sources/json_source.py:293` is not silently ignored: it constructs a site-specific parse error, `error_msg = f"JSON parse error at line {error_line}: invalid {self._encoding} encoding ({e})"`, records it through `_record_parse_error(...)`, and yields the quarantined row when present. Future drift that removed the `_record_parse_error` call or changed this from source-file decode handling into an internal/audit read would invalidate this suppression.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_load_json_array:fp=09bdc898942ffaa5
  owner: john
  reason: |-
    `plugins/sources/json_source.py:314-335` is a source-ingestion Tier-3 boundary for an external JSON file. The flagged handler wraps `json.load(f, parse_constant=_reject_nonfinite_constant)` and converts malformed external input into an explicit quarantined source row, not a silent success: `except (json.JSONDecodeError, ValueError) as e:`, then builds `error_msg`, calls `self._record_parse_error(...)`, yields the quarantined row when present, and `return  # Stop processing this file`. This is the prescribed source-boundary form: external data parse failure is reified as validation/quarantine instead of crashing or being ignored.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_load_json_array:fp=7fe9f3ef017bf33c
  owner: john
  reason: |-
    This is a source-plugin Tier-3 ingestion boundary, not a Tier-1 audit-data read path. In `src/elspeth/plugins/sources/json_source.py:308-313`, `_load_json_array` documents that external JSON input parse/decode failures are quarantined: `"external data (Tier 3) that fails to parse or decode is quarantined, not crash the pipeline."` The flagged handler at `src/elspeth/plugins/sources/json_source.py:336-348` catches only `UnicodeDecodeError`, converts it into the explicit parse error `"JSON parse error: invalid {self._encoding} encoding ({e})"`, records it through `_record_parse_error(...)`, yields the quarantined `SourceRow` when present, then returns. Future drift that removes the `_record_parse_error(...)` call, stops yielding the quarantined row, broadens the caught exception beyond this external decode failure, or moves this handler onto Tier-1 data would invalidate this suppression.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_validate_and_yield:fp=9382c3fae5eb7f0f
  owner: john
  reason: |-
    This is a legitimate Tier-3 source-boundary quarantine, not a swallowed Tier-1 invariant failure. In `src/elspeth/plugins/sources/json_source.py:513-519`, `_validate_and_yield` states that JSON keys are normalized at the source boundary and that only `ExternalHeaderError` is caught for "Tier-3 data faults: non-object row, external-header collision after normalization, header normalizes to empty"; config `ValueError`s are deliberately allowed to crash. The handler at `src/elspeth/plugins/sources/json_source.py:522-531` converts that external header fault into a recorded validation failure via `_record_validation_failure(..., error_msg=str(exc), source_row_index=source_row_index)`, yields the quarantined `SourceRow` when configured to do so, and returns. Drift that broadened this catch beyond `ExternalHeaderError`, stopped recording the failure, or caught config/internal errors here would invalidate this suppression.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_validate_and_yield:fp=fecd3868bccecb29
  owner: john
  reason: |-
    This is a source-boundary validation path, not a Tier-1 audit-data read or internal invariant swallow. In `src/elspeth/plugins/sources/json_source.py:498-507`, `_validate_and_yield` receives `row: Any` where the docstring says the row "May be a non-dict JSON value from the external source boundary." The caught exception at `src/elspeth/plugins/sources/json_source.py:586` is raised while converting external JSON into a contract-owned `SourceRow`: the function normalizes Tier-3 JSON keys at `src/elspeth/plugins/sources/json_source.py:513`, validates/coerces with `self._schema_class.model_validate(normalized_row)` at `src/elspeth/plugins/sources/json_source.py:535`, updates sparse-field contract metadata at `src/elspeth/plugins/sources/json_source.py:560-564`, then emits `SourceRow.valid(...)` only after `contract.validate(...)` passes at `src/elspeth/plugins/sources/json_source.py:566-585`.

    The `except ContractFieldLimitExceeded as e:` block is a prescribed source-boundary quarantine form: it calls `_record_validation_failure(... row=normalized_row, error_msg=str(e), source_row_index=source_row_index)` and yields the returned quarantined row if not in discard mode (`src/elspeth/plugins/sources/json_source.py:586-594`). That helper records the validation error to the source context and returns `SourceRow.quarantined(...)` with the same `source_row_index` (`src/elspeth/plugins/sources/json_source.py:638-650`). If this site stops recording `ctx.record_validation_error`, stops yielding `SourceRow.quarantined`, catches broader internal exceptions, or moves to Tier-1 persisted/audit data, this suppression should become stale and invalid.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/json_source.py:R6:JSONSource:_validate_and_yield:fp=d34ff269dbb5a891
  owner: john
  reason: |-
    This is a source ingestion boundary for untrusted JSON rows, not a Tier-1 audit-data read. In `src/elspeth/plugins/sources/json_source.py:498-507`, `_validate_and_yield` explicitly accepts `row: Any` because the row "May be a non-dict JSON value from the external source boundary." The caught `ValidationError` is raised while validating/coercing that external row with `validated = self._schema_class.model_validate(normalized_row)` at line 535. The handler at lines 595-603 does not silently swallow it: it calls `_record_validation_failure(... error_msg=str(e) ...)` and yields the resulting quarantined `SourceRow` unless validation policy is `"discard"`. `_record_validation_failure` records the error through `ctx.record_validation_error(...)` at lines 638-643 and returns `SourceRow.quarantined(...)` at lines 646-651. That is the prescribed Tier-3 source boundary form: normalize/validate external data at ingestion, record/quarantine bad rows, and continue rather than crash the run.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=f44d4a37494da4b3
  owner: john
  reason: |-
    `src/elspeth/plugins/sources/csv_source.py:252-257` defines `_load_from_file` as the CSV file-read boundary: it says the function handles `UnicodeDecodeError` at the "file-reading boundary (Tier 3 external data)." The flagged handler at `src/elspeth/plugins/sources/csv_source.py:463-490` is not silently suppressing a Tier-1 invariant failure: it converts an external decode failure into a source-boundary validation event with `ctx.record_validation_error(...)`, optionally emits `SourceRow.quarantined(...)`, and then `return`s to stop processing an unreadable external file. This is the prescribed source-plugin Tier-3 boundary pattern, not a defensive-programming defect.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/sources/csv_source.py:R6:CSVSource:_load_from_file:fp=99c99c4096a62de6
  owner: john
  reason: |-
    This is a source-ingestion Tier-3 boundary, not a Tier-1 defensive-programming defect. In `src/elspeth/plugins/sources/csv_source.py:252-257`, `_load_from_file` is explicitly the file-reading boundary: `"Load rows from an open CSV file handle"` and `"file-reading boundary (Tier 3 external data)"`. The `csv.Error` at `src/elspeth/plugins/sources/csv_source.py:491` is raised while parsing external CSV bytes via `next_nonblank_record()`.

    The exception is not silently swallowed: the site reifies the parse failure into audit/validation state with `ctx.record_validation_error(...)` at `src/elspeth/plugins/sources/csv_source.py:510-515`, optionally emits a quarantined row carrying `error=error_msg` at `src/elspeth/plugins/sources/csv_source.py:517-523`, and then stops with `return  # Don't continue with corrupted parser state` at `src/elspeth/plugins/sources/csv_source.py:524`. That is the prescribed source-boundary pattern for malformed external data: record/quarantine or discard according to policy, then stop rather than trusting a corrupted CSV parser.
  safety: |-
    Pending cicd-judge re-justification: the enclosing function changed with the
    quarantine error-text redaction work (elspeth-a300402c58), so any prior
    verdict (which binds to the exact code the judge inspected) could not be
    carried. The qname-level rationale above was re-verified against the
    current code at this site.
  expires: '2026-09-15'
- key: plugins/infrastructure/manager.py:R4:PluginManager:register:fp=0e88b71c015f8aa0
  owner: john
  reason: |-
    In PluginManager.register (manager.py:107), the broad except Exception as rollback_err sits on the *rollback* path inside the outer handler: when check_pending()/_refresh_caches() fails, the code unregisters the plugin and refreshes caches, and this inner catch guards only that rollback so a secondary rollback failure cannot bury the original diagnostic via implicit exception chaining. The original exception is unconditionally re-raised on line 109 (`raise`), so the primary failure still crashes registration as plugin-ownership policy requires; the rollback failure is recorded via _logger.error with exc_info. This is the documented rollback-protection idiom, not error swallowing — the real failure is preserved and propagated.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:08:42.383952+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.85
  judge_rationale: |-
    PRESCRIBED FORM. The broad except at line 107 guards only the rollback path (unregister + refresh) inside an outer handler that has already captured the primary failure; line 109 unconditionally re-raises the original exception, so the plugin-ownership crash-on-failure semantics are preserved. The inner catch exists specifically to prevent a secondary rollback exception from displacing the primary diagnostic via implicit chaining, and it records the rollback failure via _logger.error with exc_info rather than swallowing it silently. This is the documented rollback-protection idiom, not defensive suppression: the real failure still propagates.
  judge_signature_version: 2
  scope_fingerprint: 58a08845e9c373c7e82a6d9ea1b75d345d485342edff8a5aacec0ffd057ab753
  judge_transport: openrouter
  ast_path: 'body[11]/body[3]/body[2]/handlers[0]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b75924e5a5455122243d5cc13f5ea2bf495d136abd1b6626dbd36999169f6b5f'
- key: plugins/infrastructure/pooling/executor.py:R6:PooledExecutor:_execute_single:fp=71e586ef9fbea49d
  owner: john
  reason: |-
    In PooledExecutor._execute_single (executor.py:620), except CapacityError as e assigns the exception to retryable_error and falls through to explicit retry orchestration: throttle.on_capacity_error(), semaphore release, interruptible wait, deadline checks, and re-acquisition at the top of the loop. It is the parallel arm to the PluginRetryableError handler just above. This is EAFP/offensive control flow — a specific typed exception used as a retry signal — not silent swallowing; the error is preserved and surfaces via _build_retry_timeout_result if the capacity-retry budget is exhausted, keeping audit traceability intact. Matches the existing rationale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:08:48.700078+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.88
  judge_rationale: |-
    PRESCRIBED FORM. The except CapacityError assigns to retryable_error and falls through to the shared retry orchestration below (throttle.on_capacity_error, semaphore release, interruptible wait, deadline check, re-acquire at loop top). The exception is preserved in retryable_error and surfaces via _build_retry_timeout_result / _build_interrupted_result if the retry budget is exhausted, so this is a typed-exception retry signal, not a silent swallow — the mirror of the PluginRetryableError arm immediately above. R6's concern (errors vanishing without re-raise or explicit result) does not apply here.
  judge_signature_version: 2
  scope_fingerprint: 4001ace28343e1b0a3396b26775ef489a3ba243570437383f466c10f91b249f0
  judge_transport: openrouter
  ast_path: 'body[16]/body[19]/body[6]/body[0]/body[4]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:de39b5616155bf82bed244662085e72e25d78048d81dc1af1311bf12b30ad1b1'
- key: plugins/transforms/rag/config.py:R6:_get_providers:fp=3371a100de2c9052
  owner: john
  reason: |-
    In _get_providers (config.py:35), `except ModuleNotFoundError: pass` wraps an import of the optional azure-search-documents SDK, not any data-handling code. ModuleNotFoundError is the narrow, specific exception signalling the optional dependency is absent; it is used here as a documented control-flow signal to omit the azure_search provider from the registry. No external data flows through this path — it is a module-load capability probe, the standard Python idiom for optional-extra registration. Existing rationale still accurately describes the code; not defensive suppression of data errors.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:09:43.036874+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.92
  judge_rationale: |-
    RULE MISFIRES. The R6 silent-except rule fires by shape on `except ModuleNotFoundError: pass`, but the pattern's premise (swallowing a data-handling error) does not hold here: this is a module-load capability probe for an optional extra dependency, not error handling on any data flow. ModuleNotFoundError is the narrow, specific signal that the optional azure-search-documents SDK is not installed; the documented response is to omit the provider from the registry. No Tier-anything data passes through this path, and the idiom is the standard Python pattern for optional-extra registration.
  judge_signature_version: 2
  scope_fingerprint: 9b4e64ceee8a4be027d5b0bee4608311ab74052671f2793a1331694248665b93
  judge_transport: openrouter
  ast_path: 'body[12]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:f99a24609fc49f918f159aa5c7288df0d4a74fb0c86092ca3ce2f45e7b390e50'
- key: plugins/transforms/rag/config.py:R6:_get_providers:fp=56767cfd4c9ab197
  owner: john
  reason: |-
    In _get_providers (config.py:55), `except ModuleNotFoundError: pass` wraps the import of the optional chromadb SDK ([rag] extra). The narrow ModuleNotFoundError catch specifically signals the optional dependency is not installed and omits the chroma provider from the registry; any other error in the imported module would still propagate. This mirrors the azure_search block above and is a compile-time capability probe at module load, not defensive handling of runtime data. Existing rationale verified against current code and accurate.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:09:49.160375+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.9
  judge_rationale: |-
    RULE MISFIRES. The narrow ModuleNotFoundError catch around an optional-dependency import is a compile-time capability probe, not runtime data handling. R6's concern is swallowing errors from data operations; here the 'error' is the well-defined signal that an optional extra ([rag]) is not installed, and the specific exception type ensures any other import-time failure still propagates. Omitting the provider from the registry is the correct, explicit outcome — attempts to use 'chroma' will then fail loudly at config-resolution time with a clear 'unknown provider' error.
  judge_signature_version: 2
  scope_fingerprint: 9b4e64ceee8a4be027d5b0bee4608311ab74052671f2793a1331694248665b93
  judge_transport: openrouter
  ast_path: 'body[12]/body[3]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:bb96ad6489216ca55da9ab93a3f6ac116a107adeb250b3c1546ffadc89d92de6'
- key: plugins/infrastructure/preflight.py:R1:plugin_preflight_mode_enabled:fp=2019727813f38ff9
  owner: john
  reason: |-
    In plugin_preflight_mode_enabled (preflight.py:14), `_PLUGIN_PREFLIGHT_MODE.get()` is ContextVar.get(), not dict.get(). _PLUGIN_PREFLIGHT_MODE is declared `ContextVar[bool]` with a default of False (line 9); .get() returns the current context value via the canonical ContextVar API and has no relation to the defensive dict.get() pattern R1 targets. This is a static-analyzer false positive matching the `.get(` token. No Tier-3 data, no fabrication, no missing-field suppression. Existing rationale accurate.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:09:55.026435+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES. The static analyzer matched the `.get(` token, but `_PLUGIN_PREFLIGHT_MODE` is declared `ContextVar[bool]` on line 9, and `ContextVar.get()` is the canonical API for reading the current context value — it has no relationship to `dict.get()`'s missing-key-hiding concern that R1 targets. There is no Tier-3 data, no missing-field defensiveness, and no fabrication here; the excerpt confirms the type. The rationale correctly identifies the false-positive shape match.
  judge_signature_version: 2
  scope_fingerprint: 814d1e9f6c7b5b07b6479204f8fdf8e86b8d935a3acf17aabd9b12d34f55da92
  judge_transport: openrouter
  ast_path: 'body[6]/body[1]/value'
  judge_metadata_signature: 'hmac-sha256:v2:fad2dfbc9814e807d012f36cbdd1621b945052f584a80b54c0d551171fb05744'
  judge_excerpt_redactions:
    - pattern: dotenv_secret_assignment
      byte_count: 43
      redacted_hash: 1d6dd02648d5202b
- key: plugins/infrastructure/pooling/executor.py:R6:PooledExecutor:_execute_batch_locked:fp=d90958e285a74a8e
  owner: john
  reason: |-
    In PooledExecutor._execute_batch_locked (executor.py:314), `except RuntimeError` catches the documented stdlib contract where ThreadPoolExecutor.submit() raises RuntimeError after concurrent shutdown — a runtime-state boundary, not a bug in our code. The handler does not swallow: it completes the reserved buffer slot with a deterministic TransformResult.error(reason='shutdown_requested') and completes every remaining context's slot the same way, so no row is silently dropped and buffer/slot consistency is preserved. The narrow RuntimeError catch is the specific shutdown signal, and the audit trail records what happened to each row. Existing rationale accurate.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:10:17.777937+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.85
  judge_rationale: |-
    PRESCRIBED FORM. The narrow `except RuntimeError` catches the documented stdlib contract of `ThreadPoolExecutor.submit()` after shutdown — a runtime-state boundary external to our code, not a first-party invariant. The handler does not swallow: it completes the reserved buffer slot with an explicit `TransformResult.error(reason='shutdown_requested', retryable=False)`, applies the same deterministic error result to every remaining context, and breaks the submission loop, preserving buffer/slot consistency and per-row auditability. This is exactly R6's prescribed 'return an explicit error/quarantine result' form; the excerpt visibly supports the rationale.
  judge_signature_version: 2
  scope_fingerprint: a619a32a910c46c3fcbedb0d8691970f08bc8fcaee30b4ee21450f7abba50dec
  judge_transport: openrouter
  ast_path: 'body[16]/body[11]/body[4]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:48df13bde0e03d86e8f3202957d4daafc73049fdaa4e90ae19c12c7627cea4e2'
- key: plugins/infrastructure/batching/mixin.py:R4:BatchTransformMixin:_process_and_complete:fp=bf027f2d61c56c68
  owner: john
  reason: |-
    In BatchTransformMixin._process_and_complete (mixin.py:279), the broad except Exception runs on a worker thread that cannot let an exception propagate directly (the orchestrator thread would block forever on the waiter). The handler captures the original exception and traceback.format_exc() into an ExceptionResult, transports it through _complete_ticket/the waiter channel, and the orchestrator thread re-raises it — preserving crash-on-bug semantics required by plugin-ownership policy, just on the correct thread. The original exception and traceback are retained, so chaining and auditability are intact. This is the recognized thread-boundary transport idiom, not silent swallowing. Matches the existing rationale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:10:42.659365+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.86
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM: the broad except is not swallowing — it captures the exception and traceback into an ExceptionResult that is transported through the waiter channel to the orchestrator thread, which re-raises it. This is the thread-boundary transport idiom: a worker thread cannot let exceptions propagate directly without deadlocking the orchestrator waiter. The original exception object and formatted traceback are preserved, so crash-on-bug semantics and audit chaining are intact — the crash happens on the correct thread. The rationale is site-specific and the visible code confirms the mechanism (ExceptionResult construction, _complete_ticket transport, no default substitution).
  judge_signature_version: 2
  scope_fingerprint: 93634dc847a387fb6e5ecfc8c2b261c022847882ac6e8ea40e273ef78352ae2d
  judge_transport: openrouter
  ast_path: 'body[14]/body[18]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:7bcf7d95808ebc723605f2dd8a6fcd9fa0106881a0184cdd7bab442930b0828f'
- key: plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_complete_ticket:fp=f6b5a60d4c2ae50c
  owner: john
  reason: |-
    In BatchTransformMixin._complete_ticket (mixin.py:305), `except (KeyError, ShutdownError)` catches two specific, named exceptions representing legitimate expected races: a ticket already evicted by timeout (KeyError) or a buffer shutdown (ShutdownError). These are documented control-flow signals that a late worker completion arriving after eviction/shutdown is expected and discardable. The discard is recorded via debug log with token_id and state_id for traceability; re-raising would crash a worker thread over a benign race the design tolerates. This is a narrow EAFP catch, not a broad swallow — the handling IS the explicit policy for these two conditions. Existing rationale accurate.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:10:49.274299+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.82
  judge_rationale: |-
    PRESCRIBED FORM. The except catches two specific, named exceptions (KeyError, ShutdownError) that represent legitimate expected races in a concurrent worker/buffer design: a ticket already evicted by timeout or a buffer already shut down when a late worker completion arrives. This is narrow EAFP, not a broad swallow — the two conditions ARE the documented control-flow signals, and the discard is recorded via debug log with token_id and state_id for traceability. Re-raising would crash worker threads over benign races the design explicitly tolerates; the handling IS the policy for these conditions. The rationale addresses R6's specific concern (silent swallow) by identifying the handling as explicit tolerance of named race conditions with logged traceability.
  judge_signature_version: 2
  scope_fingerprint: 187bc822f04749d7899c6eb435f90cfe93f039f32e28bf9790c21cefaf793dab
  judge_transport: openrouter
  ast_path: 'body[14]/body[19]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:6caa05eb4a7725fd97bc4209d0602c7b11513b87609f225ae15b502a98ad83e0'
- key: plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=ba03beb6f92aa4ff
  owner: john
  reason: |-
    In BatchTransformMixin._release_loop (mixin.py:350), except TimeoutError -> continue handles the documented poll sentinel from wait_for_next_release(timeout=1.0): 'no result ready in this window'. It is a normal condition during low load, not an error; re-raising would kill the release thread and stall the pipeline. The catch is narrowly typed, and the same try/except routes ShutdownError -> break (clean exit, line 354) and other Exception -> wrap-as-ExceptionResult/emit with TIER_1_ERRORS re-raised (lines 361-380) — discriminated control flow, not a catch-all swallow. R6's concern (errors vanishing without re-raise or error result) does not apply to a documented timeout sentinel used for poll-and-retry. Matches the existing rationale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:10:55.862589+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.9
  judge_rationale: |-
    RULE MISFIRES. The `except TimeoutError: continue` is not swallowing an error — it is handling the documented sentinel return of `wait_for_next_release(timeout=1.0)` meaning 'no result this poll window'. This is a polling idiom, not error suppression: the timeout IS the normal signal to re-loop. R6's concern (errors vanishing without re-raise or explicit error result) does not apply because TimeoutError here is not an error condition. The surrounding try/except demonstrates discriminated control flow: ShutdownError→break, invariant violations→raise, output failures→ExceptionResult with TIER_1_ERRORS re-raised. The narrow catch on a specific sentinel type is the correct polling pattern.
  judge_signature_version: 2
  scope_fingerprint: 18f39e7843017fbc821335289d9808c913bda41fbd15f1f12be06237b01e72dd
  judge_transport: openrouter
  ast_path: 'body[14]/body[20]/body[1]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:e0e5878205dea9a71dba46bea8c2fb94364443e6aeed9fccb798a3ee734f0b31'
- key: plugins/infrastructure/batching/mixin.py:R6:BatchTransformMixin:_release_loop:fp=781da1282d945b7e
  owner: john
  reason: |-
    In BatchTransformMixin._release_loop (mixin.py:354), except ShutdownError -> break is the dedicated release thread's only clean-exit path. ShutdownError is an ELSPETH-internal typed control-flow signal (a RuntimeError subclass defined in row_reorder_buffer.py) raised by buffer.shutdown(); shutdown_batch_processing() drains all workers before shutting the buffer, so reaching break carries the invariant that all results have been emitted. The same try/except routes TimeoutError -> continue (line 350) and other Exception -> wrap/emit with TIER_1_ERRORS re-raised (line 361). This is a narrowly-typed wakeup signal used as control flow, not silent swallowing of a real error. Matches the existing rationale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:11:01.903930+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.9
  judge_rationale: |-
    PRESCRIBED FORM. ShutdownError is a first-party typed control-flow signal raised by our own buffer.shutdown(); catching it and breaking is the loop's designed clean-exit path, not silent error swallowing. The excerpt shows the sibling except-Exception branch wraps/emits and re-raises TIER_1_ERRORS, confirming this handler discriminates by type rather than muffling failures. R6's concern (swallowed errors) does not apply to a narrowly-typed wakeup signal used as loop termination.
  judge_signature_version: 2
  scope_fingerprint: 18f39e7843017fbc821335289d9808c913bda41fbd15f1f12be06237b01e72dd
  judge_transport: openrouter
  ast_path: 'body[14]/body[20]/body[1]/body[2]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:37841ec36826f61cce493a03d4e46d4062f0330204e6e06820270da86bfa5e07'
- key: plugins/infrastructure/batching/mixin.py:R9:BatchTransformMixin:evict_submission:fp=443d069fd81386c2
  owner: john
  reason: |-
    In BatchTransformMixin.evict_submission (mixin.py:445), ticket = self._batch_submissions.pop(key, None) implements the function's documented return contract: True if evicted, False if not found or already completed. The docstring states the worker may have already completed and removed the entry, so the None sentinel directly maps to the False-return branch (line 447-448). This is process-internal coordination state under the submissions lock, and the race is real and intended — not defensive masking of a schema invariant. No audit corruption or data loss; the False return is the audited outcome. Legitimate R9 suppression. Matches the existing rationale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:11:08.878045+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.85
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM: the docstring explicitly documents that the worker may have already completed and removed the entry, and the function's contract is to return False in that case. The pop(key, None) followed by an explicit None-check that returns False is not hiding a missing-key bug — the missing key IS a legitimate concurrent state that the return contract encodes. This is process-internal coordination under a lock where the race is real and intended, and the None sentinel is meaningfully mapped to the documented False branch rather than swallowed. R9's concern (implicit default hiding a schema invariant) does not apply here.
  judge_signature_version: 2
  scope_fingerprint: a781f2a8f5ae017989b1a82752cbb9ff18aeff9f52de1f3c0d3a5b6c5e6de0c3
  judge_transport: openrouter
  ast_path: 'body[14]/body[22]/body[2]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:d64ff48cd3484abf2eeb222ee8d7677cdf3b4152238309c38618bcd186d807da'
- key: plugins/transforms/batch_stats.py:R1:BatchStats:_group_rows:fp=959f0779c842f5e5
  owner: john
  reason: |-
    This is not a Tier-1 audit-data read and is not defensively masking missing pipeline fields. In `src/elspeth/plugins/transforms/batch_stats.py:295-304`, `groups` is a local accumulator initialized in the same function (`"groups: dict[ScalarBucketKey, tuple[Any, list[tuple[int, PipelineRow]]]] = {}"`), and `groups.get(group_key)` is used only to distinguish the first occurrence of a computed bucket key from later rows: `"if grouped is None: groups[group_key] = (group_value, [(row_index, row)])"` else append to the existing bucket. The Tier-2 row field itself is read offensively at `src/elspeth/plugins/transforms/batch_stats.py:297` via `"group_value = row[self._group_by]"`, so missing `group_by` still crashes as an upstream invariant violation. Suppression should remain valid only while the `.get()` receiver is this function-local `groups` accumulator and not `row`, Landscape/audit data, checkpoint data, or config.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:44:31.773537+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — `groups` is a function-local accumulator initialized empty, and key absence intentionally identifies the first occurrence of a computed bucket key rather than masking a missing field under a fixed contract. The actual Tier-2 field is accessed directly as `row[self._group_by]`, so a missing pipeline field still raises instead of being hidden by a default. The decorator contract does not apply because the flagged receiver is the local `groups` mapping, not data rooted at the `rows` parameter.
  judge_signature_version: 2
  scope_fingerprint: 82e4b8d952487e5103888f5a5259d095f22de1f2a2e1051c99eb61fba01c9358
  judge_transport: codex_cli
  ast_path: 'body[18]/body[19]/body[3]/body[2]/value'
  judge_metadata_signature: 'hmac-sha256:v2:9659a04acb041dbb35e100a01af83159055c59c21de20e3f5a4c63112fff6cae'
- key: plugins/transforms/web_scrape.py:R1:WebScrapeTransform:process:fp=49b15f0c241922b3
  owner: john
  reason: |-
    Legitimate Tier-3 HTTP response boundary. In `src/elspeth/plugins/transforms/web_scrape.py:721-724`, `process()` fetches an external URL and receives `response` from `self._fetch_url(safe_request, ctx)`. The flagged `.get()` at `src/elspeth/plugins/transforms/web_scrape.py:744` reads an external HTTP header: `content_type_raw = response.headers.get("content-type", "")`. The surrounding guard is boundary validation, not defensive masking of Tier-1 data: lines 738-743 state that non-text responses are rejected before extraction and that an absent `Content-Type` is "treated as unknown and rejected conservatively"; lines 747-755 then return `TransformResult.error` unless the normalized header starts with `text/` or `application/xhtml+xml`. If this code drifts so the absent-header default no longer flows into the rejection path, this suppression is invalid.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:45:33.340126+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — `response.headers` contains externally authored HTTP metadata, so the flagged `.get()` is Tier-3 boundary extraction rather than defensive access under a first-party fixed contract. The visible flow converts an absent header to an empty string only as a rejection sentinel: it cannot match an accepted content-type prefix and therefore returns `TransformResult.error`, so no fabricated value is promoted downstream. The excerpt does not show the function declaration or its decorators, so the decorator alternative cannot be safely recommended.
  judge_signature_version: 2
  scope_fingerprint: 1f326863deff7a2582c1066dfe7186e888f5ccd2d0d59992ea0bc551f1140826
  judge_transport: codex_cli
  ast_path: 'body[38]/body[20]/body[4]/value'
  judge_metadata_signature: 'hmac-sha256:v2:e86c35d575fdba7e0a895ce24302521bb74208a851cf0ec161317ca197a6d345'
- key: plugins/sources/field_normalization.py:R1:extend_field_resolution:fp=eb1d22b46784fb2d
  owner: john
  reason: |-
    This is a legitimate Tier-3 source-header boundary suppression, not a Tier-1 audit-data read. In `plugins/sources/field_normalization.py:328-337`, `extend_field_resolution` extends sparse source field resolution from newly seen `raw_headers`; lines 344-345 immediately normalize and collision-check those external headers, and lines 351-386 turn bad external header/mapping collisions into `ExternalHeaderError` / `FieldMappingCollisionError`.
    
    The flagged `.get()` at `plugins/sources/field_normalization.py:373` is not masking missing required internal state. It is an intentional diagnostic label inside a branch that has already proven a collision at line 372: `if final_header in existing_final_headers or final_header in new_final_to_raw:`. For collisions against a newly seen field in this same extension pass, `new_final_to_raw[final_header] = raw_header` is populated at line 387, so `.get(final_header, ...)` returns the actual prior raw header. For collisions against the already-established resolution, the local `new_final_to_raw` scratch dict intentionally cannot contain the prior raw field, so the prescribed fallback `"previously seen field"` is the stable message used in the external-header error. Future drift that removes the line-372 membership guard, changes `new_final_to_raw` from local scratch state, or uses this fallback for persisted audit/landscape data would invalidate this suppression.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:43:31.299137+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the flagged lookup does not conceal a missing key under a fixed contract; the preceding condition proves a collision either with `existing_final_headers` or with the current-pass `new_final_to_raw` scratch map. When the collision is current-pass, membership guarantees the lookup returns the prior raw header; when it is against established resolution, absence from the scratch map is expected and the fallback is only an explicit diagnostic label before a typed collision exception is raised. No pipeline datum is fabricated or promoted, and the subject of the lookup is a locally constructed scratch dictionary rather than an external function parameter, so the structural decorator contract does not apply.
  judge_signature_version: 2
  scope_fingerprint: 972e025ba9741f711ac072e685778fa6c2cc8e3f8bb4fc3de22ef79889aeda53
  judge_transport: codex_cli
  ast_path: 'body[21]/body[14]/body[0]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:72a5a219b74f1caead0f7fafc4cf0154d9f453b29164b4ae7f418f40aaea1cd6'
- key: plugins/sources/field_normalization.py:R1:extend_field_resolution:fp=cfda09bf674ad0b1
  owner: john
  reason: |-
    This is a source-boundary header-normalization path for external sparse records, not a Tier-1 audit-data read. In `src/elspeth/plugins/sources/field_normalization.py:329-338`, `extend_field_resolution(...)` takes newly seen `raw_headers` and says it extends sparse source field resolution when a "sparse JSON-like row introduces a field." The flagged lookup at `src/elspeth/plugins/sources/field_normalization.py:370-374` reads from `existing_final_to_effective = dict(zip(resolution.final_headers, resolution.effective_headers, strict=True))` and then, only inside the collision branch, does `existing_effective_header = existing_final_to_effective.get(final_header)`. The branch at `src/elspeth/plugins/sources/field_normalization.py:371-372` covers two different collision forms: `final_header in existing_final_headers` or `final_header in new_final_to_raw`. For a same-row duplicate in `new_final_to_raw`, absence from `existing_final_to_effective` is expected and is used to classify whether `field_mapping` caused the external-header collision. The strict zip preserves the owned invariant for the existing resolution; the `.get()` is a prescribed optional lookup at the Tier-3 source header boundary. This suppression should drift-fail if the lookup is moved out of this collision classifier, if the strict zip at line 370 is removed, or if the function stops handling newly seen external sparse headers.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:44:07.685069+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the lookup is an intentional optional-membership query, not defensive recovery from a missing key under a fixed contract. The strict zip at field_normalization.py:384 establishes the owned existing-resolution mapping, while the branch at lines 386-388 also covers same-batch collisions recorded only in `new_final_to_raw`, for which absence from `existing_final_to_effective` is expected. No malformed header is silently accepted or fabricated: every collision path raises either `FieldMappingCollisionError` or `ExternalHeaderError`. The excerpt does not show the function decorators, and the flagged subject is a derived internal classifier mapping, so no structural decorator recommendation is warranted.
  judge_signature_version: 2
  scope_fingerprint: 972e025ba9741f711ac072e685778fa6c2cc8e3f8bb4fc3de22ef79889aeda53
  judge_transport: codex_cli
  ast_path: 'body[21]/body[14]/body[0]/body[1]/value'
  judge_metadata_signature: 'hmac-sha256:v2:2c04d028e79f586abe678abe0c90ade4bb30fcf2be17518492d91d0b4b15a3b0'
- key: plugins/sources/field_normalization.py:R1:extend_field_resolution:fp=045e761225867317
  owner: john
  reason: |-
    `src/elspeth/plugins/sources/field_normalization.py:375` is a source-boundary collision classifier, not a Tier-1 audit-data read. The function is explicitly extending newly seen external headers (`raw_headers`) and validates/collides them before custody: `new_raw_headers = [header for header in raw_headers if header not in existing_mapping]` at line 340 and collision errors are raised at lines 385-386. The flagged lookup, `previous_new_effective_header = new_final_to_effective.get(final_header)`, is intentionally optional because the enclosing branch at line 372 handles two distinct collision sites: an already persisted final header (`final_header in existing_final_headers`) or a duplicate within this new external header batch (`final_header in new_final_to_raw`). For the existing-header path there is correctly no entry in `new_final_to_effective`, and line 381 explicitly treats that absence as non-mapping evidence with `previous_new_effective_header is not None`. Drift that removes the line-372 guard, changes line 381 to trust the missing value, or repurposes this for audit/Landscape data would invalidate this suppression.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:43:47.057869+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — `new_final_to_effective` is a locally built collision index whose entry is intentionally absent when the collision is with `existing_final_headers`, so this lookup is optional rather than access to a required fixed-contract field. The visible branch covers either an existing-header collision or a duplicate in the new batch, and the `is not None` check uses absence only as evidence that the latter collision source does not apply; no value is fabricated or silently substituted. Although R1 is decorator-suppressible, the excerpt does not show the function declaration or its decorators, so the structural-decorator recommendation cannot safely be made.
  judge_signature_version: 2
  scope_fingerprint: 972e025ba9741f711ac072e685778fa6c2cc8e3f8bb4fc3de22ef79889aeda53
  judge_transport: codex_cli
  ast_path: 'body[21]/body[14]/body[0]/body[2]/value'
  judge_metadata_signature: 'hmac-sha256:v2:72baa3cca824a21ed5b1181f921b691f1e02af0a2e10f8e28318d1bafd2e45d5'
- key: plugins/infrastructure/clients/http.py:R1:AuditedHTTPClient:_request_with_optional_body_cap:fp=204cec0b5169e0df
  owner: john
  reason: |-
    This is a scanner false positive at a Tier-3 HTTP boundary, not defensive access to Tier-1 audit data. In `src/elspeth/plugins/infrastructure/clients/http.py:418`, the receiver is typed as `client: httpx.Client`, and the flagged site is `src/elspeth/plugins/infrastructure/clients/http.py:441` inside the GET dispatch branch: `return client.get(full_url, **request_kwargs)`. That is an outbound HTTP verb method call on an `httpx.Client`, not `dict.get()` against internal/audit data. This suppression is valid only while the enclosing function keeps the typed `httpx.Client` receiver and the flagged line remains the GET request dispatch under `if method == "GET"`.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-24'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-26T22:22:43.710710+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the flagged expression is `client.get(full_url, **request_kwargs)`, an HTTP verb method invoked on the explicitly typed `httpx.Client`, not `dict.get()` or defensive access that could conceal a missing key. The visible function signature and GET dispatch branch independently support the site-specific rationale, so R1's premise is false at this location. The trust-boundary decorator is inapplicable because the expression is not a defensive read rooted in external data.
  judge_signature_version: 2
  scope_fingerprint: 6d1ea1aa00d57eab38d824cda6c686eb4ba9926cc29e960c3f36206ac8c027ec
  judge_transport: codex_cli
  ast_path: 'body[29]/body[16]/body[6]/body[0]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:cd21a20a5b56dcba19c2ccb4b75c25ed2dfd6f7945a785620c49dcff0a8c357a'
- key: plugins/infrastructure/clients/http.py:R4:AuditedHTTPClient:_emit_telemetry_after_audit:fp=8cb4398f454be36e
  owner: john
  reason: |-
    This is a prescribed telemetry-after-audit boundary, not a Tier-1 audit-data suppression. In `plugins/infrastructure/clients/http.py:249-260`, the audit write happens first via `call = self._record_call(...)`, and only after that succeeds does the code call `self._emit_telemetry_after_audit(...)`. Inside the cited method, `plugins/infrastructure/clients/http.py:309-312` explicitly re-raises Tier-1 integrity failures and programmer bugs: `except contract_errors.TIER_1_ERRORS: raise` and `except (TypeError, AttributeError, KeyError, NameError): raise`. The broad catch at `plugins/infrastructure/clients/http.py:313-323` is limited to non-Tier-1 telemetry emission failures after audit primacy has already been satisfied, and it explicitly acknowledges the failure with `logger.warning("telemetry_emit_failed", ..., exc_info=True)` rather than silently swallowing it. Drift that removes the prior audit write, widens the catch to include `contract_errors.TIER_1_ERRORS`, or drops the explicit telemetry failure log would invalidate this suppression.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-26'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-28T03:36:41.574157+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.97
  judge_rationale: |-
    PRESCRIBED FORM: `plugins/infrastructure/clients/http.py:298-319` shows the Landscape audit write completing before telemetry emission begins, so the broad catch cannot suppress an audit-recording failure. At `plugins/infrastructure/clients/http.py:358-372`, Tier-1 integrity exceptions and enumerated programmer errors are explicitly re-raised, while remaining telemetry failures are acknowledged with a warning and exception details. This conforms to the required audit-first, telemetry-second best-effort ordering and uses logging only after telemetry itself has failed.
  judge_signature_version: 2
  scope_fingerprint: f0a6f8fc062ac02180496e221f342a0a209b0e8e4f0221fc80ed7213ceaa777c
  judge_transport: codex_cli
  ast_path: 'body[29]/body[11]/body[2]/handlers[2]'
  judge_metadata_signature: 'hmac-sha256:v2:8245b37c19ca4a8afb3c7462974ab1c1f46f2038a523de80a16d5db6850f4520'
- key: plugins/infrastructure/clients/http.py:R5:AuditedHTTPClient:_execute_request:fp=6f35bfed0cfea005
  owner: john
  reason: |-
    This is a legitimate Tier-3 external-call boundary in `src/elspeth/plugins/infrastructure/clients/http.py:508-519`: `_execute_request` calls `_request_with_optional_body_cap(...)`, which dispatches to `httpx`/streaming HTTP and can fail on external network/response behavior. The `isinstance` at `src/elspeth/plugins/infrastructure/clients/http.py:523` is not defending Tier-1 audit data; it distinguishes the client-owned boundary exception `HTTPResponseBodyTooLargeError` so the already-built truncated response audit payload can be recorded before re-raising. The site is drift-sensitive because the suppression is only valid while the code remains `except Exception as e:` around the external request and only reads `e.response_payload` / `e.response_data` inside `if isinstance(e, HTTPResponseBodyTooLargeError):`, followed by `_record_and_emit(...)` and bare `raise`.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-24'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-26T22:23:31.225591+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the `isinstance` is exception-subtype dispatch, not a defensive shape guard that hides a contract violation. `plugins/infrastructure/clients/http.py:73-89` defines `HTTPResponseBodyTooLargeError` and its audit fields, while `plugins/infrastructure/clients/http.py:417-443` constructs that exception after an oversized external response; at lines 568-593 the handler extracts those fields only for that subtype, records the failure, and bare-raises every exception. The decorator is inapplicable because the tested subject is the caught exception `e`, not data rooted in an external function parameter.
  judge_signature_version: 2
  scope_fingerprint: 5692a00bbac4b3b3eddb1aea6f84a451a9c9bb8dfedfd675b8c94955d017216e
  judge_transport: codex_cli
  ast_path: 'body[29]/body[17]/body[10]/handlers[0]/body[3]/test'
  judge_metadata_signature: 'hmac-sha256:v2:e32f00d1cf88eced63dbc28ba523c153bf30ccd4903fa799820aeb30eb39812a'
- key: plugins/infrastructure/clients/http.py:R5:AuditedHTTPClient:_follow_redirects_safe:fp=4fd8e49bcaf54146
  owner: john
  reason: |-
    This is a prescribed exception-type narrowing at an external HTTP boundary, not defensive handling of Tier-1 audit data. In `plugins/infrastructure/clients/http.py:890`, `_follow_redirects_safe(...)` is the redirect-following helper for `httpx.Response` hops; the flagged site at `plugins/infrastructure/clients/http.py:1037` uses `isinstance(hop_err, HTTPResponseBodyTooLargeError)` solely to decide whether the caught hop failure has the type-specific `response_payload` field: `hop_response_payload = hop_err.response_payload if isinstance(hop_err, HTTPResponseBodyTooLargeError) else None`. That matches the prescribed form for caught exceptions: narrow to the specific exception type before reading exception-specific attributes. A future drift that applies this `isinstance()` to Landscape/audit rows, internal DTOs, or already-validated pipeline data would invalidate this suppression.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-24'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-26T22:23:47.190988+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM. The visible code catches an arbitrary redirect-hop exception, narrows it with `isinstance(hop_err, HTTPResponseBodyTooLargeError)` solely before accessing that exception type’s `response_payload`, records the failed hop, and then re-raises the original exception; it neither hides a contract violation nor silently recovers. The rationale specifically addresses the flagged R5 pattern and is consistent with the code, and the decorator alternative does not apply because the checked subject is a caught exception rather than data rooted in an external function parameter.
  judge_signature_version: 2
  scope_fingerprint: ccef44ed32515b836157c9965d4a8c63b52d93f1c1aa402f1065b594a2032d32
  judge_transport: codex_cli
  ast_path: 'body[29]/body[23]/body[3]/body[22]/handlers[0]/body[1]/value/test'
  judge_metadata_signature: 'hmac-sha256:v2:17b18b18a43caffea8327deb09fa5b97062b379e7bd66b237e52bfe914d92753'
- key: plugins/infrastructure/clients/http.py:R5:AuditedHTTPClient:request_ssrf_safe:fp=4a38fb85da000f2e
  owner: john
  reason: |-
    `plugins/infrastructure/clients/http.py:803` is a prescribed exception-type discrimination at an external HTTP boundary, not defensive probing of Tier-1 audit data. In `AuditedHTTPClient.request_ssrf_safe`, the `try` block performs the SSRF-pinned outbound request (`response = self._send_ssrf_safe_request(...)`) and redirect validation, then the `except Exception as e:` path records the failed external call before re-raising. The flagged code, `if isinstance(e, HTTPResponseBodyTooLargeError):`, selects the one typed boundary exception that carries a deliberately bounded audit payload (`response_payload = e.response_payload`, `error_response_data = e.response_data`) for an over-large external response body. That exception type is defined in the same file with those attributes, and is raised by `_consume_capped_response` only after building a truncated response payload for the external response body cap. If this site drifts away from `except Exception as e:` handling the SSRF-safe external request, or stops using the specific `HTTPResponseBodyTooLargeError` attributes to preserve bounded audit evidence before `raise`, this suppression should become stale.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-24'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-26T22:23:58.473568+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the flagged `isinstance` is exception-type discrimination within a broad error path, not a defensive shape check that masks a contract violation. The visible code uses the specific `HTTPResponseBodyTooLargeError` branch to extract its bounded response evidence, records the failed external call, emits telemetry after audit, and then re-raises the original exception; it neither coerces nor silently recovers. Although R5 is decorator-suppressible, the checked subject is the caught exception `e`, not data rooted in an external function parameter, so the structural decorator test is not satisfied.
  judge_signature_version: 2
  scope_fingerprint: 1e136a63713847e83cb30685661dbc411931364e39ba16e84ccfede6c540bb26
  judge_transport: codex_cli
  ast_path: 'body[29]/body[21]/body[13]/handlers[0]/body[3]/test'
  judge_metadata_signature: 'hmac-sha256:v2:3af216774ef60d2f30a81a71b13dd27f57afac20e4123e7fcaa31dca69cb3864'
- key: plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=153b600f7b824428
  owner: john
  reason: |-
    _extract_usage_from_provider_response (llm.py:196-219) normalizes provider token-usage payloads at the documented Tier-3 boundary (docstring 197-202: providers may return an SDK object, a mapping, or a partial aggregate-only payload). The flagged usage.get('prompt_tokens') at line 208 sits in the Mapping branch and supplies NO default — an absent counter flows into TokenUsage.from_dict (219), which records the explicit None absence rather than a fabricated zero. That is the prescribed recorded-absence pattern for provider-authored optional fields: no ELSPETH contract guarantees the key, absence is an expected provider variation, and nothing downstream treats None as 0. Invalid if a default is ever supplied here, if TokenUsage.from_dict stops preserving None, or if usage stops being external provider data.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T15:12:50.035480+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.82
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM. This is a genuine Tier-3 boundary: provider token-usage payloads (LiteLLM/SDK responses) are externally-sourced data whose shape ELSPETH does not contract, and the docstring plainly documents that providers vary between SDK objects, mappings, and partial payloads. The .get() calls supply NO default — absent keys become explicit None and flow to TokenUsage.from_dict, which is the policy-prescribed recorded-absence pattern (not fabrication: None stays None, not coerced to 0). The function is a decorator candidate on shape, but the parameter is a single `usage: Any` that is not clearly a dict subject (the Mapping branch is one of two shapes handled), and R1/R5 on both branches would need coverage; a per-line entry here is defensible, though a @trust_boundary(source_param='usage') refactor would also be valid.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: openrouter
  ast_path: 'body[36]/body[2]/body[0]/value/values[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b7de49f089873753a05729259d959f0b2075ab04edb2fe239e8b1fa4369bdbca'
- key: plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=3ce6385129d45fcc
  owner: john
  reason: |-
    In _extract_usage_from_provider_response (llm.py:211), usage.get('total_tokens') reads the external provider's usage payload (Tier 3 — provider-authored, possibly a partial aggregate-only object). Absence maps to None, not a fabricated 0, and TokenUsage.from_dict normalizes downstream, keeping honest absence distinguishable from a reported count per the fabrication decision test — the prescribed Tier-3 record-absence-as-None pattern, not defensive access on a typed first-party object.

    The @trust_boundary decorator nudge does NOT apply here, even though the three structural conditions are met (external `usage` parameter, rule R1, subject rooted at `usage`): the decorator is structurally INFEASIBLE for this function. TBE1 (severity error, elspeth-lints trust_boundary/tests) mandates that every @trust_boundary carry a test_ref pointing to a pytest test whose body "contains a raising assertion for malformed-input rejection and calls the decorated function through source_param." This function provably NEVER raises on malformed `usage` — its documented contract (docstring at llm.py:200-202: "missing and non-int fields become explicit None rather than raising") is record-absence-as-None, the correct Tier-3 treatment. No raising-shape test can be written for a function that does not raise, so a @trust_boundary here could never satisfy TBE1 and would itself become a build-failing violation. The per-line allowlist entry is therefore the only valid suppression mechanism for this finding; its five sibling per-line entries (R1/R2 on the same `usage` parameter, llm.py:209-217) are already ACCEPTED under this same campaign, confirming per-line as the established and consistent treatment for this boundary. Legitimate R1 suppression; the decorator is structurally unavailable, not merely unpreferred.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:09:08.747135+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.82
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM: `usage` is the provider-returned payload (Tier-3 external data per the Quick Reference's 'Transform on external calls' clause), and the `.get()` calls record absence as explicit `None` rather than fabricating 0 — the prescribed record-absence-as-None Tier-3 boundary pattern. The docstring at lines 197-202 makes the non-raising contract explicit, and `TokenUsage.from_dict` handles normalization downstream. The decorator nudge would structurally apply by the three-question test, but the agent correctly notes this function's contract is non-raising by design (honest absence recording), which is incompatible with TBE1's raising-shape test requirement; per-line suppression is the appropriate mechanism here.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: openrouter
  ast_path: 'body[36]/body[2]/body[0]/value/values[2]'
  judge_metadata_signature: 'hmac-sha256:v2:5f2ab80c8f053ca0724190bfa0ce6e0e4858518c7e4c3de0074de20aecbb42f7'
- key: plugins/infrastructure/clients/llm.py:R1:_extract_usage_from_provider_response:fp=54f676db05cacf08
  owner: john
  reason: |-
    In _extract_usage_from_provider_response (llm.py:210), usage.get('completion_tokens') reads from the external provider's usage mapping (Tier 3). The provider may omit this field; the code maps absence to None rather than a fabricated default and defers normalization to TokenUsage.from_dict, preserving the distinction between 'not reported' and a real value per the fabrication test. This is meaning-preserving record-absence-as-None at an external boundary, not defensive .get on our own typed data. Legitimate R1 suppression.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:09:01.603015+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.9
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM: `usage` is the provider's usage payload — external Tier-3 data whose shape is not guaranteed by any first-party contract, even though our code returns the wrapped object. The `.get()` calls map absent fields to `None` (record-absence-as-None), passing them through to `TokenUsage.from_dict` for normalization — meaning-preserving, non-fabricating boundary handling exactly as the Tier-3 quick reference prescribes for transform-on-external-calls. The docstring explicitly names this as the Tier-3 boundary, and the code matches: no default substitution, no silent swallow of a bad value, just explicit absence recording.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: openrouter
  ast_path: 'body[36]/body[2]/body[0]/value/values[1]'
  judge_metadata_signature: 'hmac-sha256:v2:1223e9e509f2841aa6f0b12cef2a9781b384b4d496e9a9c77eb2ac5f23813f4b'
- key: plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=0ef9c6bbb5ce37ea
  owner: john
  reason: |-
    In _extract_usage_from_provider_response (llm.py:196-219), the non-Mapping branch (212-217) handles provider SDK usage objects — Tier-3 external data typed usage: Any, per the docstring's normalization contract. The flagged getattr(usage, 'prompt_tokens', None) at line 214 probes the OpenAI-compatible usage attribute on an object whose shape the provider, not ELSPETH, authors; SDK variants legitimately omit per-side counters on aggregate-only responses. The None sentinel flows into TokenUsage.from_dict (219), which records absence explicitly — never a fabricated zero, never a skipped record. R2's dynamic-probe concern targets first-party objects with knowable shapes; this is the boundary-normalization exception where the attribute set is genuinely provider-variable. Invalid if usage becomes a first-party typed model or absence stops being recorded as None.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:33:36.526307+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — `usage` is a provider-authored Tier-3 SDK value with no first-party ELSPETH attribute contract, and the visible docstring explicitly identifies this function as its normalization boundary. The sentinel-defaulted `getattr` reads the needed provider-variable field once, while `TokenUsage.from_dict` receives `None` for absence rather than a fabricated zero, matching the prescribed external-object parse pattern. R2 is not suppressible by `@trust_boundary`, so the structural decorator alternative does not apply.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: codex_cli
  ast_path: 'body[39]/body[2]/orelse[0]/value/values[0]'
  judge_metadata_signature: 'hmac-sha256:v2:c43c66a357e369b6bc47d782445782bbbbef3bb0eb09e0626fe463bb518f0d79'
- key: plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=0a4e14a820f0ce1b
  owner: john
  reason: |-
    Same Tier-3 provider-usage normalization boundary (llm.py:196-219, docstring 197-202). The flagged getattr(usage, 'total_tokens', None) at line 216 reads the aggregate counter from a provider SDK object in the non-Mapping branch; providers that report only per-side counts legitimately omit total_tokens, and some report ONLY totals — both are expected external variations, not first-party contract violations. The None default is the recorded-absence sentinel consumed by TokenUsage.from_dict (219), which preserves it as an explicit unknown rather than fabricating arithmetic (no total is ever synthesized from the parts). The probe is data-only attribute access on external data — precisely the boundary case R2's prescribed remediation (type the object, use direct access) cannot serve, because no single shape exists to type. Invalid if totals are ever computed as a fallback or the function stops normalizing external provider payloads.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:34:00.547328+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the flagged `getattr` reads an optional field from a provider-controlled SDK object whose shape is not guaranteed by a first-party ELSPETH contract. The excerpt independently confirms that `_extract_usage_from_provider_response` is a Tier-3 normalization boundary and copies the extracted fields into `TokenUsage.from_dict`; the `None` default records absence as unknown rather than fabricating a total. R2's fixed-contract concern therefore does not apply, and the decorator alternative is unavailable because R2 is not suppressible by `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: codex_cli
  ast_path: 'body[39]/body[2]/orelse[0]/value/values[2]'
  judge_metadata_signature: 'hmac-sha256:v2:821db302019099ab73edd64dcd5897f9ca55cc3464261afbb8b89b1382152516'
- key: plugins/infrastructure/clients/llm.py:R2:_extract_usage_from_provider_response:fp=d9296b94f96c253c
  owner: john
  reason: |-
    In _extract_usage_from_provider_response (llm.py:216), getattr(usage, "completion_tokens", None) reads a provider SDK usage object that is Tier-3 external data (parameter typed `usage: Any`). The existing rationale still accurately describes the code: this is the documented Tier-3 normalization boundary, and the getattr-with-None preserves honest absence (None when the provider omits the field) instead of fabricating zero, with TokenUsage.from_dict doing the single normalization. The non-Mapping branch handles SDK objects that expose attributes rather than keys. Not defensive access on our typed data, not fabrication — absence flows through as None.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:33:47.753199+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the flagged `getattr` reads `completion_tokens` from a provider-controlled SDK object whose runtime shape is not guaranteed by an ELSPETH contract, so this is Tier-3 external-origin data rather than defensive access under a fixed contract. The visible function explicitly normalizes either mapping- or attribute-shaped provider usage, preserves an omitted field as `None`, and passes the extracted values into `TokenUsage.from_dict`, which is consistent with the prescribed parse-and-copy boundary pattern and does not fabricate zero. R2 is not suppressible by `@trust_boundary`, so the structural decorator alternative does not apply.
  judge_signature_version: 2
  scope_fingerprint: a0a988adb8e41118c21f649275d3ce685b029e8b08ae9ccc34bcb0025bc47064
  judge_transport: codex_cli
  ast_path: 'body[39]/body[2]/orelse[0]/value/values[1]'
  judge_metadata_signature: 'hmac-sha256:v2:e99376372632376735ebcbf873dfd87dcc011accda6e1b025d6bc1a078731cd9'
- key: plugins/infrastructure/clients/llm.py:R4:AuditedLLMClient:_emit_telemetry_after_audit:fp=bbfdc3112d58d4a4
  owner: john
  reason: |-
    PRESCRIBED FORM — telemetry best-effort swallow under the audit>telemetry>logger primacy order; Tier-1 and programming errors re-raise first. In AuditedLLMClient._emit_telemetry_after_audit (plugins/infrastructure/clients/llm.py:337), `except Exception as tel_err` is the LAST of three clauses wrapping self._telemetry_emit(ExternalCallCompleted(...)): line 333-334 `except contract_errors.TIER_1_ERRORS: raise` (system bugs / audit integrity violations crash), line 335-336 `except (TypeError, AttributeError, KeyError, NameError): raise` (programming errors crash), and only THEN the broad catch for genuine telemetry-transport failures. The method name and comment state the doctrine: the audit record is already persisted (Landscape is the authoritative legal record), and telemetry is best-effort per the audit>telemetry>logger primacy order, so a telemetry emission failure must not corrupt or crash the already-audited call flow. This is not a silent failure: the broad catch emits logger.warning with error, error_type, run_id, state_id, operation_id and exc_info — the sanctioned last-resort logging channel precisely because the telemetry subsystem itself is the broken component here (CLAUDE.md permits logger for telemetry-system failures). "No silent failures" is honored: the failure is acknowledged on the logger, not dropped. The broad catch is correct because a transport failure can surface as many exception types, while the two re-raise clauses ensure our-code bugs and Tier-1 violations still crash. R4 flags `except Exception` generically. Per-line allowlist is the prescribed mechanism.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T12:10:35.501905+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.85
  judge_rationale: |-
    PRESCRIBED FORM — this is the sanctioned telemetry best-effort pattern under the audit>telemetry>logger primacy order. The visible code shows the correct layered structure: TIER_1_ERRORS re-raise (audit integrity preserved), programming-error types (TypeError/AttributeError/KeyError/NameError) re-raise (our-code bugs crash), and only then the broad catch for genuine telemetry-transport failures, which are logged with full context via structured logger.warning including exc_info. The audit record is already persisted before this method runs, so telemetry failure must not corrupt the audited call flow, and logger is the policy-sanctioned last-resort channel when the telemetry subsystem itself is broken. The rationale correctly identifies the R4 concern and shows how the two prior narrow re-raise clauses prevent bug-suppression while permitting the transport-failure catch.
  judge_signature_version: 2
  scope_fingerprint: 734ab788c50e2c797bd58b4188ea6463c5c5d169577920f567ba0ad45bcffecf
  judge_transport: openrouter
  ast_path: 'body[38]/body[2]/body[1]/handlers[2]'
  judge_metadata_signature: 'hmac-sha256:v2:90bec995a0daa7d4f3f6cb11bc0bac5d2d6927b6dffd5ddb69d6a664260662a0'
- key: plugins/infrastructure/clients/retrieval/connection.py:R6:_host_url:fp=e0a7778bdc527de0
  owner: john
  reason: |-
    ipaddress.ip_address() raises ValueError on a non-IP-literal host; the except falls back to using the Tier-3 host string as a hostname (IPv6 bracket normalization applies only to successfully parsed IPs). Explicit branch selection on external input, not error suppression -- host shape was already validated above (rejects scheme/path/query).
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:34:28.396744+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the caught ValueError is the documented discriminator between an IP literal and a hostname, not a swallowed failure under a fixed contract. The Tier-3 host is preserved unchanged on the hostname branch, while only successfully parsed IP literals receive normalization; the visible precheck rejects URL syntax before this branch. R6 therefore does not describe silent recovery, coercion, or fabrication at this site.
  judge_signature_version: 2
  scope_fingerprint: c61f192f8181bd41acf992d1f25a4d21e577a1776bc86b23f39411d19aa2ad7b
  judge_transport: codex_cli
  ast_path: 'body[14]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b92aebb9d1004a40ded7b5976b123d1f34b62ecd587bb183e852a63e997776bb'
- key: plugins/infrastructure/clients/retrieval/connection.py:R6:_is_loopback_host:fp=413fe7a0e494514b
  owner: john
  reason: |-
    ipaddress.ip_address() raises ValueError when the Tier-3 host string is not an IP literal (e.g. a hostname). The except returns False = 'not a loopback IP' -- an explicit, meaning-preserving classification, not a swallowed error. The actual SSRF allow/deny decision is made by the caller (_validate_chroma_http_target).
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T10:34:42.709333+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the caught ValueError is the documented negative branch of IP-literal discrimination, and returning False explicitly means the host is not a recognized loopback literal rather than concealing a failed integrity check. The named control, _validate_chroma_http_target, is visible at lines 64–77 and always routes the host through validate_url_for_ssrf after using this classification solely to select allowed loopback ranges; its SSRF and network failures are converted into an explicit chained ValueError. The excerpt therefore verifies the control-location claim and shows that the exception does not bypass the actual SSRF allow/deny gate. R6 is not eligible for the trust-boundary decorator.
  judge_signature_version: 2
  scope_fingerprint: a04a2c257fb9c62ba6a661bc2a1875d644d5833ced11b1131b41555986d93554
  judge_transport: codex_cli
  ast_path: 'body[13]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:eb95a2b40ac7c2f1dfdc598ad4f8de023a14ba49a934abe12d2fd1ba37064ad5'
- key: plugins/sinks/database_sink.py:R1:DatabaseSink:get_post_call_hints:fp=c946686ebc7efd22
  owner: john
  reason: |-
    This is a composer-assistance hook, not a Tier-1 audit read or sink runtime contract. The base contract says `get_post_call_hints` is called after composer mutations and plugins inspect only their own config to return optional advisory hints: `src/elspeth/plugins/infrastructure/base.py:832` says "Return forward-looking hints conditional on the just-set config", and `src/elspeth/plugins/infrastructure/base.py:834-840` says it is called after `set_source`, `upsert_node`, and `patch_*_options` to return short operator/LLM imperatives. The concrete site reads a sparse user-authored config snapshot and emits a hint only when the optional destructive mode is present: `src/elspeth/plugins/sinks/database_sink.py:777` is `if config_snapshot.get("if_exists") == "replace":`, followed by the warning at `src/elspeth/plugins/sinks/database_sink.py:779` that `replace` "DROPS and recreates the target table". Future drift that made `config_snapshot` a fully reified `DatabaseSinkConfig`, a Landscape/audit record, or a required invariant-bearing runtime DTO would invalidate this suppression; at this site it is an optional Tier-3 composer/config boundary probe for advisory UX text.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-09-15'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-06-17T16:17:04.323511+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.82
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM. The parameter is typed `Mapping[str, object]` — a sparse composer/user-authored config snapshot passed to an advisory hook, not a first-party reified DTO with a guaranteed shape. The `if_exists` key is genuinely optional (the hook emits a hint only when destructive 'replace' mode is present), so `.get(...) == "replace"` is the meaning-preserving Tier-3 boundary read: absent key correctly yields no hint, present-and-'replace' yields the warning. Direct subscript would crash the advisory path on perfectly valid sparse configs. No fabrication, no silent recovery of a value that should exist.
  judge_signature_version: 2
  scope_fingerprint: 8660f6017d7ee3e60545b448eac9423bbf1ba83c99d66185d8dc677b4ef3f270
  judge_transport: openrouter
  ast_path: 'body[27]/body[21]/body[1]/test/left'
  judge_metadata_signature: 'hmac-sha256:v2:fcd25c8cef1e623bbea6cc911b81907118089340374b7c970f67efd87429965c'
- key: plugins/transforms/blob_fetch.py:R1:_normalized_content_type:fp=5248eddc9cf3f217
  owner: john
  reason: |-
    `plugins/transforms/blob_fetch.py:202-203` is a Tier-3 HTTP response boundary helper, not a Tier-1 audit-data read: `_normalized_content_type(response: httpx.Response) -> str` reads an external server header with `response.headers.get("content-type", "")`, then immediately coerces and normalizes it via `.split(";", 1)[0].strip().lower()`. The `.get()` default is the prescribed boundary behavior for absent external `Content-Type`, producing an empty normalized media type rather than treating a remote header omission as internal corruption.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-10-06'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-07-08T13:12:01.571243+00:00'
  judge_model: anthropic/claude-4.7-opus-20260416
  judge_policy_hash: 'sha256:08052cb8f2c263c39dc61336444e6f2b2859292e283a902510827744f18d68da'
  judge_confidence: 0.78
  judge_rationale: |-
    RULE MISFIRES / PRESCRIBED FORM. The `.get()` targets an httpx.Response header dict — a Tier-3 external HTTP boundary where the Content-Type header may legitimately be absent. Recording absence as an empty normalized media type is meaning-preserving coercion at the boundary, not fabrication of a first-party field (empty string flows through the same normalization pipeline and downstream logic treats it as 'unknown media type'). The function does not take the external data as a named parameter suitable for @trust_boundary source_param routing in the R1/R5 pattern the decorator was designed for (the external data is nested inside response.headers), so the per-line suppression is appropriate here.
  judge_signature_version: 2
  scope_fingerprint: f70c7ee20f7615c362432398f8df3d83c016b46e8c8e74ec1c79f71a7ed85db5
  judge_transport: openrouter
  ast_path: 'body[31]/body[0]/value/func/value/func/value/value/func/value/args[1]'
  judge_metadata_signature: 'hmac-sha256:v2:3be85c41c0705fdd8036e641fa50b16b2037b687286b56d65a81f86660723fb2'
- key: plugins/infrastructure/base.py:R3:BaseTransform:__init_subclass__:fp=41338e4f0ac82c1a
  owner: john
  reason: |-
    hasattr(type(declared_schema), '__get__') is the descriptor-protocol test on a value read from cls.__dict__ of a class ELSPETH itself is creating (Tier 1, class-creation time). It does not probe for a missing attribute on data; it distinguishes the two authored shapes a subclass body can legally carry for input_schema — a plain schema value (moved into the property's backing store) versus a descriptor that deliberately overrides the property (left alone). Any descriptor kind (property, function, cached_property) is a legitimate override, so a nominal isinstance against one class would wrongly reject some of them; CPython itself decides descriptor-ness by exactly this type-level __get__ lookup. The site is classified approved-introspection in config/cicd/masquerade_baseline.yaml.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:29:17.521920+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the flagged `hasattr` is not defensively probing data or deciding whether an object satisfies an ELSPETH contract; it is the runtime descriptor-protocol test on the type of a value explicitly obtained from the newly created class's own `__dict__`. The visible code corroborates the site-specific rationale: plain class-body schema values are redirected to the backing store, while objects whose types implement `__get__` are deliberately preserved as descriptor overrides. Because descriptor behavior is defined by that type-level protocol and may legitimately be implemented by multiple descriptor kinds, this is approved semantic introspection rather than the masquerading or missing-attribute pattern R3 targets.
  judge_signature_version: 2
  scope_fingerprint: 76ca158a81b8a4bd5054ba34d6b611426accc3c29aa81aee64d27eb178ae2863
  judge_transport: codex_cli
  ast_path: 'body[17]/body[47]/body[1]/body[1]/test/operand'
  judge_metadata_signature: 'hmac-sha256:v2:d0fe0bb772acac1b4873fb54aedb2d78f37a990f7cd5df5b9fb29eb922c8dca5'
- key: plugins/infrastructure/base.py:R5:BaseTransform:__init_subclass__:fp=a7e867a0933c542d
  owner: john
  reason: |-
    isinstance(resolved, property) is a nominal check against the concrete builtin property class on the result of inspect.getattr_static over ELSPETH's own class hierarchy (Tier 1). It is itself the anti-shadowing guard for elspeth-d6eeb3a71d: if a mixin ahead of BaseTransform in the MRO shadows input_schema with a non-property, the check raises TypeError at class creation instead of letting undemoted models reach every row. The check is not defensive re-typing of trusted data — it is the assertion that fails closed; removing it deletes the control (masquerade baseline: 'do not remove this check').
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:29:20.708150+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM: the nominal `isinstance(resolved, property)` check immediately raises an informative `TypeError` when ELSPETH-owned class construction violates the required descriptor invariant; it neither coerces nor hides the defect. The visible `inspect.getattr_static(cls, "input_schema")` lookup and comments establish that this is an anti-shadowing assertion over the class MRO, ensuring the property remains effective and preventing undemoted models from silently reaching row processing. This is the policy-prescribed offensive check-and-crash form, not defensive retyping of trusted pipeline data.
  judge_signature_version: 2
  scope_fingerprint: 76ca158a81b8a4bd5054ba34d6b611426accc3c29aa81aee64d27eb178ae2863
  judge_transport: codex_cli
  ast_path: 'body[17]/body[47]/body[3]/test/operand'
  judge_metadata_signature: 'hmac-sha256:v2:561087e1b28ab927fbd09c018020d012e048e269ff33ff083e48be3070df5116'
- key: plugins/infrastructure/base.py:R5:BaseTransform:_reject_fixed_schema_omitting_consumed_fields:fp=74f188f87570c3af
  owner: john
  reason: |-
    isinstance(value, str) dispatches over the authored config VALUE of a column-naming option while scanning self.config.items(). The keys are selected by name (is_column_naming_config_option: field/fields/group_by/*_field/*_fields) across every registered plugin's config model, and those options are legitimately typed str (url_field), list[str] (keyword_filter.fields) or a str | list[str] union (group_by) depending on the model — the base class cannot name a single owned type per key. The check is a union dispatch over already-validated Tier-2 config values (the method runs right after config validation), not a shape guard hiding a contract violation; a value of another type is an option that names no column, and is skipped by design rather than crashed on.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:29:34.510111+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    RULE MISFIRES — the checks discriminate among the declared, heterogeneous value shapes of already-validated configuration options; they do not defensively revalidate a single fixed contract or promote Tier-3 data. The visible scan selects conventionally named options across multiple plugin models, where string, string-sequence, and unrelated validated value types legitimately coexist, and only string values represent column names. Because the subject is rooted in `self.config`, not an external parameter of this function, the structural `@trust_boundary` alternative does not apply.
  judge_signature_version: 2
  scope_fingerprint: 0362124b3be8ead1dcf0b31b2ff1a01594dc45b393d53b6c52177699feb61a5c
  judge_transport: codex_cli
  ast_path: 'body[17]/body[50]/body[4]/body[1]/value/test'
  judge_metadata_signature: 'hmac-sha256:v2:a1fbc52574f2eeacac219d7c8f206ddb249cd1ff528e5496d492829293f6f799'
- key: plugins/infrastructure/clients/http.py:R5:AuditedHTTPClient:_record_call:fp=d5da9ecadd68d01b
  owner: john
  reason: |-
    isinstance(error, HTTPCallError) is a nominal check against HTTPCallError, a concrete dataclass ELSPETH defines in elspeth.contracts.call_data, on the error parameter typed CallPayload | None — a closed union of owned audit payload classes (Tier 1). _record_call is the shared audit persistence seam for every call kind; only the HTTP error variant carries a URL-bearing message that must be sanitized before it reaches the Landscape, so the branch selects the one member of the owned union that needs rewriting and passes every other variant through untouched. This is ADR-032 nominal typing of an owned type, not a shape guard over external data.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:29:46.685148+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the nominal `isinstance(error, HTTPCallError)` check discriminates one concrete member of the declared `CallPayload | None` union so its URL-bearing message can be sanitized before audit persistence; it neither revalidates a supposedly uniform fixed contract nor hides a wrong type. The visible code reconstructs that owned variant explicitly and passes all other declared variants through unchanged, with no silent suppression, fallback, or fabrication. This is not a Tier-3 parameter boundary eligible for `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: cf04b222ff5e3ebe7f5ea87ab5351e2af245657263b0f04e6fde5636c7b9ac24
  judge_transport: codex_cli
  ast_path: 'body[29]/body[5]/body[1]/test'
  judge_metadata_signature: 'hmac-sha256:v2:e95fd6953ad728caa17ef8250f6b10aba2811a3cf8083e4b2b7f2e9e8cb0d308'
- key: plugins/infrastructure/rasterize/worker.py:R5:rasterize_document:fp=9a8d3f80601997ef
  owner: john
  reason: |-
    isinstance(outcome, RenderedPage) discriminates the closed owned union RenderedPage | RefusedPage that _render_page returns (both frozen dataclasses defined in elspeth.plugins.infrastructure.rasterize.protocol, Tier 1). The nominal check against the concrete owned class is ADR-032's prescribed internal-boundary form: the class object is the credential, so no pypdfium2 object or duck-typed impostor can be routed into the rendered tuple. It is not a shape guard over the external PDF — that parse already happened inside _render_page, which converts every pdfium failure into a RefusedPage value. Removing the check would require collapsing the union into a single tagged type for one call site, which is churn with no gain in integrity.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:29:58.351840+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    RULE MISFIRES — the visible return annotation and branches establish that `_render_page` produces a closed owned union, and the nominal `isinstance(outcome, RenderedPage)` at worker.py:89 discriminates its two legitimate variants rather than defensively masking a contract violation. The check does not coerce, fabricate, suppress an exception, or admit a duck-typed external object; each valid variant is routed into its corresponding response tuple. The subject is the owned `_render_page` outcome rather than an external parameter, so the structural `@trust_boundary` alternative does not apply.
  judge_signature_version: 2
  scope_fingerprint: 52e9ec681dba6a3205364dd0fa5f49f8a2306f412ae65390b521d6e21b01320d
  judge_transport: codex_cli
  ast_path: 'body[14]/body[4]/body[5]/body[2]/test'
  judge_metadata_signature: 'hmac-sha256:v2:7039aaf0a6c5912dd6368dd9c2822c622b7d3680d54e11d98e3d45d47d63ccc4'
- key: plugins/infrastructure/templates.py:R5:_DefiniteBindingAnalyzer:_scan_assignment_target:fp=43591960608d78c6
  owner: john
  reason: |-
    isinstance(target, nodes.NSRef) is a nominal check against jinja2's concrete NSRef node class on an Assign/AssignBlock target that jinja2's parser types as Name | NSRef | Tuple | ... (`{% set ns.attr = v %}` produces NSRef). The analyzer runs on jinja2's parsed AST of an operator-authored template; the class dispatch is the same mechanism jinja2's NodeVisitor uses for every other node kind in this analyzer (visit_<Class>), applied here to an assignment TARGET, which NodeVisitor's visit() would otherwise treat as a load and mis-record. An NSRef target is a load of the namespace name (`ns` must already be bound), so the check is what turns `{% set ns.a = 1 %}` without a prior `{% set ns = namespace() %}` into a preflight-detected unbound name (test id namespace-unbound-nsref).
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:30:06.299305+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — this `isinstance` is nominal semantic dispatch over Jinja's parser-produced assignment-target variants, not a defensive revalidation that hides a contract violation. The code treats `nodes.NSRef` specially because namespace-attribute assignment has different binding semantics: it records the namespace name as used and flags it when unbound, while `_stored_names` separately handles stored `nodes.Name` targets. The rationale is site-specific and consistent with the visible code, and the finding is not a function-scoped Tier-3 shape guard suitable for `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: c4ed806fafcbd61ee54459c4c1c3c1fc7fc2523a9364135103ee915b4d8c453f
  judge_transport: codex_cli
  ast_path: 'body[10]/body[21]/body[0]/test/values[0]'
  judge_metadata_signature: 'hmac-sha256:v2:7bbdef2b1506ddbf12f7c693706252044818634a6af9190a8d5e3033205c020f'
- key: plugins/infrastructure/templates.py:R5:_stored_names:fp=6bdc70f1477d6c19
  owner: john
  reason: |-
    isinstance(target, nodes.Name) is a nominal check against jinja2's concrete Name node class on an assignment target that may be a bare Name or a compound target (Tuple/List of Names for `{% for a, b in pairs %}`). jinja2's Node.find_all deliberately excludes the receiver node itself, so the bare-Name case must be handled by this check before falling through to find_all(nodes.Name) for compound targets; every yielded node is then filtered on ctx in {'param','store'} so a load-context Name inside a target expression is never recorded as a binding. The value is jinja2's parsed AST (not ELSPETH-owned); this is class dispatch on a third-party AST, the same form the visitor uses throughout, not a shape guard hiding a contract violation.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:30:04.313880+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the `isinstance(target, nodes.Name)` expression performs nominal variant dispatch over Jinja2 AST nodes, not defensive revalidation or recovery from an ELSPETH contract violation. The visible fallback searches descendant `nodes.Name` instances because `find_all` excludes the receiver, while both paths retain only `param` or `store` contexts and neither coerces, defaults, nor suppresses failure. This is not a Tier-3 promotion guard suitable for `@trust_boundary`; it is necessary AST traversal logic over a third-party concrete node hierarchy.
  judge_signature_version: 2
  scope_fingerprint: b921f69509401d3d86cb61a3075de0e23d18c5e164e3770ccaf92ad63837b381
  judge_transport: codex_cli
  ast_path: 'body[11]/body[0]/test/values[0]'
  judge_metadata_signature: 'hmac-sha256:v2:fe8e550ece30e757c26452d8837854f98912a6d0ae8bada96963f21b52a4a0d4'
- key: plugins/llm/model_catalog.py:R6:prime_openrouter_catalog_from_live:fp=1d5dea22850cb4a4
  owner: john
  reason: |-
    Identical structure to the transport-error handler above, for the narrower failure of a 200 response whose body is not JSON (a CDN or outage page served with HTTP 200): the ValueError is bound as `exc` and reported on the structured log channel as "openrouter_catalog_prime_decode_error" with url, exc_class and action; _clear_live_openrouter_catalog() prevents a stale live snapshot surviving a failed re-prime; the False return is consumed and re-logged by web/app.py::_boot_prime_openrouter_catalog; and the resulting bundled fallback is recorded in the Landscape `runs` table via openrouter_catalog_source so the audit trail names which catalog was in force. The catch is deliberately narrow — response.json() is the only statement inside the try — so it cannot mask an unrelated error, and it must not raise: the caller is the boot lifespan and this function's contract is that a malformed upstream body degrades to the bundled catalog rather than taking the web server down. R6 fires only because that explicit outcome is a documented bool.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:36:49.584110+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — R6's premise is false because the narrow ValueError handler at plugins/llm/model_catalog.py:406-416 returns the function's documented explicit failure result, False, after clearing the live snapshot and recording structured failure context. The named control is verified: web/app.py:445-456 consumes that boolean and records the boot-prime failure, while model_catalog.py:221-240 and web/app.py:727-730 select and propagate the bundled catalog snapshot identity. Run creation then writes that identity through engine/orchestrator/run_lifecycle.py:208-218, so the Landscape records which catalog governed subsequent runs rather than relying solely on the warning log. The decorator alternative does not apply because the finding is R6, not R1 or R5.
  judge_signature_version: 2
  scope_fingerprint: 58b250dc1c77e3c0bc371ab0d06efde379e85c021237bdd9d30420a1a27d5a57
  judge_transport: codex_cli
  ast_path: 'body[34]/body[4]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:10defaeae67843fefb968832f326c8393ee1be927f7cd74f30d8b7f6b92da7a9'
- key: plugins/llm/model_catalog.py:R6:prime_openrouter_catalog_from_live:fp=a8f2d0a78bb006d1
  owner: john
  reason: |-
    The httpx.RequestError is not swallowed — it is bound as `exc`, its class is emitted on the structured log channel (_slog.warning "openrouter_catalog_prime_transport_error" with url, exc_class and the action taken), and its effect is carried structurally: _clear_live_openrouter_catalog() runs so a failed re-prime cannot leave a stale live snapshot alive behind a "falling back" warning, and the False return is consumed by the one caller, web/app.py::_boot_prime_openrouter_catalog, which logs openrouter_catalog_boot_prime_failed with the probe latency. The fallback also reaches the audit trail, not just the logs: read_openrouter_catalog_snapshot_id then reports source="bundled", which the run-create writer persists to the Landscape `runs` table (openrouter_catalog_source / openrouter_catalog_sha256), so an auditor can reconstruct that a bundled catalog — not the live one — blessed every decision made in that process. Nothing about this failure is silent on any channel. Raising instead is the wrong outcome and is the specific thing this function is designed against: the caller is the FastAPI boot lifespan, so a raise crashes the web server on a transient OpenRouter outage, and the module's documented two-tier design requires the CLI and offline validate paths to work with the bundled catalog. R6 fires only because the explicit outcome is spelled as the documented `bool` return, which the rule classifies as a default value.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:38:39.569014+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    PRESCRIBED FORM: R6’s concern applies, but the narrow `RequestError` path clears the stale snapshot and returns the function’s documented explicit failure result at `plugins/llm/model_catalog.py:386`. I verified that `web/app.py::_boot_prime_openrouter_catalog` consumes that result and records the failed prime at `web/app.py:445`, so the signal is not lost. I also verified that `read_openrouter_catalog_snapshot_id` maps the cleared snapshot to `source="bundled"` and documents the corresponding Landscape run fields at `plugins/llm/model_catalog.py:221`. The decorator alternative does not apply because R6 is not one of its suppressible rules.
  judge_signature_version: 2
  scope_fingerprint: 58b250dc1c77e3c0bc371ab0d06efde379e85c021237bdd9d30420a1a27d5a57
  judge_transport: codex_cli
  ast_path: 'body[34]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:af154863cf603f482d36665eb42af78c1740df3165e084a21c9df11bfa81514d'
- key: plugins/llm/model_catalog.py:R8:_module_:fp=4dec8f01fefcd5fb
  owner: john
  reason: |-
    os.environ.setdefault("LITELLM_LOCAL_MODEL_COST_MAP", "True") is a conditional WRITE of a process environment default, not a read of a key someone forgot to set, so R8's harm model (a missing key silently answered with a default) has no instance here: nothing downstream consumes a value this call invented. The absent case is the normal case and is the whole point of the line — litellm fetches its model-cost map from raw.githubusercontent.com at import time unless this variable is set, and elspeth-c67ba40e4a forbids a deployment silently egressing to a third party it never configured, so this module (ELSPETH's single point of truth for litellm access, imported before any lazy `import litellm` in the repo) pins the bundled map. The present case is equally deliberate: an operator who sets the variable themselves keeps their value and re-enables the remote fetch. Both branches are pinned by subprocess tests that a plain assignment would break — test_module_import_forces_litellm_local_cost_map (clean env must end up "True") and test_operator_override_of_litellm_cost_map_is_preserved (an explicit "False" must survive import). Respelling this as `if "LITELLM_LOCAL_MODEL_COST_MAP" not in os.environ: os.environ[...] = "True"` is byte-for-byte identical semantics and makes no previously hidden case visible, so it would be lint cosmetics rather than a fix.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:30:58.075183+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — `os.environ.setdefault` is an intentional conditional write establishing an application-owned process default, not a defensive read that conceals a missing key under a fixed schema. The visible comments establish that absence is expected, the default prevents unconfigured third-party egress, and an operator-supplied value must remain untouched; thus both branches are deliberate rather than silent recovery from corruption. R8 is not eligible for `@trust_boundary`, and the expression is not rooted in a function parameter.
  judge_signature_version: 2
  scope_fingerprint: de8f747fbaa379345efe73d3d64200babc355b876ea4a7318a48fb2a848be8cd
  judge_transport: codex_cli
  ast_path: 'body[13]/value'
  judge_metadata_signature: 'hmac-sha256:v2:3ed03d90f6897ce347f6a0da61d1c7f9a2744dc21d3977685c8d5ba15cc6d91f'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_PinnedBundleParent:is_still_bound:fp=8b307807ed009678
  owner: john
  reason: |-
    `except OSError: return False` in _PinnedBundleParent.is_still_bound: the method re-stats the parent's entry name through the retained anchor fd and answers whether the same (st_dev, st_ino) directory is still bound there. An OSError from that stat (entry unlinked, renamed away, permission revoked) means the parent is no longer bound, so False is the fail-closed verdict. Every caller treats False as a hard stop: prepare and commit raise AuditExportBundlePreconditionError('bundle target parent changed ...') and reconcile returns SinkEffectReconcileResult.unknown(parent_replaced).
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:32:41.459405+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — `_PinnedBundleParent.is_still_bound` converts an `OSError` from the anchored `os.stat` into the fail-closed Boolean verdict `False`, rather than allowing an unverifiable parent binding to pass. The named controls are verified: `prepare_audit_export_bundle` raises on `False` at _audit_export_bundle_effects.py:820-821, `commit_audit_export_bundle` does likewise at lines 973-974 and subsequent checks, and `reconcile_audit_export_bundle` returns an explicit `unknown(parent_replaced)` result at lines 1025-1029. Thus the exception is contained without losing the integrity decision, and R6's silent-swallow premise does not describe the effective behavior.
  judge_signature_version: 2
  scope_fingerprint: 69b8970d4d69a246858901558e0d9845b181a7c67ec03f00f70b38e6afc89467
  judge_transport: codex_cli
  ast_path: 'body[76]/body[7]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:02555264b656792ec887ed867ffe13476ebac95aa6e3ad6920641c5ad02b7168'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_assert_no_symlink_components:fp=0ca6c93def6466ff
  owner: john
  reason: |-
    `except FileNotFoundError: break` in _assert_no_symlink_components stops the component walk at the first path component that does not exist yet. A create-only bundle target is expected to be absent (its parent must exist; the bundle directory itself appears by renameat2), so an absent component is the normal terminal state, and no component beyond an absent one can be a symlink. Every existing component is still lstat-checked and a symlink raises AuditExportBundlePreflightError, so the handler swallows nothing the no-symlink invariant depends on.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:30:52.633173+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the caught FileNotFoundError is an expected terminal result of walking a create-only path, not a suppressed integrity failure. The visible loop lstat-checks every existing component and raises AuditExportBundlePreflightError for any symlink; after the first absent component, no deeper component can exist beneath that path, so breaking preserves the stated no-symlink invariant. R6 is not decorator-eligible because it is neither R1 nor R5.
  judge_signature_version: 2
  scope_fingerprint: 3adc20c60a8c1e1892ba75c7c141aa91c9fc5468003a8e616ca993f8673a7487
  judge_transport: codex_cli
  ast_path: 'body[62]/body[1]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:32bceef0e84f178d3681a56451868942f4586fe818bb100e4dd84753fa65c301'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_create_private_building_tree_at:fp=3276bcffd98c8047
  owner: john
  reason: |-
    `except FileExistsError: continue` in _create_private_building_tree_at is a bounded name-allocation retry: os.mkdir of a fresh uuid4-suffixed private name collided, no directory was created, and the loop tries the next name; after 16 collisions the function raises AuditExportBundlePreconditionError. The only non-raising exit is a successfully created 0o700 directory whose (st_dev, st_ino) identity is re-verified.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:37:01.778727+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the caught FileExistsError is an explicitly handled name collision in a bounded allocation loop, not silent recovery from an integrity failure. The visible code retries with a new UUID-derived name, propagates other mkdir errors, and after 16 collisions raises AuditExportBundlePreconditionError; its only successful return follows creation and device/inode identity verification. R6 therefore does not describe the semantics at this site.
  judge_signature_version: 2
  scope_fingerprint: a801494b6a64fb0ad842f961f0b9fbbd26620677a48e4afabcafeacdf58b3a9c
  judge_transport: codex_cli
  ast_path: 'body[83]/body[1]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:f1330251cda832ba2d65cd7768d8d1767df5b80e8854cadf512dbb6f88548b5f'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_directory_fd_matches:fp=175e1fc98c5eefe5
  owner: john
  reason: |-
    `except OSError: return False` around os.listdir(directory_fd) in _directory_fd_matches: the function answers whether the directory behind the fd is exactly the planned bundle (closed name set, per-file sizes and SHA-256). A listdir failure means exactness cannot be proven; the bool is the explicit fail-closed verdict of a filesystem predicate, not a swallowed error: commit_audit_export_bundle raises AuditExportBundleCollisionError/PreconditionError on False, reconcile_audit_export_bundle classifies the effect as unknown on False, and prepare's finally-block cleanup ignores False only while it is already propagating the original exception. Nothing is ever credited as published on False.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:40:55.567020+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — the caught OSError is converted into the explicit fail-closed result of an exactness predicate, not silently treated as successful publication. The control-location claim is verified: _tree_matches_at propagates False at plugins/sinks/_audit_export_bundle_effects.py:679; commit_audit_export_bundle raises collision or precondition errors on failed exactness checks at lines 975-986 and 995-1006; and reconcile_audit_export_bundle returns unknown rather than applied at lines 1027-1040. prepare_audit_export_bundle uses the predicate for precondition enforcement at lines 853-866, while its finally-path cleanup at lines 898-908 cannot credit publication, so no integrity failure is concealed.
  judge_signature_version: 2
  scope_fingerprint: bfc5c1824413a6a5886d526ec320ff4f47eab6883bbc034ed0099094fbfa109e
  judge_transport: codex_cli
  ast_path: 'body[78]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:9cc36ef987262a8a6890970c68c691372d5c1ad8951fd7310afe098bb8868c0f'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_directory_fd_matches:fp=3b7e88d15bb87e14
  owner: john
  reason: |-
    `except OSError: return False` around os.open(entry.relative_path, O_RDONLY|O_NOFOLLOW, dir_fd=...) in _directory_fd_matches: an absent entry, a symlink refused by O_NOFOLLOW, or a permission failure means the tree is not the exact planned bundle; the bool is the explicit fail-closed verdict of a filesystem predicate, not a swallowed error: commit_audit_export_bundle raises AuditExportBundleCollisionError/PreconditionError on False, reconcile_audit_export_bundle classifies the effect as unknown on False, and prepare's finally-block cleanup ignores False only while it is already propagating the original exception. Nothing is ever credited as published on False.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:39:31.036367+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — `_directory_fd_matches` is a fail-closed filesystem predicate, so converting `os.open` failure into `False` is an explicit negative result rather than silent recovery. The named controls are verified: `commit_audit_export_bundle` turns a failed match into collision or precondition errors at `_audit_export_bundle_effects.py:975-1006`, while `reconcile_audit_export_bundle` reports unknown rather than applied at lines 1027-1040. Preparation uses the predicate to reject divergent staging at lines 853-866, and its cleanup calls at lines 898-908 occur only while an earlier failure is already propagating; no failed match is credited as publication.
  judge_signature_version: 2
  scope_fingerprint: bfc5c1824413a6a5886d526ec320ff4f47eab6883bbc034ed0099094fbfa109e
  judge_transport: codex_cli
  ast_path: 'body[78]/body[4]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:61b4a6e6a157530afb009f6d74887cdddfb74ac77f25ca980f246a82a7a73924'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_fsync_tree_at:fp=6ed2bb02f8807c7b
  owner: john
  reason: |-
    `except OSError: return False` around the O_DIRECTORY|O_NOFOLLOW open in _fsync_tree_at, which opens the just-published tree, proves it is exact, then fsyncs it. A failed open means the published directory cannot be proven present, and False makes commit raise AuditExportBundlePreconditionError('published bundle changed before directory fsync') instead of returning a commit result whose durability was never established.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:39:47.558950+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM: `_fsync_tree_at` returns `False` as an explicit error result when the published directory cannot be opened, rather than silently continuing. The control-location claim is verified at plugins/sinks/_audit_export_bundle_effects.py:1005-1006, where `commit_audit_export_bundle` checks that result and raises `AuditExportBundlePreconditionError` before any commit result can be returned on that path. R6 expressly permits an explicit error result, and the visible caller converts it into a loud integrity failure.
  judge_signature_version: 2
  scope_fingerprint: e73d307a88f0916d9c4cba32172973e9f2b0a9848c0af362702226a1ba271fd7
  judge_transport: codex_cli
  ast_path: 'body[82]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:246d006b3c74167006dcb6b116293d00eefc3d9c5ba91903f20290d603c85557'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_exact_tree_at:fp=1259dc6b3db3db23
  owner: john
  reason: |-
    `except OSError: return False` around the proven-entry unlinks and the directory identity re-check in _remove_exact_tree_at: an OSError mid-removal means the tree could not be removed completely and its identity could not be re-confirmed; False is the explicit not-removed verdict that makes commit raise AuditExportBundlePreconditionError rather than report a clean reconciliation. The partially-unlinked leftover is a .bundle-stage sibling that recovery never credits.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:39:52.254318+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — R6 expressly permits returning an explicit error result, and `_remove_exact_tree_at` converts the failed removal into the unambiguous `False` result at `_audit_export_bundle_effects.py:711-712`. The named control was verified: `commit_audit_export_bundle` checks that result and raises `AuditExportBundlePreconditionError` on both reconciliation paths at lines 976-1000, so it cannot report clean reconciliation after a failed removal. This is neither silent success nor fabrication, and R6 is not eligible for the trust-boundary decorator.
  judge_signature_version: 2
  scope_fingerprint: 83b3423481f57e60674dc3a7604efb3d10df2d9d9822c60ce950157471ee2fc0
  judge_transport: codex_cli
  ast_path: 'body[81]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:ec08794a100abfbef925fdd756b7a0e10ab6a8c2f3cd75912c6ee4d8d993ab4b'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_exact_tree_at:fp=9a78f98dae1c180d
  owner: john
  reason: |-
    `except OSError: return False` around os.rmdir + os.fsync(parent_fd) in _remove_exact_tree_at: the emptied staging directory could not be removed or its removal could not be made durable; False is the explicit not-removed verdict consumed by commit (raises AuditExportBundlePreconditionError) and by prepare's already-propagating finally-block cleanup.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:42:59.048417+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — `_remove_exact_tree_at` converts an `OSError` from `os.rmdir` or `os.fsync` into the explicit `False` failure result at `_audit_export_bundle_effects.py:715`, satisfying R6's explicit-error-result form. The control is verified: `commit_audit_export_bundle` checks that result and raises `AuditExportBundlePreconditionError` at lines 975-1000, while `prepare_audit_export_bundle` invokes it only during the already-failing cleanup path in its `finally` block at lines 898-907. The exception is therefore not silently treated as success, and R6 is appropriately exempted at this site.
  judge_signature_version: 2
  scope_fingerprint: 83b3423481f57e60674dc3a7604efb3d10df2d9d9822c60ce950157471ee2fc0
  judge_transport: codex_cli
  ast_path: 'body[81]/body[3]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:7815ac15bbba32d1a3d20fbd7b208a3f1a462f244e1e035637b5c43d2962ee3e'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_exact_tree_at:fp=c31a8ef6d5036a8f
  owner: john
  reason: |-
    `except OSError: return False` around the O_DIRECTORY|O_NOFOLLOW open in _remove_exact_tree_at, which removes a leftover staging tree only after proving it is the exact planned bundle. A failed open means there is nothing provable to remove; False is the explicit not-removed verdict and commit raises AuditExportBundlePreconditionError('exact target has a divergent leftover staging path') on it. Raising OSError from prepare's finally-block cleanup would mask the exception already propagating there.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:44:55.218163+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — `_remove_exact_tree_at` converts an `OSError` into the function’s explicit `False` not-removed result, which satisfies R6’s permitted explicit-error-result form rather than silently claiming success. I verified that `commit_audit_export_bundle` checks this result and raises `AuditExportBundlePreconditionError` at plugins/sinks/_audit_export_bundle_effects.py:976 and :997, while `prepare_audit_export_bundle` calls it at :901 only during failure cleanup where another exception is already propagating. The rationale is site-specific and consistent with every visible call path, and R6 is not eligible for `@trust_boundary` suppression.
  judge_signature_version: 2
  scope_fingerprint: 83b3423481f57e60674dc3a7604efb3d10df2d9d9822c60ce950157471ee2fc0
  judge_transport: codex_cli
  ast_path: 'body[81]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:97e3408e13cd4e85e164fa81390aca1e495ea75aa956c8a3411cd0183c3fafe7'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_owned_building_tree_at:fp=f9af2a8c898b728a
  owner: john
  reason: |-
    `except OSError: return False` in _remove_owned_building_tree_at, which removes the private building tree prepare itself created only after proving its entries are exactly the names prepare wrote. An OSError anywhere in identity re-check, unlink, rmdir or parent fsync means removal could not be proven complete; False is the explicit verdict on which prepare raises AuditExportBundlePreconditionError('private bundle building directory could not be removed') in the staging-collision path, and which prepare's finally-block ignores only while propagating the original failure.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:41:26.249141+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the R6 premise is false because the handler returns the function’s explicit failure result, `False`, rather than silently treating removal as successful. In `prepare_audit_export_bundle` at plugins/sinks/_audit_export_bundle_effects.py:855, that result is checked and causes `AuditExportBundlePreconditionError` at line 862; the only other call, at line 903 in the `finally` block, performs best-effort cleanup while an original failure is already propagating. The visible code therefore verifies the rationale’s control-location claim and preserves failure semantics without fabricating successful cleanup.
  judge_signature_version: 2
  scope_fingerprint: 46e3bdd95f16e118e272bb51ffa1c611a50c2c79f1bb4beb1a71e94128a551fe
  judge_transport: codex_cli
  ast_path: 'body[84]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b2945d83b2cebf9253ad14b89b7b058c7fa51f177e85576f802edee93f61c0f4'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_scratch_path_at:fp=54e4677b0f619e56
  owner: john
  reason: |-
    `except FileNotFoundError: return False` in _remove_scratch_path_at: the stat classifying a stale scratch entry raced with its removal by another process. False reports 'not removed by this call' so cleanup_stale_audit_export_bundle_scratch leaves its removed counter untouched; there is nothing to recover and nothing was credited.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:41:15.076281+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    PRESCRIBED FORM — the narrowly caught FileNotFoundError represents an expected filesystem race in which the named scratch path disappeared before this call could remove it. Returning False is an explicit, meaning-preserving result from the bool-returning helper—“not removed by this call”—and the stated caller behavior consistently avoids crediting a removal, so the exception is not being replaced with fabricated success or an unrecorded recovery. R6 is therefore handled legitimately at this site, and the decorator alternative is inapplicable because the finding is R6 rather than R1 or R5.
  judge_signature_version: 2
  scope_fingerprint: 9188be0ab3bfa34e932e5744b2de10d21b6e22e8cea0c44aa7db61a26cd05740
  judge_transport: codex_cli
  ast_path: 'body[103]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:d7419b76bdc0dc7d599e5f234a92ba0af0ee22210618c55d23d8f8122e0c1297'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_scratch_path_at:fp=73aac2a01b26bc12
  owner: john
  reason: |-
    `except OSError: return False` around the child unlinks, identity re-check, rmdir and parent fsync of a stale scratch directory in _remove_scratch_path_at. Returning False rather than raising keeps one un-removable leftover from aborting preflight; the sweep refuses nested directories, never touches a name outside the probe/building glob, and a leftover is retried on the next preflight.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:41:15.430479+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the `OSError` is converted into the function’s explicit `False` failure result rather than silently discarded. The visible code consistently returns `True` only after removal and parent-directory `fsync` complete, while all incomplete or unsafe cleanup paths return `False`; the rationale also explains the operational meaning of that result: leave the scratch entry intact and allow a later bounded preflight sweep to retry it. R6’s premise of swallowing an exception without an explicit error result is therefore false at this site.
  judge_signature_version: 2
  scope_fingerprint: 9188be0ab3bfa34e932e5744b2de10d21b6e22e8cea0c44aa7db61a26cd05740
  judge_transport: codex_cli
  ast_path: 'body[103]/body[3]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:f7acff98229e6ecb7a4d9fa98cd7c9e59416f357179e9c736712708df732522e'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_scratch_path_at:fp=7632566f747ef7f0
  owner: john
  reason: |-
    `except OSError: return False` around the O_DIRECTORY|O_NOFOLLOW open of a stale scratch directory in _remove_scratch_path_at: the entry vanished or cannot be opened without following a symlink; False is the explicit not-removed verdict for the bounded, uncredited scratch sweep.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:43:13.295670+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — the caught OSError is converted into the function’s explicit boolean failure result rather than being silently ignored. The visible code consistently uses False to mean that the scratch path was not removed and True only after successful deletion and parent-directory fsync, matching R6’s permitted explicit-error-result form. This is R6, so the trust-boundary decorator does not apply.
  judge_signature_version: 2
  scope_fingerprint: 9188be0ab3bfa34e932e5744b2de10d21b6e22e8cea0c44aa7db61a26cd05740
  judge_transport: codex_cli
  ast_path: 'body[103]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:c1c05c3fe0be1442bf11a95f4b9ea3fd8b9d18f6169a6898d60a557d456fea50'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_remove_scratch_path_at:fp=f83107d769645aee
  owner: john
  reason: |-
    `except OSError: return False` around the unlink + parent fsync of a non-directory scratch entry in _remove_scratch_path_at. The sweep is bounded best-effort housekeeping over .elspeth-bundle-probe-* and crashed-prepare building names that recovery never credits; False is the explicit not-removed verdict, the counter is not incremented, and preflight's own probes raise AuditExportBundlePreflightError if the parent is genuinely unusable.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:47:28.817976+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    PRESCRIBED FORM — `_remove_scratch_path_at` converts the caught `OSError` into the function’s explicit `False` not-removed result, while its caller increments the removal count only on `True` (`plugins/sinks/_audit_export_bundle_effects.py:1202`), satisfying R6’s explicit-error-result alternative. The code identifies this as bounded cleanup of scratch names that survive crashes and are never credited by recovery (`plugins/sinks/_audit_export_bundle_effects.py:1168`), and `preflight_audit_export_bundle` subsequently exercises the relevant file and parent durability operations and raises `AuditExportBundlePreflightError` on failure (`plugins/sinks/_audit_export_bundle_effects.py:1242`). This is site-specific best-effort housekeeping rather than silent recovery of pipeline or audit data.
  judge_signature_version: 2
  scope_fingerprint: 9188be0ab3bfa34e932e5744b2de10d21b6e22e8cea0c44aa7db61a26cd05740
  judge_transport: codex_cli
  ast_path: 'body[103]/body[1]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:52aed449c6b50cd5e12f641dff2bdb979aa12b778c0e0000a3f849e19a8cfcc8'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:_tree_matches_at:fp=d10057a26cb1a7e6
  owner: john
  reason: |-
    `except OSError: return False` around os.open(name, O_DIRECTORY|O_NOFOLLOW, dir_fd=parent_fd) in _tree_matches_at: the candidate is absent, a regular file, or a symlink, so there is no exact planned tree at that name; the bool is the explicit fail-closed verdict of a filesystem predicate, not a swallowed error: commit_audit_export_bundle raises AuditExportBundleCollisionError/PreconditionError on False, reconcile_audit_export_bundle classifies the effect as unknown on False, and prepare's finally-block cleanup ignores False only while it is already propagating the original exception. Nothing is ever credited as published on False.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:45:39.743237+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — `_tree_matches_at` is an exact-tree predicate, and its `False` return is an explicit fail-closed result when `os.open` cannot establish that predicate, not silent recovery that credits publication. The named controls are verified: `prepare_audit_export_bundle` converts failed matches into `AuditExportBundlePreconditionError` at plugins/sinks/_audit_export_bundle_effects.py:853 and :865, while `commit_audit_export_bundle` raises collision/precondition errors or withholds success at :975-1013. `reconcile_audit_export_bundle` returns `unknown` when neither exact target nor exact staging can be proven at :1027-1036, and preparation cleanup at :899-908 discards the Boolean only while preserving the original exception. R6 therefore does not apply at this site, and R6 is outside the decorator’s R1/R5 scope.
  judge_signature_version: 2
  scope_fingerprint: f6c23c858927852c57a88eead5814ed129a08d7af76a8889c4aab6f794c05f92
  judge_transport: codex_cli
  ast_path: 'body[79]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:5c765fd92bcb4a44f0fc35961c546efdc63aa7106e7ba4db358bf390546947cb'
- key: plugins/sinks/_audit_export_bundle_effects.py:R6:cleanup_stale_audit_export_bundle_scratch:fp=49b86e17b683d8ad
  owner: john
  reason: |-
    `except FileNotFoundError: continue` in cleanup_stale_audit_export_bundle_scratch: the candidate name was removed between listdir and stat by another process, so there is nothing to classify or remove; the sweep moves to the next name without counting it.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:45:12.467898+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the narrowly caught FileNotFoundError represents an expected listdir-to-stat race, not a lost failure signal. The visible loop merely classifies and removes stale scratch entries; if another process has already removed a listed name, there is nothing left to classify, remove, or count, and continuing preserves the cleanup operation's meaning. No default is fabricated and no broader filesystem or integrity error is suppressed.
  judge_signature_version: 2
  scope_fingerprint: 999f0086d2e5b8403e3be8b3505cf79ef39922e8882cd755a8bbc1785e1505cd
  judge_transport: codex_cli
  ast_path: 'body[104]/body[7]/body[1]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:964ec754c9f5fcd2e455d45b4dd4d8b78f219e90c89046aec4b469b2fc43d0d2'
- key: plugins/sinks/_audit_export_bundle_effects.py:R7:_create_private_building_tree_at:fp=0d3d2251eb207568
  owner: john
  reason: |-
    `with suppress(OSError): os.rmdir(name, dir_fd=parent_fd)` sits inside the `finally: if not completed:` cleanup of _create_private_building_tree_at, which runs only while AuditExportBundlePreconditionError ('private bundle building directory changed during creation') is already propagating. Removing the just-created empty private directory is best-effort housekeeping; letting an OSError escape here would replace the real precondition error with a cleanup error, and any survivor matches _BUILDING_GLOB and is swept by cleanup_stale_audit_export_bundle_scratch on the next preflight.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:53:41.821588+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.88
  judge_rationale: |-
    PRESCRIBED FORM — The suppressed OSError is limited to best-effort rollback in a finally path and prevents cleanup failure from replacing the exception that left directory creation incomplete; execution does not proceed as though creation succeeded. The named fallback is verified in plugins/sinks/_audit_export_bundle_effects.py:1161, where cleanup_stale_audit_export_bundle_scratch removes names matching _BUILDING_GLOB, and at line 1235, where preflight invokes it; the generated name at lines 738-741 matches that glob. The rationale overstates that only AuditExportBundlePreconditionError can be propagating because fchmod, fstat, or stat may also raise, but that does not undermine the exception-preservation justification.
  judge_signature_version: 2
  scope_fingerprint: a801494b6a64fb0ad842f961f0b9fbbd26620677a48e4afabcafeacdf58b3a9c
  judge_transport: codex_cli
  ast_path: 'body[83]/body[1]/body[4]/finalbody[0]/body[1]'
  judge_metadata_signature: 'hmac-sha256:v2:af3eda0f500416f838c8355ace1c902e833be384d42c7ff2d6147d4096f303a5'
- key: plugins/sinks/aws_s3_sink.py:R5:_json_value_chars:fp=4c89aaaf2894b7d7
  owner: john
  reason: |-
    `isinstance(value, (list, tuple))` in _json_value_chars mirrors json.JSONEncoder's array dispatch (list and tuple, including subclasses) so the pre-write size estimate accepts exactly what the write path encodes; `type(value) is list` would quarantine a tuple-valued row that json.dumps writes. The value is Tier-2 pipeline row data (deep-thawed by both production callers, so tuples do not arrive from the sink path, but the estimator's contract is the encoder's accept set, not the caller's normalisation). Pinned by TestSerialization::test_json_size_estimate_accepts_exactly_what_the_encoder_accepts.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:54:02.336358+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — this `isinstance` is semantic container dispatch matching the visible `json.JSONEncoder` accept set, not a defensive re-check that hides a Tier-2 contract violation. The tuple arm preserves the write path’s behavior, while unsupported shapes and recursive containers raise `S3RecordSerializationError`, so the code neither coerces nor silently recovers. The value is identified as Tier-2 rather than external input, so the `@trust_boundary` decorator contract does not apply.
  judge_signature_version: 2
  scope_fingerprint: 7feaa8a7324b2a55df713b966774650dbbd84f2f1f754769221a54053578f69e
  judge_transport: codex_cli
  ast_path: 'body[64]/body[7]/test'
  judge_metadata_signature: 'hmac-sha256:v2:086cdda0811a8695773aaeda2c567408de1c03bad0f8f511e2f27bc7352ebdc1'
- key: plugins/sinks/aws_s3_sink.py:R6:AWSS3Sink:_preflight_effect_members:fp=3ae9618688d6846c
  owner: john
  reason: |-
    This `except S3RecordSerializationError:` in AWSS3Sink._preflight_effect_members is the per-row diversion path of the sink-effect preflight, not a swallow. The trial serialization of one Tier-2 pipeline row (schema-valid but not representable in the configured CSV codec or as standard JSON (NaN, huge int, non-JSON value)) is recorded through self._divert_row(row, row_index=member.ordinal, reason=...), which raises FrameworkBugError when no on_write_failure policy is configured, and the ordinal is appended to `diverted` and `diversion_attribution`, both sealed into the SinkEffectPlan (diverted_ordinals) and audited. The row leaves the batch as an explicit quarantine outcome; the remaining rows are serialized once more as the staged body.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:54:04.720117+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    PRESCRIBED FORM — the caught serialization failure becomes an explicit per-row quarantine result: `_divert_row` is called, the ordinal and reason-bearing `DiversionAttribution` are recorded, the row is excluded through `diverted_keys`, and the method returns the diverted ordinals and attribution. The exception is therefore neither silently swallowed nor replaced with fabricated data; the visible code establishes the remediation R6 prescribes. R6 is not eligible for the `@trust_boundary` structural alternative.
  judge_signature_version: 2
  scope_fingerprint: aacd1d80d1c6fa1b981695c63804c53c2a0d5d0eff2a3830883b1cd3d532ef28
  judge_transport: codex_cli
  ast_path: 'body[85]/body[27]/body[4]/body[3]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:368d24b289a8376605d1c48d8ddb55e10dea839647f99c78bf220430c8db1892'
- key: plugins/sinks/aws_s3_sink.py:R6:AWSS3Sink:_preflight_effect_members:fp=e455abd50eb7d1bc
  owner: john
  reason: |-
    This `except S3RecordSizeLimitError:` in AWSS3Sink._preflight_effect_members is the per-row diversion path of the sink-effect preflight, not a swallow. The trial serialization of one Tier-2 pipeline row (schema-valid but larger than the configured max_record_chars budget) is recorded through self._divert_row(row, row_index=member.ordinal, reason=...), which raises FrameworkBugError when no on_write_failure policy is configured, and the ordinal is appended to `diverted` and `diversion_attribution`, both sealed into the SinkEffectPlan (diverted_ordinals) and audited. The row leaves the batch as an explicit quarantine outcome; the remaining rows are serialized once more as the staged body.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:58:45.218491+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the exception is converted into an explicit per-row quarantine outcome, not silently swallowed: the code records the diversion, appends the ordinal and attribution, excludes the row, and returns those results. The named control `BaseSink._divert_row` was verified at plugins/infrastructure/base.py:1922; it records the `RowDiversion` and raises `FrameworkBugError` when diversion policy was not injected, while plugins/sinks/aws_s3_sink.py:1028 passes the returned diverted ordinals and attribution into the effect plan. R6 is therefore handled in its prescribed recorded-quarantine form, and the decorator alternative is inapplicable because R6 is not suppressible by `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: aacd1d80d1c6fa1b981695c63804c53c2a0d5d0eff2a3830883b1cd3d532ef28
  judge_transport: codex_cli
  ast_path: 'body[85]/body[27]/body[4]/body[3]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:096d06b4371a8043145a8ce1429935d53f4db13beba2181d966fec7f99fb37f5'
- key: plugins/sinks/aws_s3_sink.py:R6:_EncodedTextWriter:_encode:fp=55f917fce054275f
  owner: john
  reason: |-
    `except (LookupError, TypeError, UnicodeError, ValueError): encoding_failed = True` in _EncodedTextWriter._encode records the failure and S3RecordSerializationError is raised after the handler exits. That ordering is the point: a raise inside the handler, even `from None`, still attaches the codec's exception as __context__, and a UnicodeEncodeError message quotes the row's characters. tests/unit/plugins/sinks/test_aws_s3_sink.py::TestSerialization::test_csv_unencodable_value_is_static_failure and test_non_text_codec_runtime_failure_is_static_and_cause_free pin __cause__ is None and __context__ is None. The failure is not swallowed: it is re-raised as the static S3RecordSerializationError that _preflight_effect_members turns into a recorded row diversion.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:59:14.436143+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    RULE MISFIRES — the handler at plugins/sinks/aws_s3_sink.py:385-386 records the codec failure, and the immediately following branch at :387-388 always converts it into S3RecordSerializationError after exception context has cleared; the error is not silently swallowed. The named control is verified at :958-999: _preflight_effect_members catches S3RecordSerializationError and records a row diversion on the preflight path. The cited tests were outside the judge tool's permitted roots, but their claims are not load-bearing because the visible source independently establishes the deferred raise and routing.
  judge_signature_version: 2
  scope_fingerprint: bdce97391305a97800965f5fca565841242a964d3418c2f75d5aae77a52db4a5
  judge_transport: codex_cli
  ast_path: 'body[62]/body[1]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:14f30f3df5282f916a19cd54e4220f9621053405f5e557c86801cce1470b90cc'
- key: plugins/sinks/aws_s3_sink.py:R6:_check_json_record:fp=853ac6544ec60688
  owner: john
  reason: |-
    `except (ValueError, TypeError, OverflowError, RecursionError): traversal_failed = True` in _check_json_record records the failure and S3RecordSerializationError is raised after the handler exits so the traversal error (which can quote a row value) is never chained as __context__; tests/unit/plugins/sinks/test_aws_s3_sink.py::TestSerialization::test_huge_integer_conversion_is_a_static_serialization_failure[json|jsonl] pins __context__ is None. S3RecordSerializationError from _json_value_chars is re-raised unchanged by the preceding handler, and the resulting static failure is a recorded row diversion in _preflight_effect_members.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:58:16.160437+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.94
  judge_rationale: |-
    RULE MISFIRES — the handler does not silently discard the failure: `_check_json_record` records it and raises `S3RecordSerializationError from None` immediately after leaving the handler (plugins/sinks/aws_s3_sink.py:458-470), deliberately preventing sensitive row-derived text from entering `__context__`. The separately handled `S3RecordSerializationError` is re-raised unchanged, so the translation does not mask an existing typed serialization failure. The named control `_preflight_effect_members` catches this typed failure and records the row diversion and attribution at plugins/sinks/aws_s3_sink.py:958-999. The named test was unavailable within the permitted read scope, but its claim is not load-bearing because the visible raise-after-handler structure and verified diversion control establish the prescribed behavior directly.
  judge_signature_version: 2
  scope_fingerprint: eaf7df5195e7c8edd81fb3531fa2a32520e7b91f0ab8da3da1d848535f810f1d
  judge_transport: codex_cli
  ast_path: 'body[65]/body[2]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:ad7cff62e46d470d9e69b49b0f5ac1708bf425cebc1752db9191ce82ffc0a1c5'
- key: plugins/sinks/aws_s3_sink.py:R6:_csv_scalar_text:fp=defbf69b24cdb8b9
  owner: john
  reason: |-
    `except (ValueError, TypeError, OverflowError): conversion_failed = True` in _csv_scalar_text records that str(value) failed (str(int) is bounded by sys.get_int_max_str_digits()) and S3RecordSerializationError is raised after the handler exits so the conversion error is never chained as __context__; tests/unit/plugins/sinks/test_aws_s3_sink.py::TestSerialization::test_huge_integer_conversion_is_a_static_serialization_failure[csv] pins __cause__ and __context__ None. The static failure becomes a recorded row diversion in _preflight_effect_members.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:56:45.931610+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the handler does not silently suppress the conversion error: it records failure, exits the handler, and immediately raises `S3RecordSerializationError from None`. The visible code therefore provides the explicit error required by R6 while intentionally preventing the caught conversion exception from becoming chained context; the downstream diversion claim is not needed to establish compliance at this site.
  judge_signature_version: 2
  scope_fingerprint: 5ba0debe09ed5e720951d0b6d86cd88e7840ddc0b364129064adee42a58148e7
  judge_transport: codex_cli
  ast_path: 'body[66]/body[3]/body[3]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:651ef437073028a698c5c572c239dc97bb2e1cfc52c0cca2acf2fe546048bc32'
- key: plugins/sinks/chroma_sink.py:R6:ChromaSink:_member_effect_material:fp=b99d4caaca3f0716
  owner: john
  reason: |-
    except ValueError as exc around _extract_effect_member appends (member.ordinal, str(exc)) to diversions, and that append IS the explicit error result: the member is left out of ids/documents/metadata and the descriptor row_count, its ordinal is returned in the diversions tuple and lands in diverted_ordinals and diversion_attribution of the plan evidence, and prepare_effect routes every entry through self._divert_row, which fails closed with FrameworkBugError when no on_write_failure policy is configured. The value is Tier-2 pipeline row data whose field types are not guaranteed by the member contract; per ADR-020 an invalid member diverts individually with its exact reason instead of aborting the plan and blocking valid siblings (elspeth-32bf1a9b63). The divert call cannot live inside this handler because _member_effect_material is recomputed on commit and reconcile to re-bind the plan hash, and the live diversion must be logged exactly once at prepare. No path lets the member count as written.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:00:34.245997+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM — the caught ValueError is converted into an explicit quarantine result: `_member_effect_material` records the member ordinal and exact reason in `diversions`, excludes that member from the published payload and descriptor row count, and returns the diversions at `chroma_sink.py:423-478`. The named control is verified: `ChromaSink.prepare_effect` routes every returned diversion through `_divert_row` and records `diversion_attribution` and `diverted_ordinals` in plan evidence at `chroma_sink.py:505-553`. `BaseSink._divert_row` fails closed with `FrameworkBugError` when no write-failure policy exists and otherwise records a `RowDiversion` at `base.py:1922-1946`, so the exception is neither silently swallowed nor allowed to count the invalid member as written.
  judge_signature_version: 2
  scope_fingerprint: f24ff38d4b8b44b759e2ca3a93e4e411adfc9dea85a26997ba1594f61834d0ba
  judge_transport: codex_cli
  ast_path: 'body[34]/body[23]/body[3]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:72f99f47e4c60d0a53be041e5354d805fd36408d8ac414c23fd41944168c6cf5'
- key: plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_effect_rows:fp=2b8625cb5c211325
  owner: john
  reason: |-
    `except (IntegrityError, DataError) as exc:` in DatabaseSink._insert_effect_rows is the per-row SAVEPOINT catch: the single-row INSERT failed with a constraint/type error, the handler rolls back the row savepoint and calls self._divert_row(row, row_index=ordinal, reason=f'Constraint violation: {exc.orig}'), which raises FrameworkBugError when no on_write_failure policy exists, then records the ordinal in diverted_ordinals and diversion_attribution that the ledger marker persists. That is the explicit quarantine outcome the sink-effect contract expects; nothing is written silently and the accepted-row hash excludes the row.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:00:18.315950+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the caught row-level database failure is not swallowed: the handler rolls back its savepoint, invokes the named quarantine control `BaseSink._divert_row`, and appends the row's ordinal and attribution to the explicit result partition (plugins/sinks/database_sink.py:721; plugins/infrastructure/base.py:1922). I verified that `_divert_row` records the diversion or raises `FrameworkBugError` when no failure policy exists (plugins/infrastructure/base.py:1940), while `commit_effect` hashes only `accepted_rows` and persists both accepted and diverted ordinals plus diversion attribution in the ledger marker (plugins/sinks/database_sink.py:877). This satisfies R6's prescribed explicit quarantine-result form, and the decorator alternative is inapplicable because R6 is not one of the suppressible R1/R5 boundary rules.
  judge_signature_version: 2
  scope_fingerprint: bf5c9f0f2a48e3f9358ca3bec9b687a5517cf2ba2864ecb6ff7aad11fc74ed9f
  judge_transport: codex_cli
  ast_path: 'body[42]/body[30]/body[7]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:3e264309eb2304de259b8211902eb7999036a98a87dc5c2274a2a0f89f68494c'
- key: plugins/sinks/database_sink.py:R6:DatabaseSink:_insert_effect_rows:fp=9622ee5470197a67
  owner: john
  reason: |-
    `except (IntegrityError, DataError): batch_savepoint.rollback()` in DatabaseSink._insert_effect_rows is the batch SAVEPOINT catch: the whole-batch INSERT hit a row-attributable constraint/type failure, so the handler rolls back that savepoint and the function falls through to the per-row replay below, where each row commits its own savepoint or is diverted via _divert_row with the constraint reason. Non-row-attributable failures (OperationalError, ProgrammingError, connection loss) are not caught and abort the outer transaction. The resulting accepted/diverted ordinal partition and diversion attribution are sealed into the target-side ledger marker, so the outcome is explicit and audited.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T13:59:31.484589+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the batch-level IntegrityError/DataError is deliberately contained only after rolling back batch_savepoint, and the visible code then unconditionally replays every member through a row_savepoint. Each row-level failure is rolled back, attributed with its constraint reason, and passed to DatabaseSink._divert_row, while uncaught non-row-attributable exceptions still propagate; the returned ordinal and attribution tuples make the outcome explicit rather than silently losing the error. R6 is therefore matching a controlled quarantine/replay mechanism, not a swallowed failure, and the decorator alternative is inapplicable because R6 is not suppressible by @trust_boundary.
  judge_signature_version: 2
  scope_fingerprint: bf5c9f0f2a48e3f9358ca3bec9b687a5517cf2ba2864ecb6ff7aad11fc74ed9f
  judge_transport: codex_cli
  ast_path: 'body[42]/body[30]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:860d1cdf4029678b8316ebad3b246e014c0012fa8480864135ff177c878021f1'
- key: plugins/sinks/document_sink.py:R6:DocumentSink:prepare_effect:fp=f092c4e684af0f35
  owner: john
  reason: |-
    except UnicodeEncodeError around value.encode(self._encoding) assigns reason, and the code two lines below consumes reason unconditionally: a predecessor snapshot member (current_member is None) raises ValueError because the resumed snapshot is incompatible with the configured codec, and a current member is appended to pending_diversions, which _divert_row-s it with this reason and records its ordinal in the diverted partition and diversion_attribution. payload stays None on this branch, so no byte is ever staged for the row and it is never reported accepted. The value is a Tier-2 row field being encoded with the operator's configured codec; an unrepresentable character is the row's own defect and an ADR-020 per-row diversion, not a sink fault to crash on. The handler's outcome is explicit; it is delivered through the shared reason path rather than a raise inside the handler because the string-type and empty-value rejections above it must divert through the identical path.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:01:13.273689+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the exact UnicodeEncodeError converts an operation-unsafe Tier-2 value into an explicit per-row diversion reason rather than silently recovering or fabricating output. I verified the named control: DocumentSink.prepare_effect consumes every pending diversion through DocumentSink._divert_row and records both the ordinal and reason in diversion_attribution at document_sink.py:318-338, while payload remains unstaged; the predecessor-only path instead raises explicitly. R6 is not eligible for the @trust_boundary decorator, so should_use_decorator is null.
  judge_signature_version: 2
  scope_fingerprint: 9ae0ace63133a73b09a212606cf2aff4d9b4bbdf61eca9bbd455a4cc533c2e56
  judge_transport: codex_cli
  ast_path: 'body[22]/body[19]/body[12]/body[7]/orelse[0]/orelse[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:aee42b9d2381027b61da9600e40cb7d2980ae0d87c1dcc52f978f10ed8b46fde'
- key: plugins/sinks/text_sink.py:R6:TextSink:prepare_effect:chunks:fp=0fd0e3bff13af279
  owner: john
  reason: |-
    except UnicodeEncodeError around (value + '\n').encode(self._encoding) assigns reason, which the loop body consumes unconditionally right after: a predecessor snapshot member (current_member is None) raises ValueError because the resumed snapshot is incompatible with the configured codec, and a current member is diverted through self._divert_row with this reason, its ordinal appended to diverted and its attribution recorded, then 'continue' skips the yield. encoded stays None on this branch so no byte is staged for the row and it is never appended to accepted. The value is a Tier-2 row field encoded with the operator's configured codec; an unrepresentable character is the row's own defect and an ADR-020 per-row diversion, not a sink fault. The outcome is explicit and shares the reason path with the string-type and CR/LF rejections above it so all three diverge identically.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:01:17.904713+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the caught UnicodeEncodeError is converted into an explicit reason that the immediately following branch consumes on every path. A current member is quarantined through TextSink._divert_row, with its ordinal and diversion attribution recorded before continue prevents staging, acceptance, or yielding; a predecessor member instead raises ValueError because it cannot be attributed to the current effect. This is explicit per-row handling of an operation-unsafe Tier-2 value, not silent suppression, coercion, or fabrication.
  judge_signature_version: 2
  scope_fingerprint: 821a53e8c0eebf760c1491a4112fc0162c831bed48d4f5085e19b1f183d64d70
  judge_transport: codex_cli
  ast_path: 'body[23]/body[21]/body[11]/body[2]/body[6]/orelse[0]/orelse[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b8ceafb6157d44e2ba7c773f4d56235cc6c8f9f6c5619094fd43ac2add43f7f0'
- key: plugins/sources/aws_s3_source.py:R6:AWSS3Source:_get_s3_client:fp=a20b229b3b2d6013
  owner: john
  reason: |-
    The `except _s3_provider_exception_types() as exc:` around `build_s3_client(...)` (after `except ImportError: raise`, which keeps the missing-extra crash intact) stores only `_normalize_error_type(exc)` in `client_error_type`; the next statement raises `S3SourceReadError` via `_raise_safe` outside the handler. The raise is outside the handler on purpose: client construction failures from botocore can embed the endpoint URL and credential-chain diagnostics, and a raise inside the handler — even with `from None` — would attach that exception as `__context__`. `tests/unit/plugins/sources/test_aws_s3_source.py::test_client_construction_provider_failure_is_static_unchained_and_audited` pins `exc.__cause__ is None and exc.__context__ is None`, that the sentinel never reaches the error or the audit call, and that `load` records exactly one `CallStatus.ERROR` call before re-raising. Nothing is swallowed: the source fails the load loudly with a typed, audited, redacted error.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:02:10.933452+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the provider exception is not swallowed: aws_s3_source.py:988-991 records only its normalized type and then immediately routes every caught path to `_raise_safe`. `_raise_safe` at aws_s3_source.py:385-386 raises the redacted `S3SourceReadError` from `None`, while `load` at lines 1015-1032 records `CallStatus.ERROR` and re-raises it. The visible control flow independently establishes the behavior, so the cited but inaccessible test is not needed for this disposition.
  judge_signature_version: 2
  scope_fingerprint: c934343a6f68e9c0f78264980fc93533d7087803c2f7e1cea52a46e9dfaa0b24
  judge_transport: codex_cli
  ast_path: 'body[83]/body[15]/body[0]/body[1]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:78db49e24b105ba10e259487fb895a7d03bb6d831e163b4ef0411feb7eda749d'
- key: plugins/sources/aws_s3_source.py:R6:_download_s3_object:fp=62f0723837985815
  owner: john
  reason: |-
    R6 fires on `except _s3_provider_exception_types() as exc:` around the `head_object` probe, whose body assigns `head_error_type` and does not raise. Nothing is swallowed: the handler reduces the botocore exception to a regex-validated class name via `_normalize_error_type` (anything unrecognised collapses to `"ProviderError"`), and the very next statement raises unconditionally through `_raise_safe`, which is `raise error from None`. Raising outside the handler is the point — it severs `__context__`/`__cause__` so the vendor exception, which carries bucket, key, endpoint and signed-request detail, can never reach a traceback or an audit record; this is the ADR-032 external-boundary pattern applied to an exception (read one fact, assert its value, construct the owned `S3SourceReadError`, discard the original). The exception is invalidated if `_raise_safe` stops using `from None`, if the post-handler raise becomes conditional, or if the handler starts propagating the botocore object itself.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:01:45.452457+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    RULE MISFIRES — R6's premise that the provider exception is silently swallowed is false at this site. On the caught path, `_normalize_error_type` produces a non-None, regex-constrained owned error label, so the immediately following branch invokes `_raise_safe` and raises the owned `S3SourceReadError`; the rationale plainly states that `_raise_safe` uses `raise error from None` to prevent sensitive vendor exception details from propagating. The visible code is consistent with that site-specific explanation, and R6 is not eligible for the `@trust_boundary` decorator mechanism.
  judge_signature_version: 2
  scope_fingerprint: e61d3c301cb284cd06eecadfde386fe5b4a3335e728dd6e16d1ce6fea49d6974
  judge_transport: codex_cli
  ast_path: 'body[67]/body[2]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:bc879fbc99931223aec29c20384d5212c4b86ab818e849deb5388f9acb4d7513'
- key: plugins/sources/aws_s3_source.py:R6:_download_s3_object:fp=8a3c108521ec3dc1
  owner: john
  reason: |-
    R6 fires on the `except _s3_provider_exception_types() as exc:` guarding the `get_object(..., IfMatch=head_etag)` call, which records `get_error_type` rather than raising in place. This is the same deliberate raise-outside-the-handler discipline as the HEAD probe but on a distinct failure: an `IfMatch` rejection here means the object mutated between HEAD and GET, and it must surface as an owned `S3SourceReadError` carrying only a normalized error-type token, not as a botocore error whose message embeds the conditional-request headers. The unconditional `_raise_safe` on the next line makes the handler non-swallowing, and `raise ... from None` keeps the vendor traceback out of the source's error surface. Invalidated if the `if get_error_type is not None:` raise is removed or made conditional, or if the download is allowed to continue with an unset `get_response`.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:01:28.853190+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the handler does not silently suppress the provider failure: it normalizes the caught exception into `get_error_type`, and the immediately following branch passes the resulting owned read error to `_raise_safe`. The rationale plainly explains why raising outside the handler preserves a sanitized `S3SourceReadError` surface, and the visible code confirms that successful download processing cannot continue after a recorded error token. R6 is not eligible for the `@trust_boundary` structural suppression.
  judge_signature_version: 2
  scope_fingerprint: e61d3c301cb284cd06eecadfde386fe5b4a3335e728dd6e16d1ce6fea49d6974
  judge_transport: codex_cli
  ast_path: 'body[67]/body[11]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:25106162fdfa90b26692b9176753e6a9310f06820825849f962212896a7401e4'
- key: plugins/transforms/aws/guardrails_client.py:R4:BedrockGuardrailsClient:_emit_after_audit:fp=4092bce57cf339ae
  owner: john
  reason: |-
    The broad `except Exception` wraps only the telemetry emit callback, which runs AFTER the audit row has been durably recorded via `_record_call`. Telemetry is a Tier-3 best-effort sink: an exporter failure must not convert an already-audited ApplyGuardrail call into a row failure. The handler is not a swallow-all: the two clauses above it re-raise `contract_errors.TIER_1_ERRORS` and the programming-error family (TypeError/AttributeError/KeyError/NameError), so only exporter-side runtime failures reach the warning path, and those are logged with `exc_info=True`, run_id, state_id and call_type so the loss is visible. This mirrors the identical `telemetry_emit_failed` discipline in infrastructure/clients/http.py, llm.py and aws/textract_client.py.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:03:25.120703+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    PRESCRIBED FORM — The control-location claim is verified: `BedrockGuardrailsClient.apply_guardrail` calls `_record_call` before `_emit_after_audit` on the covered path at guardrails_client.py:592-606, and `AuditedClientBase._record_call` synchronously delegates to the audit recorder at base.py:119-164. The broad handler is confined to post-audit telemetry work, while Tier-1 integrity errors and the listed programming-error family are re-raised, so best-effort telemetry failure cannot erase or convert the already-recorded call and is reported with correlated context. R4 is not suppressible by `@trust_boundary`, so the decorator alternative does not apply.
  judge_signature_version: 2
  scope_fingerprint: e7c0865844d6629960a8817099e907fd84038768a37e28d3c56aeddd150ad1d3
  judge_transport: codex_cli
  ast_path: 'body[56]/body[4]/body[0]/handlers[2]'
  judge_metadata_signature: 'hmac-sha256:v2:546c0d0dbfd5f1a9b0d23adcd41c340fcd086b2ec7577e48c8e1f70ebfdba0c8'
- key: plugins/transforms/aws/guardrails_client.py:R6:BedrockGuardrailsClient:apply_guardrail:fp=0ec2780db7b46669
  owner: john
  reason: |-
    R6 fires on `except GuardrailPartialCoverageError as error:` in `apply_guardrail`, which stores `terminal_error` and builds a response payload rather than re-raising. The raise is deferred by design, not suppressed: after the try/except the method unconditionally calls `_record_call` and `_emit_after_audit` and then executes `raise terminal_error`, so the failed guardrail call is written to the audit trail before the exception propagates. Re-raising inside the handler would skip both audit writes and make a partial-coverage failure — a content-safety control that examined only some of the units — invisible in the run record, which is precisely the outcome ELSPETH's audit-primacy rule exists to prevent; the handler also pins `retryable: False`, since partial coverage is a deterministic property of the request. Invalidated if the trailing `if terminal_error is not None: raise terminal_error` is removed or the audit calls move inside the handlers.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:02:38.505158+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the exception is deferred for audit recording, not swallowed. I verified in plugins/transforms/aws/guardrails_client.py:562-608 that BedrockGuardrailsClient.apply_guardrail assigns the caught GuardrailPartialCoverageError to terminal_error, calls _record_call and _emit_after_audit, and then raises terminal_error. The visible control therefore preserves audit primacy while still propagating the failure, directly contradicting R6's premise of silent suppression.
  judge_signature_version: 2
  scope_fingerprint: 11a921181e3ddc4321378665a11701e5da29f39c1650d0035ba48e7cad9a19e7
  judge_transport: codex_cli
  ast_path: 'body[56]/body[5]/body[8]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:b1c300da02e61c10337e4bb7a6fcc401b0241172556d5ffee56a17a894670b5f'
- key: plugins/transforms/aws/guardrails_client.py:R6:BedrockGuardrailsClient:apply_guardrail:fp=5ae32372cad087cf
  owner: john
  reason: |-
    R6 fires on `except GuardrailResponseError as error:` in `apply_guardrail`, the arm for a Bedrock reply that failed `parse_guardrail_response` or whose retry-attempt count disagreed with `ResponseMetadata`. It does not swallow: `terminal_error` is captured, a `malformed_response` payload is recorded, and the shared tail raises it after `_record_call`/`_emit_after_audit` have run, so a guardrail whose verdict could not be trusted is both audited and fatal to the row. Distinguishing this from the service-error arm matters operationally — a malformed response is non-retryable and indicates provider or contract drift rather than transient failure, so it is deliberately recorded under its own status rather than folded into `service_error`. Invalidated if the deferred `raise terminal_error` is dropped, or if this arm ever returns a decision instead of terminalizing.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:02:35.375916+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the `GuardrailResponseError` is not swallowed: `BedrockGuardrailsClient.apply_guardrail` stores it in `terminal_error`, records the malformed response, and then explicitly re-raises it at plugins/transforms/aws/guardrails_client.py:607-608. The shared control was verified in the source, and no path from this exception arm returns a decision before that re-raise. R6 therefore does not describe the behavior at this site.
  judge_signature_version: 2
  scope_fingerprint: 11a921181e3ddc4321378665a11701e5da29f39c1650d0035ba48e7cad9a19e7
  judge_transport: codex_cli
  ast_path: 'body[56]/body[5]/body[8]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:ffc179afa80624faac45d7c163afd034cb34ba92bbd3b3b0efc30129c2cc3cdc'
- key: plugins/transforms/aws/guardrails_client.py:R6:BedrockGuardrailsClient:apply_guardrail:fp=b56971e35f73d863
  owner: john
  reason: |-
    R6 fires on `except _bedrock_provider_exception_types() as error:`, the vendor/transport arm of `apply_guardrail`. This is the ADR-032 external boundary: the botocore exception is parsed by `_provider_error` into two owned values (`retryable`, `attempts`), an ELSPETH-owned `GuardrailServiceError` is constructed from them, and the vendor object is discarded rather than propagated — so no botocore message, endpoint, or guardrail identifier reaches the audit payload or the caller. The error is still terminal: it is held in `terminal_error` only long enough for the unconditional `_record_call`/`_emit_after_audit` pair to write the failed call, then raised, so a guardrail that never rendered a verdict can never be mistaken for a `safe` decision. Invalidated if `_provider_error` starts returning the vendor exception or its text, or if the trailing raise becomes conditional on `retryable`.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:02:34.098625+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the handler does not silently swallow the provider failure: it converts the external exception into an owned `GuardrailServiceError`, records the failed call, and the unconditional `if terminal_error is not None: raise terminal_error` at lines 607-608 prevents any decision from being returned. The visible code confirms the rationale’s control-location claim within `BedrockGuardrailsClient.apply_guardrail`, including the audit-before-telemetry ordering and terminal raise. R6’s premise is therefore false at this site, and R6 is not eligible for the `@trust_boundary` decorator mechanism.
  judge_signature_version: 2
  scope_fingerprint: 11a921181e3ddc4321378665a11701e5da29f39c1650d0035ba48e7cad9a19e7
  judge_transport: codex_cli
  ast_path: 'body[56]/body[5]/body[8]/handlers[2]'
  judge_metadata_signature: 'hmac-sha256:v2:05ed3542f1b0585dee431735a944acc2c7d6303cb1e29420e2bbe4abc40f25de'
- key: plugins/transforms/aws/textract_bucket_region.py:R4:HeadBucketClient:_emit:fp=4f0f3af3eb396ab7
  owner: john
  reason: |-
    The broad `except Exception` wraps only the telemetry emit callback, which runs AFTER the audit row has been durably recorded via `_record_call` in `verify_bucket_region`. Telemetry is a Tier-3 best-effort sink: an exporter failure must not convert an already-audited HeadBucket verification into a row failure. The handler is not a swallow-all: the two clauses above it re-raise `contract_errors.TIER_1_ERRORS` and the programming-error family (TypeError/AttributeError/KeyError/NameError), so only exporter-side runtime failures reach the warning path, and those are logged with `exc_info=True`, run_id, state_id and call_type so the loss is visible. This mirrors the identical `telemetry_emit_failed` discipline in infrastructure/clients/http.py, llm.py and aws/textract_client.py.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:07:11.277427+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.91
  judge_rationale: |-
    PRESCRIBED FORM — the control-location claim is verified: `verify_bucket_region` records through `HeadBucketClient._record_call` before invoking `_emit` at plugins/transforms/aws/textract_bucket_region.py:375, and `_record_call` synchronously delegates to the Landscape recorder at plugins/infrastructure/clients/base.py:119. The broad handler is confined to the subsequent best-effort telemetry operation, while Tier-1 integrity exceptions and the listed programming-error family are explicitly re-raised and residual telemetry failures are recorded with contextual warning data. This conforms to audit-first, telemetry-second ordering and does not suppress the authoritative audit record.
  judge_signature_version: 2
  scope_fingerprint: 39390442e2283f721860f96b21663f2278b702db12326a645cfe0e2adc3e60da
  judge_transport: codex_cli
  ast_path: 'body[39]/body[2]/body[2]/handlers[2]'
  judge_metadata_signature: 'hmac-sha256:v2:98d3805b67fbc454f57819198ca4771a79b2a01e108c832fa95dd903c4ae50aa'
- key: plugins/transforms/aws/textract_bucket_region.py:R6:HeadBucketClient:verify_bucket_region:fp=76d0ad1a5163f688
  owner: john
  reason: |-
    The `except BucketRegionUnverifiedError as error: terminal_error = error` handler does not swallow: the typed rejection raised by the `_success_proof`/`_strict_mapping` Tier-3 boundaries is captured, the audit row records status `unverified` with the sanitized code, retryability and http_status, telemetry is emitted, and the method re-raises it unconditionally (`if terminal_error is not None and proof is None: raise terminal_error`). The deferral exists so a malformed or region-mismatched HeadBucket response is AUDITED before it fails the row; the lint cannot see the raise because it lives after the try statement rather than inside the handler.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:04:09.610803+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the exception is deferred, not swallowed. In plugins/transforms/aws/textract_bucket_region.py:330, `HeadBucketClient.verify_bucket_region` stores the typed error, records the failed call through `_record_call` at line 375, emits telemetry, and unconditionally re-raises the stored error when no proof exists at line 394. The visible control flow therefore contradicts R6’s premise, and R6 is not eligible for the `@trust_boundary` decorator mechanism.
  judge_signature_version: 2
  scope_fingerprint: 46b8d9ca93acacef0ed54a10514ed4a649aca48714597be6d1ef05a8df77c033
  judge_transport: codex_cli
  ast_path: 'body[39]/body[3]/body[7]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:f9d3b26690875902f9d095f13007f44186284765e1ada18b0719ba9f3793e350'
- key: plugins/transforms/aws/textract_bucket_region.py:R6:HeadBucketClient:verify_bucket_region:fp=e620b0fb93d85dc6
  owner: john
  reason: |-
    Same record-then-raise seam for the botocore-exception clause (`_provider_exception_types()`): the vendor exception is classified through the non-raising `_provider_outcome` boundary into either a region proof (a 301/403 envelope carrying `x-amz-bucket-region`, which is a SUCCESS outcome recorded and returned) or an owned sanitized `BucketRegionUnverifiedError` stored in `terminal_error`, which is re-raised after `_record_call`/`_emit`. Nothing is dropped: the raw botocore exception is replaced by an owned typed exception carrying only the bounded provider code, and the raise is deferred solely so the audit row precedes the failure.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:04:15.105135+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the exception is not silently swallowed: `_provider_outcome` explicitly converts every caught botocore exception into either a validated region proof or an owned `BucketRegionUnverifiedError` (`plugins/transforms/aws/textract_bucket_region.py:206`). The verified continuation records the outcome through `_record_call`, emits telemetry, and then raises `terminal_error` when no proof exists (`plugins/transforms/aws/textract_bucket_region.py:375`), preserving the required audit-before-failure ordering. R6 therefore fires on syntax whose surrounding control flow implements an explicit recorded outcome rather than lost error handling.
  judge_signature_version: 2
  scope_fingerprint: 46b8d9ca93acacef0ed54a10514ed4a649aca48714597be6d1ef05a8df77c033
  judge_transport: codex_cli
  ast_path: 'body[39]/body[3]/body[7]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:093ba968686eb7b14a240713f163cfa07a3371e82e26222055c5f538133e0b6e'
- key: plugins/transforms/aws/textract_client.py:R6:TextractClient:get_document_analysis:fp=812fdb6a9a6d7fe6
  owner: john
  reason: |-
    Same record-then-raise seam for the botocore-exception clause on GetDocumentAnalysis: the vendor exception is parsed by the `_provider_error` boundary into an owned `TextractServiceError`/`TextractIdempotencyInvariantError`, audited with sanitized code and attempt count, and re-raised as `terminal_error` after `_record_call`/`_emit_after_audit`.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:05:14.351042+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the R6 premise is false because the provider exception is translated into an owned terminal exception at textract_client.py:641-650 and explicitly raised at textract_client.py:681-682. The named control path is verified: `_record_call` records the error at textract_client.py:664-672 before `_emit_after_audit` runs at lines 673-680, and its implementation in clients/base.py:119-164 delegates directly to the audit recorder without suppressing failures. R6 is not eligible for the `@trust_boundary` structural alternative, which is limited to R1 and R5.
  judge_signature_version: 2
  scope_fingerprint: aedf48eb422c6a36eef68ebe8f91ba98dee4edbbd537ea8eb08a09903b8c766b
  judge_transport: codex_cli
  ast_path: 'body[49]/body[3]/body[9]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:dad65482e5be64231df1db2ce14e7254ca49cd9bb10d09e7c914348bd354921d'
  judge_excerpt_redactions:
    - pattern: dotenv_secret_assignment
      byte_count: 26
      redacted_hash: 6cb953151f749ea2
- key: plugins/transforms/aws/textract_client.py:R6:TextractClient:get_document_analysis:fp=d4fd730f1f3bcde5
  owner: john
  reason: |-
    Same record-then-raise seam as start_document_analysis: `except TextractResponseError` assigns `terminal_error`, writes the malformed-response category to the audit row and telemetry, and the method ends with `if terminal_error is not None: raise terminal_error`. The handler defers the raise past the audit write; it never suppresses it.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:07:41.337576+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the handler does not silently swallow `TextractResponseError`; it stores the exception in `terminal_error`, records the failed call, emits post-audit telemetry, and then re-raises it. This control-location claim is verified in `plugins/transforms/aws/textract_client.py:664`, where `_record_call` runs, and at line 681, where `terminal_error` is checked and raised. R6 therefore does not apply at this site, and the finding is not eligible for the Tier-3 boundary decorator because R6 is outside its R1/R5 scope.
  judge_signature_version: 2
  scope_fingerprint: aedf48eb422c6a36eef68ebe8f91ba98dee4edbbd537ea8eb08a09903b8c766b
  judge_transport: codex_cli
  ast_path: 'body[49]/body[3]/body[9]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:ffa13b6ab76f2e1f920982949baa0ee4c180e682b791c01863df75a493e4a1b6'
  judge_excerpt_redactions:
    - pattern: dotenv_secret_assignment
      byte_count: 26
      redacted_hash: 6cb953151f749ea2
- key: plugins/transforms/aws/textract_client.py:R6:TextractClient:start_document_analysis:fp=3d274f7aaaeea5e1
  owner: john
  reason: |-
    The botocore-exception handler (`_textract_provider_exception_types()`) classifies the vendor error through the `_provider_error` boundary into a sanitized `TextractServiceError` or `TextractIdempotencyInvariantError`, stores it in `terminal_error`, records the sanitized code/retryable/attempts on the audit row, and the function re-raises `terminal_error` after the audit write. Nothing is dropped: the raw botocore exception is replaced by an owned typed exception carrying only the bounded error code, and the raise is deferred solely so the audit row precedes the failure.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:07.422193+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the handler does not swallow the provider failure: `TextractClient.start_document_analysis` assigns an owned terminal exception at textract_client.py:535-546 and unconditionally raises it when present at textract_client.py:573-574. Before that raise, `_record_call` is invoked at textract_client.py:556-564, and its implementation synchronously delegates to the audit recorder at plugins/infrastructure/clients/base.py:119-164, verifying the rationale’s named control location. The R6 premise is therefore false at this site, and R6 is not eligible for the `@trust_boundary` structural alternative.
  judge_signature_version: 2
  scope_fingerprint: 9ad4a0aaa1c16c227d11795d1cd04c449f9629315f9ab7b7563cb13f69d6e77a
  judge_transport: codex_cli
  ast_path: 'body[49]/body[2]/body[13]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:9fdaf4674b609dd3646fb0bb06bfabe2eb0533f753957f62fdf05950dbd376b1'
- key: plugins/transforms/aws/textract_client.py:R6:TextractClient:start_document_analysis:fp=dcb30b99a5f8b208
  owner: john
  reason: |-
    The `except TextractResponseError` handler does not swallow: it captures the exception into `terminal_error`, records the failure as the audit row's error payload (category, retryable=False) and telemetry, and the function re-raises `terminal_error` unconditionally after `_record_call`/`_emit_after_audit` (`if terminal_error is not None: raise terminal_error`). The deferral exists so a malformed provider response is AUDITED before it fails the row; the lint cannot see the raise because it lives after the try statement rather than inside the handler.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:16.974210+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the flagged handler defers rather than swallows the exception: `TextractResponseError` is assigned to `terminal_error` and represented in the error payload at textract_client.py:529-534. The control path then invokes `_record_call` and `_emit_after_audit` before `if terminal_error is not None: raise terminal_error` at textract_client.py:556-574; `_record_call` is the Landscape recording control defined in plugins/infrastructure/clients/base.py:119-164. This satisfies R6 because the original malformed-response exception is re-raised after audit-first recording, and R6 is not eligible for the `@trust_boundary` decorator.
  judge_signature_version: 2
  scope_fingerprint: 9ad4a0aaa1c16c227d11795d1cd04c449f9629315f9ab7b7563cb13f69d6e77a
  judge_transport: codex_cli
  ast_path: 'body[49]/body[2]/body[13]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:bc3cc812f38913e836893bea5e3accbcb48b4d5701b26a7a6edf09f354785b66'
- key: plugins/transforms/aws/textract_client.py:R6:TextractInlineClient:analyze_document:fp=18ef5bf56371b2af
  owner: john
  reason: |-
    Same record-then-raise seam for the botocore-exception clause on AnalyzeDocument: `_provider_error` (a non-raising Tier-3 boundary) sanitizes the vendor exception, the owned `TextractServiceError` is stored in `terminal_error`, the audit row records the bounded code/retryable/attempts, and `terminal_error` is re-raised after `_record_call`/`_emit_after_audit`. The idempotency-mismatch arm is deliberately collapsed here because AnalyzeDocument sends no token.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:00.181191+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the exception is not swallowed: textract_client.py:800-809 converts the vendor exception into an owned TextractServiceError and prepares bounded audit data, textract_client.py:819-835 records the call before telemetry, and textract_client.py:836-837 re-raises the stored terminal error. The claimed sanitization boundary is verified at textract_client.py:238-275, where `_provider_error` is explicitly non-raising and bounds/classifies the external exception data. R6 is not eligible for the `@trust_boundary` structural alternative, which suppresses only R1 and R5.
  judge_signature_version: 2
  scope_fingerprint: 91105029b1f3c1ef2c00505539d7493586826cd33a6d9fd77600eb0adffaf9d3
  judge_transport: codex_cli
  ast_path: 'body[50]/body[2]/body[11]/handlers[1]'
  judge_metadata_signature: 'hmac-sha256:v2:be8d03b4cfdd2955ed658cb7b1a2185fb36d7ad21ee3e8ff38e75beae6370189'
- key: plugins/transforms/aws/textract_client.py:R6:TextractInlineClient:analyze_document:fp=8eacd854c0db8346
  owner: john
  reason: |-
    Same record-then-raise seam on the synchronous AnalyzeDocument path: `except TextractResponseError` stores the exception in `terminal_error`, audits the malformed-response category and attempt count, and the method re-raises `terminal_error` after the audit write. The deferral guarantees the audit row exists before the row fails.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:07:38.974919+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    PRESCRIBED FORM — the exception is deferred, not swallowed: `TextractInlineClient.analyze_document` stores `TextractResponseError` in `terminal_error`, records the failed call, and then raises that same error at plugins/transforms/aws/textract_client.py:836. The named control is verified in the visible source, and the sequence preserves audit-before-telemetry primacy while ensuring the malformed response still fails explicitly. R6 therefore does not represent silent recovery at this site.
  judge_signature_version: 2
  scope_fingerprint: 91105029b1f3c1ef2c00505539d7493586826cd33a6d9fd77600eb0adffaf9d3
  judge_transport: codex_cli
  ast_path: 'body[50]/body[2]/body[11]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:e55bb5c2ddb192739ce75a3793dc439a2bc240788e1e685396e2bc3ff19bfff1'
- key: plugins/transforms/aws/textract_document_analysis.py:R5:AWSTextractDocumentAnalysis:_bind_profiled_audit_identity:fp=e8b5e72fabbf6009
  owner: john
  reason: |-
    `isinstance(identity, TextractProfiledAuditIdentity)` is the nominal admission check on the Web-only audit authority that `web/execution/preflight.py` binds after runtime construction. The parameter is deliberately typed `object` because a typed signature is not a runtime control: this method is the only place the transform accepts an identity that later drives `_audit_location_identity` and the HMAC binding-fingerprint comparison below it, so an impostor must be refused here, and a raise on anything but the concrete owned frozen class is ADR-032 rule 1 (nominally type what ELSPETH owns; never a runtime_checkable Protocol). Nothing is defaulted or coerced — the check exists so that no forgeable config field can stand in for the identity.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:16.290256+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    PRESCRIBED FORM: the visible code uses a nominal `isinstance` check against the concrete ELSPETH-owned `TextractProfiledAuditIdentity` and immediately raises `TypeError` on an impostor. The identity is accepted as `object`, then its profile and binding fingerprint are verified before it is stored and used, so this is an offensive runtime admission check rather than defensive recovery, coercion, or defaulting. The parameter is an ELSPETH-owned audit-authority object, not Tier-3 external data, so the trust-boundary decorator contract does not apply.
  judge_signature_version: 2
  scope_fingerprint: f07e5f3ceaabd7895996a465c2d66930d2c93d066abd8934e7ec1484cd74b60a
  judge_transport: codex_cli
  ast_path: 'body[44]/body[17]/body[1]/test/operand'
  judge_metadata_signature: 'hmac-sha256:v2:6b62a20abd5b509735a54f9ca59a00f09de5a7d267ebd5e7848cd07fd37ddf36'
- key: plugins/transforms/aws/textract_document_analysis.py:R5:AWSTextractDocumentAnalysis:_process_single_with_state:fp=59e13b8df2751e99
  owner: john
  reason: |-
    `isinstance(location, TransformResult)` is nominal discrimination against `TransformResult`, a concrete class ELSPETH owns, over the Tier-2 union `_read_document_location` returns (`tuple[str, str, str | None] | TransformResult`): the early-return arm carries an already-built row error result (missing/invalid bucket, key or version), the other arm carries the validated S3 location that is then unpacked. This is ADR-032 rule 1 (internal boundary: isinstance against an owned concrete class), not a Tier-3 shape guard, and neither arm defaults or coerces — the same shape B20 rationalised in textract_inline_analysis._process_single_with_state.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:43.402060+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — this is nominal discrimination between the two explicitly declared arms of a first-party return contract, not a defensive re-check masking a contract violation. The helper at plugins/transforms/aws/textract_document_analysis.py:586 declares `tuple[str, str, str | None] | TransformResult`, returns validation errors as `TransformResult` at lines 593-625, and returns the validated tuple at line 628; the branch at line 708 therefore preserves both arms without coercion, defaults, or silent recovery. The input `row` is Tier-2 pipeline data, so this is not a function-scoped Tier-3 boundary eligible for the decorator.
  judge_signature_version: 2
  scope_fingerprint: d4f4407b38866f026eaf745591d44147945ffeb88d5de3265a7a2358da60103c
  judge_transport: codex_cli
  ast_path: 'body[44]/body[29]/body[3]/test'
  judge_metadata_signature: 'hmac-sha256:v2:355c436e2e2807f9999054fd69e1e0b93faf423df21dc8b9f9b34379b6aa895b'
- key: plugins/transforms/aws/textract_document_analysis.py:R5:AWSTextractDocumentAnalysis:_process_verified_document:fp=c505b6ba2633ab5c
  owner: john
  reason: |-
    `isinstance(collected, TransformResult)` discriminates the Tier-2 union `_poll_and_collect` returns (`tuple[Mapping[str, Any], ...] | TransformResult`): a routed row error (poll_timeout, shutdown_requested, malformed_response, too_many_blocks, pagination_cycle …) versus the collected SUCCEEDED pages that `normalize_textract_result` then validates. `TransformResult` is a concrete owned class, so this is ADR-032 rule 1 nominal typing of an internal seam; the error arm is returned unchanged and the page arm is fully re-validated downstream, so nothing is defaulted.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:57.036552+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — this `isinstance` is nominal discrimination between the two arms of an explicit first-party return union, not defensive revalidation or concealment of a contract violation. The verified `_poll_and_collect` definition at plugins/transforms/aws/textract_document_analysis.py:854 declares `tuple[Mapping[str, Any], ...] | TransformResult`; its error paths return `TransformResult` while the successful completion at line 938 returns `tuple(pages)`. The external page contents remain subject to downstream normalization, and this branch neither coerces nor fabricates them.
  judge_signature_version: 2
  scope_fingerprint: 8a7baecf6fc0166bf7e13c5945e3d7aabfdbeeef53b7d045c9763e9fb3326d51
  judge_transport: codex_cli
  ast_path: 'body[44]/body[30]/body[6]/test'
  judge_metadata_signature: 'hmac-sha256:v2:44b5bc2dc3231c3c44e268d56b548125e21b40bbb32296470936010582f67334'
  judge_excerpt_redactions:
    - pattern: dotenv_secret_assignment
      byte_count: 20
      redacted_hash: 2bfc0af37d30aa45
- key: plugins/transforms/aws/textract_inline_analysis.py:R5:AWSTextractInlineAnalysis:_process_single_with_state:fp=6944f1e7021a0625
  owner: john
  reason: |-
    `isinstance(document, TransformResult)` is nominal discrimination against `TransformResult`, a concrete class ELSPETH owns, over the Tier-2 union `_read_document_bytes` returns (`tuple[str, bytes] | TransformResult`): the early-return arm carries an already-built row error result, the other arm carries validated payload bytes. This is ADR-032 rule 1 (internal boundary: isinstance against an owned concrete class), not a Tier-3 shape guard, and neither arm defaults or coerces — a foreign object would fail the tuple unpack on the next line.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:35.905935+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the check discriminates the declared first-party union returned by `_read_document_bytes`, rather than defensively revalidating a value under a single fixed contract. The visible helper paths return either an owned `TransformResult.error(...)` or the `(payload_ref, content)` tuple consumed immediately afterward, and the nominal check targets the concrete ELSPETH-owned `TransformResult` class as ADR-032 permits. No malformed value is suppressed, coerced, or defaulted, and the subject is not rooted in a Tier-3 parameter requiring `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: 3a83a67eb1bfba61b515dfb7f89e3cf17859d1de28ca126d715fae124a9902bf
  judge_transport: codex_cli
  ast_path: 'body[41]/body[24]/body[3]/test'
  judge_metadata_signature: 'hmac-sha256:v2:b31aa4286ac48636aae77cf57b9e4aee22c83a62bb443289c5752763cb1c2c2a'
- key: plugins/transforms/field_mapper.py:R6:_names_a_row_key:fp=caa3a06a0cceb6b5
  owner: john
  reason: |-
    `except ExternalHeaderError: return False` is the predicate's documented negative answer, not a swallow: `_names_a_row_key` asks whether a config literal is a fixed point of `normalize_field_name`, and a literal that normalizes to NOTHING (the only case that raises `ExternalHeaderError`) names no row field at all because the source boundary rejects such a header, so `False` is the correct and only truthful value. The handler is narrowed to the one owned exception class that means exactly that; a bare `ValueError` from an algorithm bug still propagates, so the error class a bad mapping key raises at construction is unchanged. This mirrors `value_transform._row_key_aliases` deliberately, and returning `False` here is what lets `declared_input_fields` ABSTAIN rather than assert a row key that cannot exist (elspeth-f262a8c678).
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:08:45.483336+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.96
  judge_rationale: |-
    RULE MISFIRES — `_names_a_row_key` is a Boolean classification predicate, and the visible docstring establishes that `ExternalHeaderError` means the literal cannot normalize to any row-field name, making `False` the predicate’s truthful negative result rather than silent recovery. The handler is narrowly typed, while unrelated algorithm failures continue to propagate, and the result causes declaration logic to abstain instead of fabricating a positive field claim. The decorator alternative is inapplicable because the finding is R6, not R1 or R5.
  judge_signature_version: 2
  scope_fingerprint: 66abe3201f61c6c4d6a39b7dd79f8374e0b81d44ee55ab03f2b12e450fea81d4
  judge_transport: codex_cli
  ast_path: 'body[18]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:30d76ab686a43ba9e1fd1440af4eeb6c26dd5c8c0ff6ee182a1d3e87b137e099'
- key: plugins/transforms/llm/providers/azure.py:R6:_module_:fp=d94f619bf86df74c
  owner: john
  reason: |-
    The module-level `except ImportError: _imported_configure_azure_monitor = None` is an optional-SDK probe for `azure.monitor.opentelemetry`, not a swallow: the absence is recorded explicitly in a module name that `_configure_azure_monitor` re-checks and converts into a raised `ImportError` with install guidance the moment `azure_ai` tracing is actually configured. Nothing is defaulted silently — the None sentinel is the documented 'SDK absent' state, and it is consumed by an explicit error path rather than a fallback.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:10:36.582972+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the caught ImportError is converted into an explicit None sentinel rather than discarded, as shown in plugins/transforms/llm/providers/azure.py:281-292. The named control `_configure_azure_monitor` checks that sentinel and raises an install-guidance ImportError at azure.py:321-326; both visible callers reach it when `AzureAITracingConfig` is selected (plugins/sources/llm/source.py:291-292 and plugins/transforms/llm/transform.py:1722-1723). R6's silent-swallow premise is therefore false, and the decorator is inapplicable because this is R6 at module scope rather than a parameter-rooted R1/R5 boundary.
  judge_signature_version: 2
  scope_fingerprint: a07b5ee0191cf91da3515dc75bf291cb5792de0e7b27cd39e42a8899e313ab69
  judge_transport: codex_cli
  ast_path: 'body[20]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:d7353846cd23ffe5b927ed46b060d2bfeb6a5baea40e2ba66be5d6b9517ff2b4'
- key: plugins/transforms/llm/providers/azure.py:R9:AzureLLMProvider:execute_query:fp=4b32158d3c47e02e
  owner: john
  reason: |-
    `self._llm_clients.pop(cache_key, None)` in the `finally` evicts the per-audit-parent `AuditedLLMClient` cache entry. The entry is keyed by `audit_parent.cache_key` (the row state), and multi-query LLM transforms run every query for one row CONCURRENTLY through `PooledExecutor` with the same audit parent, so two `execute_query` calls legitimately share one cached client: whichever finishes first evicts it and the second finally must find the key absent. The default is the honest idempotent-eviction contract under that concurrency (Tier-1 internal cache, no data read), not a hidden missing-key; a `del` here would crash a correct concurrent row. Nothing is lost — the cache is a memory bound, and `close()` clears whatever remains.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:10:23.008543+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — this is idempotent cache eviction, not a data read or a hidden missing-key invariant. MultiQueryStrategy runs query calls concurrently with the same state_id (plugins/transforms/llm/transform.py:1001), while LLMAuditParent.cache_key depends only on that state_id (plugins/transforms/llm/provider.py:74), so concurrent calls legitimately share one cache entry and the first completion can remove it before another reaches finally. The ignored None default therefore expresses expected concurrent absence without coercing, fabricating, or suppressing pipeline data, and AzureLLMProvider.close separately clears any remaining entries (plugins/transforms/llm/providers/azure.py:271).
  judge_signature_version: 2
  scope_fingerprint: 1f1b259c1e18276b4fa7c49835f509bb438a2535dfa67870bee8e39f121ec162
  judge_transport: codex_cli
  ast_path: 'body[18]/body[2]/body[2]/finalbody[0]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:52dde7b0f914a316b3cb3285bafa8d36a80350bb154df78e7a0a45577eb97bd4'
- key: plugins/transforms/llm/providers/bedrock.py:R6:BedrockLLMProvider:execute_query:fp=0419f7eac920b410
  owner: john
  reason: |-
    `except LLMClientError as error: redacted_error = _redacted_bedrock_error(error)` does not swallow: the typed error is converted into an owned static-text `LLMClientError` subclass of the same category and re-raised unconditionally after the `finally` (`if redacted_error is not None: raise redacted_error from None`). The raise is deferred OUT of the handler deliberately: `raise ... from None` inside an `except` still attaches the original exception as `__context__`, which would carry provider-controlled error text (and any echoed request content) into the exception chain that reaches logs and the web. Raising after the handler has exited is the only way to bound that egress; the lint sees a handler with no raise because the raise is one statement later.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:09:53.966248+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the handler does not silently swallow the typed error: it stores the redacted replacement, performs cleanup in the `finally`, and then unconditionally raises that replacement when `redacted_error` is non-`None`. The visible control flow directly supports the rationale that the raise is deliberately deferred until after leaving the handler so provider-controlled exception context is not attached, while `raise redacted_error from None` prevents chain display. R6 therefore matches the handler syntactically but its premise is false at this site.
  judge_signature_version: 2
  scope_fingerprint: d0a252836390a7d4c246225032436cd05421a1ea53ee5a3fe784856549ca67d4
  judge_transport: codex_cli
  ast_path: 'body[20]/body[2]/body[4]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:138617cd8890c3ebeaf0240f18bccfadc50535108cee81b888ec373364f194c0'
- key: plugins/transforms/llm/providers/bedrock.py:R6:BedrockLLMProvider:runtime_preflight:fp=7625acdc1cfb0b83
  owner: john
  reason: |-
    Same bounded-egress seam on the preflight smoke test: the `except LLMClientError` handler stores `_redacted_bedrock_error(error)` and the function re-raises it after the `finally` closes the audited client (`if redacted_error is not None: raise redacted_error from None`). Deferring the raise past the handler is what keeps the raw LiteLLM/Bedrock exception (provider-controlled text) out of `__context__`; the error category and retryability are preserved in the owned exception, so nothing is lost.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:14:27.848850+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the caught `LLMClientError` is not silently swallowed: it is converted to an owned, redacted exception and visibly raised at `BedrockLLMProvider.runtime_preflight` after `client.close()` runs in the `finally` block. The rationale’s control-location claim is verified by the excerpt at lines 203–217, and `raise redacted_error from None` deliberately prevents provider-controlled exception context from escaping while preserving explicit failure behavior. R6 is not eligible for the `@trust_boundary` decorator mechanism.
  judge_signature_version: 2
  scope_fingerprint: e2650b6a2322e5000c33ecb3cfcf874fe82ab7d138106421c5c7abb4cc8cc54e
  judge_transport: codex_cli
  ast_path: 'body[20]/body[3]/body[3]/body[0]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:e351af0a466ed40e1ef8af5be2bf4b893b9593e7fdbd5bb514a77e8b0ece8747'
- key: plugins/transforms/llm/providers/bedrock.py:R9:BedrockLLMProvider:execute_query:fp=b17a41278a235a5e
  owner: john
  reason: |-
    `self._llm_clients.pop(cache_key, None)` in the `finally` evicts the per-audit-parent `AuditedLLMClient` cache entry keyed by `audit_parent.cache_key`. Multi-query LLM transforms run every query for one row concurrently through `PooledExecutor` with the same audit parent, so two `execute_query` calls share one cached client and the second finally legitimately finds the key already evicted. The default is the idempotent-eviction contract for a Tier-1 internal cache under that concurrency, not a hidden missing key; a `del` would crash a correct concurrent row, and `close()` clears whatever remains.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:19:31.308772+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.99
  judge_rationale: |-
    RULE MISFIRES — the missing key is an expected concurrent lifecycle state, not a hidden invariant failure. MultiQueryStrategy._execute_parallel dispatches queries concurrently with the same state_id and token_id (plugins/transforms/llm/transform.py:991), while LLMAuditParent.cache_key derives solely from state_id (plugins/transforms/llm/provider.py:74), so their Bedrock execute_query calls share the cache entry and multiple finally blocks legitimately attempt eviction. BedrockLLMProvider._get_llm_client and execute_query protect the shared cache with the same lock (plugins/transforms/llm/providers/bedrock.py:167 and :225), making pop(cache_key, None) the explicit idempotent-eviction contract rather than defensive suppression; R9 is not decorator-suppressible.
  judge_signature_version: 2
  scope_fingerprint: d0a252836390a7d4c246225032436cd05421a1ea53ee5a3fe784856549ca67d4
  judge_transport: codex_cli
  ast_path: 'body[20]/body[2]/body[4]/finalbody[0]/body[0]/value'
  judge_metadata_signature: 'hmac-sha256:v2:312c55867dac989a0f6fa229552846132f503997123845a0b1dd897da8ea4ac1'
- key: plugins/transforms/llm/providers/openrouter.py:R6:_http_error_body_text:fp=adbf37f86060de78
  owner: john
  reason: |-
    The `except (httpx.ResponseNotRead, RuntimeError): return ""` handler wraps only `error.response.text`, which httpx raises on when the Tier-3 error body was streamed and never buffered. The function feeds two internal classifiers — the context-length wording sniff and `_summarize_http_error_body`'s bounded `chars=` suffix — and an unread body legitimately contributes nothing to either: there is no text to match and no length to report. Nothing is lost: the HTTP status still drives the typed error, the audited HTTP call row carries whatever was captured, and the empty string is the honest classification input, not a fabricated body.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:16:59.480712+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.9
  judge_rationale: |-
    PRESCRIBED FORM — `_http_error_body_text` catches only retrieval failures from reading the Tier-3 provider error body and returns an explicit empty-string sentinel for optional classification, without fabricating body content. The caller at `plugins/transforms/llm/providers/openrouter.py:386`–`406` still classifies and raises the original HTTP failure from its status code, so the swallowed body-read signal does not suppress the actual failure. R6 is not supported by `@trust_boundary`, and this finding therefore does not qualify for the decorator nudge.
  judge_signature_version: 2
  scope_fingerprint: 287862232b1da310df0663e0e36857bc7c32313dde7f346dbf6de87b12020a3a
  judge_transport: codex_cli
  ast_path: 'body[32]/body[1]/handlers[0]'
  judge_metadata_signature: 'hmac-sha256:v2:9aab9c83d299522e363dbdc8647617f4bd4cbd7d4f9676ae85aea1bb95f4d68f'
- key: plugins/transforms/pdf_rasterize.py:R5:PDFRasterize:_map_document_result:fp=3a7765ad5e1c5b49
  owner: john
  reason: |-
    `isinstance(result, DocumentRefusal)` is nominal discrimination against an ELSPETH-owned frozen dataclass that is one member of the closed `RenderResult` union returned by the out-of-process renderer seam (see the 2026-08-25 pdf_rasterize entry in docs/agents/recent-code-hints.md). Each arm maps one owned variant to its TransformResult error; the fallthrough is the rendered-pages variant, which is then verified against `output_dir` and page-path containment, so nothing is defaulted. ADR-032 rule 1 prescribes exactly this isinstance-against-a-concrete-owned-class form for internal unions.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:17:27.672825+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.94
  judge_rationale: |-
    RULE MISFIRES — This `isinstance` is nominal variant dispatch over the declared `RenderResult` union, not defensive revalidation or silent recovery. The rationale identifies `DocumentRefusal` as an ELSPETH-owned concrete frozen type, and the visible code consistently maps union variants without coercion, fabrication, or defaulting. The decorator alternative does not apply because `result` is an already-promoted owned union rather than raw Tier-3 parameter data.
  judge_signature_version: 2
  scope_fingerprint: 5c15fdfb801ce7f72c080fbd0da7d9d56f843e59b99de412fd4a0de6f3b1d87b
  judge_transport: codex_cli
  ast_path: 'body[53]/body[21]/body[0]/test'
  judge_metadata_signature: 'hmac-sha256:v2:d1543768ae62a142e3632fcf97b97a369aa82509e223a9f34b387a808ee3e447'
- key: plugins/transforms/pdf_rasterize.py:R5:PDFRasterize:_map_document_result:fp=52b9ef4c1f113d8a
  owner: john
  reason: |-
    `isinstance(result, RenderTimedOut)` is the second nominal arm over the same owned `RenderResult` union: `RenderTimedOut` is an ELSPETH-owned frozen dataclass and the arm maps it to the non-retryable `render_timeout` row error carrying its own `timeout_seconds`. Nominal discrimination against a concrete owned class is ADR-032's internal-boundary control; no coercion or default is applied and the remaining variant is verified, not assumed.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:14:33.330346+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.97
  judge_rationale: |-
    RULE MISFIRES — the flagged `isinstance` is nominal variant dispatch over the ELSPETH-owned `RenderResult` union, not a defensive re-check that hides a contract violation. The visible code handles `RenderTimedOut` explicitly using its declared `timeout_seconds`, applies no coercion or default, and returns a recorded row error; it also separately dispatches `DocumentRefusal`. This is concrete internal-boundary discrimination consistent with ADR-032, and the value is not Tier-3 data rooted in a function parameter requiring `@trust_boundary`.
  judge_signature_version: 2
  scope_fingerprint: 5c15fdfb801ce7f72c080fbd0da7d9d56f843e59b99de412fd4a0de6f3b1d87b
  judge_transport: codex_cli
  ast_path: 'body[53]/body[21]/body[1]/test'
  judge_metadata_signature: 'hmac-sha256:v2:c9c06b365cdb023c13ee16b6bb36c32d2f6ea4d72f7275b0ccf9558aaddc67c7'
- key: plugins/transforms/reference_join.py:R5:build_reference_index:fp=98f952ae97025a85
  owner: john
  reason: |-
    `isinstance(exc.__cause__, KeyError | IndexError)` discriminates the CAUSE of an `ExpressionEvaluationError` raised by ELSPETH's own expression parser (Tier 1), not external data: the parser chains a `KeyError`/`IndexError` when an output path does not fit a sparse reference entry (legitimately `_UNRESOLVED`, governed by `on_miss`) and anything else when the authored expression is broken (refused at load). Exception-class hierarchy matching is the only correct Python discriminator here — `except KeyError` matches by hierarchy for exactly the same reason — and the else-branch is a loud `ReferenceTableError`, not a default. No coercion or shape guess is involved.
  safety: |-
    Suppression gated by cicd-judge; see judge_rationale below.
  expires: '2026-11-28'
  judge_verdict: ACCEPTED
  judge_recorded_at: '2026-08-30T14:14:44.465495+00:00'
  judge_model: gpt-5.6-sol
  judge_policy_hash: 'sha256:f1b6f8bf56c4aca42b0a591ca75762788c727d9c7d023b4c036a774f86691ccb'
  judge_confidence: 0.98
  judge_rationale: |-
    RULE MISFIRES — the flagged `isinstance` is exception-class dispatch on `ExpressionEvaluationError.__cause__`, not defensive revalidation of data under a fixed contract. The visible comments and branches establish that `KeyError`/`IndexError` specifically represent a path absent from a legitimate sparse entry and are preserved as `_UNRESOLVED`, while every other evaluation failure raises `ReferenceTableError` loudly. No malformed shape is coerced or hidden, and `_UNRESOLVED` records absence rather than fabricating a value; the rationale's Tier-1 label is unnecessary to, and does not undermine, this site-specific justification.
  judge_signature_version: 2
  scope_fingerprint: 99989abd21f7fe4b922266817cb5b9a9892a890e91c12bcb5ae870244929049f
  judge_transport: codex_cli
  ast_path: 'body[30]/body[7]/body[5]/body[0]/handlers[0]/body[0]/test/operand'
  judge_metadata_signature: 'hmac-sha256:v2:651a1c2de5a9e0e9510f68d332ef2c2ce0b032790a8537fa11828c8af42c0601'
