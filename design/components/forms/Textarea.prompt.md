Multiline input matching ELSPETH's text fields, vertical-resizable. Used for the chat composer and prompt/amend editing.

```jsx
<Textarea label="Describe your pipeline" rows={4}
  placeholder="Classify tender submissions and route abusive ones to review…" />
```

Optional `label`/`hint`; accepts all native textarea attributes.
