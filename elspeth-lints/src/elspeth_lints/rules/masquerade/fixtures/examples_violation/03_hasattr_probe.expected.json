[
  {
    "column": 11,
    "file_path": "src/elspeth/capability_probe.py",
    "fingerprint": "43c9dcf434cc82a1",
    "line": 13,
    "message": "hasattr site at 'supports_batch' in src/elspeth/capability_probe.py is not covered by a permanent amnesty and has no entry in config/cicd/masquerade_baseline.yaml. It has 1 current occurrence(s). Per ADR-032, classify it \u2014 external-parse (sentinel getattr + value asserts + owned type), approved-introspection, reflection-owned-table, module-getattr, or data-container-api \u2014 then use the baseline seeder to add the occurrence count and normalized probe_shapes, or rewrite it to direct access / isinstance-against-a-concrete-owned-class if the shape is actually guaranteed.",
    "rule_id": "masquerade.attribute-probes",
    "severity": "error",
    "suggestion": "Run `python -m elspeth_lints.rules.masquerade.seed_baseline`, then adjudicate the new entry in config/cicd/masquerade_baseline.yaml; or rewrite the site."
  }
]
