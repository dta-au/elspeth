# Lane C06 — wave 1 — model `opus`

**CWD:** `.claude/worktrees/tier-c06` (create it: `git worktree add .claude/worktrees/tier-c06 -b tier-c06 tier/blanket-burndown`, then `ln -s "$(git -C . rev-parse --show-toplevel)/.venv" .venv` — the symlink target is the MAIN checkout's venv). Export `PYTHONPATH=$PWD/src:$PWD/elspeth-lints/src` and verify both `elspeth.__file__` and `elspeth_lints.__file__` resolve inside the worktree before trusting any result.

**Read first:** `docs/plans/2026-08-30-per-file-blanket-migration.md` (§ Global Constraints, § Disposition classes, § Lane contract — binding verbatim), `docs/agents/recent-code-hints.md` (2026-08-29 `@trust_boundary` entries), ADR-032, `src/elspeth/contracts/trust_boundary.py` docstring, `CONTRIBUTING.md § Whole-tree gates`.

**Do NOT edit:** `config/cicd/enforce_tier_model/*.yaml`, `.elspeth/`, `elspeth-lints/`. Do NOT commit to the shared checkout. Never hold `ELSPETH_JUDGE_METADATA_HMAC_KEY`.

## Your findings (35)

Every line below is a finding that surfaces the moment the hub deletes your files' blankets. Each must end as **F** (fixed — pattern gone from the raw corpus), **D** (`@trust_boundary`/`@observation_boundary`, honest Tier-3, param-rooted, with `test_ref`+`test_fingerprint` or `non_raising`), or **J** (per-site rationale in `docs/agents/sweeps/tier-burndown/C06.rationales.json`, key exactly as `elspeth-lints check` prints it: `file:RULE:symbol:ast=<path>`).

Expected disposition for this bucket: see the wave-1 table row for C06 in the plan.

### `contracts/audit.py` (1830 LOC; 4 — {'R5': 4})

```
contracts/audit.py:97:R5
contracts/audit.py:110:R5
contracts/audit.py:118:R5
contracts/audit.py:126:R5
```

### `contracts/secret_scrub.py` (212 LOC; 4 — {'R5': 4})

```
contracts/secret_scrub.py:153:R5
contracts/secret_scrub.py:155:R5
contracts/secret_scrub.py:163:R5
contracts/secret_scrub.py:165:R5
```

### `contracts/header_modes.py` (128 LOC; 1 — {'R5': 1})

```
contracts/header_modes.py:50:R5
```

### `contracts/diversion.py` (86 LOC; 1 — {'R5': 1})

```
contracts/diversion.py:73:R5
```

### `tui/screens/explain_screen.py` (670 LOC; 11 — {'R5': 11})

```
tui/screens/explain_screen.py:356:R5
tui/screens/explain_screen.py:371:R5
tui/screens/explain_screen.py:383:R5
tui/screens/explain_screen.py:528:R5
tui/screens/explain_screen.py:547:R5
tui/screens/explain_screen.py:560:R5
tui/screens/explain_screen.py:566:R5
tui/screens/explain_screen.py:571:R5
tui/screens/explain_screen.py:594:R5
tui/screens/explain_screen.py:627:R5
tui/screens/explain_screen.py:650:R5
```

### `tui/widgets/node_detail.py` (391 LOC; 5 — {'R5': 5})

```
tui/widgets/node_detail.py:107:R5
tui/widgets/node_detail.py:110:R5
tui/widgets/node_detail.py:117:R5
tui/widgets/node_detail.py:119:R5
tui/widgets/node_detail.py:285:R5
```

### `tui/widgets/lineage_tree.py` (317 LOC; 2 — {'R5': 2})

```
tui/widgets/lineage_tree.py:30:R5
tui/widgets/lineage_tree.py:67:R5
```

### `testing/__init__.py` (866 LOC; 7 — {'R5': 7})

```
testing/__init__.py:243:R5
testing/__init__.py:276:R5
testing/__init__.py:280:R5
testing/__init__.py:300:R5
testing/__init__.py:326:R5
testing/__init__.py:346:R5
testing/__init__.py:366:R5
```

## Evidence you hand back (Filigree comment on your bucket issue + message to the hub with the branch name)

- raw corpus count before/after: `ELSPETH_JUDGE_METADATA_SIGNATURE_VERIFY_MODE=shape-only-when-key-missing elspeth-lints check --rules trust_tier.tier_model --root src/elspeth --allowlist-dir <copy of config/cicd/enforce_tier_model with per_file_rules: [] in every file> 2>&1 | grep -cE '^[a-z0-9_/]+\.py:[0-9]+:'` — COUNT, never `tail`.
- if you added any decorator: `elspeth-lints check --rules trust_boundary.tests,trust_boundary.scope,trust_boundary.tier --root src/elspeth` clean (CI-only gate; a green pytest proves nothing about it).
- `pytest -n 2 tests/unit/<your area>` green.
- counts `fixed + decorated + justified == 35`, listing each key under its class.
