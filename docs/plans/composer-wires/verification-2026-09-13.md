# Composer wires verification — 2026-09-13

**Local campaign verification complete.** Final candidate gates and the
amended generic review and mode acceptance passed. Partial live cases retain
their measured limitations; this is not a claim of a clean battery, remote
publication, release/main integration or signature clearance.

Candidate `c56b807d5a7c7f79d8c3d30fcf7e8e3702b77f38` was tested in its isolated
donor and then fast-forwarded into the canonical campaign branch. Its parent is
`58e9128ac633ac1eef8a6008c1937d50386af1f1`. Latest incorporated release input
`1e2a31d7eb4ab4257ac81faa40afdfbd63f97830` is a verified ancestor of the final
candidate. These are local commit identities. Live services are stopped and the
browser is closed.

## Contract outcomes

1. **MODEL:** Public tool inputs reach owned model admission before budget,
   provider or blob effects. Controls cover required fields, strict scalar and
   schema bounds, withholding of unknown keys, and safe feedback. Advisor state,
   version, budget and failure accounting remain enforced; malformed owned data
   is not converted into plausible empty results.
2. **READ/ADMITTED:** The practical READ relation measures original-input
   extraction; separate behavior tests demonstrate representative persisted
   options, model filtering and defaults, and advisor request fields. Admission
   covers 30 closed declarative policies and 12 type-driven models. Root-name
   admission does not certify sensitive-value disclosure.
3. **Diagnostic guidance:** Immutable exact-code records share one authority
   with help and teaching. The captured ordered legacy regex inventory retains
   first-match compatibility; new direct guidance does not grow legacy regexes.
   Dynamic forwarding does not prove absence of uncoded errors.
4. **Count applicability:** One rule rejects passthrough with non-null expected
   output count across authoring, runtime settings, state validation and
   import/export. Structural argument rejection remains distinct from semantic
   unsuccessful results; allowed attempted mode/count values survive audit
   serialization. Historical hydration remains readable.
5. **Restricted responses:** Producer-owned fixed roots and nested records
   retain legitimate dynamic leaves, omission, order and independent error and
   disclosure authority. Immutable constructor copies detach backing aliases,
   including mapping proxies. Cache readmission checks original owned identity
   and nested corruption before encoding, preserving current state and schema
   budgets. Performance measurements retained 37 byte and semantic controls;
   the targeted duplicate-default traversal refinement reduced the local median
   from 0.690509 ms to 0.361978 ms without relaxing validation. This is not a
   whole-Composer latency claim.
6. **Sparse presence and errors:** Phase 5 checkpoint
   `1cee88f3045f0ccc90870be457406bd81a7477c7` preserves authored display through
   persistence and frontend comparison. Only the approved singular
   `inline_blob: null` equivalence affects semantic dispatch binding; other
   hashes remain exact. Frozen records with required `message`, `error_code`
   and `component` keys cross actual storage, HTTP, guided replay, frontend and
   evaluator boundaries. Durable first-dispatch, retry, settlement and tampering
   controls cover omission, explicit null and reserved mapping domains.

Phase 6 checkpoint `40c4c61e7969c7a967fc260aecd38b0f2ec56c43` removes redundant
destination notes, proves private mapping-local response labels through actual
SQLite persistence and reload, and makes battery approval uncertainty explicit.
Missing or redacted outcome evidence cannot manufacture applied, clean or
optimal status. Overlapping focused test totals are not added together.

## Final static and integration evidence

The final candidate scorecard completed with exit 0: 42 tools, 104 public root
keys, 13 frontend projected members, no failures or unresolved MODEL/READ cells.
Its extraction and lexical relations do not replace runtime consumption,
sensitive-value tests or frontend dispatch execution.

Final key-free lints completed with exit 1 and 2,038 findings, matching the
parent's full finding multiset after normalizing checkout prefixes and
positions: 0 added and 0 removed, including fingerprints. Three path-prefix
changes and one line move explain the raw differences. Existing fail-closed
checks and signature work remain; this is neither zero-findings success nor
signature authentication.

| Gate | Measured result |
|---|---|
| Final c56 default | 52,664 passed, 87 skipped, 2 expected failures, 75 warnings in 1,397.13 seconds; exit 0; Ruff, mypy and contracts exited 0; frozen |
| Final c56 PostgreSQL | 491 passed, 1 skipped, 52,908 deselected, 11 warnings in 823.36 seconds; exit 0; frozen |
| Tested frontend, unchanged by mode repair | 4,662 passed across 246 files; app, OIDC and workspace types, ESLint, CSS and production build exited 0 |

Default JUnit contains 52,753 cases, zero failures/errors and 89 skipped entries
including the two expected failures. PostgreSQL JUnit contains 492 cases, zero
failures/errors and one skip. Both final gate summaries report PASS and frozen
source. The optional PostgreSQL-selection skip is unavailable ddtrace.

The final default donor lacked the ignored RGR scenario corpus. Compared with
the parent selection, 21 corpus-dependent cases collapsed to 7 skips and
another 9 became skips; the candidate also adds 5 tests. This explains the
different totals. The exact 30 affected cases then passed on canonical c56
with the original corpus: exit 0, 30 passed in 7.06 seconds, JUnit zero skips,
failures or errors. Requested and observed case IDs match; source, tests and
corpus remained unchanged. This closes the omitted coverage without relabeling
the donor's skipped cases or inventing a combined suite total.

Final browser acceptance served the same tested `index-DwnA50aq.js`; dist digest
`aa8c6796d03eeed0b02b1b52dc9f20eaa984de96f3c2cb8f969368b2cdd29028`.

## Live results and boundaries

On 58e, complaint triage completed all 6 records after the two unchanged prompts
and five individually inspected reviews. Downloaded CSV preserved every
complaint and exactly `id`, `complaint`, `category` and `response_sla_hours`,
with billing = 24, outage = 4 and other = 48. Composer calls were 13 main and
1 advisor on the first turn, then 4 main and 0 advisor; no extra user repair.

Vendor risk completed 6 outputs after one repair restored the exact original
template and string-valued source fields. All six retained runtime prompts
matched that template rendered against actual records. Calls were 4 main and
1 advisor, then 6 main and 0 advisor. A removed risk-tier interpretation remains
pending in history; this is not a clean conversation. Manufacturing completed
5 outputs after one lowercase-assignment repair. The four turns contained
5, 7, 7 and 7 calls; the last includes a GLM timeout and withheld prose. All
source fields survived; actual medium and high assignments matched smith and
allens. The low-to-jones mapping was present in the lookup but no low-rated
output exercised it. Successful vendor and manufacturing preflights used
35 and 34 completion tokens respectively, within the new finite cap of 256;
neither proves every model fits that budget.

Earlier f54 complaint and manufacturing preflight failures and guided failures
remain recorded failures. The missing-content repair distinguishes explicit
refusal from budget-exhausted or unexplained missing output without accepting
either as success. The 58e rootless reload unexpectedly selected guided mode;
c56 repairs four freeform initializers without reclassifying genuine guided
histories.

The user explicitly replaced specialized interpretation-widget acceptance with
existing generic review. On c56, fresh session
`06d0ad20-d324-4220-840f-e5a5c36a0dff` made seven successful DeepSeek calls,
producing two real proposals. Browser reload showed one ordinary banner entry
per proposal. The earlier proposal was explicitly rejected;
`cc90a5e4-acd8-4e6f-8d54-c9471b549ab0` was accepted, then its separate content
and hash review for the two source records was acknowledged. Full reload
remained freeform at version 2 with no pending proposal or review. Repeated
acceptance returned 409 with identical state and provider cohort. Three failed
tool invocations remain in history. No full pipeline execution is claimed for
this source-only request.

Genuine guided session `490aa1e1-461e-4b1f-8f8c-6a74aebe8062` also resumed
correctly in the browser. Its upload transition rejected one malformed provider
configuration, then accepted the same Send's provider-authored correction.
Numeric amount, utf-8-sig, discard and exact blob custody survived inspection
and confirmation. Main call counts were 0, 2, 2 and 2; state versions were
1, 2, 3 and 3 before chat, after chat, after confirmation and after replay.
Exact confirmation replay preserved the response. This proves source review,
not a completed wizard. Separate 58e schema-form starting-position acceptance
also preserved the provider's narrower schema rather than substituting server
prefill.

The repaired Case A remains partial: BOM and three-row output and delimiter-only
amendment passed, but fail-whole-run was replaced by a disclosed failure sink.
Both proposals committed and no draft remained pending in this rerun. The
planner misstated the prior delimiter, and its third-turn schema explanation
was withheld after advisor timeout and recovery. No failure path was injected.
Its finalizer exited 1 after the successful run because the descriptor was
absent; the unchanged ledger-only block later completed without repeating
execution. Case B produced one aggregate with count 3, sum 60 and no mean, and
demonstrated direct-code guidance. However, the planner falsely described
omitted policies as fail-fast when the actual defaults discard. Execution was
explicitly limited to the output criterion. Six other proposals remain pending;
no duplicate application was observed. Its finalizer also exited 1 after
completed execution because a descriptor was absent; execution was not repeated.

These policy limitations map to existing `elspeth-0aace271b4` and outstanding
parity work `elspeth-22ebd35458`; the campaign does not absorb that coordinated
policy sweep. Pending and stale proposal browser coverage relates to
`elspeth-f7cb0d6d38`, without proving a new supersession defect. Partial
acceptance is retained rather than described as a clean battery.

## Completion boundary

The local implementation, final gates, supplemental tests and live acceptance
are recorded at c56. The campaign's completion documentation changes no tested
code. Release integration remains a separate action; no push, deployment,
merge-to-release or signature clearance follows from this report. Operational
captures remain privately under `.claude/lanes/composer-wires-execution-20260912/`,
principally `live-candidate-repaired-20260913/` and
`live-candidate-mode-final-20260913/`. This self-contained summary requires no
private hyperlinks and does not authorize publishing credentials or raw
provider payloads.
