# Phase 2 detail — conflict surface between the landed release/0.8.0 and every queued lane

Ticket: `elspeth-4e002bd629` (Phase 2 of milestone `elspeth-b63124d973`).
Measured 2026-09-04 against `release/0.8.0 == landing/multi-replica == 7e34eef6c`
(`origin/release/0.8.0` also `7e34eef6c`; the landing worktree is clean).
Landing range measured as `b21f17a0a..7e34eef6c`: **78 commits, 232 changed files**
(`git --no-optional-locks diff --name-only b21f17a0a 7e34eef6c | wc -l`).

Everything below is read-only. Trial merges are the deprecated trilateral
`git merge-tree <merge-base> release/0.8.0 <lane-HEAD>` form, which writes
nothing (not `--write-tree`). The one executable measurement is the
soft-type-burndown lane's own gate *functions* (`scan_writers` /
`violations` / `stale_reviewed_writers`) driven from a scratch script over both
trees — a scanner run, not a pytest run. Working files, merge-tree outputs and
the scanner script/output are under
`scratchpad/plan-detail/phase2-work/` (`*-mergetree.txt`, `*-files.txt`,
`*-intersect.txt`, `identity-conflicts.txt`, `lane-scanner-out.txt`).

## 0. Headline

| lane | branch @ HEAD | ahead / behind release | dirty | ∩ landing | textual conflicts | class |
|---|---|---|---|---|---|---|
| soft-type-burndown | `lane/soft-type-burndown` @ `f765ea2e1` (pushed, `origin/lane/soft-type-burndown` = HEAD) | 13 / 78, base `b21f17a0a` (= landing's parent) | clean | 2 files | **1 file, 1 hunk** | one conflict + a stale-entry class the lane's OWN gate detects |
| identity-sprint | `identity-sprint/phase1-contracts-epoch` @ `759deba56` (no upstream) | 20 / 170, base `91816d0f3` | 1 modified file | 17 files | **15 files, 41 hunks** | epoch renumber (52/37) + signature fusion + 1 re-home + 2 landed gates with no git signal |
| agentic-interface | `plan/agentic-interface` @ `65bbc77e7` (no upstream) | 12 / 80, base `cbae1ef0c` | clean | 1 file | **0** (hunks disjoint) | trivial |
| lane-elspeth-24d79f64cd | `lane/elspeth-24d79f64cd` @ `284260506` (no upstream) | 0 / 379 | 3 modified src files (+86 lines) + 1 untracked test | 0 files | n/a — no commits | not a rebase: a hand re-application onto files rewritten since |
| guided-remediation | `fix/guided-phase2` @ `147767684` | 0 / 177 | clean | — | — | nothing queued; reset to release before 2.1 starts |
| lane-elspeth-0077cb7789 | @ `284260506` | 0 / 379 | 1 untracked `test_manifest_drift_probe_tmp.py` | — | — | scratch only |
| agentic/decline/interim/68721c71d7/strany/recon | various | 0 ahead | clean | — | — | nothing to rebase |

No other local or remote branch carries queued work: `git branch --no-merged
release/0.8.0` with ahead>0 lists only the three lanes above plus two
`recovery/*` branches from August (1 commit each, superseded by the landing)
and `gh-pages`; remote-side only `origin/lane/soft-type-burndown` and two
dependabot branches.

Corrections to the ticket text, measured (§7): the soft-type-burndown file does
**not** silently auto-merge (git stops on one real conflict hunk); the identity
sprint is **20** commits + 1 dirty file, not 14, and has **no upstream** at
all; `dead_site_supersession.py` is outside the static-writer gate's
tracked-table scope.

## 1. Worktree census (step 1)

`git --no-optional-locks worktree list` from the landing worktree, then per
worktree `status --short`, `rev-list --count release/0.8.0..HEAD` and `@{u}`:

```
<repo-root>                                   7e34eef6c [release/0.8.0]
.claude/worktrees/agentic-interface                  65bbc77e7 [plan/agentic-interface]        ahead 12  clean   no upstream
.claude/worktrees/guided-decline-brick               d09fe8062 [fix/guided-decline-rebind]     ahead 0   clean
.claude/worktrees/guided-remediation                 147767684 [fix/guided-phase2]             ahead 0   clean   (177 behind)
.claude/worktrees/identity-sprint                    759deba56 [identity-sprint/phase1-contracts-epoch]  ahead 20  1 dirty  no upstream
.claude/worktrees/interim-integrate                  4b34d972c [interim-merge-target]          ahead 0   clean
.claude/worktrees/lane-elspeth-0077cb7789            284260506 [lane/elspeth-0077cb7789]       ahead 0   1 untracked
.claude/worktrees/lane-elspeth-24d79f64cd            284260506 [lane/elspeth-24d79f64cd]       ahead 0   3 modified + 1 untracked
.claude/worktrees/lane-elspeth-68721c71d7            0f7c2b53c [fix/strany-coalesce-reachability-facts]  ahead 0  clean  pushed
.claude/worktrees/lane-strany-toolresult             c19e5e629 [strany/tool-result-envelope]   ahead 0   clean
.claude/worktrees/multi-replica-landing              7e34eef6c [landing/multi-replica]         == release
.claude/worktrees/recon-diagnosis                    680afe64b [fix/review-reconciliation-diagnosis]  ahead 0  clean
.claude/worktrees/soft-type-burndown                 f765ea2e1 [lane/soft-type-burndown]       ahead 13  clean   pushed, 0 unpushed
```

identity-sprint's dirty file is
`elspeth-lints/src/elspeth_lints/rules/trust_tier/tier_model/rule.py`: a
one-line deletion of the `"web/auth/local.py": frozenset({"_required_visible_string_claim"})`
boundary entry (rule.py:515 in its tree). The landing does not touch that file
(0 hits in `landing-files.txt`), so it carries no conflict — but it is
uncommitted, and a rebase refuses to start over a dirty tree.

lane-elspeth-24d79f64cd's dirty edit (`git diff --stat`): `composer/tools/generation.py`
+19, `composer/tools/sessions.py` +14, `web/interpretation_state.py` +53 (a
`vague_term_wiring_count` addition and two validation-error pattern entries),
plus untracked `tests/unit/web/composer/test_set_pipeline_vague_term_wiring.py`.
Those three files changed in release since its base `284260506` by
+416/−151, +217/−104 and +273/−77 respectively; there are no commits to rebase,
so this is a manual re-application, not `git rebase`.

## 2. soft-type-burndown (step 3)

Lane set: 21 files (`soft-type-burndown-files.txt`); intersection with the
landing set: **2** —
`config/cicd/contracts-whitelist.yaml` and
`tests/unit/web/sessions/test_static_direct_writers.py`.
Trilateral merge-tree: `2 changed in both`, `7 added in remote`, conflict
markers in exactly one file.

### 2.1 `tests/unit/web/sessions/test_static_direct_writers.py` — 1 textual conflict + a stale-entry class

Landing side (`git diff b21f17a0a 7e34eef6c -- <file>`: +155/−4, three hunks):

* `@@ -899` — **14 new `ReviewedWriter` entries** appended to `_REVIEWED_ALLOWLIST`
  (4 × `src/elspeth/web/coordination/repository.py`, 2 × `_seed_run_and_blob`,
  3 × `test_session_operation_fence.py`, 3 × `test_sqlite_session_operation_authority.py`,
  `test_schema.py::_seed_session_state`, `shareable_reviews/test_service.py::…supersede_after_readiness`).
* `@@ -1403` — one entry's `enclosing_symbol` renamed
  `test_set_active_state_refuses_…` → `test_guided_revert_refuses_to_copy_a_legacy_unbindable_active_row`.
* `@@ -1749` — `_BLOBS_EXPECTED_LINES` re-pinned `(318, …)` → `(829, 888, 952, 1170, 4616, 4771, 5511, 5879)` with an explanatory comment.

Lane side (`b21f17a0a..f765ea2e1`: +940/−158, 21 hunks): deletes
`_BLOBS_ALLOWLIST_PATH`, `_BLOBS_EXPECTED_LINES`, `_COMPOSER_TOOLS_*` and
`_expand_dynamic_allowlist`; adds `_TEST_FIXTURE_REVIEWED_WRITERS` (a committed
literal: 8 blobs + 5 composer-tools + 24 other fixture entries),
`_ALLOWLIST = _REVIEWED_ALLOWLIST + _TEST_FIXTURE_REVIEWED_WRITERS`, a
`count=` multiplicity key (`reviewed_counts`, `_match_key`), a widened scanner
(`_tracked_table_identifier`, `_imported_insert_names`) and
`stale_reviewed_writers` with `StaleReviewedWriter`.

**The trial merge produces ONE conflict hunk** (`soft-type-burndown-mergetree.txt`
lines 499–554): `.our` = landing's `_BLOBS_EXPECTED_LINES` re-pin comment +
the old `_expand_dynamic_allowlist` body; `.their` = the lane's "READ THIS
BEFORE CHANGING ANYTHING BELOW" preamble to `_TEST_FIXTURE_REVIEWED_WRITERS`.
The other two landing hunks (`@899`, `@1403`) auto-merge into the lane's
`_REVIEWED_ALLOWLIST`. So the ticket's "git will auto-merge them into a
semantically broken shape" is **not** what happens — git stops. Resolution:
take the lane's side wholesale; the landing's `_BLOBS_EXPECTED_LINES` re-pin
and its comment are moot once the dynamic expansion is gone.

**The semantic layer, measured by running the lane's own scanner over the landed
tree** (`lane-scanner-out.txt`; scanner and `tests/helpers/tree_gate.py` imported
from the lane tree, which the landing did not change):

```
LANE tree (self-check):  115 tracked-table writer matches;  0 violations; 0 stale entries
LANDED tree 7e34eef6c:   130 tracked-table writer matches; 19 violations; 4 stale entries
```

The 19 violations against the lane's *pre-rebase* allowlist decompose exactly:

* **14** = the landing's 14 new `_REVIEWED_ALLOWLIST` entries (`@899`), one-for-one
  by (path, symbol, table, operation) — they auto-merge, so these go to zero.
* **1** = `test_guided_custody_gate.py::…test_guided_revert_refuses_…` — the
  renamed symbol; the landing's `@1403` hunk auto-merges the rename, so this goes
  to zero too (and its pre-rename twin is the 4th stale entry, which the same
  hunk retires).
* **4** = `tests/unit/web/sessions/test_static_direct_writers.py <module>`
  `raw_string_*` hits — an artefact of my cross-tree run: the scanner skips
  itself by resolved-path equality (`_SCANNER_SELF_PATH = Path(__file__).resolve()`,
  lane file :106, :514), so scanning the *landing* tree includes the landing's copy of
  the gate file. Not real.

**No multiplicity violation** ("one write more than the review blessed") appears
for any landed site, so no `count=` edits are needed.

The 4 stale entries are the real post-rebase work. Three are dead entries the
landing carried but its own gate cannot see (it has no stale check):

```
landed gate file :833  enclosing_symbol="SessionServiceImpl.save_composition_state._sync"   composition_states insert
landed gate file :853  enclosing_symbol="SessionServiceImpl.set_active_state._sync"         composition_states insert
landed gate file :865  enclosing_symbol="SessionServiceImpl.fork_session._sync"             chat_messages insert
```

`src/elspeth/web/sessions/service.py` in the landed tree contains **0**
`composition_states_table.insert(`/`chat_messages_table.insert(` sites (the
writers moved to `coordination/repository.py`, see `writers/writer-diff.txt`
"mainline -> merged"). After rebase the lane's gate reports these three as
`StaleReviewedWriter`; the lane deletes the three entries (a `-` diff a human
reads, which is the lane's stated design). The fourth stale entry is the
pre-rename `test_set_active_state_refuses_…` symbol, retired by the auto-merged
`@1403` hunk.

Fixture-literal staleness check: the two files the lane's literal was generated
from changed in the landing by +29/−8 (`blobs/test_service.py`) and +3/−1
(`composer/test_tools.py`); tracked-table site counts are unchanged
(`composition_states_table.insert(` 9→9 and 5→5, `chat_messages_table.insert(` 1→1
and 1→1 at `b21f17a0a` vs `7e34eef6c`); the only `def test_` rename in either file
(`test_pending_delete_proposal_does_not_retain_its_own_target` →
`…_retains_its_target_from_a_direct_delete`) is not a writer symbol — the scanner
run confirms 0 stale entries among the fixture literal. The lane's
`purpose` strings carry line numbers (813/872/936/1154…) that are now +16
against the landed file (829/888/952/1170); by the lane's own design those are
navigation aids and nothing keys on them.

On the ticket's "re-derive its writer inventory from the landed tree (epoch 51,
new `dead_site_supersession.py`)": `dead_site_supersession.py`'s only writer is
an `update interpretation_events` (`writer-diff.txt` "platform -> merged"), and
this gate tracks only `chat_messages` and `composition_states`
(`_TABLE_IDENTIFIER_TO_NAME`, landed :59–62; identical in the lane). It needs
no entry here; it is the architecture test's concern
(`tests/unit/architecture/test_session_db_mutation_authority.py`), which this
lane does not touch.

### 2.2 `config/cicd/contracts-whitelist.yaml` — disjoint, auto-merges, semantically quiet

Landing hunks: `@770` removes `service.py:_patch_structured_interpretation_prompt:return`;
`@1011` removes three `user_store.py:_*_upsert:values` entries (−4 total).
Lane hunks: `@577` MCP-section header rewrite + 6 alias-typed-return entries
(`mcp/analyzer.py` / `mcp/analyzers/queries.py`) (+31/−1). Different sections;
git merges cleanly. The lane's scanner widening (`aec538e20`: resolve
`X = dict[str, Any]` aliases) would only surface new findings if the landing
introduced new module-level `dict[str, Any]` aliases in `src/` — measured
**0** (`git diff b21f17a0a 7e34eef6c -- 'src/*.py' | grep -E '^\+\w+\s*(:\s*TypeAlias)?\s*=\s*dict\[str, Any\]'`).
Detector regardless: run `scripts/check_contracts.py` after rebase and diff the
finding set against the lane's pre-rebase run.

### 2.3 Everything else in the lane (19 files)

No intersection with the landing (frontend `ProposalDiff`/`redactedArguments`,
`composer/redaction.py`, `mcp/types.py`, `scripts/check_contracts.py`, the
composer redaction/parity tests, `skill_rgr` batch3 stub). Clean.

Inherited-red overlap: **none** of the lane's test files appear in `wf2-bad.txt`
(`test_static_direct_writers` is green on the landed tree; see §6 for what that
means once the lane's stale check arrives).

## 3. identity-sprint (step 3 + step 4)

Lane set: 75 files (`identity-sprint-files.txt`); intersection with the landing set
**17**; merge-tree `17 changed in both`, `13 added in remote`, **15 files with
conflict markers, 41 hunks** (`identity-conflicts.txt`). The two shared files
that auto-merge are `config/cicd/contracts-whitelist.yaml` (lane +3, disjoint from
the landing's −4) and `tests/integration/web/test_shareable_reviews_routes.py`
(lane +31 new tests vs landing +7/−2).

Note the lane's base `91816d0f3` is 170 commits behind, so its rebase crosses
92 release commits *before* the landing as well; the per-file
`release-since-mb` vs `landing-only` numstats (phase2-work run) show every
intersecting file's release-side change is essentially the landing's except
`test_schema9_epoch.py` (+13/−1 vs +9/−1) and `test_blob_inline_resolutions_schema.py`.

### 3.1 Class A — epoch renumber, mechanical (13 hunks, 9 files)

Both sides bumped the same lines from the merge-base's 49/36. Landing = 51/36,
identity = 50/37. Resolution everywhere: **take the landing's text, then re-apply
identity's paragraph as epoch 52, keep Landscape 37.**

| file | hunks | landing (`.our`) | identity (`.their`) |
|---|---|---|---|
| `CHANGELOG.md` | 2 (:57–58, :94–111) | `to 51` … `29 to 36` | `to 50` + SSO paragraph … `29 to 37` |
| `docs/guides/sharing-pipelines.md` | 1 (:71) | `SESSION_SCHEMA_EPOCH=51` / `SQLITE_SCHEMA_EPOCH=36` | `=50` / `=37` |
| `docs/runbooks/aws-ecs-deployment.md` | 1 (:1518–1520) | `{"session_epoch": 51, "landscape_epoch": 36 …}` + `…_35_to_51_landscape_epoch_29_to_36_blob_cleanup_guided_decline_row_union_barrier_and_coordination_schema` | `50 / 37` + `…_35_to_50_landscape_epoch_29_to_37_blob_cleanup_guided_decline_and_row_union_barrier` |
| `docs/runbooks/staging-session-db-recreation.md` | 3 (:5, :120–122, :594–595) | heading/prose/`# expect 51` `# expect 36` | heading names "the identity substrate" / `# expect 50` `# expect 37` |
| `src/elspeth/web/sessions/models.py` | 1 (:270–281) | `# 50 -> ck_proposal_events_type …` paragraph + `SESSION_SCHEMA_EPOCH = 51` | `# 50 → pluggable SSO …` paragraph + `SESSION_SCHEMA_EPOCH = 50` |
| `tests/integration/web/composer/guided/test_schema9_epoch.py` | 1 (:36–42) | `== 51` / `== 36` | `== 50` / `== 37` |
| `tests/unit/web/sessions/test_blob_inline_resolutions_schema.py` | 1 | `test_…_epoch_is_51`, `== 51`, `PRAGMA … == 51` | `_is_50`, `== 50` |
| `tests/unit/web/sessions/test_interpretation_events_table.py` | 2 | `test_current_session_schema_epoch_is_51`, `== 51` | `_is_50`, `== 50` |
| `website/get-started.html` | 1 (:105) | `35 → 51` … `29 → 36` | `35 → 50` … `29 → 37` |

Two of these are executable contracts, not prose (from `decision-epoch.md` §4,
re-verified on the landed tree):

* `aws-ecs-deployment.md:1518–1520` must equal
  `receipt_contracts._expected_schema_facts("B")` byte for byte. In the landed
  `src/elspeth/web/_aws_ecs_acceptance/receipt_contracts.py` the label is fully
  derived: `:144 f"session_epoch_{_ROLLBACK_BASELINE_SESSION_EPOCH}_to_{SESSION_SCHEMA_EPOCH}"`
  `:145 f"_landscape_epoch_{_ROLLBACK_BASELINE_LANDSCAPE_EPOCH}_to_{SQLITE_SCHEMA_EPOCH}"`,
  `:160 "landscape_epoch": SQLITE_SCHEMA_EPOCH`. Identity's diff touches this file
  **not at all** (empty `git diff` for it), so after renumbering the record must read
  `"candidate": {"session_epoch": 52, "landscape_epoch": 37, …}` and
  `session_epoch_35_to_52_landscape_epoch_29_to_37_blob_cleanup_guided_decline_row_union_barrier_and_coordination_schema`
  — **the landing's `_row_union_barrier_and_coordination_schema` suffix, not
  identity's mainline-era `_and_row_union_barrier`**. The jq rollback gate in the
  same runbook (`:3220–3221 … .schema_facts.candidate.landscape_epoch == 36`)
  moves to 37 (`test_scenario_b_executable_jq_gate_binds_live_landscape_epochs`).
* `staging-session-db-recreation.md` — `test_staging_session_recreation_policy`
  regex-extracts `# expect N (== SESSION_SCHEMA_EPOCH)` and requires exactly one
  match. Identity's version (+168/−15) keeps exactly one current pair
  (`:747–748` in its tree) plus the historical `:764 # expect 26` / `:768 # expect 22`;
  the landed tree has the pair at `:594–595` and historical `:611`/`:615`. The
  lane also edits `tests/unit/docs/test_staging_session_recreation_policy.py`
  (in its file set, not in the landing's) — re-check its assertion after the
  merge, since the landed runbook's current-cutover heading (`:5`) is the one
  that must name the identity substrate.

### 3.2 Class B — signature fusion, semantically coupled (25 hunks, 4 files)

The landing added a keyword-only `session_operation_context: SessionOperationContext`
to `ShareableReviewService.mark_ready_for_review`; identity added a keyword-only
`username: str`. Every call site was rewritten on both sides, so git conflicts on
all of them:

* `src/elspeth/web/shareable_reviews/service.py` — 2 hunks: the `typing` import
  line (landing `Any, Final, Protocol, TypedDict, cast` / `UUID`; identity adds
  `NotRequired` and `uuid4`) and the signature
  (`.our`: `session_id, user_id, session_operation_context`; `.their`:
  `session_id: UUID, user_id: str, username: str`).
  **Merged signature: `(self, *, session_id: UUID, user_id: str, username: str, session_operation_context: SessionOperationContext)`.**
* `src/elspeth/web/shareable_reviews/routes.py` — 1 hunk (31 lines). The landed
  route (`routes.py:84–110`) already acquires a `SessionOperationLease`
  (`BLOB_READ`) and already carries identity's `try/except CompositionNotRunnableError → 409`
  block *inside* the lease's `try/finally` (identity's hunk is the older
  mainline shape of that same block). Identity's net contribution reduces to
  adding `username=user.username` to the call at `routes.py:93–97`.
* `tests/unit/web/shareable_reviews/test_service.py` — **20 hunks**, every one the
  same shape: `.our` passes `session_operation_context=session_operation_context`,
  `.their` passes `username=_OWNER_USERNAME`. Merged: pass both.
* `tests/unit/web/shareable_reviews/test_telemetry_session_completed.py` — 2 hunks,
  same shape (`username="alice-the-analyst"`).

Because both kwargs are keyword-only and required, **any call site that keeps only
one side fails mypy** — this is exactly the class the pre-commit mypy hook
detects. Two of the landing's shareable-review tests are already in the inherited
red (`wf2-bad.txt`: `test_service.py::test_mark_ready_for_review_genuine_failure_keeps_validation_failed`,
`::test_mark_ready_for_review_pending_review_gets_honest_reason`, and
`test_shareable_reviews_routes.py::test_mark_ready_for_review_audit_write_failure_returns_no_token`);
the lane must diff its post-rebase failure set against those three, not against zero.

### 3.3 Class C — re-home: the landing moved the code identity edits (2 hunks, 1 file)

`src/elspeth/web/sessions/service.py`: identity's whole delta is +34/−0 — four
table imports and four `or conn.execute(select(...))` clauses appended to the
`durable_history_exists` predicate inside `SessionServiceImpl.archive_session`
(identity tree `service.py:6380–6439`), counting `approvals`, `review_requests`,
`review_attestations` and `library_entries` (via `published_from_session_id`) as
audit-bearing history so archive soft-archives instead of physically deleting.
The landing rewrote `service.py` by +3843/−4779 (203 hunks) and **moved that
predicate** to
`src/elspeth/web/coordination/repository.py:583–606` (`durable_history_exists = bool(...)`,
returning `SessionArchiveDisposition.PHYSICAL_DELETE` at `:607` or
`SOFT_ARCHIVED` at `:613`). `grep durable_history src/elspeth/web` on the landed
tree hits only `repository.py:583/:607`. So:

* conflict 11 (112 lines, `.their` = identity's predicate inside the old inline
  `archive_session` body) → **take the landing's side entirely**, then add the
  four clauses to the `bool(...)` at `repository.py:583` (with the four table
  imports in `repository.py`, and `published_from_session_id` for `library_entries`);
* conflict 10 (`.our` `proposal_blob_effect_receipts_table,` / `.their`
  `library_entries_table,` in the `models` import list) → keep the landing's
  line; identity's four service.py imports become unused once the clauses move.

The identity lane should add a repository-level test (in `tests/unit/web/coordination/`)
that a session whose only history is an approval row resolves to
`SOFT_ARCHIVED`, since its existing pin — if any — targets the old service path.

### 3.4 Class D — composition (1 hunk, `src/elspeth/web/app.py`)

Landed `app.py:1428–1433`: `user_secret_authority = RepositoryUserSecretAuthority(session_engine)` … `UserSecretStore(session_engine, settings.secret_key, mutation_authority=user_secret_authority)`.
Identity `app.py:1523`: `UserSecretStore(session_engine, derive_user_secret_master_key(settings.secret_key))`.
Merged: `UserSecretStore(session_engine, derive_user_secret_master_key(settings.secret_key), mutation_authority=user_secret_authority)`
with identity's comment. The rest of identity's app.py delta (+162/−39: profile
registry, readiness, key derivation wiring) does not overlap the landing's +41/−4.

### 3.5 Landed gates with NO git signal (the dangerous class)

These produce no conflict marker and no hunk — git merges the lane cleanly and
the tree is broken or red.

1. **`_COORDINATION_HARD_CUT_EPOCH`** — `src/elspeth/web/sessions/schema.py:34 = 51`,
   strict equality at `:376–377`:
   `if SESSION_SCHEMA_EPOCH != _COORDINATION_HARD_CUT_EPOCH: _schema_error("coordination schema epoch mismatch", …)`.
   Identity's tree has **no** such constant (it predates the landing). Bumping
   `SESSION_SCHEMA_EPOCH` to 52 without moving `:34` to 52 makes every
   session-schema validation raise `SessionSchemaError` on every dialect — the
   build cannot open a session DB. Also update the docstring at `:372`
   ("Epoch 51 (the multi-replica hard cut, 44 then 48 on the original lane)").
   Detector: `tests/unit/web/sessions/test_schema.py` (any schema test), or simply
   starting the app.
2. **Exact table policy** — `tests/unit/architecture/test_session_db_mutation_authority.py:3873–3876`
   `test_sessions_metadata_table_policy_is_exact_and_protected` asserts
   `Counter(sessions_metadata.tables.keys()) == Counter(policy.table for policy in _TABLE_POLICIES)`.
   The landed `_TABLE_POLICIES` has 34 rows for 31 `*_table = Table(` in the landed
   `models.py`; identity's `models.py` has 32 (it lacks the landing's 10 coordination
   tables, which auto-merge in, and adds **11**):
   `approval_decisions, approvals, identities, identity_relationships, identity_roles, library_entries, quota_policies, review_attestations, review_requests, sso_handoffs, token_usage_ledger`.
   This test is **green on the landed tree** (not in `wf2-bad.txt`); identity turns
   it red unless it adds a `TablePolicy(<table>, scope, "<NamedAuthority>")` for
   each of the 11 with a named authority (`_PROTECTED_LOGICAL_TABLES` also demands
   an authority string). The lane owns naming those authorities — the obvious
   candidate for the identity tables is an `IdentityAuthority` bound to
   `identity_repository.py`.
3. **Writer manifest** — the same file's
   `test_all_production_sessions_writers_are_reviewed_typed_authorities` is already
   in the inherited red (`wf2-bad.txt`). Identity adds 5 production writer sites in
   `src/elspeth/web/sessions/identity_repository.py` (`:173 quota_policies_table.insert`,
   `:279/:387/:462 identities_table.update`, `:318 identities_table.insert`). They will
   enlarge that test's finding corpus; per AGENTS.md the lane compares the corpus
   before and after and either routes the writers through a typed authority or
   records the delta on its ticket for Phase 4 (`elspeth-44a9e49139`). The
   static-direct-writers gate (§2) is *not* affected: it tracks only
   `chat_messages`/`composition_states`, which identity does not write.
4. **Receipt/runbook derivation** — covered in §3.1: the label rotates on its own;
   the runbook JSON and jq literals do not.

### 3.6 Epoch collision confirmed (step 4)

| constant | landed tree `7e34eef6c` | identity tree `759deba56` | after identity rebase |
|---|---|---|---|
| `SESSION_SCHEMA_EPOCH` | `src/elspeth/web/sessions/models.py:281 = 51` | `models.py:270 = 50` | **52** |
| `SQLITE_SCHEMA_EPOCH` | `src/elspeth/core/landscape/schema.py:346 = 36` | `schema.py:364 = 37` | **37** (unchanged; 37 is free) |
| `_COORDINATION_HARD_CUT_EPOCH` | `src/elspeth/web/sessions/schema.py:34 = 51` | absent | **52** |

The sentinel is exact-equality (`schema.py:271 if user_ver not in {0, SESSION_SCHEMA_EPOCH}` per
`decision-epoch.md`; hard-cut gate at `:376`), so 50 cannot be reused: on the
landed line, 50 already names mainline's `proposal.rebased` / `base_state_id`
shape (landed `models.py` comment block `# 50 ->`), and 51 names the coordination
substrate. Identity's "50 → pluggable SSO" paragraph becomes `# 52 →` and its
rejection clause names 51 as the predecessor.

**Every coupled surface identity must move to 52 / keep at 37** (measured from
its diff's added lines and the landed tree; line numbers are identity-tree unless
stated):

Production
* `src/elspeth/web/sessions/models.py:270` `SESSION_SCHEMA_EPOCH = 50` → 52, and the `# 50 →` paragraph → `# 52 →`.
* `src/elspeth/web/sessions/schema.py:34` (landed) `_COORDINATION_HARD_CUT_EPOCH = 51` → 52 (+ docstring `:372`).
* `src/elspeth/core/landscape/schema.py:364` `SQLITE_SCHEMA_EPOCH = 37` stays; its comment "epoch 50 (one window, two stores)" → 52.
* `src/elspeth/core/landscape/database.py` comment "Epoch 37: every auth event carries the identity…" stays.
* `receipt_contracts.py` — no edit (derived), but see the runbook literal below.

Tests (literal pins; `grep -rnE '(SESSION_SCHEMA_EPOCH|SQLITE_SCHEMA_EPOCH) == [0-9]+'`)
* identity tree: `test_schema9_epoch.py:36–37` (`== 50`/`== 37`), `test_interpretation_events_table.py:218` (`== 50`), `test_blob_inline_resolutions_schema.py:52` (`== 50`) → 52; `test_schema_epoch_and_required_columns.py:47` and `test_token_ownership_run_scope.py:39` (`== 37`) stay.
* landed tree pins that must ALSO move to 52 (no conflict marker — identity's tree does not have these files/lines): `tests/unit/contracts/test_web_blob_fencing.py:3182 == 51`, `tests/unit/web/sessions/test_schema.py:277 == 51`, `tests/unit/web/sessions/test_proposal_blob_effect_receipts_schema.py:14 == 51`. Also rename `test_…_epoch_is_51` functions in `test_blob_inline_resolutions_schema.py` / `test_interpretation_events_table.py` to `_is_52` so the names stop asserting a false fact.
* After resolution the closing check from `decision-epoch.md` applies verbatim: `git grep -nE 'SESSION_SCHEMA_EPOCH == [0-9]+' -- 'src/*' 'tests/*'` must return exactly six rows, all `== 52`; `_COORDINATION_HARD_CUT_EPOCH` must show `= 52` at `schema.py:34` and nothing else numeric.

Docs / site (all `50`→`52`, `36`→`37` where the landed text says 36)
* `CHANGELOG.md:57–58` "advances from 35 to 52"; `:94–111` SSO paragraph + "`SQLITE_SCHEMA_EPOCH` advances from 29 to 37".
* `docs/guides/sharing-pipelines.md:71`.
* `docs/runbooks/aws-ecs-deployment.md:1518–1520` (JSON record, landing's suffix), `:1546`, `:1551`, `:3220–3221` (jq `landscape_epoch == 37`), `:3239`.
* `docs/runbooks/staging-session-db-recreation.md` heading `:5` (names the identity substrate), `:120–122`, current `# expect 52` / `# expect 37` pair (one match only; leave `# expect 26`/`22`).
* `docs/reference/configuration.md` "Landscape schema epoch 37" section (identity-added) — its "epoch 50" cross-references → 52.
* `website/get-started.html:105` "35 → 52 … 29 → 37".
* Leave `docs/specs/2026-09-02-pluggable-sso-design.md` and `docs/plans/*` mentions of 50 alone (dated records), *unless* the lane wants the spec's §Data model to state the live number — that is its call.

Detectors for the epoch class: `tests/integration/web/composer/guided/test_schema9_epoch.py`,
`tests/unit/web/sessions/test_schema.py`, `tests/unit/docs/test_staging_session_recreation_policy.py`,
`tests/unit/docs/test_release_version_surfaces.py`, `tests/unit/website/test_release_site_contract.py`,
`tests/unit/web/_aws_ecs_acceptance/` (the runbook-record and jq-gate tests), all `-n 0`.

## 4. agentic-interface (step 3)

Lane set: 2 files — `docs/specs/2026-09-04-agentic-tool-interface-design.md`
(+440) and `tests/unit/web/composer/test_pipeline_planner.py` (+241/−1).
Intersection: the test file only. Merge-tree: `1 changed in both`, **0 conflict
markers**. Hunks are disjoint: lane at `@@ -11` (adds `Callable` import),
`@@ -1225` (+42, `test_candidate_shape_hash_is_identity_blind…`) and `@@ -7554`
(+198, three withholding-arm tests); landing at `@@ -129` (replaces the
`SessionServiceImpl` import with `tests.unit.web.sessions.guided_test_authority.DualFencedSessionServiceHarness`)
and `@@ -5784` (constructs that harness in `_session_context`). The lane's added
code references none of `SessionServiceImpl`, `_session_context`,
`session_operation_*` or `create_session_engine` (grep of its diff: 0 hits), so it
is not exposed to the fence-kwarg change. Its base `cbae1ef0c` is two commits
before the landing's parent; those two (`f6600812f`, `b21f17a0a`) touch 8 files,
none the lane's. Trivial rebase; the branch has no upstream and should be pushed
when it lands.

## 5. Rebase order, detectors, pings (step 5)

Lane-vs-lane overlap is a single file: soft-type-burndown ∩ identity-sprint =
`config/cicd/contracts-whitelist.yaml` (lane hunks `@577` vs identity's +3
elsewhere — disjoint). agentic ∩ anything = ∅. So ordering is about size and
risk, not dependency.

**Order**

1. **agentic-interface** — zero conflicts, tests-and-docs only, ~minutes. Clears a
   queue slot and proves the sequence works before the two real ones.
2. **soft-type-burndown** — one textual conflict with a measured resolution, three
   stale-entry deletions, no multiplicity work; it is the P0 fail-open-guard
   sprint's blocker (`project_fail_open_guard_sprint_2026-09-04`), and landing it
   before identity means identity's rebase runs against a tree whose
   static-writer gate is already the literal-inventory version (identity writes
   no tracked table, so nothing to add either way). Its other 19 files are
   untouched by the landing.
3. **identity-sprint** — largest surface (41 hunks + 3 no-signal gates), and its
   epoch 52 must be the last integer taken: landing it last guarantees no
   later lane bumps the epoch out from under it, and its 11 `TablePolicy`
   rows and the `repository.py:583` re-home are made against the final tree.
   Commit the dirty `rule.py` first (a rebase will not start over it).
4. **lane-elspeth-24d79f64cd** (composer live-review epic `elspeth-cce536a2ba`) —
   not a rebase: re-apply the +86-line dirty edit by hand onto the landed
   `generation.py` / `tools/sessions.py` / `interpretation_state.py`, then run
   its untracked test. Sequence-independent; can run in parallel with 1–3.
5. **guided-remediation** — nothing queued; `git reset --hard release/0.8.0` in
   that worktree (or recreate it) before Phase 2.1 begins, so 2.1 authors against
   the fenced writers (`session_operation_context` / `session_operation_lease`
   now required on every writer it calls).

**Detector per lane, before pushing** (all runs `-n 0`, log to a file, read the exit code):

| lane | semantic-merge detector | gate(s) that must be green | inherited red to subtract |
|---|---|---|---|
| agentic-interface | pre-commit mypy | `tests/unit/web/composer/test_pipeline_planner.py` | none of its files are in `wf2-bad.txt` |
| soft-type-burndown | pre-commit mypy; the lane's own `tests/unit/web/sessions/test_static_direct_writers.py` (its `stale_reviewed_writers` half IS the detector for the dead-entry class) | that file; `scripts/check_contracts.py` (diff findings vs pre-rebase); `tests/unit/scripts/test_check_contracts.py`; its composer redaction/parity tests | none |
| identity-sprint | pre-commit mypy (catches every `mark_ready_for_review` call site missing a kwarg); app start (hard-cut gate) | `tests/unit/web/sessions/test_schema.py`; `tests/unit/architecture/test_session_db_mutation_authority.py::test_sessions_metadata_table_policy_is_exact_and_protected`; `test_schema9_epoch.py`; `tests/unit/docs/test_staging_session_recreation_policy.py`; `tests/unit/website/test_release_site_contract.py`; `tests/unit/web/_aws_ecs_acceptance/`; `tests/unit/web/shareable_reviews/`; `tests/integration/web/test_shareable_reviews_routes.py`; its own `tests/unit/web/auth/`, `test_identity_*`, `test_workflow_governance_schema.py` | 3 shareable-review ids + `test_all_production_sessions_writers_are_reviewed_typed_authorities` (compare corpus size before/after) |
| 24d79f64cd | pre-commit mypy | its untracked `test_set_pipeline_vague_term_wiring.py` + `tests/unit/web/composer/` | per `wf2-bad.txt` for those files |

Exit per ticket: rebased and green on its own scope, or parked with the blocker
on its ticket. The full `pytest tests/` remains Phase 4's bar (370 inherited), not
this phase's.

**Ping texts** (for the parent to send verbatim; each names its delivery channel
as the lane's own branch + ticket):

*agentic-interface (`plan/agentic-interface`)*
> release/0.8.0 is now 7e34eef6c (multi-replica landed). Rebase `plan/agentic-interface` onto it: measured zero conflicts — your three hunks in `tests/unit/web/composer/test_pipeline_planner.py` (@11, @1225, @7554) are disjoint from the landing's two (@129 import swap to `DualFencedSessionServiceHarness`, @5784), and your added tests construct no session service. Run pre-commit mypy and `pytest tests/unit/web/composer/test_pipeline_planner.py -n 0 > <lane-log> 2>&1; echo exit=$?`, then set an upstream and push. Report the rebased HEAD on your ticket.

*soft-type-burndown (`lane/soft-type-burndown`, elspeth-47 / elspeth-7eac6c2e24 / elspeth-9b3cf0d52d)*
> release/0.8.0 is now 7e34eef6c. Rebase `lane/soft-type-burndown` onto it. Measured surface: exactly ONE conflict hunk, in `tests/unit/web/sessions/test_static_direct_writers.py`, where the landing re-pinned `_BLOBS_EXPECTED_LINES` against your removal of `_expand_dynamic_allowlist` — take your side; the re-pin is moot. The landing's 14 new `_REVIEWED_ALLOWLIST` entries and its `test_guided_revert_refuses_…` rename auto-merge. Running your scanner over the landed tree shows 0 multiplicity violations and 4 stale entries: 3 dead `SessionServiceImpl.{save_composition_state,set_active_state,fork_session}._sync` entries (landed file :833/:853/:865; `service.py` has no tracked insert left) that your `stale_reviewed_writers` will report — delete them — plus the pre-rename custody-gate symbol, which the auto-merged rename retires. `contracts-whitelist.yaml` merges cleanly (different sections); the landing adds no new `dict[str, Any]` aliases, so diff `scripts/check_contracts.py` findings before/after anyway. `dead_site_supersession.py` writes only `interpretation_events`, outside this gate's tracked tables — no entry. Detectors: pre-commit mypy, then `pytest tests/unit/web/sessions/test_static_direct_writers.py tests/unit/scripts/test_check_contracts.py -n 0 > <lane-log> 2>&1; echo exit=$?`. Push and report HEAD on the ticket.

*identity-sprint (`identity-sprint/phase1-contracts-epoch`, elspeth-07cd19ba73)*
> release/0.8.0 is now 7e34eef6c: session epoch 51 is taken (coordination substrate) and 50 names mainline's `proposal.rebased` shape, so your cutover renumbers to **SESSION_SCHEMA_EPOCH = 52**; Landscape **37** stays. First commit your dirty `elspeth-lints/.../tier_model/rule.py` (no conflict, but rebase refuses a dirty tree). Then rebase; expect 15 conflicting files / 41 hunks, in four classes: (A) epoch text in CHANGELOG, sharing-pipelines, both runbooks, models.py, get-started.html, three epoch tests — take the landing's text, re-apply your paragraph as 52; (B) `ShareableReviewService.mark_ready_for_review` — the landing added keyword-only `session_operation_context`, you added `username`: merged signature takes BOTH, and all 20 call sites in `tests/unit/web/shareable_reviews/test_service.py`, 2 in `test_telemetry_session_completed.py`, and the route (landed `routes.py:93–97`, which already has your 409 block inside a lease) pass both — mypy is the detector; (C) your `service.py` archive predicate (4 `or` clauses) targets code the landing MOVED to `src/elspeth/web/coordination/repository.py:583–606` (`durable_history_exists = bool(...)`) — take the landing's `service.py` entirely and add the four clauses + imports there, with a repository-level test; (D) `app.py:1430` — `UserSecretStore(session_engine, derive_user_secret_master_key(settings.secret_key), mutation_authority=user_secret_authority)`. Three landed gates give NO git signal: (1) `src/elspeth/web/sessions/schema.py:34 _COORDINATION_HARD_CUT_EPOCH = 51` is strict-equality at `:376` — set it to 52 with the epoch or no session DB opens; (2) `tests/unit/architecture/test_session_db_mutation_authority.py::test_sessions_metadata_table_policy_is_exact_and_protected` requires a `TablePolicy` with a named authority for each of your 11 new tables (approval_decisions, approvals, identities, identity_relationships, identity_roles, library_entries, quota_policies, review_attestations, review_requests, sso_handoffs, token_usage_ledger) — it is green today; (3) your 5 `identity_repository.py` writers enlarge the already-red `test_all_production_sessions_writers_are_reviewed_typed_authorities` corpus — measure before/after and record the delta or route them. Also move the landed-tree pins `tests/unit/contracts/test_web_blob_fencing.py:3182`, `tests/unit/web/sessions/test_schema.py:277`, `tests/unit/web/sessions/test_proposal_blob_effect_receipts_schema.py:14` to 52; the aws-ecs runbook record at `:1518–1520` must be byte-equal to `receipt_contracts._expected_schema_facts("B")` — `session_epoch_35_to_52_landscape_epoch_29_to_37_blob_cleanup_guided_decline_row_union_barrier_and_coordination_schema` (the landing's suffix), and its jq gate `:3220–3221` → 37; keep exactly one `# expect 52` in the staging runbook. Closing check: `git grep -nE 'SESSION_SCHEMA_EPOCH == [0-9]+' -- 'src/*' 'tests/*'` = six rows, all `== 52`. Inherited red you may subtract: `test_shareable_reviews_routes.py::test_mark_ready_for_review_audit_write_failure_returns_no_token`, `test_service.py::test_mark_ready_for_review_genuine_failure_keeps_validation_failed`, `::test_mark_ready_for_review_pending_review_gets_honest_reason`. Set an upstream and push; report HEAD and the corpus delta on the ticket.

*lane-elspeth-24d79f64cd (epic elspeth-cce536a2ba)*
> Your worktree has +86 uncommitted lines across `composer/tools/generation.py`, `composer/tools/sessions.py`, `web/interpretation_state.py` on a base 379 commits behind release/0.8.0 (now 7e34eef6c); those files changed by +416/−151, +217/−104, +273/−77 since. There is nothing to rebase — save your diff (`git diff > <lane-private path>`), move the worktree to release/0.8.0, and re-apply the three hunks by hand plus your untracked `test_set_pipeline_vague_term_wiring.py`. Every writer you call now requires `session_operation_context` / `session_operation_lease`. Pre-commit mypy, then your test file `-n 0`, then commit and push.

*guided-remediation (fix/guided-phase2)*
> Nothing is queued on `fix/guided-phase2` (0 ahead, 177 behind). Before Phase 2.1/2.2 begins, reset that worktree to release/0.8.0 @ 7e34eef6c; the guided routes and `SessionServiceImpl` writers now take the fence kwargs on every call.

## 6. One thing the parent should know about Phase 4's arithmetic

When soft-type-burndown lands, `test_static_direct_writers.py` gains a stale-entry
check the landed tree never had. Measured today it would report exactly the
three dead `_sync` entries (§2.1) — which the lane deletes as part of its rebase,
so the count does not change. But any *future* landing that removes a
tracked-table writer without pruning the entry becomes a red in that file, not a
silent pass. That is the intended tightening; note it so Phase 4 does not
misattribute a later red there to the multi-replica merge.

## 7. Corrections to the ticket / plan text, measured

* Ticket & plan: "Our landing edits ENTRIES in the same file; git will auto-merge
  them into a semantically broken shape." — Measured: the trilateral merge stops
  on one real conflict hunk (`_BLOBS_EXPECTED_LINES` re-pin vs `_expand_dynamic_allowlist`
  removal). The entries themselves (14 adds + 1 rename) DO auto-merge, correctly.
  The semantic hazard is the stale-entry class, and the lane's own gate detects it
  (`stale_reviewed_writers`); pre-commit mypy stays the detector for the rest of
  the tree.
* Ticket: "identity-sprint worktree (elspeth-07cd19ba73, 14 unpushed commits)" —
  measured **20** commits ahead of release plus one uncommitted file, and the
  branch has **no upstream configured** (nothing has ever been pushed).
* Ticket: "It must re-derive its writer inventory from the landed tree (epoch 51,
  new dead_site_supersession.py)" — the epoch is irrelevant to that gate, and
  `dead_site_supersession.py` writes only `interpretation_events`, which the gate
  does not track (`_TABLE_IDENTIFIER_TO_NAME` = `chat_messages`, `composition_states`).
  The re-derivation the lane actually owes is the three stale deletions in §2.1.
* Ticket: "guided-remediation worktree (phase 2.1/2.2 in flight)" — nothing is in
  flight on that branch (0 ahead); it is a stale worktree to reset, not a lane to rebase.
* Ticket/plan "Landscape 37 is still free" — confirmed: landed
  `src/elspeth/core/landscape/schema.py:346 SQLITE_SCHEMA_EPOCH = 36`.
* The ticket does not mention `_COORDINATION_HARD_CUT_EPOCH` or the exact-table
  `TablePolicy` assertion; both are landed gates with no git signal (§3.5) and
  should be written onto elspeth-07cd19ba73 before that lane starts.
