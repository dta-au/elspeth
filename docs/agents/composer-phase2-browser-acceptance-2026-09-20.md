# Phase 2 browser acceptance

Final inspected source: `347ade3f5b82b87f67d28454c338db40867a233e`, clean working tree. `browser-evidence/final-source-provenance.json` records SHA256 for every changed web source path relative to current release base `627b72cf1`. Browser evidence was captured from the rebased candidate with session schema epoch62. Backend product source was stable when restarted; frontend source remained stable for final acceptance.

Preserved artifacts and rerunnable CLI instruments: `.claude/lanes/composer-phase2-20260920/browser-evidence/`. Copied75 files from candidate `output/playwright/` after stopping services; every copied file's SHA256 matched. This includes screenshots, request/response captures, isolated synthetic databases, service logs and CLI scripts. `run-cli.py` uses Playwright CLI browser session `composer-phase2`; recreating acceptance requires starting the isolated backend/Vite and logging into the synthetic fixture account. No shared service/database was changed.

## Final browser results

All following CLI instruments terminated with exit0. An exit0 is the instrument outcome; individual HTTP outcomes are stated separately.

- **Proposal native rows and confirmation:** `actions-phase2.js.log` measured `pendingChanges:0`, `approvalSection:1`. Exact names `Accept proposal: Change one option on source.` and `Reject proposal: Change one option on source.` were used. Cancel returned focus to Reject. Confirm Reject and Accept dispatched existing proposal routes. This first matrix used explicitly synthetic transient rows and intercepted503 mutations.
- **Real persisted proposal outcomes:** `seed-real-proposals.py` created explicitly synthetic pending proposals through the real session service, with ordinary operation fencing and audit events in the owned database. `persisted-proposals.js.log` measured cancel retained the pending row; confirmed Reject returned200 and durable `status:rejected`. Its initial Accept fixture used unsupported tool arguments: the backend correctly returned422 and automatically rejected it. After correcting the fixture against the real `SetSourceFromBlobArgumentsModel`, `accept-persisted-valid.js.log` returned200, `status:committed`, committed state `19585566-dcdb-4a5a-ba5e-a5d0ae10eb00`. These were real backend mutations of synthetic fixtures, not provider-authored proposals.
- **Prompt disclosure and amendment:** `actions-phase2.js.log` measured Approve initially `aria-disabled:true`; View prompt removed the gate. Amendment textbox received focus; Cancel returned focus to Change. Intercepted POST bodies were exactly `choice:accepted_as_drafted` and `choice:amended, amended_value:Use the exact category column`.
- **Real persisted interpretation amendment:** `persisted-amendment.js.log` returned200 for event `537404e5-28fb-498b-a44c-9abf378adccb`, `choice:amended`, exact accepted text and a persisted approved prompt artifact hash. New state `3f54b531-a975-4cb2-8c14-8561b297c955`; resolved Change control disappeared and focus returned to Message input.
- **Real persisted prompt approval/history:** `persisted-prompt.js.log` measured the View→Approve gate, then200 `accepted_as_drafted` for event `f1e56096-b06c-48e4-932a-fa8209cd89f0`, persisted approval hash, and new state `4d342883-f298-4fd4-9ce6-d70efeff3c17`. Reload retained read-only interpretation confirmations and `remainingApprove:0`. The fixture has no originating chat-message anchor, so this browser proof covers the unanchored history fallback; anchored placement is covered by the frontend regression suite.
- **Reload and Apply:** `reload-apply-phase2.js.log` performed two actual reloads and two real `/state` GETs, both200 with backend-generated suggestion `Source has no explicit schema. Downstream field references depend on runtime column names.` The Apply row remained visible. Apply dispatched exactly one ordinary POST `/api/sessions/13741681-73a9-4f3e-a57a-a9574ba0f034/messages` with the normal suggestion prompt and current `state_id:503242e7-c56b-4f8d-b89e-23a4f0145cb0`. Apply's request was intercepted503: no live provider completion is claimed. The omitted-schema fixture is intentionally invalid, and the normal validation failure remained visible alongside its advice.
- **Guided/freeform geometry:** `geometry-phase2.js.log` passed six combinations:1440×1000,375×812,320×568, each freeform and completed-guided. In every combination Reject/cancel, View prompt, Change/cancel were pointer reachable; Send and Upload hit their own controls; no horizontal document overflow. At375×812 input bottom753.5 equals pane bottom753.5 in both modes. At320×568 input bottom507.5 equals pane bottom507.5; Send407.5–451.5 and Upload459.5–503.5 both hit:true. Decision content remains in the bounded dock scroller. Screenshots `geometry-<width>-<mode>.png` preserve each result.
- **Keyboard Checks navigation:** the same geometry instrument ended at320×568 with keyboard focus on Open checks, Enter selected Pipeline and Checks, focused `Checks: Ready`, and showed its tabpanel: `pipeline:true, checks:true, visible:true`.
- **Announcements:** `announcements-phase2.js.log` used a DOM MutationObserver. First proposal arrival produced exactly one nonempty status mutation. Replacing it with a different proposal at the same count produced exactly one further mutation. Removing all decisions cleared the text without adding an announcement, while preserving the same always-mounted live-region DOM node. Exactly one decision status was present. Screen-reader audio was not tested.
- **Axe:** final `axe.js.log` reports `violations:[]`, `passes:24`, scoped to the decision panel after action interactions. This is not a whole-application accessibility certification.

## Developmental defect and reconciliation

Before rebasing onto current local release, `development-geometry-red.log` reproduced completed-guided375×812 clipping: input bottom738.03 exceeded pane675 and Upload hit:false. Computed geometry isolated the old policy summary inside the fixed input wrapper. The inherited release change removed that summary. The complete six-combination geometry matrix then passed on the rebased source without another CSS change.

Early transient-fixture attempts during concurrent source edits hit Vite HMR duplicate module instances: dynamic unversioned imports mutated a different store from the rendered application. Restarting Vite and waiting for session hydration fixed the instrument. Those failures are not product regressions or acceptance evidence.

## Scope and limits

Real browser Chromium, real authentication/session/upload/e2e state-seed routes, actual reload validation, and real proposal/interpretation mutation APIs were exercised. Pending proposals and interpretation events were explicitly synthetic fixtures seeded through the real session service; actor/provider fields label them as fixtures. Interpretation fixtures deliberately lack an available LLM profile, so their returned state is not runnable; successful approval proves persistence/attestation flow, not pipeline execution.

Provider credentials were blank and no successful live planner/advisor call or full guided authoring walk occurred. Synthetic store state supplied advisor blocker copy and completed-guided status for renderer/geometry checks. Backend advisor suggestion ownership/redaction/staleness proofs belong to the backend wire tests. Active guided sheet/fallback exclusivity and anchored history placement belong to the targeted frontend regressions and review; this browser matrix specifically covers freeform and completed-guided authoring surfaces.

## Cleanup

Owned Chromium session closed exit0, isolated Vite5190 stopped exit130, isolated uvicorn8490 stopped exit0. Main web service and shared databases were untouched. The main checkout's two async-operation specifications are now tracked and retain their initial hashes; its unrelated registered worktree was preserved. No identity-finalization worktree was registered at initial inspection. Preserved evidence remains under the main lane directory, so retiring the candidate worktree will not discard it.


## E2E fixture reconciliation after browser acceptance

The initial browser proof source remains unchanged through the following test-only repairs: every web-source SHA256 captured in `final-source-provenance.json` was rechecked and matched before this report was added. The final candidate adds the two E2E fixture repairs and this report; the parent release lane records the resulting commit and terminal backend gate.

- **Measured Phase 2 regression:** the existing `composer-proposals.spec.ts` failed at its obsolete Pending changes region assertion (exit1, `proposal-e2e-red.log`). The repaired assertion scopes the unchanged native Accept/Reject names to Awaiting your decision and explicitly verifies that the old region does not exist.
- **Measured readiness fixture regression:** `typecheck:workspace-e2e` failed because `validationIssues()` omitted required `suggestion` on non-advisor blockers. Two deterministic workspace accessibility consumers also failed with Checks: Check failed instead of Checks:25 issues (`workspace-e2e-red.log`). The fixture now supplies `suggestion:null`; no frontend suggestion is fabricated.
- **Inherited fixture debt:** the dedicated typecheck also exposed an InterpretationEvent fixture using the retired `resolved_prompt_template_hash` field. Both that fixture and the current `approved_prompt_artifact_hash:string|null` type were confirmed in base627b72cf1. The pending event now carries `approved_prompt_artifact_hash:null`; the separate node requirement's resolved template hash is unchanged.
- **Measured inherited runtime failure:** once the panel locator was repaired, proposal acceptance reached its200 intercepted response but the strict composition-state decoder rejected the old partial state fixture, preventing the Applied card. The original unchanged proposal spec was then run against exact base `627b72cf1a0bcadfa260bfbbd9a111df79eb7b98` on isolated Vite5191 and reproduced the same post-acceptance Applied-card failure (exit1, `base-proposal-e2e.log`). Its acceptance/state endpoints are intercepted by the test; the shared isolated backend supplies authentication only. The fixture now supplies the complete current state envelope, source/output failure routes, and full state-version objects. A `satisfies CompositionState` check keeps its static shape explicit. No decoder or product behavior changed.
- **Terminal green:** the repaired proposal test and both affected workspace accessibility tests passed: `3 passed (6.8s)`, exit0 (`e2e-fixtures-green.log`). The second workspace test includes scoped axe checks with Checks closed and open. `typecheck:workspace-e2e` passed, as did a scoped TypeScript check including the proposal spec (`workspace-e2e-typecheck-green.log`, `e2e-fixtures-typecheck.log`). Scoped ESLint passed both touched files (`e2e-fixtures-eslint.log`); the proposal spec lies outside the default config's file patterns, so an ignored diagnostic config extended those patterns with the existing parser and rules.

The test fixture remains deterministic and intercepts provider/proposal/state responses. These automated E2E results supplement the earlier actual backend mutations; they do not add a live-provider acceptance claim. An initial workspace reproduction used mismatched fixture/backend data directories and was discarded as a harness setup failure; the reported red and green runs pin the same owned data directory.
