{
  "web/_aws_ecs_acceptance/bedrock.py:R4:run_bedrock_guardrails_live:ast=body[58]/body[4]/body[1]/body[0]/body[10]/handlers[1]": "R4 `except Exception: failure = AcceptanceCheckError(\"guardrails_live_check\")` in `run_bedrock_guardrails_live`, the sibling arm of the handler above. The breadth is deliberate because everything in the guarded block is Tier-3: `verify_bedrock_guardrails` drives real Bedrock ApplyGuardrail calls through `checker`, `repositories.run_lifecycle.get_web_plugin_policy_evidence` reads the persisted trail, and `manager.flush()` drives an OTLP exporter - none of whose exception types or messages are ours, and whose text can carry raw provider bodies, ARNs, request ids and endpoint URLs. Rewriting it to the static code `AcceptanceCheckError(\"guardrails_live_check\")` therefore makes this a redaction boundary as well as a catch; `test_verify_bedrock_guardrails_rejects_version_drift_and_redacts_checker_failure` pins the identical redaction on the inner `verify_bedrock_guardrails` handler. It is not a swallow: `failure` takes the same recorded-then-`raise failure` path as the `AcceptanceCheckError` arm, so the run still ends `RunStatus.FAILED` with a `TerminalOutcome.FAILURE` token and the operator still receives a raise.",
  "web/_aws_ecs_acceptance/bedrock.py:R4:run_bedrock_guardrails_live:ast=body[58]/body[4]/finalbody[0]/handlers[0]": "R4 `except Exception: manager_failed = True` in the `finally` that closes the telemetry manager in `run_bedrock_guardrails_live`. `manager` is built by `telemetry_manager_factory` (`_create_guardrail_telemetry_manager` -> `create_telemetry_manager`), an OTLP exporter whose `close()` can fail in any provider-defined way, so the breadth matches what is not ours to type. The swallow is bounded by the `manager_failed` flag, which the statements immediately after the `try/finally` convert into `raise AcceptanceCheckError(\"guardrails_telemetry\")` - a close fault is reported to the operator, never dropped. Raising from inside the `finally` would be strictly worse: when the guarded body has already failed, `if manager_failed:` is never reached and the original named failure propagates untouched, so a telemetry-close fault can neither mask a real check failure nor be mistaken for one.",
  "web/_aws_ecs_acceptance/bedrock.py:R6:run_bedrock_guardrails_live:ast=body[58]/body[4]/body[1]/body[0]/body[10]/handlers[0]": "R6 `except AcceptanceCheckError as exc: failure = exc` inside `run_bedrock_guardrails_live`. This is a DEFERRED re-raise, not a swallow: the caught error is bound to `failure`, and the branch below records it in the Landscape audit trail - `repositories.execution.complete_node_state(state.state_id, NodeStateStatus.FAILED, error=ExecutionError(...))`, `repositories.data_flow.record_token_outcome(token_ref, TerminalOutcome.FAILURE, TerminalPath.UNROUTED, error_hash=...)` and `repositories.run_lifecycle.complete_run(run.run_id, RunStatus.FAILED)` - before executing `raise failure` unconditionally. Catching is what makes that audit write reachable at all: an escaping exception would leave the run row open and the token outcome unrecorded, which is exactly the evidence this acceptance lane exists to produce. No path reaches `RunStatus.COMPLETED` while `failure` is set, and `if result is None: raise AcceptanceCheckError(\"guardrails_live_check\")` at the end of the function is the backstop should the success branch never assign `result`.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:ast=body[45]/body[5]": "R7 `contextlib.suppress(Exception)` around `sys.stdout.flush()` before the redirect is installed in `_suppress_process_output`. This is a best-effort ORDERING flush: it drains whatever Python-level buffer already exists so pre-existing output is not carried across the fd swap and emitted afterwards. It is deliberately not the disclosure control - that is `os.dup2(null_fd, 1)` in the `try` below, whose failure raises `AcceptanceCheckError(\"bedrock_output_boundary\")` through the `except OSError` arm. `sys.stdout` is not owned by this module (a pytest capture object, a replaced or already-closed stream, a reentrant-write `RuntimeError`), so the broad breadth matches what is genuinely not ours to type, and suppressing it cannot admit provider bytes because no provider call has been made at this point in the function.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:ast=body[45]/body[6]": "R7 `contextlib.suppress(Exception)` around `sys.stderr.flush()` before the redirect is installed in `_suppress_process_output`. Same best-effort pre-redirect ordering flush as the stdout line above, on the other stream: it exists only so buffered pre-existing text is not replayed after the fd swap. The value it protects is nothing yet - the Bedrock call happens later, inside the `yield` - and the actual containment is the `os.dup2(null_fd, 2)` redirect below, which fails closed with `AcceptanceCheckError(\"bedrock_output_boundary\")` rather than proceeding unredirected.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:ast=body[45]/body[8]/finalbody[0]": "R7 `contextlib.suppress(Exception)` around `sys.stdout.flush()` in the `finally` of `_suppress_process_output`, draining buffers written during the Bedrock call before `restore()` rebinds descriptors 1 and 2. A raise here would replace the in-flight acceptance failure - `bedrock_provider`, `bedrock_timeout` or `bedrock_content` - with an unrelated stream error AND skip the `restore()` call on the line below, leaving the process with stdout and stderr still bound to `/dev/null`. The control that actually keeps provider text off the operator's terminal is the fd-level redirect installed above, not this flush: `test_verify_bedrock_timeout_and_provider_failures_are_static_and_fd_suppressed` and `test_verify_bedrock_suppresses_fd_output_on_success` both assert via `capfd` that bytes written by the provider reach neither captured stdout nor captured stderr.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:ast=body[45]/body[8]/finalbody[1]": "R7 `contextlib.suppress(Exception)` around `sys.stderr.flush()` in the `finally` of `_suppress_process_output`. Same post-call ordering flush as the stdout line above, on descriptor 2, and with the same reason for breadth: `sys.stderr` may have been replaced or closed by the provider stack, and this module does not own its exception vocabulary. The suppress is what guarantees `restore()` on the next line always runs, which is the invariant `test_suppress_process_output_restores_writable_stdout_and_stderr` pins; the disclosure guarantee itself belongs to the `os.dup2(null_fd, 2)` redirect, verified by the two `capfd` provider tests.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:restore:ast=body[45]/body[4]/body[0]/body[0]": "R7 `contextlib.suppress(OSError)` around `os.dup2(saved_stdout, 1)` in `_suppress_process_output.restore`. The Tier-3 value here is a kernel fd-table outcome rather than data; what the block protects is the raw Bedrock provider text - returned model ids, provider request ids, credential URLs and role ARNs - that the redirect keeps off process stdout. `restore` is reachable from exactly two places: the `except OSError` arm that raises `AcceptanceCheckError(\"bedrock_output_boundary\")` when the redirect could not be ESTABLISHED, and the `finally` of the `yield`. In both, letting a restore OSError escape would replace the named acceptance code the operator is meant to see with a bare stream error and abandon the remaining restore steps below this line. The disclosure control is not this call but the `os.dup2(null_fd, 1)` / `os.dup2(null_fd, 2)` pair inside the `try` that fails closed on OSError, and `test_suppress_process_output_restores_writable_stdout_and_stderr` pins that fds 1 and 2 are writable and correctly bound once the block exits.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:restore:ast=body[45]/body[4]/body[1]/body[0]": "R7 `contextlib.suppress(OSError)` around `os.dup2(saved_stderr, 2)` in `_suppress_process_output.restore`. Identical shape and identical bounded outcome to the stdout restore above, on descriptor 2 instead of 1: the guard `if saved_stderr is not None` proves a dup was taken, so the only admitted failure is the kernel refusing the rebind, and the worst case is that fd 2 stays pointed at `/dev/null` for the remainder of the acceptance process - a loss of operator visibility, never a disclosure. Raising instead would swallow the in-flight `AcceptanceCheckError` that `restore` is unwinding for and skip the `os.close` loop below, so the fail-closed contract of `_suppress_process_output` depends on this suppress. `test_suppress_process_output_restores_writable_stdout_and_stderr` asserts the post-block stderr binding via `capfd`.",
  "web/_aws_ecs_acceptance/bedrock.py:R7:_suppress_process_output:restore:ast=body[45]/body[4]/body[2]/body[0]/body[0]": "R7 `contextlib.suppress(OSError)` around `os.close(descriptor)` in the cleanup loop of `_suppress_process_output.restore`. By the time this loop runs, both `os.dup2` restores above have already rebound descriptors 1 and 2, so the only value at stake is the three bookkeeping descriptors (`null_fd`, `saved_stdout`, `saved_stderr`) and the only reachable consequence of a failed close is a leaked descriptor for the life of the acceptance process. The suppress is what makes the loop total: without it a single EBADF would leave the remaining descriptors unclosed and would replace the propagating `AcceptanceCheckError` - `bedrock_output_boundary` on the setup path, or the provider/content failure on the `finally` path - with an unrelated OSError.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R4:AWSOperatorMetricEmitter:emit_web_metric:ast=body[46]/body[2]/body[1]/handlers[0]": "R4 `except Exception: return False` in `AWSOperatorMetricEmitter.emit_web_metric`. The value is Tier-3: the OpenTelemetry provider runtime and whatever the AWS OTLP collector does with a counter add and a `force_flush`. `False` is not a silent default - it is this method's stated outcome for `TelemetrySentinelEmitter.emit_web_metric`, which is declared to return whether delivery was receipted, and both callers treat it as a verdict rather than an absence. `verify_operator_telemetry` raises `OperatorTelemetryAcceptanceError('operator telemetry delivery was unavailable')` the moment it is False, so the positive phase cannot proceed on an unreceipted metric. `verify_operator_telemetry_outage` deliberately wants False: it seeds `cloud_receipt` from it and raises `OperatorTelemetryAcceptanceError('telemetry outage produced a false delivery receipt')` if any receipt later appears, so swallowing here is what lets the outage phase distinguish a genuinely degraded collector from a working one. Narrowing the handler is not available: a delivery failure inside a vendor exporter surfaces as an arbitrary provider exception, and the honest reading of any of them is the same - no receipt.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R4:verify_operator_telemetry_live:ast=body[55]/body[19]/finalbody[0]/body[1]/handlers[0]": "R4 `except Exception: close_failed = True` in the `finally` of `verify_operator_telemetry_live`, closing the X-Ray client, the CloudWatch client and the metric emitter. The value is Tier-3: three vendor client objects whose `close()` may raise anything. The swallow is bounded by the `close_failed` flag and the statement that immediately follows the loop, `if close_failed and sys.exc_info()[0] is None: raise OperatorTelemetryAcceptanceError('operator telemetry resource close failed')` - so a close fault is reported as a named acceptance failure, never dropped. The `sys.exc_info()` guard is the reason the raise cannot be moved into the handler: when the body already failed, raising from the `finally` would replace the real telemetry verdict with a teardown error and lose it, and it would also abandon the remaining clients unclosed. Deferring lets every client be closed and preserves whichever failure actually matters.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R5:AWSOperatorTelemetryQueries:_sentinel_observed:ast=body[36]/body[4]/body[0]/body[11]/body[0]/test/values[2]/operand": "R5 `isinstance(timestamp, datetime)` in `AWSOperatorTelemetryQueries._sentinel_observed`, on the loop target of `for value, timestamp in zip(values, timestamps, strict=True)`. The value is Tier-3: one datapoint timestamp inside a CloudWatch GetMetricData response, so the shape check is the admission test, not a nominal assertion on a class ELSPETH owns. `isinstance` rather than `type(x) is datetime` is deliberate and load-bearing: botocore hands back timezone-aware `datetime` instances whose class is a subclass, and an exact-type test would reject every real response. The function carries `@trust_boundary(source_param='raw', suppresses=('R1','R5'))` and every other read of the same response is suppressed as derived from `raw`; this one is not, because `subject_is_rooted` cannot root a `zip(...)` iteration target - the iterable is a `Call` whose func is a bare `Name`, so the derivation walk has nothing to chain through back to `timestamps`. Removing `strict=True` would make the target rootable and is exactly the dodge this sweep bans; it would also drop the length agreement between the response's Values and Timestamps arrays. What the check protects is the acceptance sentinel's window binding: it gates the `timestamp.tzinfo is None` and `start_time <= timestamp <= end_time` tests immediately after it, which are what stop a datapoint from outside the query window being counted as proof that the operator metric was delivered.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R5:_connection_budget_series:ast=body[57]/body[1]/body[11]/body[0]/test/values[0]/operand": "R5 `isinstance(timestamp, datetime)` in `_connection_budget_series`, on the loop target of `for timestamp, value in zip(timestamps, values, strict=True)`. Identical mechanism to the sentinel-series site in `AWSOperatorTelemetryQueries._sentinel_observed`, on the AWS/RDS DatabaseConnections response instead: Tier-3 datapoint timestamp, `isinstance` because botocore returns aware `datetime` subclass instances, and a `zip(..., strict=True)` target that the boundary's `source_param='response'` derivation cannot root. What it protects is the connection-budget high-water grid: it gates `timestamp.tzinfo is None` and the `astimezone(UTC)` normalisation, and the normalised timestamps are then required to be exactly the ten expected minute boundaries before any high-water number is admitted into the receipt.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R5:_connection_budget_series:ast=body[57]/body[1]/body[11]/body[0]/test/values[2]": "R5 `isinstance(value, bool)` in `_connection_budget_series`, on the `value` loop target of the same `zip(timestamps, values, strict=True)` and unreachable by the boundary walk for the same reason. This is the bool-is-an-int rejection: Python's `bool` is a subclass of `int`, so without it a JSON `true` in a Tier-3 CloudWatch Values array would pass the numeric check below and be admitted as the connection count 1. `isinstance` is required here rather than `type(x) is bool` precisely because the guard exists to catch the subclass relationship. What it protects is the `high_water` figure the connection-budget receipt asserts and the `approved_budget` comparison an operator signs off on.",
  "web/_aws_ecs_acceptance/operator_telemetry.py:R5:_connection_budget_series:ast=body[57]/body[1]/body[11]/body[0]/test/values[3]/operand": "R5 `isinstance(value, (int, float))` in `_connection_budget_series`, the positive half of the same bool-then-number pair on the same unrootable `zip` target. The value is Tier-3: one CloudWatch datapoint. `isinstance` accepts the numeric subclasses JSON decoding and botocore can legitimately produce, which an exact-type test would reject. It gates the `math.isfinite(float(value))` and `float(value) < 0` tests that follow, so a NaN, an infinity, or a negative count cannot reach the high-water maximum the receipt asserts.",
  "web/_aws_ecs_acceptance/orphan_sweep.py:R4:orphan_sweep:close_client:ast=body[39]/body[18]/body[1]/handlers[0]": "R4 `except Exception: resource_close_failed = True` in `orphan_sweep.close_client`, registered as an `contextlib.ExitStack` callback for all fourteen AWS clients. The value is Tier-3: vendor client objects whose `close()` may raise anything. The swallow is bounded by the `resource_close_failed` flag and by the statement at the end of `orphan_sweep`, `if resource_close_failed: raise AcceptanceCheckError('orphan_sweep_resource_close')` - so a close fault is always reported as a named acceptance failure. It cannot be raised in place: this callback runs during ExitStack unwind, where raising would abandon the remaining thirteen clients unclosed and would replace whichever real sweep verdict is in flight (`orphan_sweep_survivors`, `orphan_sweep_api`, or a clean receipt) with a teardown error. The deferred flag is also checked AFTER `_validate_bounded_receipt_document(receipt)` and after the survivor check, so a teardown fault can never mask a surviving orphan.",
  "web/_aws_ecs_acceptance/orphan_sweep.py:R5:_orphan_call:ast=body[24]/body[1]/test/operand": "R5 `isinstance(response, Mapping)` in `_orphan_call`. The value is Tier-3: whatever a bound boto3 client operation returned, so this is the admission test for every AWS response the sweep reads, not a nominal assertion on a class ELSPETH owns. `isinstance` rather than `type(x) is dict` is required: botocore returns plain dicts today but the guard is written against the `Mapping` contract the rest of this module consumes, and a stubbed or fake client legitimately returns other mapping types. The function carries `@trust_boundary(source_param='method', suppresses=('R1','R5'))`, but this site is not suppressed because `response` is bound inside the `try:` and read after it, and the tier-model walk intersects derived-name snapshots across the handler paths - the `except Exception as exc` arm does not bind `response`, so the name leaves the try undelivered. Moving the check inside the try would place its `raise AcceptanceCheckError` under that same `except Exception`, making the module's central error projection depend on the handler re-deriving the same verdict from its own exception - a worse contract for a one-finding gain. What the check protects is every caller in this module: `_orphan_response_items`, `_orphan_paged_items`, `_assert_iam_role_identity` and the surface loops in `orphan_sweep` all index the returned mapping, and a non-mapping reaching them would surface as an attribute error inside the sweep rather than as the named `orphan_sweep_api` failure an operator can act on.",
  "web/_aws_ecs_acceptance/orphan_sweep.py:R5:orphan_sweep:ast=body[39]/body[19]/body[1]/body[7]/body[56]/body[2]/test/args[0]/elt/operand": "R5 `isinstance(item, Mapping)` in `orphan_sweep`, over the items of a Bedrock ListGuardrails page obtained through `_orphan_paged_items`. The value is Tier-3: a provider response page, so the shape check is the admission test. It is an all-or-nothing reject-gate - `if any(not isinstance(item, Mapping) for item in guardrails): raise AcceptanceCheckError('orphan_sweep_api')` - rather than a per-item projection, because unlike every other surface in this sweep the guardrail count is taken from `len(guardrails)` directly: the sweep does not read a field off each guardrail, it only needs to know how many survived. There is therefore no named projection for this page to be lifted into; a helper whose whole body is this assertion would be indirection, not a parse entry point. What the check protects is the `counts['bedrock']['unapproved_survivors'] += len(guardrails)` on the next line, which feeds `total_unapproved_survivors` in the signed orphan-sweep receipt: a page of non-mapping junk must fail the sweep rather than be counted as a credible survivor tally.",
  "web/_aws_ecs_acceptance/orphan_sweep.py:R7:_build_orphan_sweep_clients:ast=body[21]/body[4]/handlers[0]/body[0]/body[0]": "R7 `with contextlib.suppress(Exception): created_client.close()` in `_build_orphan_sweep_clients`. The value is Tier-3: a partially built bundle of boto3 clients, each of whose `close()` may raise anything the vendor SDK chooses. This runs only on the error path - the handler is reached when `OrphanSweepClients(...)` failed part-way through constructing the fourteen clients - and its job is to close every client that WAS created before re-raising. The control that makes the swallow safe is the statement the loop guards: `raise AcceptanceCheckError('orphan_sweep_runtime') from None` runs unconditionally after it, so the construction failure is always reported. Letting a close fault propagate here would replace that named failure with an arbitrary SDK exception AND abandon the remaining clients in `created` unclosed, which is the opposite of what this handler exists to do. Nothing downstream reads a result from these closes; the sweep has not yet queried anything, so no evidence can be affected.",
  "web/_aws_ecs_acceptance/receipt_contracts.py:R5:_validate_guardrail_receipt_details:ast=body[40]/body[15]/body[0]/test/values[0]/operand": "R5 `isinstance(selected, Mapping)` in `_validate_guardrail_receipt_details`, on the loop target of `for selected, (capability, plugin_id, profile_alias) in zip(selected_controls, expected_controls, strict=True)`. Identical trail loss to the `control` site above and for the same mechanical reason: the boundary's `source_param=\"details\"` derivation reaches `plugin_policy` and `selected_controls` by subscript, but a `zip(...)` target is not rootable, and removing `strict=True` to make it rootable is banned and would additionally discard the length agreement with `expected_controls`. The value is Tier-3: the plugin-policy selected-control records inside an untrusted receipt. What the check protects is the required-control attestation itself - it guards the `set(selected) != _PLUGIN_POLICY_CONTROL_FIELDS` and the capability / plugin_id / profile_alias / `mode == \"required\"` equalities immediately below, which are what bind the receipt to the two guardrail controls whose aliases this function already validated.",
  "web/_aws_ecs_acceptance/receipt_contracts.py:R5:_validate_guardrail_receipt_details:ast=body[40]/body[5]/body[0]/test/values[0]/operand": "R5 `isinstance(control, Mapping)` in `_validate_guardrail_receipt_details`, on the loop target of `for control, expected_plugin in zip(controls, expected_plugins, strict=True)`. The value is Tier-3: one guardrail control record inside a receipt the acceptance controller decoded from Session Manager output or read back from the receipt store, so the shape check is the admission test, not a nominal assertion on a class ELSPETH owns. The function carries `@trust_boundary(source_param=\"details\", suppresses=(\"R1\",\"R5\"))` and every other read of the same document is suppressed as derived from `details`; this one is not, because `subject_is_rooted` cannot root a `zip(...)` iteration target - the iterable is a `Call` whose func is a bare `Name`, so the derivation walk has nothing to chain through back to `controls`. Dropping `strict=True` would make the target rootable and is exactly the dodge the sweep bans: it would also drop the length agreement between the receipt's controls and `expected_plugins`. What this check protects is the closed control-field set an operator signs off on - it is the gate before `set(control) != _GUARDRAIL_CONTROL_FIELDS`, so a non-mapping control would otherwise reach the plugin-id, alias, version, and sha256 reads below it and let a receipt attest guardrail coverage it never demonstrated.",
  "web/_aws_ecs_acceptance/receipt_contracts.py:R6:resolve_exec_receipt_env:ast=body[43]/body[4]/handlers[0]": "R6 `except ValueError: valid = False` in `resolve_exec_receipt_env`. The value is Tier-3: `ECS_CONTAINER_METADATA_URI_V4` as it arrives in the container process environment, parsed by `urlsplit` whose `parsed.port` accessor raises `ValueError` on a non-numeric or out-of-range port and whose parse raises on a malformed authority - the exception IS one of the malformed-input outcomes, not an unexpected fault. The swallow is safe because the handler's only effect is to clear the local `valid` flag, and control falls straight into the UNCONDITIONAL `if not valid: raise AcceptanceCheckError(\"exec_receipt_binding\")` that follows the try - the same error and the same static message the success path's failed predicate produces, so no execution path leaves this block with a URI that was not proved to be the exact link-local v4 metadata origin. What that protects is the task-ARN binding of every in-task receipt: the resolved ARN is hashed into `task_arn_sha256` and later compared against the ARN the controller expected, so admitting an attacker-chosen metadata origin here would let a receipt bind to the wrong task. R6 is also structurally outside what `@trust_boundary` may suppress - the decorator's authorised set is exactly R1 and R5 - so this finding cannot be cleared by the boundary the function already carries."
}
