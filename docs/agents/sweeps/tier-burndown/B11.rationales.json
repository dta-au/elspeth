{
  "engine/executors/sink_effects.py:R1:SinkEffectCoordinator._predecessor_descriptor:ast=body[35]/body[41]/body[4]/body[7]/body[1]/test/left": "plan.safe_evidence.get(\"publication_kind\") reads a PREDECESSOR effect's durable, plugin-authored safe-evidence mapping while walking the artifact lineage chain backwards. The key is absent for every plan written before the reaffirmation kind existed and for every sink that never publishes a reaffirmation, so absence is the common shape, not a missing-key bug. Absence and a present-but-not-\"reaffirmed\" value are the same fact here (this predecessor established no new remote identity) and both must take the walk-back branch that continues to the previous predecessor (elspeth-fac5260c6a). Direct indexing would raise KeyError on an audit-lineage walk for every legacy or non-reaffirming plan; a membership-form read would spell the identical two-way outcome in three lines and change nothing. The mapping is Tier-2 plugin-authored evidence the engine does not own the schema of.",
  "engine/executors/sink_effects.py:R4:_SinkEffectLeaseHeartbeat._run:ast=body[30]/body[6]/body[0]/body[0]/handlers[0]": "This handler TRANSPORTS the exception across a thread boundary; it does not swallow it. _run is the body of the background lease-heartbeat thread, where an escaping exception would be consumed by threading's excepthook and the owning thread would keep issuing external sink writes under a lease it can no longer prove it holds. The handler stores the exception object in self._error and sets _failed_event; check_and_raise() re-raises that exact object on the owning thread, and refresh_and_check() calls it before AND after every observable adapter call, so the failure is surfaced before the next side effect rather than after it. BaseException rather than Exception because a KeyboardInterrupt, SystemExit or cancellation delivered into the heartbeat thread must also stop the lease holder from publishing. Nothing is logged-and-continued and nothing is discarded.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator.__init__:ast=body[35]/body[1]/body[0]/test/operand": "isinstance(factory, RecorderFactory) is the ADR-032 case-1 internal boundary: RecorderFactory is a concrete class ELSPETH defines, and the coordinator reaches its entire authority through it - the durable sink-effect store, the payload store, lease ownership and the artifact register. An impostor accepted here forges the audit trail, which is exactly the internal-masquerade threat nominal typing is the control for. isinstance rather than the exact-type form used two lines below for lease_ttl because subclassing an owned repository facade is a supported extension and the resume/quarantine suites legitimately pass MagicMock(spec=RecorderFactory) doubles, which satisfy isinstance and would be rejected by an exact-type test. The three sibling scalar checks in this constructor were converted to the exact-type form in this pass; this one is the case where the class identity, not the exact type, is the credential.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator._execute_member_effects:ast=body[35]/body[13]/body[10]/test": "result here is the value returned by _latest_exact_member_result, whose declared type is the closed union SinkEffectCommitResult | SinkEffectReconcileResult - two frozen ELSPETH-owned dataclasses. This is union DISCRIMINATION driving different audit writes, not validation of a trusted value: the commit arm finalizes with the result's own descriptor and records evidence_kind=\"returned\", while the reconcile arm must first assert kind is APPLIED_WITH_EXACT_DESCRIPTOR and a non-None descriptor before finalizing with evidence_kind=\"reconciled\" and the reconcile kind. Unlike this file's per-action decode sites - which this pass narrowed statically by overloading _returned_attempt on the action Literal - the union is genuinely open at this point, because the helper scans durable attempts backwards and returns whichever action's result was last exact.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator._is_member_effect_adapter:ast=body[35]/body[12]/body[0]/value/values[1]": "MemberSinkEffectCapability is a plain ELSPETH-owned base class, NOT a Protocol - its docstring states it is the nominal opt-in for the capability. The population being tested is runtime sink plugin classes, genuinely unknown Tier-2 code, so this is ADR-032 case 1 in its intended form: inheritance from a class ELSPETH defines is the credential a plugin must present before the coordinator will drive one durable effect per pipeline member for it. Structural typing is deliberately refused here - a sink that merely happens to define a method of the right name must not be routed down this path, and ADR-032 bans runtime_checkable Protocol isinstance as both a security and a dispatch control. The companion test on the same line is the exact-type form (type(effect_input) is SinkEffectPipelineMembersInput) because that input is an engine-owned closed value with no subclasses, whereas the capability marker exists precisely to be subclassed.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator._is_restaging_adapter:ast=body[35]/body[11]/body[0]/value/values[1]": "RestagingSinkEffectCapability is a plain ELSPETH-owned base class, NOT a Protocol - its docstring states it is the nominal opt-in for the capability. The population being tested is runtime sink plugin classes, genuinely unknown Tier-2 code, so this is ADR-032 case 1 in its intended form: inheritance from a class ELSPETH defines is the credential a plugin must present before the coordinator will drive reconstruction of a lost staged body before commit for it. Structural typing is deliberately refused here - a sink that merely happens to define a method of the right name must not be routed down this path, and ADR-032 bans runtime_checkable Protocol isinstance as both a security and a dispatch control. The companion test on the same line is the exact-type form (type(effect_input) is SinkEffectPipelineMembersInput) because that input is an engine-owned closed value with no subclasses, whereas the capability marker exists precisely to be subclassed.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator._latest_exact_member_result:ast=body[35]/body[18]/body[0]/body[2]/test/values[0]": "This function calls decode_sink_effect_returned_result with attempt.action - a VARIABLE read off each durable attempt row as the loop scans backwards - so, unlike the per-action decode sites in this file that this pass narrowed statically by overloading _returned_attempt on the action Literal, the returned union cannot be narrowed here at all. The two isinstance tests ARE the selection predicate the function exists to compute: the most recent member attempt whose durable result actually carries an exact descriptor, which is any SinkEffectCommitResult or a SinkEffectReconcileResult of kind APPLIED_WITH_EXACT_DESCRIPTOR. This operand admits the commit half: a returned commit result always carries a descriptor. Both operands are frozen ELSPETH-owned dataclasses (ADR-032 case 1). Dropping the predicate would let a non-exact reconcile result finalize the member set with a descriptor no publication ever established.",
  "engine/executors/sink_effects.py:R5:SinkEffectCoordinator._latest_exact_member_result:ast=body[35]/body[18]/body[0]/body[2]/test/values[1]/values[0]": "This function calls decode_sink_effect_returned_result with attempt.action - a VARIABLE read off each durable attempt row as the loop scans backwards - so, unlike the per-action decode sites in this file that this pass narrowed statically by overloading _returned_attempt on the action Literal, the returned union cannot be narrowed here at all. The two isinstance tests ARE the selection predicate the function exists to compute: the most recent member attempt whose durable result actually carries an exact descriptor, which is any SinkEffectCommitResult or a SinkEffectReconcileResult of kind APPLIED_WITH_EXACT_DESCRIPTOR. This operand is the reconcile half, and is deliberately conjoined with the kind test - a reconcile result of any other kind is NOT exact and must not be selected. Both operands are frozen ELSPETH-owned dataclasses (ADR-032 case 1). Dropping the predicate would let a non-exact reconcile result finalize the member set with a descriptor no publication ever established.",
  "engine/executors/sink.py:R4:SinkExecutor._record_boundary_failure_operation:ast=body[40]/body[6]/body[2]/handlers[0]": "The guarded expression is str() on a plugin-authored contract-violation exception, whose __str__ interpolates row values and can itself raise. The handler sits inside the audit write for that boundary failure: an exception escaping here would abandon complete_operation and leave the sink_write operation permanently open in the trail - the exact audit-integrity failure this record exists to prevent, and the same doctrine _render_exception_message implements for node states (elspeth-d67453c22f). Nothing is suppressed: the fallback is type(violation).__name__, a guaranteed non-empty and secret-free value that is still written to the operation record, and the violation itself continues to propagate on the caller's own path, which is where it is classified and routed. BaseException rather than Exception because a hostile or merely broken __str__ can raise anything. Its sibling handler in this file (_complete_states_failed cleanup) shows the same doctrine's other half: contract_errors.TIER_1_ERRORS is re-raised above the broad arm, so audit corruption is never absorbed.",
  "engine/executors/sink.py:R5:SinkExecutor._write_primary_effect:ast=body[40]/body[12]/body[4]/handlers[0]/body[0]/value/keywords[0]/value/generators[0]/ifs[0]": "all_states is declared as NodeState - the closed union of the concrete ELSPETH-owned NodeStateOpen | NodeStateFailed | NodeStateCompleted records - because _open_or_reuse_effect_states REUSES the exact state set left by an interrupted effect call, so on a resume some entries are already terminal. _complete_states_failed writes a terminal FAILED record; applying it to an already-terminal state is a SECOND terminal write, which corrupts the audit trail (the invariant NodeStateGuard._terminal_persisted protects on the other side of the same doctrine). The filter is therefore the control this boundary-failure path needs - it selects the states that still require terminalizing - not a defensive re-check of a value already known to be OPEN. ADR-032 case 1: nominal discrimination over a concrete class ELSPETH defines. This site terminalizes the primary sink's states after a declaration/plugin contract violation is caught at the sink write boundary.",
  "engine/executors/sink.py:R5:SinkExecutor._handle_failsink_effect_diversions:ast=body[40]/body[14]/body[11]/handlers[0]/body[1]/value/keywords[0]/value/generators[0]/ifs[0]": "failsink_states is declared as NodeState - the closed union of the concrete ELSPETH-owned NodeStateOpen | NodeStateFailed | NodeStateCompleted records - because _open_or_reuse_effect_states REUSES the exact state set left by an interrupted effect call, so on a resume some entries are already terminal. _complete_states_failed writes a terminal FAILED record; applying it to an already-terminal state is a SECOND terminal write, which corrupts the audit trail (the invariant NodeStateGuard._terminal_persisted protects on the other side of the same doctrine). The filter is therefore the control this boundary-failure path needs - it selects the states that still require terminalizing - not a defensive re-check of a value already known to be OPEN. ADR-032 case 1: nominal discrimination over a concrete class ELSPETH defines. This site terminalizes the quarantine states just opened for the failsink when its own boundary check rejects the enriched row (elspeth-2a75af7f8f).",
  "engine/executors/sink.py:R5:SinkExecutor._handle_failsink_effect_diversions:ast=body[40]/body[14]/body[11]/handlers[0]/body[2]/value/keywords[0]/value/generators[0]/ifs[0]": "primary_divert_states is declared as NodeState - the closed union of the concrete ELSPETH-owned NodeStateOpen | NodeStateFailed | NodeStateCompleted records - because _open_or_reuse_effect_states REUSES the exact state set left by an interrupted effect call, so on a resume some entries are already terminal. _complete_states_failed writes a terminal FAILED record; applying it to an already-terminal state is a SECOND terminal write, which corrupts the audit trail (the invariant NodeStateGuard._terminal_persisted protects on the other side of the same doctrine). The filter is therefore the control this boundary-failure path needs - it selects the states that still require terminalizing - not a defensive re-check of a value already known to be OPEN. ADR-032 case 1: nominal discrimination over a concrete class ELSPETH defines. This site terminalizes the still-open PRIMARY divert anchors in the same failure, so no diverted token is left in progress after the failsink refused its row.",
  "engine/executors/sink.py:R5:SinkExecutor._handle_failsink_effect_diversions:ast=body[40]/body[14]/body[22]/body[3]/test": "Exhaustive three-way discrimination over the owned closed NodeState union, on a record read back from the repository with get_node_state after a linked-failsink finalization that may have been interrupted and resumed by another generation. OPEN means THIS process must still write the routing/terminal evidence; FAILED means a previous generation already did, and that arm VERIFIES its evidence rather than duplicating it; anything else raises AuditIntegrityError. The trailing else: raise is what makes this fail-closed - there is no silent third path and no default. The checks are the control, not padding: removing them would either double-write a terminal node state or lose the divert evidence across a resume. All three operands are concrete ELSPETH-owned classes (ADR-032 case 1). This is the OPEN arm: it records the DIVERT routing event and completes the primary anchor FAILED with the disclosable diversion reason.",
  "engine/executors/sink.py:R5:SinkExecutor._handle_failsink_effect_diversions:ast=body[40]/body[14]/body[22]/body[3]/orelse[0]/test": "Exhaustive three-way discrimination over the owned closed NodeState union, on a record read back from the repository with get_node_state after a linked-failsink finalization that may have been interrupted and resumed by another generation. OPEN means THIS process must still write the routing/terminal evidence; FAILED means a previous generation already did, and that arm VERIFIES its evidence rather than duplicating it; anything else raises AuditIntegrityError. The trailing else: raise is what makes this fail-closed - there is no silent third path and no default. The checks are the control, not padding: removing them would either double-write a terminal node state or lose the divert evidence across a resume. All three operands are concrete ELSPETH-owned classes (ADR-032 case 1). This is the FAILED arm: it asserts exactly one prior routing event on the expected failsink edge in DIVERT mode, and raises on divergent evidence.",
  "engine/executors/sink.py:R5:SinkExecutor._handle_discard_diversions:ast=body[40]/body[15]/body[2]/body[4]/test": "Exhaustive three-way discrimination over the owned closed NodeState union, on a record read back from the repository with get_node_state after a discard-mode diversion finalization that may have been interrupted and resumed by another generation. OPEN means THIS process must still write the routing/terminal evidence; FAILED means a previous generation already did, and that arm VERIFIES its evidence rather than duplicating it; anything else raises AuditIntegrityError. The trailing else: raise is what makes this fail-closed - there is no silent third path and no default. The checks are the control, not padding: removing them would either double-write a terminal node state or lose the divert evidence across a resume. All three operands are concrete ELSPETH-owned classes (ADR-032 case 1). This is the OPEN arm: it completes the primary anchor FAILED with the disclosed discard reason (ADR-019 predicate-input failure).",
  "engine/executors/sink.py:R5:SinkExecutor._handle_discard_diversions:ast=body[40]/body[15]/body[2]/body[4]/orelse[0]/test/operand": "Exhaustive three-way discrimination over the owned closed NodeState union, on a record read back from the repository with get_node_state after a discard-mode diversion finalization that may have been interrupted and resumed by another generation. OPEN means THIS process must still write the routing/terminal evidence; FAILED means a previous generation already did, and that arm VERIFIES its evidence rather than duplicating it; anything else raises AuditIntegrityError. The trailing else: raise is what makes this fail-closed - there is no silent third path and no default. The checks are the control, not padding: removing them would either double-write a terminal node state or lose the divert evidence across a resume. All three operands are concrete ELSPETH-owned classes (ADR-032 case 1). This is the negative FAILED guard: a recovered anchor that is neither OPEN nor FAILED is an audit inconsistency and raises immediately.",
  "engine/executors/state_guard.py:R5:NodeStateGuard.__exit__:ast=body[26]/body[4]/body[1]/body[0]/test/operand": "The abandoned-attempt carve-out is the ONE path in this guard that deliberately leaves a node state OPEN as crash evidence, on the premise that a replacement generation now owns that token's terminal audit writes. It must therefore be gated on proof that scheduler ownership really was lost. SchedulerLeaseLostError and RunWorkerEvictedError are concrete ELSPETH-owned exception classes and this is ADR-032 case 1 over a genuinely unknown exc_val: BaseException | None. isinstance rather than an exact-type test because both are legitimately subclassed by more specific ownership-loss errors and an exception hierarchy is the credential. The negative branch is a loud OrchestrationInvariantError, not a fallback: abandoning a guard on any other exception is an engine bug and fails closed. Deleting the check would let any exception strand a node state permanently OPEN, which is the audit-integrity violation this whole class exists to make structurally impossible."
}
