{
  "_key_form": "<file>:<RULE>:<Class>:<method>:ast=<path>. ast= paths recomputed from the POST-edit tree with the rule's own scan_file(file, root=src/elspeth). B55's worked set is the allowlist-DISABLED corpus for web/secrets/service.py (8 findings) MINUS the 5 sites the real allowlist binds (3x R6 WebSecretService.resolve fp=e201cb7204d089d8/2661d9a427c7a309/00a3b8aa94bc1df2, 2x R6 WebSecretService.check_user_ref_resolvable fp=10b6ba219b62a2e6/0fe0ecc2659a3fa2 — scope_fingerprints verified unchanged after this lane's edits) = 3, all in WebSecretService.resolve_scoped. None removable in code: raising would fork scoped and unscoped secret markers onto different failure paths inside `resolve_secret_refs` (core/secrets.py), whose contract is one aggregated SecretResolutionError per pass, and would contradict the three binding rulings on the identical `resolve` handlers. The lane's code contribution is the missing pin: resolve_scoped had zero tests.",
  "web/secrets/service.py:R6:WebSecretService:resolve_scoped:ast=body[22]/body[5]/body[1]/handlers[0]": "R6 `except FingerprintKeyMissingError: _log_fingerprint_missing_rate_limited(); return None` in `WebSecretService.resolve_scoped`. The exception is a Tier-1 typed signal raised by our own stores (`UserSecretStore.get_secret` / `ServerSecretStore.get_secret`) when ELSPETH_FINGERPRINT_KEY is unset — a deployment misconfiguration, not external data. The handler is not a silent swallow: the outcome is recorded distinctly from absence by the control `_log_fingerprint_missing_rate_limited` (rate-limited `secret_resolve_fingerprint_key_missing` slog error carrying `suppressed_since_last_emit`), and None is the documented contract value that `_resolve_marker` in core/secrets.py turns into `missing.append(name)` so `resolve_secret_refs` raises ONE `SecretResolutionError` listing every unresolvable ref per validation/execution pass. Fail-closed: a missing key can never produce a ResolvedSecret, and no store fallback happens (scope selects exactly one store). The typed signal is deliberately preserved on the sibling paths that need HTTP mapping — `check_user_ref_resolvable` and `set_user_secret` propagate it to the app-level `handle_fingerprint_missing` (503). Same shape and same reasoning as the signed, binding ruling on `resolve` fp=e201cb7204d089d8. Pinned per exception class by tests/unit/web/secrets/test_service.py::TestResolveScoped (mutation-checked: dropping the breadcrumb or falling through to the other store turns the named test red). The five sibling handlers in `resolve` and `check_user_ref_resolvable` are signed and binding (unchanged by this lane).",
  "web/secrets/service.py:R6:WebSecretService:resolve_scoped:ast=body[22]/body[5]/body[1]/handlers[1]": "R6 `except SecretDecryptionError: _log_secret_decryption_rate_limited(); return None` in `WebSecretService.resolve_scoped`. Tier-1 typed signal from `UserSecretStore._decrypt_secret_value` (Fernet InvalidToken / UnicodeDecodeError re-raised as our owned type): the row exists but is unreadable under the current web secret_key (rotation, corruption, tamper). The outcome is recorded distinctly from absence by the control `_log_secret_decryption_rate_limited` (`secret_resolve_decryption_failed` slog error), and None is the aggregation-contract value consumed by `_resolve_marker` / `resolve_secret_refs` (core/secrets.py). What it protects: an undecryptable user row asked for as scope=\"user\" must NOT be substituted by a same-named server secret — resolve_scoped has no inter-store fallback, so the handler returns None rather than reading `_server_store`; pinned by TestResolveScoped::test_undecryptable_user_row_returns_none_and_emits_decryption_breadcrumb, which plants a same-named server secret and asserts it is never returned. The propagating sibling is `check_user_ref_resolvable` → app-level `handle_secret_decryption_failed` (409, re-save guidance). Same shape as the signed, binding ruling on `resolve` fp=2661d9a427c7a309. Pinned per exception class by tests/unit/web/secrets/test_service.py::TestResolveScoped (mutation-checked: dropping the breadcrumb or falling through to the other store turns the named test red). The five sibling handlers in `resolve` and `check_user_ref_resolvable` are signed and binding (unchanged by this lane).",
  "web/secrets/service.py:R6:WebSecretService:resolve_scoped:ast=body[22]/body[5]/body[1]/handlers[2]": "R6 `except SecretNotFoundError: return None` in `WebSecretService.resolve_scoped`. Tier-1 typed signal from our own stores meaning the selected store holds no row / no allowlisted env var for `name` (including a scope mismatch: a user-only secret requested as scope=\"server\", or vice versa). Absence is the honest outcome here, so None IS the explicit result — it is not fabrication and it is deliberately NOT accompanied by a deployment-error breadcrumb (TestResolveScoped::test_absent_secret_returns_none_with_no_breadcrumb and ::test_scope_mismatch_never_falls_through_to_the_other_store assert `_slog.error` is never called), which is what keeps genuine absence distinguishable from the two failure classes above in operator logs. Downstream, `_resolve_marker` (core/secrets.py) appends the name to `missing` and `resolve_secret_refs` raises one aggregated `SecretResolutionError`; `_secret_ref_exists` (web/execution/_validation_authoring.py) reads the None as 'ref does not exist' at validate time. No fallback to another store, no success path. Same shape as the signed, binding ruling on `resolve` fp=00a3b8aa94bc1df2 and on `check_user_ref_resolvable` fp=10b6ba219b62a2e6. Pinned per exception class by tests/unit/web/secrets/test_service.py::TestResolveScoped (mutation-checked: dropping the breadcrumb or falling through to the other store turns the named test red). The five sibling handlers in `resolve` and `check_user_ref_resolvable` are signed and binding (unchanged by this lane)."
}
