{
  "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:ast=body[53]/body[5]/body[6]/body[0]/test/values[0]": "R5 `isinstance(node, ast.Constant)` in the nested `_extract_string_key` helper: it decides whether a subscript/argument node is a literal at all. Returning None here is not a swallow — both callers turn None into `raise ValueError('... must be string literals')`. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:ast=body[53]/body[5]/body[6]/body[0]/test/values[1]": "R5 `isinstance(node.value, str)` in `_extract_string_key`: distinguishes `row['batch_count']` from `row[0]`/`row[None]` — a non-str constant must NOT be admitted as a key name. The None return is converted to a ValueError by every caller. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/test/values[0]": "R5 `isinstance(node, ast.Subscript)`: the first arm of the dispatch, recognising the `row['key']` form so its slice can be checked against the allowed batch-level keys. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/test/values[1]": "R5 `isinstance(node.value, ast.Name)`: guards `node.value.id == 'row'` — a subscript on anything other than the bare `row` name (e.g. `row['a']['b']`, a call result) is not a row-key access and must not be mistaken for one. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[0]": "R5 `isinstance(node, ast.Call)`: the second dispatch arm, recognising the `row.get('key')` / `row.get('key', default)` form. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[1]": "R5 `isinstance(node.func, ast.Attribute)`: `row.get(...)` is a Call whose func is an Attribute; without this arm `node.func.attr` would raise AttributeError on a plain-name call such as `len(x)`, which is a legitimate expression, not a row-key access. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[2]": "R5 `isinstance(node.func.value, ast.Name)`: guards `node.func.value.id == 'row'` so only `row.get` (not `something.row.get` or `f().get`) is treated as a row-key access. The subject is a node from `ast.walk(ast.parse(v, mode='eval'))`, i.e. Python's own `ast` class hierarchy, not the YAML value `v`. `ExpressionParser(v)` has already accepted the expression syntactically and security-wise on the lines above; this walk only enforces the Tier-1 batch-level contract that `row[...]`/`row.get(...)` name one of the two allowed keys. It is an after-validator (`@field_validator('condition')` without mode='before'), so `_is_pydantic_before_validator` does not apply. Invalidated if the walk stopped raising on a non-literal or unknown key, or if the dispatch grew a fall-through that let an unrecognised `row` access pass silently.",
  "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/test": "R5 `isinstance(value, str)`: the only leaf type that can contain a `${VAR}` / `${VAR:-default}` reference; strings are handed to `_expand_string`, which fails closed (ValueError) on an unset variable with no default. `_expand_value` is the recursive walk inside `_expand_env_vars(config)`, which runs on the raw Tier-3 config tree (Dynaconf YAML, or a web-authored dict when the caller opts into host env expansion) before `ElspethSettings` validation. It is a nested closure, so the outer function's parameter cannot be named by a `@trust_boundary`, and it takes `value: Any` by design: YAML admits any scalar here. Non-str/dict/list values (int, bool, None, the nominal `LoweredLLMProfileAlias` handled by the exact `type()` check above) pass through untouched so that `ElspethSettings` classifies them. Invalidated if a new container type (tuple/set) could carry `${VAR}` strings through this pass unexpanded, or if the walk started coercing rather than passing through unknown scalars.",
  "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/orelse[0]/test": "R5 `isinstance(value, dict)`: mapping arm of the walk — recurse into every value, preserving keys, so nested plugin `options` are expanded. `_expand_value` is the recursive walk inside `_expand_env_vars(config)`, which runs on the raw Tier-3 config tree (Dynaconf YAML, or a web-authored dict when the caller opts into host env expansion) before `ElspethSettings` validation. It is a nested closure, so the outer function's parameter cannot be named by a `@trust_boundary`, and it takes `value: Any` by design: YAML admits any scalar here. Non-str/dict/list values (int, bool, None, the nominal `LoweredLLMProfileAlias` handled by the exact `type()` check above) pass through untouched so that `ElspethSettings` classifies them. Invalidated if a new container type (tuple/set) could carry `${VAR}` strings through this pass unexpanded, or if the walk started coercing rather than passing through unknown scalars.",
  "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/orelse[0]/orelse[0]/test": "R5 `isinstance(value, list)`: sequence arm of the walk — recurse per element so `transforms[*]`/`aggregations[*]` entries are expanded. `_expand_value` is the recursive walk inside `_expand_env_vars(config)`, which runs on the raw Tier-3 config tree (Dynaconf YAML, or a web-authored dict when the caller opts into host env expansion) before `ElspethSettings` validation. It is a nested closure, so the outer function's parameter cannot be named by a `@trust_boundary`, and it takes `value: Any` by design: YAML admits any scalar here. Non-str/dict/list values (int, bool, None, the nominal `LoweredLLMProfileAlias` handled by the exact `type()` check above) pass through untouched so that `ElspethSettings` classifies them. Invalidated if a new container type (tuple/set) could carry `${VAR}` strings through this pass unexpanded, or if the walk started coercing rather than passing through unknown scalars.",
  "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[0]/test/values[0]/operand": "R5 `isinstance(component, dict)`: a raw YAML entry that is not a mapping (a bare string or list) cannot select a profile; it is left for ElspethSettings to reject rather than crashing this rewrite pass with an AttributeError. `lower_component` is a closure inside `_lower_llm_profile_nodes`, whose `raw_config` parameter carries the `@trust_boundary` for this pass; the rule scopes suppression to the decorated function's own lexical body, so the closure's sites surface here. `component` is one raw `sources[name]` / `transforms[i]` entry from that same pre-validation Tier-3 tree (Dynaconf YAML or web-authored dict); at this point nothing has yet asserted it is a mapping with `plugin`/`options`. Every skip here is followed by `ElspethSettings(**raw_config)`, which rejects the malformed entry with a field error — the skip defers classification, it does not hide it. Invalidated if this pass ran after settings validation (then the shape would be owned and exact-typed), or if a skipped entry could reach plugin construction without passing `ElspethSettings`.",
  "core/config.py:R1:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[0]/test/values[1]/left": "R1 `component.get('plugin') != 'llm'`: the `plugin` key is optional in raw YAML at this point (its absence is an ElspethSettings error, not this pass's concern); only `llm` components participate in profile lowering, so a missing or different plugin is an honest 'not mine'. `lower_component` is a closure inside `_lower_llm_profile_nodes`, whose `raw_config` parameter carries the `@trust_boundary` for this pass; the rule scopes suppression to the decorated function's own lexical body, so the closure's sites surface here. `component` is one raw `sources[name]` / `transforms[i]` entry from that same pre-validation Tier-3 tree (Dynaconf YAML or web-authored dict); at this point nothing has yet asserted it is a mapping with `plugin`/`options`. Every skip here is followed by `ElspethSettings(**raw_config)`, which rejects the malformed entry with a field error — the skip defers classification, it does not hide it. Invalidated if this pass ran after settings validation (then the shape would be owned and exact-typed), or if a skipped entry could reach plugin construction without passing `ElspethSettings`.",
  "core/config.py:R1:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[1]/value": "R1 `component.get('options')`: `options` is optional in raw YAML — a component authored without options has no `profile` selector to lower, and ElspethSettings decides whether the omission is legal for that plugin. `lower_component` is a closure inside `_lower_llm_profile_nodes`, whose `raw_config` parameter carries the `@trust_boundary` for this pass; the rule scopes suppression to the decorated function's own lexical body, so the closure's sites surface here. `component` is one raw `sources[name]` / `transforms[i]` entry from that same pre-validation Tier-3 tree (Dynaconf YAML or web-authored dict); at this point nothing has yet asserted it is a mapping with `plugin`/`options`. Every skip here is followed by `ElspethSettings(**raw_config)`, which rejects the malformed entry with a field error — the skip defers classification, it does not hide it. Invalidated if this pass ran after settings validation (then the shape would be owned and exact-typed), or if a skipped entry could reach plugin construction without passing `ElspethSettings`.",
  "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[2]/test/operand": "R5 `isinstance(options, dict)`: a non-mapping `options` cannot carry a `profile` key; the very next statement, `require_lowered_llm_profile_alias(options)`, and the `'profile' in options` probe both need a mapping, and ElspethSettings rejects the non-mapping shape afterwards. `lower_component` is a closure inside `_lower_llm_profile_nodes`, whose `raw_config` parameter carries the `@trust_boundary` for this pass; the rule scopes suppression to the decorated function's own lexical body, so the closure's sites surface here. `component` is one raw `sources[name]` / `transforms[i]` entry from that same pre-validation Tier-3 tree (Dynaconf YAML or web-authored dict); at this point nothing has yet asserted it is a mapping with `plugin`/`options`. Every skip here is followed by `ElspethSettings(**raw_config)`, which rejects the malformed entry with a field error — the skip defers classification, it does not hide it. Invalidated if this pass ran after settings validation (then the shape would be owned and exact-typed), or if a skipped entry could reach plugin construction without passing `ElspethSettings`.",
  "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[6]/test/operand": "R5 `isinstance(alias, str)`: the `profile` selector is operator-authored YAML and could be any scalar/mapping; a non-str alias is rejected here with a located ValueError (`{location} llm component 'profile' option must be a string alias`) instead of being looked up in `profiles` and producing an unlocated KeyError. This is the raise arm, not a skip. `lower_component` is a closure inside `_lower_llm_profile_nodes`, whose `raw_config` parameter carries the `@trust_boundary` for this pass; the rule scopes suppression to the decorated function's own lexical body, so the closure's sites surface here. `component` is one raw `sources[name]` / `transforms[i]` entry from that same pre-validation Tier-3 tree (Dynaconf YAML or web-authored dict); at this point nothing has yet asserted it is a mapping with `plugin`/`options`. Every skip here is followed by `ElspethSettings(**raw_config)`, which rejects the malformed entry with a field error — the skip defers classification, it does not hide it. Invalidated if this pass ran after settings validation (then the shape would be owned and exact-typed), or if a skipped entry could reach plugin construction without passing `ElspethSettings`.",
  "core/config.py:R5:_plugin_bearing_sections:ast=body[81]/body[2]/body[4]/test/operand/values[0]": "R5 `isinstance(element, type)` guarding `issubclass(element, BaseModel)`: `element` is a typing-level object pulled from `get_args()` of an `ElspethSettings` field annotation — for `dict[str, SourceSettings]` it is a class, but for other fields it can be a `typing` construct (`Literal[...]`, `Annotated[...]`, a union) on which `issubclass` raises TypeError. The check is what lets the section discovery be DERIVED from the settings model instead of a hand-written tuple (the docstring records the two omissions that tuple caused, elspeth-bc1b2c2959 / elspeth-8f0a6b3391). Not a Tier-3 boundary: it inspects ELSPETH's own model metadata. Invalidated if the discovery were re-keyed on a hand-listed set of section names, or if a non-class element could legitimately denote a plugin-bearing entry.",
  "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/test": "R5 `isinstance(value, dict)` in `_process_value`: mapping arm — recurse via `_recurse` so nested secrets (e.g. `headers.authorization`) are fingerprinted, not just top-level ones. `_fingerprint_secrets(options)` walks a plugin `options` mapping — free-form `dict[str, Any]` at the settings layer, so the values are still operator-authored YAML content even though the container is owned — and replaces every secret-named string with its HMAC fingerprint before the tree reaches Landscape audit storage. The walk is in nested closures (`_process_value`, `_recurse`), so no parameter is available for a `@trust_boundary`. Invalidated if plugin options became schema-validated before this walk (then the arms would be exact-typed on an owned shape), or if a secret could be carried in a non-str leaf (bytes, a nested marker) that this arm does not descend into.",
  "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/orelse[0]/test": "R5 `isinstance(value, list)` in `_process_value`: sequence arm — each element is processed with an empty key so a list of mappings is descended into while a list of plain strings is not treated as a secret field. `_fingerprint_secrets(options)` walks a plugin `options` mapping — free-form `dict[str, Any]` at the settings layer, so the values are still operator-authored YAML content even though the container is owned — and replaces every secret-named string with its HMAC fingerprint before the tree reaches Landscape audit storage. The walk is in nested closures (`_process_value`, `_recurse`), so no parameter is available for a `@trust_boundary`. Invalidated if plugin options became schema-validated before this walk (then the arms would be exact-typed on an owned shape), or if a secret could be carried in a non-str leaf (bytes, a nested marker) that this arm does not descend into.",
  "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/orelse[0]/orelse[0]/test/values[0]": "R5 `isinstance(value, str) and is_secret_field(key)` in `_process_value`: the leaf arm that actually fingerprints. Only a string can be HMAC'd (`secret_fingerprint(value)` takes str); a non-str under a secret-named key (None, int) is passed through, because the fail-closed decision for 'secret present but no key' must only fire when there is a secret value to protect. `_fingerprint_secrets(options)` walks a plugin `options` mapping — free-form `dict[str, Any]` at the settings layer, so the values are still operator-authored YAML content even though the container is owned — and replaces every secret-named string with its HMAC fingerprint before the tree reaches Landscape audit storage. The walk is in nested closures (`_process_value`, `_recurse`), so no parameter is available for a `@trust_boundary`. Invalidated if plugin options became schema-validated before this walk (then the arms would be exact-typed on an owned shape), or if a secret could be carried in a non-str leaf (bytes, a nested marker) that this arm does not descend into.",
  "core/config.py:R5:_fingerprint_secrets:_recurse:ast=body[84]/body[4]/body[0]/body[0]/test/values[0]": "R5 `isinstance(d[key], str) and is_secret_field(key)` in `_recurse`: the collision pre-check — before rewriting, detect a config that supplies both `<secret>` and `<secret>_fingerprint`, which would otherwise let a pre-existing `_fingerprint` value silently overwrite the computed HMAC (a forged-fingerprint injection). It must use the same str-leaf predicate as the rewrite arm so the two never disagree about what counts as a secret. `_fingerprint_secrets(options)` walks a plugin `options` mapping — free-form `dict[str, Any]` at the settings layer, so the values are still operator-authored YAML content even though the container is owned — and replaces every secret-named string with its HMAC fingerprint before the tree reaches Landscape audit storage. The walk is in nested closures (`_process_value`, `_recurse`), so no parameter is available for a `@trust_boundary`. Invalidated if plugin options became schema-validated before this walk (then the arms would be exact-typed on an owned shape), or if a secret could be carried in a non-str leaf (bytes, a nested marker) that this arm does not descend into.",
  "core/config.py:R6:_fingerprint_secrets:ast=body[84]/body[2]/handlers[0]": "R6 `except ValueError: have_key = False` around `get_fingerprint_key()`. `get_fingerprint_key` (contracts/security.py) raises ValueError for exactly one condition — `ELSPETH_FINGERPRINT_KEY` unset or empty — and the handler's outcome is the named error result `have_key = False`, which is consumed explicitly a few lines later: with `fail_if_no_key=True` (the default, and what production callers pass unless `ELSPETH_ALLOW_RAW_SECRETS=true`) a secret field then raises `SecretFingerprintError` naming the missing key; with `fail_if_no_key=False` the dev-mode branch keeps the raw value, which is the operator's documented opt-in. Nothing is swallowed: the absence of the key is either re-raised as a typed error or honoured as a declared mode. Invalidated if `get_fingerprint_key` began raising ValueError for any other reason (a malformed key would then be misreported as 'not set'), or if the `have_key` result stopped being the sole input to the raise/dev-mode decision.",
  "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/body[1]/body[0]/value/test": "R5 `value if isinstance(value, str) else value[0]`: picks the password source for fingerprinting from a `password`/`pwd`/`passwd`/`pass` query parameter, taking the first element when the driver URL repeated the parameter. `parsed` is the `sqlalchemy.engine.URL` returned by `make_url(url)`; SQLAlchemy documents `URL.query` as `Mapping[str, str | tuple[str, ...]]` — repeated query parameters arrive as a tuple, single ones as str. The check is a closed two-member library type envelope, not a Tier-3 shape guess, and `url` itself is not derivable through the `make_url` call for a decorator. Invalidated if SQLAlchemy changed the `URL.query` value type, or if the code stopped normalising both forms (a tuple-valued password param must still be scrubbed and fingerprinted from its first element).",
  "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/orelse[0]/body[0]/value/test": "R5 `value if isinstance(value, tuple) else (value,)`: normalises the `odbc_connect` parameter to a tuple so every repeated connection string is scrubbed by `_scrub_odbc_connect_value`, not just the first. `parsed` is the `sqlalchemy.engine.URL` returned by `make_url(url)`; SQLAlchemy documents `URL.query` as `Mapping[str, str | tuple[str, ...]]` — repeated query parameters arrive as a tuple, single ones as str. The check is a closed two-member library type envelope, not a Tier-3 shape guess, and `url` itself is not derivable through the `make_url` call for a decorator. Invalidated if SQLAlchemy changed the `URL.query` value type, or if the code stopped normalising both forms (a tuple-valued password param must still be scrubbed and fingerprinted from its first element).",
  "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/orelse[0]/body[4]/body[1]/value/test": "R5 `tuple(scrubbed_values) if isinstance(value, tuple) else scrubbed_values[0]`: writes the scrubbed `odbc_connect` back in the same cardinality it arrived with, so `URL.create` re-encodes a repeated parameter as repeated and a single one as single. `parsed` is the `sqlalchemy.engine.URL` returned by `make_url(url)`; SQLAlchemy documents `URL.query` as `Mapping[str, str | tuple[str, ...]]` — repeated query parameters arrive as a tuple, single ones as str. The check is a closed two-member library type envelope, not a Tier-3 shape guess, and `url` itself is not derivable through the `make_url` call for a decorator. Invalidated if SQLAlchemy changed the `URL.query` value type, or if the code stopped normalising both forms (a tuple-valued password param must still be scrubbed and fingerprinted from its first element).",
  "core/config.py:R6:_sanitize_dsn:ast=body[85]/body[15]/handlers[0]": "R6 `except ValueError: have_key = False` around `get_fingerprint_key()`, reached only after the URL has been proven to carry a password. As in `_fingerprint_secrets`, `get_fingerprint_key` raises ValueError solely for an unset/empty `ELSPETH_FINGERPRINT_KEY`, and the handler's named result `have_key = False` is consumed immediately below: `fail_if_no_key=True` raises `SecretFingerprintError('Database URL contains a password but ELSPETH_FINGERPRINT_KEY is not set ...')`; `fail_if_no_key=False` (dev mode, `ELSPETH_ALLOW_RAW_SECRETS=true`) removes the password without a fingerprint and reports `had_password=True` so the caller records `url_password_redacted`. The password is never passed through in either branch. Invalidated if `get_fingerprint_key` acquired another ValueError cause, or if a `have_key=False` path could return the URL with its password intact.",
  "core/config.py:R5:_sanitize_dsn_option_for_audit:ast=body[88]/body[3]/test/operand": "R5 `isinstance(value, str)` on `options[option_name]` after the membership check. `options` is either the deep-copied `landscape` settings dump (`url: str`, always a string) or a database sink's `options` mapping, which the settings layer types as free-form `dict[str, Any]` — so at audit time `url` may be whatever the operator authored (None, an int, a `{secret_ref: NAME}` marker on a validation-only path). `_sanitize_dsn` requires a str DSN; a non-str value cannot contain an embedded `user:password@` or `?password=` credential, so there is nothing to scrub and returning is correct rather than crashing the audit write. Invalidated if a non-str `url` could carry credential material (e.g. a structured DSN mapping with a `password` key), which would then need its own sanitisation arm here, or if sink options became schema-validated before audit persistence.",
  "core/config.py:R5:load_settings:ast=body[91]/body[8]/test/operand": "R5 `isinstance(_yaml_only, dict)` on the result of `yaml.safe_load(config_path)` — the raw operator YAML file, read a second time here purely to distinguish keys that came from the file from `ELSPETH_*` env keys Dynaconf injected. `safe_load` returns whatever the document's top level is (a list, a scalar, None→`{}` via the `or {}`); a non-mapping document is rejected with a ValueError naming the file and the actual type, which is the Tier-3 boundary check for the file's shape. The value is not rooted at a parameter (`config_path` is a path), so no decorator can bind it. Invalidated if this arm stopped raising (falling through to `{str(k).lower() for k in _yaml_only}` on a list would silently accept a malformed file), or if the unknown-key check moved to the Dynaconf-merged dict, at which point this re-read would be dead.",
  "core/config.py:R5:load_settings_from_yaml_string:ast=body[93]/body[2]/test/operand": "R5 `isinstance(config_dict, dict)` on the result of `load_bounded_pipeline_yaml(yaml_content)` — web-authored pipeline YAML (user-controlled, Tier-3). The bounded loader returns the parsed top-level object; a list or scalar document is rejected here with a ValueError naming the actual type before `load_settings_from_config_dict` would fail on `dict(config_dict)` with a less useful error. The value is derived through a call, so it is not decorator-bindable to `yaml_content`. Invalidated if `load_bounded_pipeline_yaml` guaranteed a mapping return itself, or if this arm stopped raising.",
  "core/secrets.py:R5:parse_secret_ref_marker:ast=body[14]/body[1]/test/values[0]": "R5 `isinstance(value, Mapping) and set(value) in ({'secret_ref'}, {'secret_ref','secret_scope'})`: the single authority for 'is this exactly a wired secret marker'. Only a mapping with precisely those keys qualifies; anything else returns None so a mapping that merely contains a `secret_ref` key among others is NOT treated as a marker (a placement/forgery guard the wiring gate relies on). `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:parse_secret_ref_marker:ast=body[14]/body[1]/body[2]/test/values[0]": "R5 `isinstance(ref, str) and (scope is None or scope in ('user','server','org'))`: the marker's `secret_ref` must be a name string and its optional scope one of the three closed scopes; a non-str ref (a nested mapping, a number) makes the value a non-marker, returning None, so it is later reported as a literal-credential or unknown-shape violation rather than resolved. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_walk_redact:ast=body[17]/body[1]/test": "R5 `isinstance(obj, Mapping)` in `_walk_redact`: mapping arm of the preflight redaction walk that replaces every marker with `SECRET_REF_VALIDATION_PLACEHOLDER` so structure can be validated without a resolver. Mirrors `_walk`'s traversal exactly; after `deepcopy` the mapping is a plain dict so in-place assignment is safe. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_walk_redact:ast=body[17]/body[1]/orelse[0]/test": "R5 `isinstance(obj, list)` in `_walk_redact`: sequence arm — markers inside lists (e.g. a list of header mappings) are replaced per index; other leaves are left untouched. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_is_secret_env_ref:ast=body[18]/body[1]/test/values[0]/operand": "R5 `not isinstance(value, str)` in `_is_secret_env_ref`: only a string can be an exact `${NAME}` / `${NAME:-default}` reference; the `fullmatch` regex that follows requires str, and a non-str value is correctly 'not an env ref' (None), not an error. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:is_wired_secret_value:ast=body[21]/body[2]/test": "R5 `isinstance(value, str)` in `is_wired_secret_value`: after the marker form is checked, only a str can be the second approved wired form (exact `${NAME}` naming a declared secret); any other type is not wired and returns False so the credential-field check reports it. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/test": "R5 `isinstance(options, Mapping)` in `_collect_credential_field_violations`: mapping arm — a mapping that is itself a marker is skipped, otherwise every credential-named key is inspected and other values recursed into. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/body[1]/body[0]/test/values[0]": "R5 `isinstance(key, str) and _field_allows_secret_ref(key, ...)`: keys in a user-authored mapping may be non-str (YAML admits int keys); `is_secret_field` and the lower-casing exact-field comparison need a str, and a non-str key can never name a credential field. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/body[1]/body[0]/body[2]/test": "R5 `isinstance(value, str)` deciding a violation: a literal string under a credential-named key that is not a wired form is the reportable violation (a plaintext credential in config). None/empty are left for plugin validation and non-str values are not credentials; the docstring pins this classification. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/orelse[0]/test": "R5 `isinstance(options, (list, tuple))`: sequence arm — recurse per item so credential fields inside list entries are still checked. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/test": "R5 `isinstance(obj, Mapping)` in `_collect_secret_ref_marker_sites`: mapping arm of the single marker-site authority that feeds the secret-wiring authorization gate (elspeth-f3c1aafd25); the marker/env-ref match for the node itself has already been made above, so this arm only descends. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/body[0]/body[0]/value/test": "R5 `(*path, key) if isinstance(key, str) else (*path, f'<{type(key).__name__}>')`: builds the dotted field path reported to the wiring gate; a non-str key is rendered by its type name so the path stays a `str` tuple and never carries the raw key object into an audit record. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/orelse[0]/test": "R5 `isinstance(obj, (list, tuple))` in `_collect_secret_ref_marker_sites`: sequence arm — recurse per index with an `[i]` path segment so a marker inside a list is reported at its exact location. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_walk:ast=body[28]/body[1]/test": "R5 `isinstance(obj, Mapping)` in `_walk`: mapping arm of the resolving walk (`resolve_secret_refs`); each value is tested for a marker/env-ref and replaced with the resolved secret in place, unresolved names are accumulated into `missing` and raised together as `SecretResolutionError` by the caller. The docstring pins `Mapping` deliberately to cover `MappingProxyType`/`OrderedDict` inputs, which `deepcopy` turns into dicts. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_walk:ast=body[28]/body[1]/orelse[0]/test": "R5 `isinstance(obj, list)` in `_walk`: sequence arm — markers inside list entries are resolved by index with the same missing-name accumulation. `core/secrets.py` walks user-authored composer/web config trees (Tier-3: the pipeline dict a composer session submits, before `ElspethSettings`) to find `{secret_ref: NAME}` markers and exact `${NAME}` references. The walkers use `Mapping`/`list` structural dispatch because the input arrives as dict, `MappingProxyType`, or frozen mapping depending on the caller, and every walker is a closure-free recursive function whose non-matching arms return/skip rather than raise. Per the plan row for this bucket these tree-walk sites are justified individually rather than decorated. Invalidated if the walked tree became an owned, exact-typed structure (then the arms should be `type() is dict`), or if a walker started coercing an unrecognised node instead of skipping it.",
  "core/secrets.py:R5:_resolve_marker:ast=body[29]/body[1]/test/operand": "R5 `not isinstance(resolver, ScopedSecretResolverContract)` in `_resolve_marker`: an ADR-032 nominal admission gate against the ELSPETH-owned ABC, taken only when a marker carries an explicit `secret_scope`. It raises TypeError — it is a Tier-1 crash on an impostor resolver, not a Tier-3 skip — and the inline comment records why the `runtime_checkable` Protocol is deliberately not used (structural typing would admit an impostor). Invalidated if the scoped resolver became the only resolver type (then the check would be dead), or if it stopped raising.",
  "_canonical_fp_keys": {
    "core/config.py:R1:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[0]/test/values[1]/left": "core/config.py:R1:_lower_llm_profile_nodes:lower_component:fp=5b6888591b31f35b",
    "core/config.py:R1:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[1]/value": "core/config.py:R1:_lower_llm_profile_nodes:lower_component:fp=40eeea1cb560a430",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:ast=body[53]/body[5]/body[6]/body[0]/test/values[0]": "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:fp=393d89a1eb5c3d50",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:ast=body[53]/body[5]/body[6]/body[0]/test/values[1]": "core/config.py:R5:TriggerConfig:validate_condition_expression:_extract_string_key:fp=b4af6f9cbbb5f16c",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[0]": "core/config.py:R5:TriggerConfig:validate_condition_expression:fp=318c835768a475a2",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[1]": "core/config.py:R5:TriggerConfig:validate_condition_expression:fp=516b98a41851c0cd",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/orelse[0]/test/values[2]": "core/config.py:R5:TriggerConfig:validate_condition_expression:fp=5920e8ef211b20c0",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/test/values[0]": "core/config.py:R5:TriggerConfig:validate_condition_expression:fp=f7ef7a90d0026148",
    "core/config.py:R5:TriggerConfig:validate_condition_expression:ast=body[53]/body[5]/body[9]/body[1]/test/values[1]": "core/config.py:R5:TriggerConfig:validate_condition_expression:fp=49e1567566fa5e21",
    "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/orelse[0]/orelse[0]/test": "core/config.py:R5:_expand_env_vars:_expand_value:fp=8ed2d3cce60483f6",
    "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/orelse[0]/test": "core/config.py:R5:_expand_env_vars:_expand_value:fp=0012ea6e72562b75",
    "core/config.py:R5:_expand_env_vars:_expand_value:ast=body[78]/body[3]/body[2]/test": "core/config.py:R5:_expand_env_vars:_expand_value:fp=feeddf02ee8d64c6",
    "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/orelse[0]/orelse[0]/test/values[0]": "core/config.py:R5:_fingerprint_secrets:_process_value:fp=1cb539be91b0aab2",
    "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/orelse[0]/test": "core/config.py:R5:_fingerprint_secrets:_process_value:fp=6bef2dbeff8d914c",
    "core/config.py:R5:_fingerprint_secrets:_process_value:ast=body[84]/body[3]/body[1]/test": "core/config.py:R5:_fingerprint_secrets:_process_value:fp=c79d562773a78a05",
    "core/config.py:R5:_fingerprint_secrets:_recurse:ast=body[84]/body[4]/body[0]/body[0]/test/values[0]": "core/config.py:R5:_fingerprint_secrets:_recurse:fp=0b6fb8d9373d8462",
    "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[0]/test/values[0]/operand": "core/config.py:R5:_lower_llm_profile_nodes:lower_component:fp=13305b26c9c4e1dc",
    "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[2]/test/operand": "core/config.py:R5:_lower_llm_profile_nodes:lower_component:fp=3fd1098fbd7c59bc",
    "core/config.py:R5:_lower_llm_profile_nodes:lower_component:ast=body[80]/body[6]/body[6]/test/operand": "core/config.py:R5:_lower_llm_profile_nodes:lower_component:fp=4d7b11b4182caa5b",
    "core/config.py:R5:_plugin_bearing_sections:ast=body[81]/body[2]/body[4]/test/operand/values[0]": "core/config.py:R5:_plugin_bearing_sections:fp=3496d2782a0aff86",
    "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/body[1]/body[0]/value/test": "core/config.py:R5:_sanitize_dsn:fp=9abc852ae2fa8727",
    "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/orelse[0]/body[0]/value/test": "core/config.py:R5:_sanitize_dsn:fp=6bd161988afe2928",
    "core/config.py:R5:_sanitize_dsn:ast=body[85]/body[10]/body[0]/orelse[0]/body[4]/body[1]/value/test": "core/config.py:R5:_sanitize_dsn:fp=ead9e7a89593859c",
    "core/config.py:R5:_sanitize_dsn_option_for_audit:ast=body[88]/body[3]/test/operand": "core/config.py:R5:_sanitize_dsn_option_for_audit:fp=b657b0931e8c85b8",
    "core/config.py:R5:load_settings:ast=body[91]/body[8]/test/operand": "core/config.py:R5:load_settings:fp=dc1233340d69f224",
    "core/config.py:R5:load_settings_from_yaml_string:ast=body[93]/body[2]/test/operand": "core/config.py:R5:load_settings_from_yaml_string:fp=daf4b2c90d5b6998",
    "core/config.py:R6:_fingerprint_secrets:ast=body[84]/body[2]/handlers[0]": "core/config.py:R6:_fingerprint_secrets:fp=7227bac24a624dd8",
    "core/config.py:R6:_sanitize_dsn:ast=body[85]/body[15]/handlers[0]": "core/config.py:R6:_sanitize_dsn:fp=74a194fc28b0ca2e",
    "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/body[1]/body[0]/body[2]/test": "core/secrets.py:R5:_collect_credential_field_violations:fp=bbedceb47465c201",
    "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/body[1]/body[0]/test/values[0]": "core/secrets.py:R5:_collect_credential_field_violations:fp=29c6441a4be07fc0",
    "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/orelse[0]/test": "core/secrets.py:R5:_collect_credential_field_violations:fp=c11906e87454b7da",
    "core/secrets.py:R5:_collect_credential_field_violations:ast=body[23]/body[1]/test": "core/secrets.py:R5:_collect_credential_field_violations:fp=52427f0748b0e2a3",
    "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/body[0]/body[0]/value/test": "core/secrets.py:R5:_collect_secret_ref_marker_sites:fp=885583b5183ec69f",
    "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/orelse[0]/test": "core/secrets.py:R5:_collect_secret_ref_marker_sites:fp=1045d7fbb4d67ab4",
    "core/secrets.py:R5:_collect_secret_ref_marker_sites:ast=body[27]/body[3]/test": "core/secrets.py:R5:_collect_secret_ref_marker_sites:fp=cd5534410ba60d72",
    "core/secrets.py:R5:_is_secret_env_ref:ast=body[18]/body[1]/test/values[0]/operand": "core/secrets.py:R5:_is_secret_env_ref:fp=2e7e27c70a984749",
    "core/secrets.py:R5:_resolve_marker:ast=body[29]/body[1]/test/operand": "core/secrets.py:R5:_resolve_marker:fp=11b63fe7b9a9b29a",
    "core/secrets.py:R5:_walk:ast=body[28]/body[1]/orelse[0]/test": "core/secrets.py:R5:_walk:fp=5ddcd129f2854b0b",
    "core/secrets.py:R5:_walk:ast=body[28]/body[1]/test": "core/secrets.py:R5:_walk:fp=cd6789c169ff2027",
    "core/secrets.py:R5:_walk_redact:ast=body[17]/body[1]/orelse[0]/test": "core/secrets.py:R5:_walk_redact:fp=8f384610036e7b0e",
    "core/secrets.py:R5:_walk_redact:ast=body[17]/body[1]/test": "core/secrets.py:R5:_walk_redact:fp=f9673394dd035894",
    "core/secrets.py:R5:is_wired_secret_value:ast=body[21]/body[2]/test": "core/secrets.py:R5:is_wired_secret_value:fp=9ae3ef9da996f1ae",
    "core/secrets.py:R5:parse_secret_ref_marker:ast=body[14]/body[1]/body[2]/test/values[0]": "core/secrets.py:R5:parse_secret_ref_marker:fp=1078580189025a46",
    "core/secrets.py:R5:parse_secret_ref_marker:ast=body[14]/body[1]/test/values[0]": "core/secrets.py:R5:parse_secret_ref_marker:fp=135ebbddde8030bf"
  }
}
