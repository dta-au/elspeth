{
  "web/sessions/service.py:R1:SessionServiceImpl:_assert_session_write_lock_held:ast=body[152]/body[14]/body[1]/test/comparators[0]": "R1 flags _SESSION_WRITE_LOCK_HELD.get(), which is contextvars.ContextVar.get() - not Mapping.get(). The ContextVar is declared with default=frozenset() and .get() is the ONLY read API a ContextVar has; there is no membership-form alternative and no missing-key ambiguity being hidden (the default empty set means 'no session write locks held on this thread', which makes the guard below fail CLOSED by raising RuntimeError for any caller that did not enter _session_write_lock). Tier-1 process-local state, not external input.",
  "web/sessions/service.py:R1:SessionServiceImpl:_session_write_lock:ast=body[152]/body[15]/body[2]/value": "R1 flags _SESSION_WRITE_LOCK_HELD.get(), which is contextvars.ContextVar.get() - not Mapping.get(). Same ContextVar as _assert_session_write_lock_held: default=frozenset(), .get() is the ContextVar read API, and the read seeds the token set that the paired finally-block reset() unwinds. Tier-1 process-local lock bookkeeping; no dict-default masking is possible at this site.",
  "web/sessions/service.py:R4:SessionServiceImpl:_run_sync_with_post_commit_projection:ast=body[152]/body[6]/body[3]/body[0]/handlers[1]": "R4 except BaseException: break in the shield-drain loop. When awaiting asyncio.shield(worker) raises a non-cancellation error the worker task itself has failed; the handler exits the drain loop and the exception is re-raised immediately below by `worker.result()` (a done task's .result() re-raises its exception), preserving the recorded cancellation as the outer exception via `raise cancellation from failure`. Nothing is swallowed: every path out of the loop either returns the worker's committed result or re-raises the worker's own error. The handler ends in `break`, so it cannot be extracted into a returning helper (control flow belongs to the loop). Control by symbol: worker.result() inside _run_sync_with_post_commit_projection.",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/body[2]/value/test/values[0]": "R5 isinstance(sources, Mapping) guarding the invented-source lookup over the state row's sources JSON column (Tier-3 read-back through _unwrap_envelope; frozen in-memory records carry MappingProxyType). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). A non-mapping yields source=None and the next check raises ValueError (fail-closed).",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/body[3]/test/operand": "R5 isinstance(source, Mapping) admitting the named persisted source entry in the invented-source arm. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises ValueError (fail-closed).",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/body[5]/test/values[0]/operand": "R5 isinstance(source_options, Mapping) admitting source.options in the invented-source arm. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises ValueError (fail-closed).",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/body[7]/test/operand": "R5 isinstance(source_authoring, Mapping) admitting source.options.source_authoring in the invented-source arm. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises ValueError (fail-closed).",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/orelse[0]/orelse[0]/orelse[3]/body[1]/value/values[0]": "R5 isinstance(requirements_value, (list, tuple)) in the vague-term structured-match probe of the pending-event writer boundary. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). A non-sequence means the term is wired by a legacy placeholder; that arm is validated by the INTERPRETATION_PLACEHOLDER_RE surface checks and the draft-mismatch gate below (fail-closed).",
  "web/sessions/service.py:R5:SessionServiceImpl:_prepare_or_create_pending_interpretation_event:_sync:ast=body[152]/body[71]/body[8]/body[2]/body[5]/orelse[0]/orelse[0]/orelse[3]/body[1]/value/values[1]/args[0]/elt/values[0]": "R5 isinstance(requirement, Mapping) inside the same structured-match any() - comprehension element twin. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). A structured match is afterwards re-validated fail-closed by _matching_pending_requirement_index.",
  "web/sessions/service.py:R5:SessionServiceImpl:stage_guided_full_pipeline_proposal:ast=body[152]/body[117]/body[18]/body[5]/value/test/values[0]": "R5 isinstance(staged_source, Mapping) reading the planner-authored pipeline's source block (Tier-3 LLM output) to extract blob_id for the deferred-custody equality check. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Any non-mapping (or absent blob_id) yields staged_blob_id=None, which fails the exact equality against custody_preparation.blob_id and raises AuditIntegrityError (fail-closed) - the tolerant read feeds a strict gate, never an acceptance.",
  "web/sessions/service.py:R5:_classify_authoritative_composition_proposal:ast=body[124]/body[2]/test/operand": "R5 isinstance(payload, Mapping) admitting a proposal creation-event payload (Tier-3 persisted event read back from proposal_events). ProposalEventRecord.payload arrives as an exact dict from the raw row path but as MappingProxyType from frozen in-memory records (the sibling reader _verify_committed_pipeline_authority deep_thaws before its exact-dict test for exactly this reason). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises AuditIntegrityError (fail-closed); the subsequent exact key-set and equality comparisons do the value-level authentication.",
  "web/sessions/service.py:R5:_matching_pending_requirement_index:ast=body[138]/body[0]/test/operand": "R5 isinstance(requirements_value, (list, tuple)) admitting options.interpretation_requirements (Tier-3 composition-state read-back). The pair IS deep_freeze's output pair for a sequence plus the JSON list form. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). The parameter is typed `object`, and mypy needs the isinstance narrowing for the enumerate() below; the negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_matching_pending_requirement_index:ast=body[138]/body[4]/body[0]/test/operand": "R5 isinstance(requirement_value, Mapping) on each interpretation-requirement row while scanning for the exactly-one pending match. Rows are dicts on the JSON path and MappingProxyType under frozen state. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_patch_structured_interpretation_prompt:ast=body[143]/body[12]/test/operand": "R5 isinstance(parts_value, (list, tuple)) admitting options.prompt_template_parts for the structured resolution render. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_patch_structured_interpretation_prompt:ast=body[143]/body[19]/body[0]/test/operand": "R5 isinstance(part_value, Mapping) per prompt part in the structured render loop. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_patch_structured_interpretation_prompt:ast=body[143]/body[3]/test/operand": "R5 isinstance(requirements_value, (list, tuple)) admitting options.interpretation_requirements before the prompt patch (Tier-3 composition-state read-back). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_patch_structured_interpretation_prompt:ast=body[143]/body[8]/body[0]/test/operand": "R5 isinstance(requirement_value, Mapping) per requirement row in the prompt-patch scan. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed).",
  "web/sessions/service.py:R5:_require_mapping:ast=body[131]/body[0]/test/operand": "R5 isinstance(value, Mapping) in the file's ONE shared container gate for interpretation-review reads over composition-state structures (nodes/sources/options re-read from session storage, Tier-3 on read-back). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Negative arm raises InterpretationPlaceholderConsumedError (fail-closed). Every interpretation helper routes its container admission through this single symbol rather than growing per-site checks.",
  "web/sessions/service.py:R5:_resolve_vague_term:ast=body[145]/body[3]/value/values[0]": "R5 isinstance(requirements_value, (list, tuple)) in the structured-site probe of the vague-term resolver (Tier-3 composition-state read-back). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). A non-sequence routes to the legacy surfacing-prompt equality arm, which raises InterpretationPlaceholderConsumedError on any divergence (fail-closed); narrowing the pair would misroute frozen structured nodes onto the legacy arm.",
  "web/sessions/service.py:R5:_resolve_vague_term:ast=body[145]/body[3]/value/values[1]/args[0]/elt/values[0]": "R5 isinstance(requirement, Mapping) inside the structured-site any() of the vague-term resolver - the comprehension element twin of the pair check above. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). The matched requirement is afterwards re-validated fail-closed by _patch_llm_transform_prompt.",
  "web/sessions/service.py:R5:_reviewed_content_identity:ast=body[141]/body[6]/body[1]/value/values[0]": "R5 isinstance(raw_requirements, (list, tuple)) guarding the structured-vague-term detection in the reviewed-content identity projection (Tier-3 composition-state read-back). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). This is an eligibility PROBE, not an admission gate: a non-sequence routes to the legacy prompt-hash arm, whose own type() is str check fails closed; narrowing the pair would misroute frozen structured nodes onto the legacy arm and change which artifact the review identity hashes.",
  "web/sessions/service.py:R5:_reviewed_content_identity:ast=body[141]/body[6]/body[1]/value/values[1]/args[0]/elt/values[0]": "R5 isinstance(requirement, Mapping) inside the structured-match any() over interpretation requirements - the comprehension element twin of the container check above. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Non-mapping rows are skipped by the probe and the site then resolves through _review_requirement_identity, which re-validates fail-closed.",
  "web/sessions/service.py:R5:_reviewed_content_identity:ast=body[141]/body[6]/body[2]/body[4]/test/operand/args[0]/elt/values[0]": "R5 isinstance(part, Mapping) over options.prompt_template_parts entries while proving the resolved requirement is referenced by at least one interpretation_ref part. Parts are dicts (JSON path) or MappingProxyType (frozen state). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). The enclosing `if not any(...)` raises InterpretationPlaceholderConsumedError when no matching part exists (fail-closed); an exact-type element test on a frozen node would trip that raise for every structurally valid frozen state.",
  "web/sessions/service.py:R5:_value_references_parent_blob:ast=body[109]/body[1]/test": "R5 isinstance(value, Mapping) in the recursive fork-custody leak walk over an arbitrary composition-state payload tree (Tier-3 on read-back). deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). This walk is the last defence that a forked child state retains no parent blob id or storage path; an exact-type test would skip every frozen subtree and let a parent blob reference cross the session boundary undetected (fail-open). Its verdict feeds _verify_fork_settlement_blob_custody, which raises AuditIntegrityError on any hit.",
  "web/sessions/service.py:R5:_value_references_parent_blob:ast=body[109]/body[2]/test/values[0]": "R5 isinstance(value, Sequence) in the recursive fork-custody leak walk: the sequence arm must traverse BOTH json lists and deep-frozen tuples. deep_freeze rewrites every nested dict to MappingProxyType and every list to tuple (measured, W3-1), while the JSON read path yields exact dict/list, so the reachable domain of this value genuinely spans both representations. An exact-type narrowing (type(x) is dict / in (list, tuple) alone) would silently reject one half of the real population; the ABC test is the structural-container half of the house structural-container/nominal-element split (GuidedSession._validated_component_mapping precedent, and the in-file mappingproxy note inside _surfacing_prompt_structure_hash). Fail-open if narrowed: a frozen tuple subtree would be skipped and a parent blob reference could survive the fork. Verdict consumed fail-closed by _verify_fork_settlement_blob_custody.",
  "web/sessions/service.py:R5:_value_references_parent_blob:ast=body[109]/body[2]/test/values[1]/operand": "R5 isinstance(value, (str, bytes, bytearray)) - the string-exclusion operand paired with the Sequence acceptance in the fork-custody walk. The exclusion must catch str SUBCLASSES too (an exact-type spelling would send a str subclass into the element loop and iterate it character-by-character), so isinstance is the correct and maximally-defensive form here - the B46-adjudicated pattern for every Sequence/str-exclusion pair.",
  "web/sessions/service.py:R6:SessionServiceImpl:_load_staged_fork_on_connection:ast=body[152]/body[134]/body[6]/body[0]/body[0]/handlers[0]": "R6 except (TypeError, json.JSONDecodeError) binding decoded=None in the staged-fork scan over the child session's audit rows - the same candidate-filter shape as _settlement_fork_blob_plan. Non-JSON content means the row is not a fork-blob-plan candidate; decoded=None makes the schema test below fail and the row is classified as a non-plan audit row. Covering controls, by symbol: the `len(plan_candidates) != 1` exactly-one gate later in _load_staged_fork_on_connection raises AuditIntegrityError when the scan found no (or ambiguous) plan, and _fork_blob_plan_from_content re-parses the accepted candidate fail-closed.",
  "web/sessions/service.py:R6:SessionServiceImpl:_run_sync_with_post_commit_projection:ast=body[152]/body[6]/body[3]/body[0]/handlers[0]": "R6 except asyncio.CancelledError recording the FIRST cancellation while draining a shielded DB worker. This is the record-then-raise seam: the handler stores the exception in `cancellation` and the function re-raises it deterministically after the worker settles - `raise cancellation from failure` on worker failure, or the unconditional `raise cancellation` after project(result). The rule cannot see that the bound flag is consumed. Restructuring to raise inside the handler would abandon the shielded worker mid-transaction, exactly the cancellation-hole this wrapper exists to close (see _await_custody_settlement precedent). Controls by symbol: the trailing `raise cancellation` statements in _run_sync_with_post_commit_projection.",
  "web/sessions/service.py:R6:SessionServiceImpl:_verify_guided_failure_audit_cohort:ast=body[152]/body[82]/body[4]/body[0]/handlers[0]": "R6 except (TypeError, ValueError) binding content=None around json.loads(row.content) while scanning chat rows for guided-failure audit lineage. A row whose content is not JSON cannot carry the lineage key; content=None routes it through `content_has_lineage/envelope_has_lineage`, and a row with NO lineage marker on either channel is legitimately skipped (it is not part of the cohort). Covering controls, by symbol: the malformed-row gate in the same loop raises AuditIntegrityError for any row that DOES carry the lineage key on either channel but is not the exact audit shape (role/writer_principal/type checks), and the terminal-event exactly-one check on event_commitments raises 'absent or ambiguous terminal-event authority' - a decode failure can therefore never silently drop a committed cohort row, only fail the verification closed.",
  "web/sessions/service.py:R6:SessionServiceImpl:archive_session:_sync:ast=body[152]/body[55]/body[2]/body[2]/handlers[0]/body[0]/body[0]/handlers[0]": "R6 except OSError as restore_exc inside the archive rollback path: the staged blob directory could not be renamed back after the archive transaction failed. The handler attaches a RecoveryFailed note (add_note) to primary_exc - the exception that is unconditionally re-raised by the bare `raise` statement immediately after the inner try - so the operator sees BOTH the archive failure and the manual-cleanup obligation on one propagating exception. Raising restore_exc instead would replace the primary transaction failure with the secondary cleanup failure. Control by symbol: the bare `raise` terminating the `except Exception as primary_exc` block in archive_session._sync. (Stale signed entry fp=a21e74d99bf57fa2; stale at wave base, listed for operator stale_delete.)",
  "web/sessions/service.py:R6:SessionServiceImpl:persist_compose_turn:ast=body[152]/body[48]/body[2]/handlers[0]": "R6 except RuntimeError: pass around asyncio.get_running_loop(). The RuntimeError IS the affirmative probe result - 'no running event loop in this thread' - which is the precondition persist_compose_turn requires before doing synchronous DB I/O; the else-arm raises RuntimeError instructing callers to use persist_compose_turn_async when a loop IS running. The value probed is Tier-1 interpreter state, not external input. Control by symbol: the `else: raise RuntimeError(...)` arm of the same try statement, which is the only non-silent outcome the probe can have. (A stale signed allowlist entry fp=2546bd05b28345ac exists for this site; stale at wave base, listed for operator stale_delete.)",
  "web/sessions/service.py:R6:_settlement_fork_blob_plan:ast=body[108]/body[1]/body[0]/handlers[0]": "R6 except (TypeError, json.JSONDecodeError): continue around json.loads(row.content) in a candidate scan over the child session's Tier-1 audit rows (role='audit', writer_principal='session_fork'). A row whose content does not decode as JSON is by definition not a fork-blob-plan candidate; the handler's `continue` IS the not-a-candidate outcome, not a swallow (it cannot be extracted into a returning helper - the control flow belongs to the loop). The covering control is the exactly-one gate at the end of _settlement_fork_blob_plan: `if len(candidates) != 1: raise AuditIntegrityError` fails closed when the scan yields zero or multiple plans, and every accepted candidate is fully re-parsed fail-closed by _fork_blob_plan_from_content, which raises AuditIntegrityError on any malformed byte.",
  "web/sessions/service.py:R6:_surfacing_prompt_structure_hash:ast=body[146]/body[5]/body[4]/handlers[0]": "R6 except (TypeError, KeyError, ValueError) returning the documented None sentinel around prompt_structure_hash_from_options. This function is a declared @observation_boundary whose invariant states it never raises and answers None for absent-or-malformed prompt parts; the caller (_resolve_prompt_template_review via resolve_interpretation_event) treats None as 'no skeleton' and falls back to rendered-text equality, which fails closed on any mismatch. Returning the sentinel is the explicit error result of the boundary's contract - re-raising would convert a composer review flow with a documented soft fallback into an unhandled 500. Control by symbol: the caller's surfacing/live skeleton comparison in _resolve_prompt_template_review raises InterpretationPlaceholderConsumedError on any inequality.",
  "web/sessions/service.py:R7:SessionServiceImpl:archive_session:_sync:ast=body[152]/body[55]/body[2]/body[3]/body[2]": "R7 contextlib.suppress(OSError) around quarantine_root.rmdir() after the committed archive's quarantine directory was successfully removed by shutil.rmtree. The rmdir targets the shared PARENT quarantine directory and is pure tidiness: it legitimately fails with ENOTEMPTY while a sibling session's quarantine coexists, and that is not a failure of this archive. The real cleanup failure path is covered by symbol QuarantineCleanupError, raised when the session's own staged directory cannot be removed - the suppress arm is only reachable after that rmtree succeeded. (Stale signed entry fp=e358d8cf0fb2db78; stale at wave base, listed for operator stale_delete.)"
}
