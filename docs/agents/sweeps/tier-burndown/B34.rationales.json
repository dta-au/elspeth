{
  "web/composer/state.py:R6:_routing_label_errors:ast=body[51]/body[6]/body[1]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around `validate_composer_output_name(output.name)`. The handler calls the local `add` helper, which appends `ValidationEntry(component, str(exc), \"high\", \"output_name_invalid\")` to this function's `found` list; `_routing_label_errors` returns `found` and `CompositionState.validate` extends `errors` with it. What it protects: a composer-authored sink name that violates the runtime's label rules is rejected at authoring time with the runtime's own wording instead of dying at `settings_load` (elspeth-2ed41f0a4a). Control by symbol: `_routing_label_errors.add` -> `ValidationEntry` error_code `output_name_invalid`.",
  "web/composer/state.py:R6:_collector_intrinsic_errors:ast=body[70]/body[14]/body[0]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around the `_validate_max_length` / `_validate_node_name_chars` / reserved-label mirror of `ScopeSettings.validate_name`. The handler appends `_err(component, str(exc), \"high\", \"scope_name_invalid\")` to the function's `errors` list, which is returned to `CompositionState.validate`. What it protects: a committed collector scope name that cannot survive settings load is rejected at authoring time rather than at run. Control by symbol: `_collector_intrinsic_errors.errors` -> `_err` error_code `scope_name_invalid`.",
  "web/composer/state.py:R6:_check_schema_contracts:ast=body[153]/body[56]/body[9]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except UnionTypeConflictError as conflict:` around `merge_union_field_flags`. The handler does strictly more than report a string -- it builds a structured `CoalesceUnionTypeDetail` from `conflict.field`, `conflict.branch_a`, `conflict.type_a`, `conflict.branch_b`, `conflict.type_b` and appends a blocking `_err(..., \"coalesce_union_type_incompatible\", coalesce_union_type=...)`. That structured detail is exactly why this cannot be restructured into a `return`: the caller needs the typed detail on the entry, not a message. What it protects: a coalesce whose branches declare incompatible types for a shared field is rejected at authoring time instead of failing the union merge at run. Control by symbol: the `errors` accumulator -> `_err` error_code `coalesce_union_type_incompatible`, carrying `CoalesceUnionTypeDetail`.",
  "web/composer/state.py:R6:_check_schema_contracts._sweep_schema_syntax:ast=body[153]/body[87]/body[1]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around `get_raw_schema_config` / `get_aggregation_contract_options`. This IS the eager reporter the other schema-parse abstentions in this file defer to: the handler both records the owner in `schema_config_reported` (the dedupe that keeps one owner to at most one contract-config error per pass) and appends `_err(owner, f\"Invalid contract config: {exc}\", \"high\", \"contract_config_invalid\")`. Because the handler mutates the dedupe set as well as appending, converting it to a `return` would either drop the dedupe or duplicate the error. What it protects: every malformed `schema:` block on a source, node or output is reported exactly once against its real owner. Control by symbol: `_sweep_schema_syntax` itself -> `_err` error_code `contract_config_invalid`.",
  "web/composer/state.py:R6:CompositionState.validate:ast=body[154]/body[23]/body[21]/body[15]/orelse[0]/orelse[0]/orelse[0]/body[0]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around the row_union name mirror of the runtime's node-name rules (non-empty, max length, character class, reserved edge labels, no `__` prefix), several arms of which `raise ValueError` inside the same `try` on purpose so one handler renders them all. The handler appends `_err(f\"node:{node.id}\", str(exc), \"high\", \"row_union_name_invalid\")` to `validate`'s own `errors` list, which is returned inside the `ValidationSummary`. What it protects: a row_union authored with a name/branch/connection the engine will reject is refused at authoring time with the runtime's wording, instead of validating green and dying at settings load (elspeth-2ed41f0a4a). Control by symbol: `CompositionState.validate`'s `errors` accumulator -> `_err` error_code `row_union_name_invalid`.",
  "web/composer/state.py:R6:CompositionState.validate:ast=body[154]/body[23]/body[21]/body[15]/orelse[0]/orelse[0]/orelse[0]/body[8]/body[0]/body[0]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around the per-branch name/connection label check; the handler additionally records `node.id` in `invalid_row_union_branch_nodes`, which later topology rules read to stay silent about a node already reported as malformed. The handler appends `_err(f\"node:{node.id}\", str(exc), \"high\", \"row_union_branch_invalid\")` to `validate`'s own `errors` list, which is returned inside the `ValidationSummary`. What it protects: a row_union authored with a name/branch/connection the engine will reject is refused at authoring time with the runtime's wording, instead of validating green and dying at settings load (elspeth-2ed41f0a4a). Control by symbol: `CompositionState.validate`'s `errors` accumulator -> `_err` error_code `row_union_branch_invalid`.",
  "web/composer/state.py:R6:CompositionState.validate:ast=body[154]/body[23]/body[21]/body[15]/orelse[0]/orelse[0]/orelse[0]/body[10]/orelse[0]/handlers[0]": "This handler is NOT a swallow: it is the explicit error path. R6 fires only because `_handler_is_silent` in the tier_model rule recognises `raise` and non-default `return`/`yield` as explicit outcomes and does not recognise \"append a ValidationEntry to the validator's accumulator\" -- which is the shape EVERY validator in this file uses, because `CompositionState.validate` must return a verdict rather than raise (the /validate-500 defect class elspeth-bceffeba19). Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError as exc:` around the `_validate_connection_or_sink_name` check on the row_union's on_success processing connection. The handler appends `_err(f\"node:{node.id}\", str(exc), \"high\", \"row_union_on_success_invalid\")` to `validate`'s own `errors` list, which is returned inside the `ValidationSummary`. What it protects: a row_union authored with a name/branch/connection the engine will reject is refused at authoring time with the runtime's wording, instead of validating green and dying at settings load (elspeth-2ed41f0a4a). Control by symbol: `CompositionState.validate`'s `errors` accumulator -> `_err` error_code `row_union_on_success_invalid`.",
  "web/composer/state.py:R6:_check_schema_contracts._walk_producer_entry_to_real_producer:ast=body[153]/body[34]/body[3]/body[4]/body[0]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError:` around `_producer_entry_propagation_vote` for a QUEUE producer, setting `queue_participates = False`. A fan-in ARM may sit later in `nodes` than the consumer being checked, so this vote can be the first parse of that arm's `options[\"schema\"]`. This site is doubly covered: on abstention the vote collapses to non-participation and control falls through to an explicit `warnings.append(_warn(...))` recording that the contract check was skipped for this connection -- so the user sees an honest \"not yet checked\" signal here as well as the `contract_config_invalid` error against the real owner. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._walk_producer_entry_to_real_producer:ast=body[153]/body[34]/body[3]/body[5]/body[0]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError:` around `_producer_entry_propagation_vote` for a ROW_UNION producer, setting `union_participates = False` -- the exact sibling of the queue arm above, for the same reason (a branch may sit later in `nodes`). As there, abstention falls through to an explicit `warnings.append(_warn(...))` skip warning, so this abstention is visible to the user as well as reported against the real owner. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._string_input_field_type_conflict:ast=body[153]/body[50]/body[1]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return None` around `get_raw_schema_config(producer.options, ...)`. Returning None means \"no typed-input conflict provable here\", which is the correct verdict from a rule that mirrors `validate_transform_string_typed_input_fields`: it cannot prove a type conflict from a declaration it cannot parse. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._known_producer_schema_config:ast=body[153]/body[53]/body[3]/body[1]/body[0]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return None` around `get_aggregation_contract_options` when unwrapping an aggregation producer's contract options. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._known_producer_schema_config:ast=body[153]/body[53]/body[4]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return None` around `get_raw_schema_config(contract_options, ...)`, the sibling of the aggregation-unwrap handler immediately above it in the same function. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._mirrored_coalesce_merged_guarantees:ast=body[153]/body[62]/body[3]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return None` around `_producer_entry_propagation_vote` for a mirrored coalesce. Returning None means \"no mirrored merged guarantee available\", the same abstention the queue and row_union arms of `_walk_producer_entry_to_real_producer` take. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._producer_is_typed_source:ast=body[153]/body[80]/body[2]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return False` around `get_raw_schema_config(producer.options, ...)`. This predicate is a bool GATE, not a reporter -- it answers \"is this producer a typed source?\", and a declaration that will not parse is not a provable typed source. The in-code comment notes the abstention was previously safe only by loop ordering; the `_sweep_schema_syntax` argument above is stronger than that ordering claim and does not depend on it, because the sweep runs over every owner regardless of which parser reached the options first. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:_check_schema_contracts._edge_field_type_conflict:ast=body[153]/body[81]/body[2]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError: return None` around the producer/consumer `get_raw_schema_config` pair (with `get_aggregation_contract_options` for an aggregation consumer). Returning None means \"no edge TYPE conflict provable\", which is the honest verdict when either side's declaration will not parse. Control that makes the abstention safe: `_check_schema_contracts._sweep_schema_syntax`. It calls the same `get_raw_schema_config` (and, for aggregation owners, `get_aggregation_contract_options`) on every owner and converts the ValueError into a blocking `ValidationEntry` with error_code `contract_config_invalid`, deduped by owner through `schema_config_reported` so one owner owes at most one such error per pass. Its reach is total for this abstention: it is invoked unconditionally over `sources.items()`, `nodes` and `outputs`, and every `ProducerEntry.options` is by construction one of those mappings -- `ProducerResolver.build` mints entries only from `SourceSpec.options` and `NodeSpec.options` (including the queue-node entries) -- while `_producer_owner` returns exactly the owner keys the sweep uses (`source_producer_id(name)` for sources, `f\"node:{id}\"` otherwise). Ordering is not a hazard either: the only two early returns in `_check_schema_contracts` (the reserved-node-id arm and the `if errors: return` after the connection/sink overlap check) both sit ABOVE every helper flagged here, so reaching this abstention implies control flow will reach the sweep. The malformed declaration is therefore always reported once; abstaining here avoids double-attributing one defect to two components. Raising instead would crash `CompositionState.validate`, whose contract is to RETURN a verdict -- the /validate-500 defect class tracked as elspeth-bceffeba19.",
  "web/composer/state.py:R6:gate_condition_is_constant:ast=body[130]/body[2]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except (ExpressionSyntaxError, ExpressionSecurityError): return False` around `ExpressionParser(condition).is_constant_expression()`. Returning False is a first-class ANSWER, not a dropped error: this predicate is diagnosis-only and asks \"is this condition a bare literal that reads no row data?\", and an unparseable or forbidden condition is not a constant expression. Control by symbol: `_validate_gate_expression`, called from `CompositionState.validate` for every gate node's `condition`, catches both of these same exception types and returns the message that becomes a blocking `ValidationEntry` -- so a syntactically invalid or security-forbidden gate condition is ALWAYS reported, by that function, once. The function's own docstring already names `_validate_gate_expression` as the check that rejects those; reporting again here would double-attribute one defect.",
  "web/composer/state.py:R6:_validate_prompt_template_variable_bindings:ast=body[145]/body[5]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except TemplateSyntaxError: return ()` around `create_sandboxed_environment().parse(masked)` and `extract_jinja2_field_usage(masked)`. Returning the empty tuple means \"this rule has no findings to report\", which is correct: this rule reports UNBOUND VARIABLES in a prompt template, and a template that does not parse has no provable variable set to reason about -- reporting an unbound-variable error off an unparseable parse would be a fabricated finding. Control by symbol -- and it is deliberately CROSS-LAYER, which is the honest description: `CompositionState.validate` has an established, test-pinned contract that a DRAFT pipeline (incomplete config, unregistered plugin, half-written template) must not crash validation. That tolerance is implemented by `_is_config_probe_exception` / `_is_plugin_config_probe_exception` here and by `_semantic_validator._instantiate_consumer`, and is pinned by `test_contract_probe_constructor_exception_falls_back_instead_of_crashing`. So an unparseable Jinja2 template is NOT rejected by composer /validate at all, by design. It is rejected at run preflight by this chain: `LLMConfig.validate_prompt_template` (src/elspeth/plugins/transforms/llm/base.py) constructs `PromptTemplate(v)`, whose `__init__` (src/elspeth/plugins/transforms/llm/templates.py) catches `TemplateSyntaxError` and raises the structured `TemplateError`; the field validator re-raises it as `ValueError`, pydantic wraps that into its `ValidationError`, and plugin config construction (`elspeth.plugins.infrastructure.config_base`) converts that into `PluginConfigError` - the ONLY class `web/execution/_validation_runtime.py` catches on the plugin-instantiation phase, converting it into a reported diagnostic through `web/execution/_validation_diagnostics.py` before any run starts. (`LLMConfig._validate_required_input_fields_appear_in_template` contains no try/except at all; the only other `TemplateSyntaxError` handler in that module is in `_validate_resolved_prompt_template_hash_matches_template`, guarding hash-anchor extraction.) Raising here instead would turn every mid-edit draft template into a /validate 500.",
  "web/composer/state.py:R6:_parse_template_names:ast=body[147]/body[2]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except TemplateSyntaxError: return None` around the same masked parse. Returning None is the documented sentinel of this helper's signature (`tuple[frozenset[str], frozenset[str]] | None`) and its callers branch on it to stay silent, so the abstention is part of the contract rather than a dropped error: there are no names to return from a template that does not parse. Control by symbol -- and it is deliberately CROSS-LAYER, which is the honest description: `CompositionState.validate` has an established, test-pinned contract that a DRAFT pipeline (incomplete config, unregistered plugin, half-written template) must not crash validation. That tolerance is implemented by `_is_config_probe_exception` / `_is_plugin_config_probe_exception` here and by `_semantic_validator._instantiate_consumer`, and is pinned by `test_contract_probe_constructor_exception_falls_back_instead_of_crashing`. So an unparseable Jinja2 template is NOT rejected by composer /validate at all, by design. It is rejected at run preflight by this chain: `LLMConfig.validate_prompt_template` (src/elspeth/plugins/transforms/llm/base.py) constructs `PromptTemplate(v)`, whose `__init__` (src/elspeth/plugins/transforms/llm/templates.py) catches `TemplateSyntaxError` and raises the structured `TemplateError`; the field validator re-raises it as `ValueError`, pydantic wraps that into its `ValidationError`, and plugin config construction (`elspeth.plugins.infrastructure.config_base`) converts that into `PluginConfigError` - the ONLY class `web/execution/_validation_runtime.py` catches on the plugin-instantiation phase, converting it into a reported diagnostic through `web/execution/_validation_diagnostics.py` before any run starts. (`LLMConfig._validate_required_input_fields_appear_in_template` contains no try/except at all; the only other `TemplateSyntaxError` handler in that module is in `_validate_resolved_prompt_template_hash_matches_template`, guarding hash-anchor extraction.) Raising here instead would turn every mid-edit draft template into a /validate 500.",
  "web/composer/state.py:R6:_check_schema_contracts._unguaranteed_target_remedies:ast=body[153]/body[45]/body[7]/body[1]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except ValueError:` around `normalize_field_name(source)`, assigning `stable_spelling = \"a normalization-stable spelling\"`. Nothing is dropped here: this handler is inside the builder of the REMEDY PROSE for an error that is already being reported. The enclosing loop appends a clause to `clauses` for every unguaranteeable mapping target regardless of which branch is taken, and the joined result becomes the message of a blocking `ValidationEntry`. The exception means the mapped source literal (e.g. `'!!!'`) has no normalized form at all, so there is no concrete spelling to quote -- the handler substitutes a description of the repair instead of a name, and the repair remains statable. Control by symbol: the enclosing `_unguaranteed_target_remedies` -> `clauses` -> the caller's `_err` entry. Raising would suppress a real, already-detected guarantee error in order to complain about its wording.",
  "web/composer/state.py:R6:CompositionState.validate:ast=body[154]/body[23]/body[40]/body[1]/body[7]/body[5]/body[2]/handlers[0]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `except PydanticValidationError: continue` around `TriggerConfig.model_validate(deep_thaw(downstream.trigger))`, inside the scan for aggregations downstream of a row_union. `continue` is the correct outcome: this rule asks the narrow question \"does this downstream aggregation declare a count/timeout/condition trigger that could split an indivisible union group?\", and a trigger that will not parse cannot be classified as any of those. Control by symbol: `_validate_aggregation_trigger`, which runs in this SAME `validate()` pass for every node with `node_type == \"aggregation\"` and `trigger is not None`, parses the trigger through the identical `TriggerConfig` parser and appends a blocking `ValidationEntry` with error_code `aggregation_trigger_invalid`. The malformed trigger is therefore always reported, against the aggregation that owns it rather than against the row_union; reporting it here as well would double-attribute one defect to two components.",
  "web/composer/state.py:R5:_row_union_normalized_branches:ast=body[43]/body[1]/test/values[2]": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `isinstance(branches, Mapping)` as the third disjunct of the early-out in `_row_union_normalized_branches`. `CoalesceBranches` is the ELSPETH-owned union alias `tuple[str, ...] | Mapping[str, str]` declared in this module, so this is a nominal discriminator over a type we define -- the ADR-032 sanctioned form. `type(x) is C` is NOT a drop-in here on two independent grounds: `Mapping` is an ABC and the persisted payload arrives as a `MappingProxyType` after `deep_freeze` (an exact-type test against `dict` would misroute every real branches mapping), and the else-branch is USED rather than a failure path, so per recent-code-hints 2026-08-29 the exact-type form would leave it un-narrowed under mypy. What it protects: this is the ONE normalisation shared by `NodeSpec.__post_init__` and `NodeSpec.from_dict`. It deliberately PRESERVES a duplicate-bearing list form rather than collapsing it, so the invalid shape survives long enough for `CompositionState.validate` to reject it with a specific message -- a shape guard that exists to keep an authoring error reportable, not to hide it.",
  "web/composer/state.py:R5:_validate_runtime_route_destinations:ast=body[128]/body[7]/value/generators[1]/iter/test": "Tier of the value: Tier-3 -- composer/LLM/user-authored pipeline text read back from a persisted session payload, so a malformed value is ordinary recoverable external input, not an invariant break. Flagged pattern: `candidate.branches.keys() if isinstance(candidate.branches, Mapping) else candidate.branches` in the comprehension that collects `barrier_branch_names`. `CoalesceBranches` is the ELSPETH-owned union alias `tuple[str, ...] | Mapping[str, str]` declared in this module, so this is a nominal discriminator over a type we define -- the ADR-032 sanctioned form. `type(x) is C` is NOT a drop-in here on two independent grounds: `Mapping` is an ABC and the persisted payload arrives as a `MappingProxyType` after `deep_freeze` (an exact-type test against `dict` would misroute every real branches mapping), and the else-branch is USED rather than a failure path, so per recent-code-hints 2026-08-29 the exact-type form would leave it un-narrowed under mypy. What it protects: the mirror of the engine's fork-branch destination rule, which tracks arrival by fork BRANCH NAME. Both limbs of the conditional must yield branch NAMES: the mapping form's names are its keys, the tuple form's names are its elements. Dropping the discriminator would silently collect branch CONNECTIONS from the mapping form and make the rule reject valid fork targets.",
  "web/composer/state.py:R1:_composer_node_id_validation_message:ast=body[49]/body[1]/value": "Flagged pattern: `_NODE_TYPE_NAME_LABELS.get(node_type, \"Node name\")`. Tier of the value: Tier-1 -- `_NODE_TYPE_NAME_LABELS` is a module-level constant this file owns, not external input. The default is a first-class VALUE, not a mask for a missing key: the table specialises the label only for the node types that need distinct wording, and every other composer node type is CORRECTLY described by the generic \"Node name\". A membership-form read would have to crash, or re-state the same fallback less clearly, for a node type the table simply does not specialise -- and `node_type` is validated against `COMPOSER_NODE_TYPES` by the `unknown_node_type` arm in `CompositionState.validate`, so an absent key here is never evidence of corruption. This is the contrast case recent-code-hints 2026-08-29 draws: a `.get()` whose absent key is a first-class value, not a hidden corruption fallback.",
  "web/composer/state.py:R5:_routing_label_errors.label:ast=body[51]/body[3]/body[0]/test/operand": "Flagged pattern: `if not isinstance(value, str): return` in the nested `label` helper, whose parameter is typed `object` on purpose. Tier of the value: Tier-3 -- a persisted composer session payload. `NodeSpec.from_dict` admits a non-string branch name or connection verbatim (the shape is deliberately preserved so `validate()` can reject it), so at this point the value is genuinely `object` and the annotation says so. What it protects: `_routing_label_errors` mirrors the runtime's LABEL rules, and a label rule can only speak about a well-typed label; the malformed-shape case is owned by the intrinsic node-shape checks in `CompositionState.validate` (the row_union forbidden-fields and branch arms, error_codes `row_union_branch_invalid` / `row_union_branches_invalid`), which report it specifically. Reporting here as well would double-attribute one defect. Why no boundary decorator removes these: `@observation_boundary` names exactly ONE `source_param`, and `_routing_label_errors` takes THREE independently untrusted parameters -- `sources`, `nodes` and `outputs`. Declaring one of them would misstate the boundary as narrower than it is, which is worse than an explicit rationale.",
  "web/composer/state.py:R5:_routing_label_errors:ast=body[51]/body[5]/body[2]/orelse[0]/orelse[0]/orelse[0]/orelse[0]/body[3]/test": "Flagged pattern: `isinstance(raw_branches, Mapping)` discriminating the two authored branch forms before building `items: list[tuple[object, object]]`. `CoalesceBranches` is the ELSPETH-owned union alias `tuple[str, ...] | Mapping[str, str]` declared in this module, so this is a nominal discriminator over a type we define -- the ADR-032 sanctioned form. `type(x) is C` is NOT a drop-in here on two independent grounds: `Mapping` is an ABC and the persisted payload arrives as a `MappingProxyType` after `deep_freeze` (an exact-type test against `dict` would misroute every real branches mapping), and the else-branch is USED rather than a failure path, so per recent-code-hints 2026-08-29 the exact-type form would leave it un-narrowed under mypy. Tier of the value: Tier-3 -- a persisted composer session payload. `NodeSpec.from_dict` admits a non-string branch name or connection verbatim (the shape is deliberately preserved so `validate()` can reject it), so at this point the value is genuinely `object` and the annotation says so. What it protects: `_routing_label_errors` mirrors the runtime's LABEL rules, and a label rule can only speak about a well-typed label; the malformed-shape case is owned by the intrinsic node-shape checks in `CompositionState.validate` (the row_union forbidden-fields and branch arms, error_codes `row_union_branch_invalid` / `row_union_branches_invalid`), which report it specifically. Reporting here as well would double-attribute one defect. Why no boundary decorator removes these: `@observation_boundary` names exactly ONE `source_param`, and `_routing_label_errors` takes THREE independently untrusted parameters -- `sources`, `nodes` and `outputs`. Declaring one of them would misstate the boundary as narrower than it is, which is worse than an explicit rationale.",
  "web/composer/state.py:R5:_routing_label_errors:ast=body[51]/body[5]/body[2]/orelse[0]/orelse[0]/orelse[0]/orelse[0]/body[3]/orelse[0]/body[0]/value/generators[0]/ifs[0]": "Flagged pattern: `[name for name in raw_branches if isinstance(name, str)]`, filtering the list branch form to well-typed names before the duplicate-detection count. Tier of the value: Tier-3 -- a persisted composer session payload. `NodeSpec.from_dict` admits a non-string branch name or connection verbatim (the shape is deliberately preserved so `validate()` can reject it), so at this point the value is genuinely `object` and the annotation says so. What it protects: `_routing_label_errors` mirrors the runtime's LABEL rules, and a label rule can only speak about a well-typed label; the malformed-shape case is owned by the intrinsic node-shape checks in `CompositionState.validate` (the row_union forbidden-fields and branch arms, error_codes `row_union_branch_invalid` / `row_union_branches_invalid`), which report it specifically. Reporting here as well would double-attribute one defect. Why no boundary decorator removes these: `@observation_boundary` names exactly ONE `source_param`, and `_routing_label_errors` takes THREE independently untrusted parameters -- `sources`, `nodes` and `outputs`. Declaring one of them would misstate the boundary as narrower than it is, which is worse than an explicit rationale.",
  "web/composer/state.py:R5:_routing_label_errors:ast=body[51]/body[5]/body[2]/orelse[0]/orelse[0]/orelse[0]/orelse[0]/body[5]/body[0]/test/values[0]/operand": "Flagged pattern: `if not isinstance(branch_name, str) or not isinstance(connection, str): continue` -- the pair of guards that skips a malformed (name, connection) entry before the label rules run on it. Tier of the value: Tier-3 -- a persisted composer session payload. `NodeSpec.from_dict` admits a non-string branch name or connection verbatim (the shape is deliberately preserved so `validate()` can reject it), so at this point the value is genuinely `object` and the annotation says so. What it protects: `_routing_label_errors` mirrors the runtime's LABEL rules, and a label rule can only speak about a well-typed label; the malformed-shape case is owned by the intrinsic node-shape checks in `CompositionState.validate` (the row_union forbidden-fields and branch arms, error_codes `row_union_branch_invalid` / `row_union_branches_invalid`), which report it specifically. Reporting here as well would double-attribute one defect. Why no boundary decorator removes these: `@observation_boundary` names exactly ONE `source_param`, and `_routing_label_errors` takes THREE independently untrusted parameters -- `sources`, `nodes` and `outputs`. Declaring one of them would misstate the boundary as narrower than it is, which is worse than an explicit rationale.",
  "web/composer/state.py:R5:_routing_label_errors:ast=body[51]/body[5]/body[2]/orelse[0]/orelse[0]/orelse[0]/orelse[0]/body[5]/body[0]/test/values[1]/operand": "Flagged pattern: `if not isinstance(branch_name, str) or not isinstance(connection, str): continue` -- the pair of guards that skips a malformed (name, connection) entry before the label rules run on it. Tier of the value: Tier-3 -- a persisted composer session payload. `NodeSpec.from_dict` admits a non-string branch name or connection verbatim (the shape is deliberately preserved so `validate()` can reject it), so at this point the value is genuinely `object` and the annotation says so. What it protects: `_routing_label_errors` mirrors the runtime's LABEL rules, and a label rule can only speak about a well-typed label; the malformed-shape case is owned by the intrinsic node-shape checks in `CompositionState.validate` (the row_union forbidden-fields and branch arms, error_codes `row_union_branch_invalid` / `row_union_branches_invalid`), which report it specifically. Reporting here as well would double-attribute one defect. Why no boundary decorator removes these: `@observation_boundary` names exactly ONE `source_param`, and `_routing_label_errors` takes THREE independently untrusted parameters -- `sources`, `nodes` and `outputs`. Declaring one of them would misstate the boundary as narrower than it is, which is worse than an explicit rationale.",
  "web/composer/state.py:R5:_is_plugin_config_probe_exception:ast=body[110]/body[5]/test": "Flagged pattern: `isinstance(exc, (PluginConfigError, PluginNotFoundError, TemplateError, UnknownPluginTypeError))`. Tier of the value: an EXCEPTION INSTANCE raised by plugin construction. This is a nominal check against four concrete ELSPETH-owned exception classes, and `isinstance` is load-bearing AS `isinstance`: exception classification is subclass semantics by definition. Honesty about the tree as it stands: `git grep` finds NO in-tree subclass of any of the four today, so on current in-tree raises `type(exc) is C` would behave identically. The isinstance is load-bearing against the hierarchy's CONTRACT rather than present code: all four are ELSPETH-owned classes that a plugin's config model - including an out-of-tree plugin - is entitled to subclass, and under `type(exc) is C` the first such subclass would miss all four arms, fall out of the probe-tolerance branch, and propagate out of `CompositionState.validate` as a 500 on an ordinary DRAFT pipeline. That draft tolerance is the contract pinned by `test_contract_probe_constructor_exception_falls_back_instead_of_crashing`. What it protects: exactly that -- an incomplete or unregistered plugin config answers \"cannot probe\" instead of crashing the authoring validator.",
  "web/composer/state.py:R5:_well_formed_query_entries:ast=body[148]/body[2]/body[1]/body[0]/test/operand": "Flagged pattern: `if not isinstance(item, Mapping): continue` -- skipping a non-mapping element of the list authoring form. Tier of the value: Tier-3 -- `node.options['queries']`, web-authored multi-query definitions. This function ALREADY carries `@observation_boundary(tier=3, source_param=\"queries\", suppresses=(\"R5\",))` and the sibling reads directly on `queries` in the same function ARE suppressed by it. These sites are not suppressed for a mechanical reason, not a policy one: the tier_model dataflow walk roots a `for` target with `subject_is_rooted(node.iter, ...)`, and `enumerate(queries)` is an `ast.Call` that `subject_is_rooted` roots at the FUNCTION `enumerate` rather than at `queries` -- documented behaviour in `trust_boundary_suppress.subject_is_rooted` (\"`foo(arguments)` is rooted at the function, not at `arguments`\"). So `item`, and `name` derived from it, drop out of the derived-name set even though they are literally the decorator's declared source. This is a tracer limitation. Replacing `enumerate(queries)` with a hand-maintained counter purely to restore the trail would be reshaping code to move a finding out of sight, which the burn-down forbids; the honest disposition is this rationale. Filed as observation against the lints suppressor. What it protects: the function's declared invariant -- it returns only well-formed (label, entry) pairs, and malformed entries are skipped because entry shape is `QueryDefinition`'s contract, reported by plugin schema validation rather than double-reported here.",
  "web/composer/state.py:R1:_well_formed_query_entries:ast=body[148]/body[2]/body[1]/body[1]/value": "Flagged pattern: `name = item.get(\"name\")`, probing an OPTIONAL label on an authored query entry. The absent key is a first-class value: an unnamed entry is legal and falls back to the positional label `f\"#{index}\"` on the next line, so there is no missing-key corruption to mask. Tier of the value: Tier-3 -- `node.options['queries']`, web-authored multi-query definitions. This function ALREADY carries `@observation_boundary(tier=3, source_param=\"queries\", suppresses=(\"R5\",))` and the sibling reads directly on `queries` in the same function ARE suppressed by it. These sites are not suppressed for a mechanical reason, not a policy one: the tier_model dataflow walk roots a `for` target with `subject_is_rooted(node.iter, ...)`, and `enumerate(queries)` is an `ast.Call` that `subject_is_rooted` roots at the FUNCTION `enumerate` rather than at `queries` -- documented behaviour in `trust_boundary_suppress.subject_is_rooted` (\"`foo(arguments)` is rooted at the function, not at `arguments`\"). So `item`, and `name` derived from it, drop out of the derived-name set even though they are literally the decorator's declared source. This is a tracer limitation. Replacing `enumerate(queries)` with a hand-maintained counter purely to restore the trail would be reshaping code to move a finding out of sight, which the burn-down forbids; the honest disposition is this rationale. Filed as observation against the lints suppressor. What it protects: the function's declared invariant -- it returns only well-formed (label, entry) pairs, and malformed entries are skipped because entry shape is `QueryDefinition`'s contract, reported by plugin schema validation rather than double-reported here.",
  "web/composer/state.py:R5:_well_formed_query_entries:ast=body[148]/body[2]/body[1]/body[2]/value/args[0]/elts[0]/test/values[0]": "Flagged pattern: `name if isinstance(name, str) and name else f\"#{index}\"` -- accepting the authored label only when it is a non-empty string, else falling back to the positional label. Tier of the value: Tier-3 -- `node.options['queries']`, web-authored multi-query definitions. This function ALREADY carries `@observation_boundary(tier=3, source_param=\"queries\", suppresses=(\"R5\",))` and the sibling reads directly on `queries` in the same function ARE suppressed by it. These sites are not suppressed for a mechanical reason, not a policy one: the tier_model dataflow walk roots a `for` target with `subject_is_rooted(node.iter, ...)`, and `enumerate(queries)` is an `ast.Call` that `subject_is_rooted` roots at the FUNCTION `enumerate` rather than at `queries` -- documented behaviour in `trust_boundary_suppress.subject_is_rooted` (\"`foo(arguments)` is rooted at the function, not at `arguments`\"). So `item`, and `name` derived from it, drop out of the derived-name set even though they are literally the decorator's declared source. This is a tracer limitation. Replacing `enumerate(queries)` with a hand-maintained counter purely to restore the trail would be reshaping code to move a finding out of sight, which the burn-down forbids; the honest disposition is this rationale. Filed as observation against the lints suppressor. What it protects: the function's declared invariant -- it returns only well-formed (label, entry) pairs, and malformed entries are skipped because entry shape is `QueryDefinition`'s contract, reported by plugin schema validation rather than double-reported here.",
  "web/composer/state.py:R5:_check_schema_contracts._edge_field_type_conflict:ast=body[153]/body[81]/body[1]/test": "Flagged pattern: `if isinstance(consumer, OutputSpec):` selecting the sink component label. Tier of the value: Tier-1 -- `consumer` is annotated `NodeSpec | OutputSpec`, a CLOSED union of two ELSPETH-owned frozen dataclasses defined in this module, built by the composer itself. This is the nominal `isinstance`-against-a-concrete-owned-class form ADR-032 sanctions, not a Tier-3 shape guard. `type(x) is C` is not a drop-in: the else-branch is USED (it binds `consumer_id` / `consumer_component` for the `NodeSpec` arm, and the second site's `and consumer.node_type == \"aggregation\"` needs `consumer` narrowed to `NodeSpec` to typecheck at all), and per recent-code-hints 2026-08-29 the exact-type form leaves the negative arm un-narrowed under mypy. What it protects: `_edge_field_type_conflict` mirrors the runtime's Phase-2 edge TYPE check and must attribute the error to the right component -- `output:<name>` for a sink, `node:<id>` for a node -- and must unwrap aggregation contract options only for the aggregation arm.",
  "web/composer/state.py:R5:_check_schema_contracts._edge_field_type_conflict:ast=body[153]/body[81]/body[2]/body[3]/test/values[0]": "Flagged pattern: `if isinstance(consumer, NodeSpec) and consumer.node_type == \"aggregation\":` gating the `get_aggregation_contract_options` unwrap. Tier of the value: Tier-1 -- `consumer` is annotated `NodeSpec | OutputSpec`, a CLOSED union of two ELSPETH-owned frozen dataclasses defined in this module, built by the composer itself. This is the nominal `isinstance`-against-a-concrete-owned-class form ADR-032 sanctions, not a Tier-3 shape guard. `type(x) is C` is not a drop-in: the else-branch is USED (it binds `consumer_id` / `consumer_component` for the `NodeSpec` arm, and the second site's `and consumer.node_type == \"aggregation\"` needs `consumer` narrowed to `NodeSpec` to typecheck at all), and per recent-code-hints 2026-08-29 the exact-type form leaves the negative arm un-narrowed under mypy. What it protects: `_edge_field_type_conflict` mirrors the runtime's Phase-2 edge TYPE check and must attribute the error to the right component -- `output:<name>` for a sink, `node:<id>` for a node -- and must unwrap aggregation contract options only for the aggregation arm."
}
