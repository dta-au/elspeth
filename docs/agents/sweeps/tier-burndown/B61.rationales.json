{
  "_key_form": "<file>:<RULE>:<Symbol>:ast=<module-rooted ast path>. Every ast= path was recomputed from the POST-edit worktree with the rule's own trust_tier.tier_model.rule.scan_file(file, root=src/elspeth) and Finding.ast_path; none was copied from the worklist, and all 31 were re-verified against the live tree AFTER merging feature/unified-lineage (d0cea8c8e) into tier/B61 — the key set and the live finding set match exactly, with no orphaned key and no unkeyed finding. B61's worked set is the allowlist-DISABLED corpus for its nine files at base ce7ee6ceb (58 findings). All five signed allowlist entries naming these files (schema.py:R8:_validate_named_checks x2, schema.py:R2/R5:_ddl_constraint_applies_to_dialect, _auto_title.py:R6:maybe_auto_title_session) are reported STALE by the real-allowlist run, so no finding in this bucket was covered and none of these rationales restates a still-binding signed ruling.",
  "web/sessions/_auto_title.py:R2:_admit_auto_title_completion:ast=body[34]/body[1]/value": "R2 `choices = getattr(response, \"choices\", _MISSING_PROVIDER_FIELD)`. `response` is the return value of `_litellm_acompletion` -- a LiteLLM/provider completion object, Tier 3 by ELSPETH's own module docstring (\"the LLM response is Tier 3 (external -- model output)\"). This is the exact four-step pattern ADR-032 prescribes for an external boundary and explicitly exempts from the presence-probing ban: read each needed field ONCE with a sentinel default, assert the VALUE not the type, construct an owned frozen type, discard the original. All four steps are present in this one function -- the sentinel `_MISSING_PROVIDER_FIELD` is a module-private `object()` no provider payload can forge, the value assertion on the next line is `type(choices) is not list or not choices` (exact type, non-empty), and the survivors are copied into the owned frozen `_AdmittedAutoTitleCompletion` which is all `maybe_auto_title_session` reads afterwards. The forms that would clear R2 are both worse here: a direct `response.choices` needs `response` typed `Any` (the parameter is deliberately `object`, so nothing downstream can read an unvalidated attribute) and turns a vendor property raising AttributeError from inside itself into a misclassified 'missing field'; and a `runtime_checkable` Protocol `isinstance` is the control ADR-032 withdrew after elspeth-9ea866438b -- it rejected every genuine LiteLLM tool call because pydantic `extra=\"allow\"` fields resolve only through `__getattr__`, which `getattr_static` bypasses. `@trust_boundary` cannot suppress R2: `BoundaryRule` is `Literal[\"R1\", \"R5\"]` (contracts/trust_boundary.py). Failure path is fail-closed: a missing or wrong-typed field raises the owned `_MalformedAutoTitleResponseError`, counted on `composer.auto_title.failed` and leaving the session's minted default title. Pinned by tests/unit/web/sessions/test_auto_title.py::test_auto_title_malformed_provider_response_records_telemetry_and_returns, which feeds a response object with no usable `choices` and asserts the owned error path is taken and counted.",
  "web/sessions/_auto_title.py:R2:_admit_auto_title_completion:ast=body[34]/body[3]/value": "R2 `message = getattr(choices[0], \"message\", _MISSING_PROVIDER_FIELD)`. Second read of the same ADR-032 Tier-3 parse in `_admit_auto_title_completion` (see the `choices` entry for the full argument). The subject `choices[0]` is a provider-owned choice object reached only after the preceding `type(choices) is not list or not choices` value assertion, so the index is proven safe before the read. The value assertion for this read is `message is _MISSING_PROVIDER_FIELD` -- absence alone is rejected here because the only thing taken from `message` is `content`, whose own type is asserted on the next read. No owned type is constructed from `message` itself; it is a transient handle discarded when `_AdmittedAutoTitleCompletion` is built. Fail-closed on absence via `_MalformedAutoTitleResponseError`. R2 is not suppressible by `@trust_boundary` (`BoundaryRule` is `Literal[\"R1\", \"R5\"]`). Pinned by tests/unit/web/sessions/test_auto_title.py::test_auto_title_malformed_provider_response_records_telemetry_and_returns.",
  "web/sessions/_auto_title.py:R2:_admit_auto_title_completion:ast=body[34]/body[5]/value": "R2 `content = getattr(message, \"content\", _MISSING_PROVIDER_FIELD)`. Third read of the same ADR-032 Tier-3 parse (see the `choices` entry). This is the one read whose value actually becomes Tier-1 data: `content` is the string the LLM proposed as a session title. Its value assertion is the strictest of the four -- `content is _MISSING_PROVIDER_FIELD or (content is not None and type(content) is not str)` -- distinguishing 'field absent' (reject) from 'provider sent an explicit null' (accepted, and returns without titling) from 'present but not an exact str' (reject), and admitting only an exact `str`, never a `str` subclass. The admitted string is then handed to `_admit_title_candidate`, the fail-closed allowlist shape gate (Unicode category L/N plus a fixed punctuation set, word and length bounds, `reject_credential_shaped_content`) before anything reaches `session.title`. The sentinel is a module-private `object()`, so no provider payload can impersonate 'absent'. R2 is not suppressible by `@trust_boundary`. Both value arms are pinned: tests/unit/web/sessions/test_auto_title.py::test_auto_title_null_provider_content_is_an_explicit_no_title (explicit null accepted, no title written) and ::test_auto_title_rejects_non_string_provider_content (non-str rejected as malformed).",
  "web/sessions/_auto_title.py:R2:_admit_auto_title_completion:ast=body[34]/body[7]/value": "R2 `finish_reason = getattr(choices[0], \"finish_reason\", _MISSING_PROVIDER_FIELD)`. Fourth read of the same ADR-032 Tier-3 parse (see the `choices` entry). `finish_reason` is advisory triage telemetry only and is documented as such on `_AdmittedAutoTitleCompletion`: litellm defaults an omitted field to `\"stop\"`, so 'provider said stop' and 'provider said nothing' are indistinguishable, and a gate keyed on it would not be fail-closed (elspeth-308d1e0831). The sentinel default is exactly what lets this site tell those two states apart where litellm cannot -- `_MISSING_PROVIDER_FIELD` is rejected, an explicit `None` becomes the owned `None`, an exact `str` is admitted, anything else raises. The admitted value is only ever lowered to a bounded label by `_finish_reason_label` (five known values plus `other`/`absent`) for counter cardinality; it never influences whether a title is accepted. R2 is not suppressible by `@trust_boundary`. Pinned by tests/unit/web/sessions/test_auto_title.py::test_auto_title_rejects_non_string_finish_reason_as_malformed and ::test_auto_title_labels_openrouter_error_finish_reason.",
  "web/sessions/_auto_title.py:R6:maybe_auto_title_session:ast=body[35]/body[6]/handlers[0]": "R6 `except (LiteLLMAPIError, TimeoutError, asyncio.CancelledError, _MalformedAutoTitleResponseError) as exc:` returning without re-raising. Auto-titling is best-effort UI metadata spawned as a background task from the send_message route; the handler's four classes are exactly the expected external/scheduling failures of one outbound provider call -- a provider error, a timeout, cooperative cancellation, and this module's own owned classification for an unusable response shape. The control that makes the swallow safe is `_record_auto_title_failure` (this file), which is called on every path through the handler and increments the `composer.auto_title.failed` counter labelled by `_auto_title_exception_class(exc)`, so 'provider declined' never looks identical to 'feature silently broke'. The swallow is deliberately NARROW and the narrowness is the safeguard: the docstring states it, and everything else -- programmer bugs, and in particular a DB write failure from `service.update_session_title` -- propagates to the caller awaiting the task, because swallowing those would hide regressions in the persistence path. Nothing Tier-1 is fabricated on this path: the session simply keeps the default title minted at creation. Widening the handler or catching `Exception` here would be the actual defect. Pinned on BOTH arms: tests/unit/web/sessions/test_auto_title.py::test_auto_title_timeout_records_telemetry_and_returns (a caught class is counted and returns) and ::test_auto_title_programmer_error_propagates plus ::test_auto_title_title_write_failure_propagates (an uncaught class, and specifically a DB write failure, still escapes) -- the pair is what makes the narrowness testable rather than assumed. NOTE: the pre-existing signed entry `web/sessions/_auto_title.py:R6:maybe_auto_title_session:fp=819a9041915266f8` is reported STALE at this base (its scope_fingerprint no longer matches the function), so this site is uncovered and rationalised here rather than left to a binding ruling; the stale entry is a stale_delete for the re-stage.",
  "web/sessions/guided_audit.py:R6:_is_canonical_uuid:ast=body[21]/body[0]/handlers[0]": "R6 `except ValueError: return False` around `str(UUID(value)) == value`. This is a total predicate whose entire purpose is to answer 'is this string the canonical hyphenated lowercase spelling of a UUID?'. `UUID(value)` raises `ValueError` for exactly the answer-is-no case, so the handler is the predicate's False arm, not a swallowed error: `False` here is an explicit, load-bearing verdict, not a silent default. The only caller is `_valid_guided_synthetic_payload` (same file, `git grep _is_canonical_uuid -- src` returns one production call site, in the `guided_intent_cancelled` arm), where `False` propagates through `is_authentic_guided_synthetic_invocation` to deny the invocation synthetic-event status -- which routes it to the generic unknown-tool redaction path in `prepare_guided_audit_rows` and makes `web/sessions/service.py::_require_exact_guided_intent_cancellation_audit` raise `AuditIntegrityError` rather than accept it. The failure direction is therefore closed: a non-canonical id can never be blessed as an authentic server-authored event. Re-raising would be wrong (the caller has no separate 'malformed id' outcome, only 'not authentic'), and there is no explicit error result to return because the function's declared return type is `bool`. Pinned by tests/unit/web/sessions/test_guided_atomic_settlement.py::test_intent_cancellation_is_allowlisted_only_for_the_exact_structural_schema.",
  "web/sessions/guided_audit.py:R6:is_authentic_guided_synthetic_invocation:ast=body[24]/body[2]/handlers[0]": "R6 `except (TypeError, ValueError): return False` around `json.loads(invocation.arguments_canonical)`. Same shape as `_is_canonical_uuid` above and for the same reason: this function is a fail-CLOSED authenticity predicate ('is this invocation an exact server synthetic event?'), and unparseable canonical arguments are conclusively 'no'. `TypeError` and `ValueError` (which `json.JSONDecodeError` subclasses) are precisely the two ways `json.loads` reports 'these bytes are not JSON', which is the answer, not an unexpected error. `False` is an explicit verdict: the two callers -- `prepare_guided_audit_rows` (same file) and `web/sessions/service.py::_require_exact_guided_intent_cancellation_audit`, both confirmed by `git grep is_authentic_guided_synthetic_invocation -- src` (callers, not definitions) -- treat it as 'not a synthetic event': the first routes the row through the generic unknown-tool redaction path and refuses the payload-custody binding, the second raises `AuditIntegrityError` outright. There is no direction in which a False can bless a forged event. The value under `json.loads` is Tier-3 on read-back (a persisted canonical string), so a parse failure is an expected input state, not a bug. Pinned by tests/unit/web/sessions/test_guided_atomic_settlement.py::test_intent_cancellation_is_allowlisted_only_for_the_exact_structural_schema and tests/unit/web/sessions/test_guided_atomic_settlement.py::test_audit_preparation_uses_real_typed_evidence_and_omits_hidden_provider_data.",
  "web/sessions/schema.py:R5:probe_current_schema:ast=body[15]/body[1]/value/test": "R5 `supplied_connection = bind if isinstance(bind, Connection) else None`. `bind` is declared `Engine | Connection` -- both concrete classes owned by SQLAlchemy, not by ELSPETH -- and this is a two-member DISPATCH over that union, not an admission gate: nothing is accepted or rejected, the branch only decides whether the probe holds a caller-supplied connection whose transaction state it must restore in the `finally`. ADR-032's nominal rule (`isinstance` against a concrete class) is satisfied in form; what it cannot supply is an ELSPETH-owned class, because the union members are vendor types by construction. The MEASURED reason to keep `isinstance` is the static one: `type(x) is C` gives no negative-branch narrowing (probed with `reveal_type` on an equivalent two-member union), so the `else` arm's `Engine`-only reads would fail mypy. The SECOND reason is ADR-032's own external-boundary guidance -- pinning a concrete VENDOR class exactly is brittle across SDK versions, and `Connection` is not `@final`; today `Connection.__subclasses__()` is empty on SQLAlchemy 2.0.45 (measured), so the two forms agree on every value this tree can produce, which is why this is a justify and not a live defect either way. Were a subclass ever to appear, the exact form would classify a live supplied connection as an Engine and skip the idle-transaction rollback in the `finally` -- the failure direction the nominal form forecloses. The value is Tier-1 (an object ELSPETH constructed and passed in), the population is closed at two, and the only caller is `web/schema_probe.py::_probe_state` (`git grep probe_current_schema -- src`, callers not definitions), which passes its own bind. Pinned by tests/unit/web/test_schema_probe.py and tests/unit/web/sessions/test_schema.py.",
  "web/sessions/schema.py:R6:probe_current_schema:ast=body[15]/body[3]/handlers[0]": "R6 `except SessionSchemaError: return False`. `probe_current_schema` is documented as a read-only PROBE returning whether the bind already carries the current session schema, so `False` is the function's explicit negative verdict, not a swallowed error. `SessionSchemaError` is the single owned exception both `_assert_schema_sentinels` and `_validate_current_schema` raise for every 'this database is not at the current schema' condition -- wrong `application_id`, wrong `user_version`, missing or mismatched `elspeth_schema_identity` row, missing column/FK/constraint/trigger -- which is exactly the question asked. The control that keeps the swallow safe is that this handler does NOT sit on the enforcement path: `initialize_session_schema` calls the same two validators WITHOUT this try/except, so an operator running a stale DB still gets the actionable 'Delete the session DB file and restart' crash. The sole consumer of the boolean is `web/schema_probe.py:162` (`SchemaState.CURRENT if probe_current_schema(bind) else SchemaState.STALE`), i.e. False maps to an explicit STALE state, and the handler is narrow -- any non-`SessionSchemaError` (an operational DB error, a programmer bug) still propagates. The `finally` clause runs on this path too, so a caller-supplied idle connection is rolled back before the verdict returns. Pinned by tests/unit/web/test_schema_probe.py and tests/testcontainer/web/test_schema_probe_postgres.py.",
  "web/sessions/schema.py:R5:_stamp_schema_sentinels:ast=body[16]/body[1]/test": "R5 `if isinstance(bind, Connection):`. Identical two-member vendor-union dispatch to `probe_current_schema` above: `bind` is `Engine | Connection`, both SQLAlchemy-owned concrete classes, and the branch chooses between reusing the caller's connection and opening one under `bind.begin()`. It is a dispatch control, never an admission gate -- neither arm rejects anything, and the actual integrity check for this function lives one call down in `_stamp_schema_sentinels_on_connection`, which refuses to stamp unless `SCHEMA_IDENTITY_TABLE_NAME` is already present. The MEASURED reason to keep `isinstance` is the static one: `type(x) is C` gives no negative-branch narrowing (probed with `reveal_type` on an equivalent two-member union), so the `else` arm's `Engine`-only reads would fail mypy. The SECOND reason is ADR-032's own external-boundary guidance -- pinning a concrete VENDOR class exactly is brittle across SDK versions, and `Connection` is not `@final`; today `Connection.__subclasses__()` is empty on SQLAlchemy 2.0.45 (measured), so the two forms agree on every value this tree can produce, which is why this is a justify and not a live defect either way. The failure direction if a subclass appeared is quiet rather than loud: `Connection` also exposes `begin()`, so the exact form would open a nested transaction on a connection the caller owns instead of raising. Tier-1 value, closed two-member population, ELSPETH-constructed callers. Pinned by tests/unit/web/sessions/test_schema.py.",
  "web/sessions/schema.py:R5:_assert_schema_sentinels:ast=body[18]/body[1]/test": "R5 `if isinstance(bind, Connection):`. Same two-member vendor-union dispatch as `_stamp_schema_sentinels` above, on the read side: reuse the supplied `Connection` or open one under `bind.connect()`. Not an admission gate -- the admission decisions are all inside `_assert_schema_sentinels_on_connection`, which raises the owned `SessionSchemaError` with the operator-actionable 'Delete the session DB file and restart' message for a wrong `application_id`, a wrong `user_version`, or a missing/malformed/mismatched `elspeth_schema_identity` row. The MEASURED reason to keep `isinstance` is the static one: `type(x) is C` gives no negative-branch narrowing (probed with `reveal_type` on an equivalent two-member union), so the `else` arm's `Engine`-only reads would fail mypy. The SECOND reason is ADR-032's own external-boundary guidance -- pinning a concrete VENDOR class exactly is brittle across SDK versions, and `Connection` is not `@final`; today `Connection.__subclasses__()` is empty on SQLAlchemy 2.0.45 (measured), so the two forms agree on every value this tree can produce, which is why this is a justify and not a live defect either way. Tier-1 value; closed vendor-owned union; ELSPETH-constructed callers only. Pinned by tests/unit/web/sessions/test_schema.py and tests/testcontainer/web/test_schema_probe_postgres.py.",
  "web/sessions/schema.py:R5:_validate_required_triggers:ast=body[22]/body[2]/test": "R5 `if isinstance(bind, Connection):`. The fourth and last instance of the file's one `Engine | Connection` dispatch idiom (see `_stamp_schema_sentinels`), here choosing how to execute the already-built dialect-specific trigger-inventory query. Not an admission gate: the check that matters is the two lines below it -- `missing = _REQUIRED_AUDIT_TRIGGERS - present`, which raises through `_schema_error` naming every absent audit trigger. The MEASURED reason to keep `isinstance` is the static one: `type(x) is C` gives no negative-branch narrowing (probed with `reveal_type` on an equivalent two-member union), so the `else` arm's `Engine`-only reads would fail mypy. The SECOND reason is ADR-032's own external-boundary guidance -- pinning a concrete VENDOR class exactly is brittle across SDK versions, and `Connection` is not `@final`; today `Connection.__subclasses__()` is empty on SQLAlchemy 2.0.45 (measured), so the two forms agree on every value this tree can produce, which is why this is a justify and not a live defect either way. Tier-1 value, closed vendor union, internal callers. Pinned by tests/unit/web/sessions/test_schema.py.",
  "web/sessions/routes/composer/pipeline_settlement.py:R5:settle_pipeline_proposal_under_compose_lock:ast=body[23]/body[17]/body[0]/test": "R5 `if isinstance(prepared, RecoveredPipelineCommit):`. This IS the ADR-032 nominal form -- `isinstance` against a concrete class ELSPETH defines (`RecoveredPipelineCommit`, web/composer/pipeline_commit.py), which cannot be duck-typed past because the class object is the credential. It discriminates a closed two-member owned union returned by `_await_guided_atomic_settlement`'s prepare step: a commit RECOVERED against an existing durable executor-content binding (whose `dispatch` is already persisted, so the branch reuses it) versus a freshly prepared one (whose `invocation` must still be persisted through `_persist_tool_invocations`). Getting the arm wrong would double-persist or fail to persist a dispatch audit binding, so the discriminator is load-bearing for the audit trail. The house `type(prepared) is RecoveredPipelineCommit` form is NOT available here and this was measured, not assumed: a `reveal_type` probe on the identical two-dataclass union shows `type() is` yields no negative-branch narrowing, so the `else` arm's `prepared.invocation` / `prepared.result` reads fail mypy with `Item \"RecoveredPipelineCommit\" ... has no attribute \"invocation\"` -- the exact trap recorded in docs/agents/recent-code-hints.md. Neither class is subclassed anywhere (`git grep '(RecoveredPipelineCommit)' -- src tests` is empty), so nominal `isinstance` and exact type agree on every reachable value; only the static proof differs, and `isinstance` is the form that keeps it. Pinned by tests/integration/web/composer/test_pipeline_custody.py and tests/integration/web/composer/test_pipeline_proposal_lifecycle.py.",
  "web/sessions/routes/guided_operations.py:R5:reserve_or_replay_guided_operation:ast=body[28]/body[9]/body[3]/test/operand": "R5 `if not isinstance(outcome, GuidedOperationActive):` -- the terminal arm of the `GuidedOperationOutcome` dispatch, deliberately left as `isinstance` while the three arms above it were converted to the house exact-type form in this same commit. `GuidedOperationOutcome` is a closed union of five exact, unsubclassed frozen dataclasses ELSPETH owns (`git grep -E 'class [A-Za-z_]+\\(GuidedOperation(Claimed|TakenOver|Completed|Failed|Active)\\)' -- src tests` is empty), so this is the ADR-032 nominal form: isinstance against concrete classes we define, which no impostor can satisfy. It stays `isinstance` for a measured static reason: `type(x) is not C` provides NO negative-branch narrowing, and the code immediately after this guard reads `outcome.expired` and `outcome.attempt`, which exist only on `GuidedOperationActive`. Confirmed with a `reveal_type` probe over an equivalent five-member union -- `type(x) is A or type(x) is B` narrows the POSITIVE arm correctly (so the Claimed/TakenOver, Completed and Failed arms above converted cleanly), while only `not isinstance(...)` + raise narrows the survivor to `GuidedOperationActive`. Converting it would forfeit that proof and silently un-type the polling loop that follows. The guard is fail-closed -- an outcome outside the five raises `AuditIntegrityError`, it does not fall through. Pinned by tests/unit/web/sessions/routes/test_guided_operations.py::test_active_operation_polls_to_terminal_replay and ::test_replay_only_lookup_returns_none_for_expired_operation_without_takeover.",
  "web/sessions/routes/composer/guided_plan.py:R7:_note_guided_full_secondary_failure:ast=body[29]/body[1]": "R7 `with contextlib.suppress(Exception):` around a single `slog.error(...)` call. This helper exists for one purpose, stated in its docstring and enforced by its shape: record bounded cleanup diagnostics WITHOUT replacing the primary outcome. Every one of its six call sites in this file is inside an exception handler that is about to re-raise the primary exception or settle the guided operation with an already-decided failure code; if structured logging itself failed -- a broken sink, a serialisation error in `_safe_frame_strings`, a request object whose id cannot be read -- and that failure escaped, it would REPLACE a truthful primary outcome (a fence loss, a durable winner, a client cancellation) with an unrelated logging error, corrupting exactly the diagnostic the helper exists to preserve. The suppression is therefore the control, not the hole. Its scope is minimal and honest: one statement, no state mutation, no return value, nothing downstream depends on the log having been written. The alternative shapes are worse -- `except Exception: pass` is the same swallow spelled as R4, and re-raising inverts the helper's contract. Note the suppression covers only the log call: the primary exception's own path is untouched and always re-raises or settles. Pinned by tests/integration/web/composer/guided/test_guided_full.py and tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:_publish_guided_full_terminal_preserving_primary:ast=body[30]/body[1]/handlers[0]": "R4 `except (asyncio.CancelledError, Exception) as progress_exc:` with no re-raise. Same 'secondary cleanup must not replace the primary outcome' contract as `_note_guided_full_secondary_failure` above, applied to the progress sink rather than the log. This coroutine runs ONLY after a fence failure or a durable winner has already established the authoritative outcome; `progress` is a UI-facing `ComposerProgressSink` whose delivery has no bearing on what was durably settled. The breadth is deliberate and documented: a sink is a caller-supplied async callable, so its failure modes are open-ended, and `asyncio.CancelledError` is named EXPLICITLY alongside `Exception` because it is a `BaseException` in 3.8+ and a cancellation raised by this secondary sink is cleanup failure, not the route's primary cancellation. The catch is still bounded -- every other `BaseException` subclass (`KeyboardInterrupt`, `SystemExit`) escapes untouched, which is the point of spelling the tuple instead of writing `except BaseException`. The control that makes the swallow safe is `_note_guided_full_secondary_failure` (same file), invoked unconditionally in the handler with the site label `terminal_progress` and the primary failure code, so a failing sink is recorded against the primary outcome rather than lost. Re-raising here would let a UI progress failure overwrite a settled durable result -- the precise defect this function was written to prevent. Pinned by tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[0]/test": "R5 `if isinstance(exc, GuidedOperationSettlementConflictError): return \"stale_conflict\"`. `_guided_full_failure_code` maps a raised exception onto the closed `GuidedOperationFailureCode` vocabulary, and every class it tests is a concrete exception ELSPETH defines -- the ADR-032 nominal form, which no impostor can duck-type past because the class object is the credential. `isinstance` is not merely acceptable here, it is REQUIRED: exception dispatch is a hierarchy walk by construction, and this function's correctness depends on subclass relationships that `type() is` would destroy (see the `BlobError` entry). Converting any single arm would also be incoherent with the rest of the chain. `GuidedOperationSettlementConflictError` is tested first because it is the most specific settlement outcome and must not be absorbed by a later, broader arm; `stale_conflict` is the code the route surfaces as HTTP 409 with 'Reload the authoritative state.' via `_SAFE_FAILURES`. Spelling the same test as `try: raise exc / except ...` would be the identical check written to evade the matcher and is not used. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py and tests/unit/web/sessions/routes/test_guided_operations.py::test_terminal_stale_settlement_conflict_maps_to_safe_http_409.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[1]/test": "R5 `if isinstance(exc, AuditIntegrityError): return \"integrity_error\"`. Second arm of the owned-exception dispatch in `_guided_full_failure_code` (see the first arm for the full argument). `AuditIntegrityError` is ELSPETH's own concrete class (contracts/errors.py) -- the ADR-032 nominal form. `isinstance` rather than `type() is` is required across this whole chain because the ordering encodes exception-hierarchy semantics: this arm must also catch any future `AuditIntegrityError` subclass, and an exact-type test would let one fall through to the unclassified tail and be reported as `provider_unavailable`, blaming the provider for an ELSPETH integrity failure. The resulting code maps to HTTP 500 'The operation failed an integrity check.' through the closed `_SAFE_FAILURES` table -- deliberately opaque to the user while the specific exception is recorded on the guided operation's audit evidence. Pinned by tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[2]/test": "R5 `if isinstance(exc, BlobIntegrityError | BlobContentMissingError): return \"integrity_error\"`. Third arm of the owned-exception dispatch (see the first arm). Both classes are ELSPETH-owned concrete exceptions from contracts/blobs.py and both are SUBCLASSES of `BlobError`, which is tested three arms later -- this ordering is the entire reason `isinstance` cannot be converted here. The specific-before-general sequence is load-bearing: a missing or corrupt blob is an `integrity_error` (HTTP 500, 'Restore the authoritative input or session state before retrying'), while a generic `BlobError` is a `custody_error`. An exact-type test would still work for these two leaf classes but would break the contract the chain expresses -- that any narrower integrity subclass must be classified here and not fall through to the broader custody arm -- and mixing exact and nominal tests within one hierarchy walk is precisely how a later subclass gets silently misrouted. Pinned by tests/integration/web/composer/guided/test_guided_full.py and tests/unit/web/blobs/test_service.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[3]/test": "R5 `if isinstance(exc, BlobQuotaExceededError): return \"quota_exceeded\"`. Fourth arm of the owned-exception dispatch (see the first arm). `BlobQuotaExceededError` is another ELSPETH-owned `BlobError` subclass, tested BEFORE its base for the same specific-before-general reason: exceeding the configured per-session storage quota is an operator-actionable 413 ('Remove stored session data or ask an operator to raise the configured storage quota') and must never be reported as the generic 500 `custody_error` that the `BlobError` arm below produces. That ordering is exactly what an exact-type conversion would put at risk, and it is why the whole chain stays nominal `isinstance` against concrete classes ELSPETH defines (ADR-032). Pinned by tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[4]/test": "R5 `if isinstance(exc, BlobError): return \"custody_error\"`. Fifth arm of the owned-exception dispatch, and the one that makes `type() is` impossible for this function rather than merely inadvisable: `BlobError` is a BASE class (contracts/blobs.py) whose subclasses -- `BlobIntegrityError`, `BlobContentMissingError`, `BlobQuotaExceededError`, and `BlobGuidedOperationFenceLostError` -- are exactly what this arm is here to catch after the three narrower arms above have taken their specific cases. `type(exc) is BlobError` would match ONLY a bare `BlobError` and let every current and future subclass fall through to `ComposerServiceError` and then to the unclassified tail, mislabelling a custody failure as `provider_unavailable` (HTTP 503, 'Retry with a new operation id') -- telling the user to retry an operation that cannot succeed and blaming the provider for an ELSPETH storage fault. Nominal `isinstance` against a concrete ELSPETH-owned class is the ADR-032-correct form and the only correct form here. Pinned by tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[5]/test": "R5 `if isinstance(exc, ComposerServiceError): return \"provider_unavailable\"`. Sixth arm of the owned-exception dispatch (see the first arm). `ComposerServiceError` is ELSPETH's own concrete class (web/composer/protocol.py) and, like `BlobError` above, a base whose subclasses must classify here -- so `type() is` would let a narrower composer-service failure escape to the unclassified tail. Position matters: it is tested AFTER every blob and audit-integrity arm so that a storage or integrity fault raised through a composer-service call is not relabelled as a provider outage, and BEFORE `PipelinePlannerError` so planner-specific detail codes still get their own arm below. Maps to HTTP 503 through the closed `_SAFE_FAILURES` table. Pinned by tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R5:_guided_full_failure_code:ast=body[31]/body[6]/test": "R5 `if isinstance(exc, PipelinePlannerError):`. Final and most consequential arm of the owned-exception dispatch (see the first arm). `PipelinePlannerError` is ELSPETH-owned and concrete, and the body of this arm reads `exc.code` and calls `planner_failure_is_policy_blocked(exc)` -- so the guard is doing double duty as the narrowing that makes those planner-only reads type-safe, which `type(exc) is PipelinePlannerError` does not provide. The classification inside it is deliberately detail-code-FIRST: a categorical deployment-policy refusal is permanent and can arrive under whichever planner code exhausted, and collapsing it into `invalid_provider_response` previously blamed the provider for a policy decision and told the user to retry an operation that can never succeed (see the in-code note and elspeth-b4a286d517). `planner_failure_is_policy_blocked` is shared verbatim with the freeform mirror (`routes/_helpers.py`, which is also the home of `_FREEFORM_PLANNER_FAILURE_HTTP`) so the two surfaces cannot drift. An exact-type test would drop any `PipelinePlannerError` subclass into the unclassified tail and lose the policy/timeout/exhaustion distinction entirely. Pinned by tests/integration/web/composer/test_freeform_planner_failure_translation.py and tests/integration/web/composer/guided/test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[0]/body[2]/handlers[0]": "R4 `except Exception as lookup_exc:` in the fence-lost handler, guarding the `reserve_or_replay_guided_operation` winner lookup. The primary outcome is already fixed at this point -- this route LOST the guided operation fence, so another worker holds it and the only remaining question is whether that winner's durable result can be joined and returned. The lookup is a best-effort secondary read against a service that may itself be unavailable; letting its failure escape here would REPLACE the truthful fence-loss outcome with an unrelated service error. The handler does not swallow: it records through `_note_guided_full_secondary_failure` (site `main_fence_winner_lookup`) and sets the `lookup_failed` flag, and eleven lines below `if lookup_failed:` publishes the terminal progress event and `raise` re-raises the PRIMARY fence-lost exception. The flag exists precisely because a bare `raise` inside the handler would re-raise `lookup_exc`, not the primary -- so the R4 'no re-raise in the handler' reading is a true statement about the AST and a false statement about the control flow. The breadth is honest: a winner lookup can fail in open-ended ways (DB, transport, cancellation of the joined operation) and none of them changes the primary answer. Control symbols: `_note_guided_full_secondary_failure` and the `lookup_failed` re-raise, both in this file. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[1]/body[0]/body[0]/handlers[0]": "R4 `except Exception as replay_exc:` inside the cancellation branch taken when the atomic settlement ALREADY COMPLETED (`_GUIDED_ATOMIC_SETTLEMENT_COMPLETED` marker on the CancelledError). The durable outcome here is beyond doubt -- the proposal was settled -- and the route is unwinding a cancellation that arrived after the commit. The replay read exists only to discover whether the settled result was a proposal or a decline, so the terminal progress event can carry the right `declined` flag. If it fails, the code sets `joined = None`, publishes the complete-progress event anyway, and then `raise` re-raises the original `CancelledError` on the very next statement outside the handler -- so cancellation semantics are preserved exactly and nothing is swallowed. Letting `replay_exc` escape instead would convert 'the work was durably committed and then the client went away' into an unrelated server error, and would break the cooperative-cancellation contract the caller is relying on. Control symbols: `_note_guided_full_secondary_failure` (site `completed_cancellation_replay`) records the failed read, and the unconditional `raise` immediately after the progress publish preserves the primary. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py and ::test_guided_full.py.",
  "web/sessions/routes/composer/guided_plan.py:R6:post_guided_plan:ast=body[32]/body[10]/handlers[1]/body[8]/handlers[0]": "R6 `except GuidedOperationFenceLostError as fence_lost:` around `service.fail_guided_operation_with_audit` during cancellation settlement. Losing the fence while trying to record a FAILURE is not an error condition -- it is positive evidence that another worker took the operation over and settled it, which is the retry-safe protocol working as designed. The handler is therefore a branch of the protocol, not a swallow: it performs the winner lookup, and on success assigns `joined_winner`, which the code below turns into the durable winner's response (`return joined_winner`) with a complete-progress event. On failure to find a winner it records `fence_lost` itself through `_note_guided_full_secondary_failure` (site `fence_lost_no_winner`) and falls through to the terminal path, which ends in either `raise exc from settlement_failure`, `raise HTTPException(499)`, a bare `raise`, or `raise_guided_operation_failure(...)` -- every exit from this block raises or returns a durable winner, none silently continues. The lint reads the handler as silent because the raise statements are in the shared tail below rather than lexically inside it; that placement is required, since the tail must run identically for the fence-lost and settled-normally paths. Control symbols: `_note_guided_full_secondary_failure`, `joined_winner`, and `raise_guided_operation_failure` (routes/guided_operations.py), which raises `HTTPException` from the closed `_SAFE_FAILURES` table. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[1]/body[8]/handlers[0]/body[0]/handlers[0]": "R4 `except Exception as lookup_exc:` -- the winner lookup nested inside the fence-lost-during-cancellation-settlement handler above. Same contract, one level deeper: the primary outcome (a cancellation whose failure settlement lost the fence) is already established, and this read only asks whether the winner's durable result exists. On failure it records through `_note_guided_full_secondary_failure` (site `fence_lost_winner_lookup`) and leaves `joined_winner` as `None`, which routes the shared tail below to the failure terminal event and one of its four raising exits (`raise exc from settlement_failure`, `HTTPException(499)`, bare `raise`, or `raise_guided_operation_failure`). Nothing is swallowed and no default value reaches a caller: the absence of a winner is exactly the information the tail needs. Allowing an arbitrary lookup failure to escape would replace a truthful cancellation outcome with an unrelated error, and -- because this sits on the cancellation unwind path -- could mask the client disconnect the route must report as 499. Control symbols: `_note_guided_full_secondary_failure`, the `joined_winner is not None` branch, and `raise_guided_operation_failure`. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[1]/body[8]/handlers[1]": "R4 `except Exception as cleanup_exc:` around `service.fail_guided_operation_with_audit` on the cancellation path. The primary outcome -- a cancellation, already classified into `cancel_failure_code` by `_guided_full_failure_code` or by the disconnect/caller-cancelled predicates above -- is fixed before this call runs. The call is the durable FAILURE settlement, i.e. cleanup; if the database or audit write fails, the correct behaviour is to preserve and report the primary cancellation, not to replace it with a settlement error the client cannot act on. The handler records through `_note_guided_full_secondary_failure` (site `failure_settlement`) and falls into the shared tail, where `failed` remains `None` and `raise_guided_operation_failure(failed or GuidedOperationFailed(failure_code=cancel_failure_code))` raises the closed HTTP failure for the primary code -- so an unsettled operation still produces a truthful, closed-vocabulary response rather than a 500 leak. Every exit from the tail raises. Breadth is honest here: an audit-write failure is open-ended (DB, serialisation, transport) and none of those change the classification. Control symbols: `_note_guided_full_secondary_failure` and `raise_guided_operation_failure` (routes/guided_operations.py). Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[2]": "R4 `except Exception as exc:` -- the route's outermost non-cancellation handler. This is the deliberate top-level classifier for the whole guided-plan operation, and its breadth is its function: everything the planner, the composer service, the blob store, the audit layer, or the settlement path can raise must be mapped onto the closed `GuidedOperationFailureCode` vocabulary and durably settled on the guided operation, or the operation is left holding a live fence that no retry can take over until the lease expires. The handler does not swallow -- it computes `failure_code = _guided_full_failure_code(exc)` (the owned-exception dispatch rationalised above), settles the failure with full audit evidence via `service.fail_guided_operation_with_audit`, publishes the terminal progress event, and ends every path in `raise_guided_operation_failure(...)`, which raises `HTTPException` built from the closed `_SAFE_FAILURES` table. R4 fires because the rule looks for an `ast.Raise` in the handler body and the raise is inside that `Never`-returning helper; the helper is the explicit error path the rule's own remediation text asks for, factored out so the guided and freeform surfaces cannot drift. Note `asyncio.CancelledError` is handled by the SEPARATE preceding handler, so no `BaseException` is caught here. Control symbols: `_guided_full_failure_code`, `service.fail_guided_operation_with_audit`, `raise_guided_operation_failure`. Pinned by tests/integration/web/composer/guided/test_guided_full.py and tests/unit/web/sessions/routes/test_guided_operations.py::test_failed_operation_maps_only_closed_safe_failure.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[2]/body[1]/handlers[0]/body[0]/handlers[0]": "R4 `except Exception as lookup_exc:` -- the winner lookup nested inside the fence-lost handler of the outermost failure path. Identical contract to the two winner lookups above: the primary failure code is already computed by `_guided_full_failure_code`, the fence is already lost to another worker, and this read only asks whether that winner's durable result can be returned instead of an error. On failure it records through `_note_guided_full_secondary_failure` (site `fence_lost_winner_lookup`), publishes the failed terminal progress event, and calls `raise_guided_operation_failure(GuidedOperationFailed(failure_code=failure_code))` -- an unconditional raise of the closed HTTP failure for the PRIMARY code, inside the handler itself. So this site is not merely non-swallowing, it terminates; R4 fires only because the raise is behind the `Never`-returning helper rather than a literal `raise` statement. Control symbols: `_note_guided_full_secondary_failure` and `raise_guided_operation_failure` (routes/guided_operations.py). Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py.",
  "web/sessions/routes/composer/guided_plan.py:R4:post_guided_plan:ast=body[32]/body[10]/handlers[2]/body[1]/handlers[1]": "R4 `except Exception as cleanup_exc:` around `service.fail_guided_operation_with_audit` on the outermost failure path. Mirror of the cancellation-path settlement handler above, and the same argument: the primary failure is already classified into the closed vocabulary by `_guided_full_failure_code`, and this call is the durable settlement of that already-decided outcome. If the settlement write fails, replacing the truthful failure code with a settlement error would tell the user the wrong thing about their request and lose the classification the audit evidence was gathered for. The handler records through `_note_guided_full_secondary_failure` (site `failure_settlement`), publishes the failed terminal progress event, and then unconditionally calls `raise_guided_operation_failure(GuidedOperationFailed(failure_code=failure_code))`, so control never returns and the client receives the closed-vocabulary HTTP failure for the primary code. As with the sibling handlers, R4 fires because the raise is inside a `Never`-returning helper rather than a literal `raise`. Control symbols: `_note_guided_full_secondary_failure`, `raise_guided_operation_failure`. Pinned by tests/integration/web/composer/guided/test_retry_safe_routes.py."
}
