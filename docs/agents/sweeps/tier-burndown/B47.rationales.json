{
  "web/composer/audit.py:R5:dispatch_with_audit:ast=body[44]/body[7]/finalbody[0]/body[2]/test": "R5 `isinstance(current_exc, asyncio.CancelledError)` in `dispatch_with_audit`'s single `finally` record point. The value is not external data at all: it is the live propagating exception recovered from `sys.exc_info()[1]`, so this is an EXCEPTION-HIERARCHY discriminator, the one case where `isinstance` is the only correct form. It chooses the audit status the row is sealed with -- `finish_cancelled` for coordinator cancellation versus `finish_plugin_crash` for every other BaseException that the typed handlers above do not catch. `type(current_exc) is asyncio.CancelledError` would be a defect: asyncio, anyio, and trio-compat shims all raise CancelledError SUBCLASSES, and an exact-type test would record an ordinary coordinator cancellation as PLUGIN_CRASH -- corrupting failure attribution in the durable audit trail this finally block exists to guarantee. The branch is reached only after the offensive guard immediately above has already proved `current_exc is not None`.",

  "web/composer/llm_response_parsing.py:R6:_pydantic_extra_fields:ast=body[25]/body[1]/body[4]/handlers[0]": "R6 `except AttributeError: return None` around `descriptor.__get__(value, type(value))`. Tier-3: `value` is a LiteLLM/provider response object and the read is of its pydantic v2 `__pydantic_extra__` slot. A `__slots__` member descriptor raises AttributeError when the slot is simply UNSET on that instance -- that is the normal 'this object carries no extras' answer, not an error condition, so there is nothing to re-raise or report. Control by symbol: this function's declared `@observation_boundary` invariant is non-raising and returns None for exactly this case; `_merge_pydantic_extra` then returns the object's own `__dict__` fields unchanged, and `_provider_field_map` / `_token_usage_from_usage` record absence as None rather than a fabricated zero. No provider-controlled code runs on this path -- a provider that defines `__pydantic_extra__` as its own property is rejected by the `type(descriptor) is not MemberDescriptorType` test above rather than having its descriptor invoked. Pinned by `test_provider_reasoning_does_not_invoke_provider_descriptors`.",

  "web/composer/llm_response_parsing.py:R5:_pydantic_extra_fields:ast=body[25]/body[1]/body[5]/value/test/values[0]": "R5 `isinstance(extra, dict)` on the value read out of a Tier-3 provider object's `__pydantic_extra__` slot -- whatever that provider chose to store there. `isinstance` rather than `type(extra) is dict` is load-bearing: pydantic v2 and several provider SDKs store a dict SUBCLASS in that slot, and an exact-type test would classify a real overflow mapping as absent and silently drop every undeclared provider field from the audit record. That is precisely the defect class this helper was written to fix (ADR-032, elspeth-9ea866438b): a reader consulting `__dict__` alone sees a real `ChatCompletionMessageToolCall` as field-less. The enclosing `@observation_boundary(source_param='value')` does declare R5, but the value reaches this line through `descriptor.__get__`, a dynamic escape the suppression dataflow walk cannot follow, so the site surfaces unsuppressed.",

  "web/composer/llm_response_parsing.py:R6:_provider_field_map:ast=body[27]/body[2]/handlers[0]": "R6 `except TypeError: return None` around `vars(value)`. Tier-3: `value` is one provider response value -- a Mapping payload or an attribute-style provider/SDK object. `vars()` raises TypeError for any object with no `__dict__` (slots-only classes, builtins, C extension types), which is the 'this value has no readable field map' answer, not a failure to report. Control by symbol: the declared `@observation_boundary` invariant on this function is non-raising and returns None for exactly this shape; every caller -- `_provider_field`, `_provider_details_payload`, `_token_usage_from_usage`, `_provider_cost_from_captured_usage`, `admit_llm_provider_metadata` -- treats None as absence and records None on the audit row rather than a fabricated zero. Missing provider metadata must surface as absence; raising here would instead destroy the whole audit record over a provider that ships a slots-only usage object.",

  "web/composer/llm_response_parsing.py:R5:_provider_field_map:ast=body[27]/body[3]/test/operand": "R5 `not isinstance(fields, Mapping)` on `vars(value)` for a Tier-3 provider response object. `Mapping` (the ABC) is the honest test here and `type(fields) is dict` would be wrong: an object's instance store is legitimately a `mappingproxy` (classes) or a dict subclass (several provider SDKs and pydantic versions), and an exact-type test would reject those and report a real provider payload as field-less -- re-introducing the silent field-drop of elspeth-9ea866438b. This is a shape guard at an honest Tier-3 boundary, not a bug-hiding check: the negative arm returns None (absence) and never coerces. The enclosing `@observation_boundary(source_param='value')` declares R5, but `fields` is derived through `vars()`, which the suppression dataflow walk cannot follow.",

  "web/composer/llm_response_parsing.py:R5:_provider_cost_from_captured_usage:ast=body[33]/body[4]/test/operand": "R5 `not isinstance(private, Mapping)` on `object.__getattribute__(response, '__pydantic_private__')`. Tier-3: this is the provider response object's pydantic private-data store, where LiteLLM parks Bedrock's calculated cost. What it protects is the audit row's `provider_cost` and `provider_cost_source`: a malformed or unexpected private store must yield `PROVIDER_COST_SOURCE_NOT_AVAILABLE`, never a fabricated or half-read cost, because a wrong cost in the durable run-accounting trail is worse than a recorded absence. `isinstance` because pydantic's private store is a dict subclass in some versions, so an exact-type test would report every cost from those versions as unavailable. The read deliberately goes through `object.__getattribute__` so no provider-named property is invoked -- which is also why the enclosing `@observation_boundary(source_param='response')` R5 suppression does not reach this line.",

  "web/composer/llm_response_parsing.py:R5:_provider_cost_from_captured_usage:ast=body[33]/body[7]/test/values[0]/operand": "R5 `not isinstance(hidden_params, Mapping)` one level deeper in the same Tier-3 pydantic private store: LiteLLM's `_hidden_params` mapping, read only after the public `usage.cost` field was found absent. Same protection and same reasoning as the `private` guard above -- a malformed `_hidden_params` yields `PROVIDER_COST_SOURCE_NOT_AVAILABLE` rather than a fabricated cost, and `isinstance` admits the dict subclasses LiteLLM actually ships where an exact-type test would silently report every Bedrock cost as unavailable. The paired `'response_cost' not in hidden_params` membership test on the same line is what makes the subsequent read membership-form. Derived through `object.__getattribute__`, so the enclosing boundary's declared R5 suppression cannot reach it.",

  "web/composer/llm_response_parsing.py:R6:_provider_artifact_owned_fields:ast=body[46]/body[1]/handlers[0]": "R6 `except (AttributeError, TypeError): return None` around `object.__getattribute__(value, '__dict__')`. Tier-3: `value` is an arbitrary provider artifact being walked for a JSON-safe projection. Both exceptions mean the same thing -- this object exposes no data-only instance store -- and returning None is load-bearing rather than a swallow. Control by symbol: `_normalize_provider_artifact` converts the None field map into `JsonBoundaryError('provider artifact has no data-only JSON representation')`, and `_json_safe_provider_artifact` converts that into the closed, value-free `PROVIDER_ARTIFACT_UNAVAILABLE` sentinel. The anomaly is therefore recorded in the audit row, not lost. The read uses `object.__getattribute__` rather than `getattr` specifically so a provider `__getattribute__` override cannot execute during audit capture.",

  "web/composer/llm_response_parsing.py:R6:_reasoning_metadata_from_message:ast=body[50]/body[4]/orelse[0]/handlers[0]": "R6 `except JsonBoundaryError: reasoning_content = PROVIDER_ARTIFACT_UNAVAILABLE`. Tier-3: `require_bounded_text` raises this exactly when provider-authored reasoning text exceeds the traversal budget. The substitution IS the module's documented graceful-degradation contract for provider strings that reach a stored and rendered projection: truncate or sentinel, keep the audit row, and make the anomaly legible -- dropping the record because a field was oversized would discard the very evidence that the endpoint misbehaved. Control by symbol: `require_bounded_text` is the bound authority that decides, and `PROVIDER_ARTIFACT_UNAVAILABLE` is a closed, value-free sentinel, so no provider bytes, reprs, or rejection text enter the audit record. Exactly one exception type is caught; any other failure -- including an internal defect in the bounding code -- still propagates rather than being mislabelled as bad provider data.",

  "web/composer/llm_response_parsing.py:R6:_captured_field:ast=body[51]/body[2]/handlers[0]": "R6 `except KeyError: return None` around a membership read of one key on a Tier-3 provider field mapping. KeyError here IS the absence answer and nothing else: the helper's entire contract, stated in its docstring, is to read one provider mapping field once and return absence rather than fabricating a value. It is written as try/except rather than `.get()` deliberately, so the read stays membership-form on external data and a provider mapping whose `__contains__` and `__getitem__` disagree cannot yield a silently defaulted value. Control by symbol: `admit_llm_provider_metadata`, whose declared `@observation_boundary` invariant records every absent provider identifier as None (absence, never a fabricated value) and bounds every provider string it does retain.",

  "web/composer/required_controls.py:R6:_parse_candidate_state:ast=body[24]/body[1]/handlers[0]": "R6 `except (KeyError, TypeError, ValueError): return None`. This is not an incidental swallow -- it is the fold this function's declared `@observation_boundary` invariant names verbatim: the KeyError/TypeError/ValueError a nested field parser raises is caught here and folded into the same None sentinel, so this boundary itself never raises. Control by symbol: `_parse_node` and `_parse_source` are both `@trust_boundary`-declared RAISERS ('raises TypeError on any malformed node field shape' / 'never coerces silently'), and `validate_composer_source_name` raises ValueError; catching them here converts a malformed Tier-3 candidate into 'does not project into a CompositionState'. `wire_required_controls` then returns the INPUT CANDIDATE BY IDENTITY, so nothing is certified and nothing is rewritten, and the fail-closed authorities downstream still fire: the `control_coverage_findings` required-control finding survives untouched and `elspeth.web.execution.validation` owns the rejection at the run gate. Swallowing is MANDATORY here, not merely tolerable: this pass runs inside the planner `candidate_finalizer` seam, where an unprefixed exception is a terminal failure rather than repair feedback. Pinned by `tests/unit/web/composer/test_required_control_autowire.py::TestAutoWireRefusals::test_malformed_candidate_is_returned_unchanged_without_raising` and `::TestLLMSourceAutoWireSplicing::test_malformed_source_candidates_fail_closed_without_raising`.",

  "web/composer/required_controls.py:R5:_reserved_names:ast=body[26]/body[2]/body[6]/body[1]/value/args[0]/test": "R5 `isinstance(branches, Mapping)` discriminating the `CoalesceBranches` union, which `state.py` declares as `tuple[str, ...] | Mapping[str, str]` -- an OPEN union, not a closed owned type. The Mapping arm is populated by `_parse_node` via `cast('Mapping[str, str]', branches)` applied to the caller's authored payload, so at runtime it can be any Mapping implementation the author or planner supplied, not necessarily an exact `dict`. `type(branches) is dict` would therefore route a non-dict Mapping down the SEQUENCE arm, and `names.update(branches)` would then add the branch KEYS instead of the branch VALUES to the reserved-name set. What that protects is concrete: `_reserved_names` feeds `_allocate_node_id`, so a reserved name lost this way lets the pass mint an auto-wired control id or derived stream name that COLLIDES with a live stream in the graph it is repairing. The else-branch is genuinely used, so per the 2026-08-29 recent-code-hints entry the exact-type form is not a drop-in here; it also fails mypy, since `type(x) is C` does not narrow the negative arm of a union the way `isinstance` does.",

  "web/composer/required_controls.py:R6:_control_node_is_creditable:ast=body[29]/body[1]/handlers[0]": "R6 `except (KeyError, TypeError, ValueError): return False` around `_parse_node(control)`. `_parse_node` is a `@trust_boundary`-declared raiser whose invariant is 'raises TypeError on any malformed node field shape ... never coerces silently'; this function calls it on the control options this pass has just authored, BEFORE insertion. Returning False on that raise is the FAIL-CLOSED direction and is the whole point of the guard: an un-authorable control is not credited, so nothing is spliced, the `input_not_dominated` / `output_not_post_dominated` coverage finding survives intact, and the fail-closed required-control execution gate still blocks the run. Control by symbol: `_parse_node` (the raiser) plus `node_has_blocking_control`, the coverage credit authority -- no path through this function can credit a control that `node_has_blocking_control` would reject, so a swallowed parse failure can never manufacture coverage a pipeline does not have (the defect class of elspeth-b231af0c16). The guard additionally prevents splice churn: without it an un-creditable control would re-fire the same finding and chain redundant nodes to budget exhaustion. Pinned by `tests/unit/web/composer/test_required_control_autowire.py::TestAutoWireRefusals::test_uncreditable_control_inserts_nothing_instead_of_churning`.",

  "web/composer/required_controls.py:R5:merge_required_control_affected_components:ast=body[35]/body[3]/body[2]/value/values[0]": "R5 `isinstance(requirements, (list, tuple))` on `node.options[INTERPRETATION_REQUIREMENTS_KEY]`. `node.options` is a `Mapping[str, Any]` carrying author- and plugin-authored option payloads that have round-tripped through `CompositionState.to_dict()` / `CompositionState.from_dict` and `_canonicalize_authored_interpretation_requirements`, so the concrete container types are neither owned nor closed. This is a fail-closed INTEGRITY ASSERTION, not a shape convenience: its negative arm raises `AuditIntegrityError('Required-control finalization inserted a node without its server disclosure')`. Narrowing to `type(requirements) is list` would reclassify a legitimate tuple-shaped disclosure as a forgery and convert a correct insertion into a hard audit failure, so the WIDER test is the safe direction here -- accept every real disclosure shape and raise only on genuine absence or a genuinely foreign shape. What it protects is the audit-visible `affected_nodes` attribution: every node this pass inserted must carry its `required_control_auto_wired` disclosure, and no pre-existing node may be attributed to the triggering mutation.",

  "web/composer/required_controls.py:R5:merge_required_control_affected_components:ast=body[35]/body[3]/body[2]/value/values[1]/args[0]/elt/values[0]": "R5 `isinstance(requirement, Mapping)` on each entry of the requirements sequence read from `node.options` -- the same fail-closed integrity assertion as the enclosing `isinstance(requirements, (list, tuple))` test, applied one level down. The entries have round-tripped through `CompositionState.to_dict()` / `from_dict` and `_canonicalize_authored_interpretation_requirements`, so a disclosure entry is not guaranteed to be an exact `dict`; `type(requirement) is dict` would treat a non-dict Mapping disclosure as absent, and because the surrounding `any(...)` is negated into `raise AuditIntegrityError`, that mis-read turns a correctly disclosed insertion into a hard audit failure rather than merely losing a lint finding. The paired `'user_term' in requirement` membership test on the following line is what keeps the actual value read membership-form. Protects the same invariant: an inserted control node is attributed to the mutation only when it genuinely carries its `REQUIRED_CONTROL_AUTO_WIRED_USER_TERM` disclosure."
}
