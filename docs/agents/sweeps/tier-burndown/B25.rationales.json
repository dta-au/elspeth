{
  "web/aws_ecs_acceptance.py:R4:main:ast=body[113]/body[3]/handlers[1]": "The broad `except Exception` is `main`'s last-resort arm and is the SECOND clause: the one above it already handles the module's five declared failure types (`AcceptanceCheckError`, `AcceptanceHttpError`, `AcceptanceInputError`, `AcceptanceStateError`, `OperatorTelemetryAcceptanceError`) by rendering `_acceptance_error_envelope(exc)`, so only genuinely unanticipated faults reach this handler. It exists because this is a CLI entry point whose stdout contract is a JSON envelope consumed by an AWS ECS acceptance runbook: an escaping traceback would both break that contract and print unredacted internals of an operator's live deployment - the command talks to real endpoints and reads control documents from stdin. The in-source comment states the discipline the handler implements, and the code matches it exactly: nothing derived from the exception is projected - not its message, not its class - only the closed internal code `AcceptanceInternalError` / `acceptance_internal` and `_current_acceptance_step()`, the module's own tagged step marker, so an operator still learns WHERE acceptance failed without any untrusted content crossing the boundary. It is not silent: `_print_error` writes the envelope and the function `return 1`, which `__main__` turns into `SystemExit(1)`, so the acceptance run fails loudly. `Exception` rather than `BaseException` is deliberate - `KeyboardInterrupt` and `SystemExit` must still terminate the CLI normally.",
  "web/config.py:R1:WebSettings._validate_bedrock_guardrail_profiles:ast=body[44]/body[115]/body[5]/body[0]/value": "`default_alias = self.bedrock_guardrail_default_profiles.get(plugin)` reads a genuinely OPTIONAL operator setting while iterating `by_plugin`, which was seeded with EVERY id in `BEDROCK_GUARDRAIL_PLUGIN_IDS`, not with the operator's keys. Most plugins legitimately have no configured default, so absence is a first-class value meaning 'no default profile for this plugin' and a membership read would raise `KeyError` on the ordinary configuration. `None` is not defaulted past - it is the subject of the very next policy decision, `if len(profiles) > 1 and default_alias is None: raise ValueError('multiple Bedrock Guardrail profiles require an explicit plugin default')`, so the absent case is what the validator explicitly RAISES on when it is ambiguous, and the following line raises again when a present default does not name a profile for the same plugin. The key space is also already validated: `unknown_defaults = set(...) - set(BEDROCK_GUARDRAIL_PLUGIN_IDS)` raises above this loop, so no unrecognised plugin id can reach the `.get` and be silently ignored.",
  "web/config.py:R1:_allow_insecure_test_keys:ast=body[37]/body[0]/value/values[1]/values[1]/left": "`os.environ.get('ELSPETH_ENV') == 'test'` reads an OPTIONAL process environment variable whose absence is the normal production state; `.get` returning `None` compares unequal to `'test'` and the term is False. A membership read would be actively harmful here - `os.environ['ELSPETH_ENV']` raises `KeyError` on every ordinary deployment, converting a routine absent-variable case into a crash inside a security predicate. The absence is not being defaulted into permission: this term can only ever WIDEN a decision that is already gated by `host in _LOCAL_HOSTS` on the left of the `and`, so no environment variable can relax the insecure-test-key allowance for a non-local host. The `.get` is also not the sole or primary signal - it is the right-hand side of an `or` whose left side is `'pytest' in sys.modules` - and reading an environment variable with `.get` is the only form available for a value the process may legitimately not have been given.",
  "web/config.py:R1:settings_from_env:ast=body[49]/body[10]/handlers[0]/body[1]/value/generators[1]/iter": "`detail.get('loc', ())` reads the location tuple from one entry of `ValidationError.errors(include_input=False)` - a THIRD-PARTY Pydantic structure ELSPETH does not own and whose per-error keys are not guaranteed by any contract we control. The empty-tuple default is the correct and safe answer for an error entry that carries no location: the comprehension simply contributes no path for it, and the surrounding logic still works, because `if policy_fields & safe_paths` falls through to a bare `raise` that re-surfaces the original `ValidationError` untouched. Absence therefore cannot suppress the error - it can only make the message less specific. A membership read would be a defect at this site: raising `KeyError` inside an `except ValidationError` handler would replace a precise configuration diagnosis with an unrelated internal error during startup. This whole expression is a REDACTION path, gated by `include_input=False` so operator input never reaches the message, and the `.get` is part of tolerating a foreign shape while redacting rather than part of trusting it.",
  "web/config.py:R1:settings_from_env:ast=body[49]/body[4]/value": "`aws_region = os.environ.get('AWS_REGION')` reads an AMBIENT, optional process environment variable - the deployment region supplied by the ECS task environment - and absence is both normal (any non-AWS deployment) and meaningful. `None` is a first-class value carried through three explicit checks below: `if aws_region is not None and (not aws_region.strip() or not is_well_formed_aws_region(aws_region))` raises on a malformed value, the cross-check `if aws_region is not None and aws_default_region is not None and aws_region != aws_default_region` raises on disagreement, and the final assignment `kwargs['deployment_aws_region'] = aws_region if aws_region is not None else aws_default_region` uses absence to select the fallback variable. A membership read would raise `KeyError` on every non-AWS deployment. This is also the ONLY sanctioned way to set the region: the loop above raises `RuntimeError('ELSPETH_WEB__DEPLOYMENT_AWS_REGION is reserved; deployment region comes from ambient AWS_REGION')`, so the `.get` is the deliberate single entry point rather than a lenient default.",
  "web/config.py:R1:settings_from_env:ast=body[49]/body[5]/value": "`aws_default_region = os.environ.get('AWS_DEFAULT_REGION')` is the fallback arm of the same ambient-region read, and probing both names is required rather than redundant: `AWS_REGION` and `AWS_DEFAULT_REGION` are both standard AWS SDK variables and a deployment may set either. Absence is a genuine first-class value - it is what makes `kwargs['deployment_aws_region'] = aws_region if aws_region is not None else aws_default_region` resolve to `None` for a non-AWS deployment - and a membership read would raise `KeyError` in exactly that ordinary case. Neither variable is trusted merely because it is present: the value is validated by `not aws_default_region.strip() or not is_well_formed_aws_region(...)` which RAISES, and the two are cross-checked for agreement so a deployment cannot silently run against a different region than its SDK clients. So the `.get` selects between two ambient sources; every downstream use of the result is explicit about the absent case.",
  "web/config.py:R5:_annotation_scalar_types:ast=body[47]/body[3]/value/test": "`return {annotation} if isinstance(annotation, type) else set()` in `_annotation_scalar_types`, whose job is to flatten a Pydantic field annotation into the set of actual CLASSES it admits. The subject is a typing OBJECT, not data: `WebSettings.model_fields[...].annotation` is `Any` and may be a class (`int`), a parameterised generic (`tuple[str, ...]`), a `Literal[...]`, or a `types.UnionType` - and `isinstance(x, type)` is precisely the standard-library test for 'is this a class', which the recursive union arm above depends on to terminate. `type(annotation) is type` would be WRONG here rather than equivalent, and that is the reason this is not converted: any class with a metaclass fails an exact-type check, so a `StrEnum`-typed field (metaclass `EnumMeta`) or a nested Pydantic model (metaclass `ModelMetaclass`) would be dropped from the returned set. The consumer is `_coerce_env_scalar`, which uses the set only to decide whether to attempt `int()`/`float()` coercion of an environment string, and whose `else` path returns the value UNCHANGED for Pydantic to validate - so the empty-set arm defers to Pydantic rather than fabricating anything.",
  "web/config.py:R5:settings_from_env:ast=body[49]/body[10]/handlers[0]/body[1]/value/generators[1]/ifs[0]": "`if isinstance(item, (str, int))` filters the elements of a Pydantic `loc` tuple before `str(item)` renders them into an operator-facing startup error message. This is a REDACTION control, which is why it is a filter rather than a raise: `safe_paths` becomes part of `RuntimeError(f'Invalid ELSPETH_WEB__ plugin policy setting at: {rendered_paths}')`, and the surrounding code is built to keep operator-supplied secrets out of that message - `error.errors(include_input=False)` on the line above exists for exactly that purpose. Pydantic documents `loc` members as field names (`str`) and sequence indices (`int`); anything else is an unrecognised member of a third-party structure, and excluding it is what stops an unanticipated object's `__str__` from rendering into the message. Dropping an unrecognised member cannot hide the failure: the `if policy_fields & safe_paths` test below either raises the redacted policy error or falls through to a bare `raise` that re-surfaces the original `ValidationError`. `isinstance` is correct against a third-party shape where a `str` subclass is a legitimate field name.",
  "web/config.py:R5:settings_from_env:ast=body[49]/body[3]/body[4]/body[1]/body[0]/test/operand": "`if not isinstance(parsed, list): raise RuntimeError(...)` is an ADMISSION GATE on Tier-3 input, not a defensive shape guard - the else-branch RAISES, it does not coerce, default, or skip. `parsed` is `json.loads(value)` where `value` came from an operator-set `ELSPETH_WEB__*` process environment variable, which is external data by definition, and this is the check that turns `ELSPETH_WEB__PLUGIN_ALLOWLIST='{\"a\": 1}'` into the named startup error `'ELSPETH_WEB__PLUGIN_ALLOWLIST must be valid JSON array.'` The error names the ENVIRONMENT VARIABLE, which is the value the operator can actually edit - the whole reason the check lives here rather than being left to Pydantic, which would report an opaque failure against an internal field name. It is load-bearing for the line it guards, `kwargs[field_name] = tuple(parsed)`: without it a JSON object would be silently converted to a tuple of its KEYS, so a mis-typed allowlist would become a plausible-looking but wrong policy value. `isinstance` is deliberately kept over an exact-type check: this is a container admission test on foreign data whose fall-through is a failure path, the same shape the project already declined to convert for JSON-facing container checks, and an exact-type form here would buy no behaviour (a hook-free `json.loads` cannot produce a subclass) while adding a closed-type commitment to third-party JSON shapes.",
  "web/config.py:R5:settings_from_env:ast=body[49]/body[3]/body[4]/body[1]/orelse[0]/test/operand": "`if not isinstance(parsed, dict): raise RuntimeError(...)` is the object-valued twin of the array gate above, on the `_JSON_OBJECT_FIELDS` branch, and is equally an admission gate rather than a shape guard - the else-branch raises `'ELSPETH_WEB__<NAME> must be valid JSON object.'`, naming the environment variable the operator set. The fields it protects are the policy mappings `plugin_preferences`, `plugin_control_modes`, `llm_profiles` and `bedrock_guardrail_default_profiles`, which are assigned straight into `kwargs[field_name] = parsed` and reach `WebSettings` as security-relevant plugin policy; admitting a JSON array or scalar here would hand Pydantic a wrong-shaped policy value and produce a startup failure that names an internal field instead of the variable at fault. The value is Tier-3 - `json.loads` output from a process environment variable - so per ADR-032 this is the parse side of the contract, not the nominal-typing side. `isinstance` is retained for the same reason as the list arm: an exact-type conversion would change no behaviour on hook-free `json.loads` output and would add a closed-type commitment about foreign JSON containers for no gain.",
  "web/config.py:R6:_is_loopback_or_private_origin:ast=body[41]/body[4]/handlers[0]": "`except ValueError: return False` around `ip_address(hostname)` in `_is_loopback_or_private_origin`. The `ValueError` IS the classification answer, not a swallowed fault: `ipaddress.ip_address` raises it to report 'this string is not an IP literal', and a DNS hostname is therefore correctly classified as not a loopback-or-private ORIGIN. The function is scoped to what is statically provable from the URL text, which is what its two callers need - `_validate_public_base_url` and `_validate_auth_fields` both use a True result to REJECT (`raise ValueError`), so True is the restrictive direction, and the fail-closed arms are already explicit above: `hostname is None` returns True and `'localhost'` returns True. Returning False for a DNS name is not a permissive default - `validate_credential_safe_https_url` has already enforced HTTPS-or-loopback-HTTP on the same value before this predicate runs. The honest limit, and it is a limit of the question rather than of this handler, is that a DNS name resolving at runtime to a private address cannot be detected from configuration text; nothing about that is hidden by catching `ValueError`, which is narrow and names exactly the not-an-IP-literal case.",
  "web/config.py:R6:_is_loopback_origin:ast=body[42]/body[4]/handlers[0]": "`except ValueError: return False` around `ip_address(hostname)` in `_is_loopback_origin` - the same not-an-IP-literal classification as its sibling, for the stricter loopback-only question, and here the False answer is unambiguously the SAFE direction. The single caller is `_validate_public_base_url`'s `if _is_loopback_or_private_origin(safe_url) and not (parsed.scheme == 'http' and _is_loopback_origin(safe_url))`, where a True result is what EXEMPTS a URL from the public-origin requirement so that plain-HTTP local development is permitted. Returning False for a DNS name therefore withholds that exemption and keeps the rejection in force - the fail-closed outcome. The `hostname is None` arm above returns False for the same reason, deliberately differing from the sibling function whose restrictive direction is True. The clause is narrow (`ValueError` only) and names precisely the condition `ipaddress.ip_address` uses to report a non-literal, so nothing else is absorbed.",
  "web/doctor.py:R4:_aws_operator_telemetry_check:ast=body[28]/body[2]/handlers[0]": "The broad `except Exception as exc` in `_aws_operator_telemetry_check` wraps a deferred import of `build_aws_operator_pipeline_telemetry` plus the construction of the effective telemetry policy and the long boolean comparison of that policy against the expected AWS task-local OTLP shape. The width matches the surface: policy construction reads OpenTelemetry SDK types and operator-supplied `WebSettings` identity fields, so it can raise anything from `ImportError` (OTel extras absent from the image) to `ValueError`/`TypeError` from the SDK, and every one of them answers the question the check asks - the expected operator telemetry policy could not be shown to be in force. Re-raising would be especially wrong here because this check is only reached on the AWS ECS deployment path, where the doctor report is the operator's pre-deployment evidence. The explicit result is `ContractCheck('aws_operator_telemetry', False, sanitize_error('AWS operator telemetry policy discovery failed', exc))`, distinct from the policy-mismatch `ok=False` wording so a missing SDK is not confused with a misconfigured policy. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_aws_s3_plugin_check:ast=body[25]/body[1]/handlers[0]": "The broad `except Exception as exc` in `_aws_s3_plugin_check` wraps a deliberate plugin-DISCOVERY probe: a deferred `import` of `elspeth.plugins.infrastructure.manager`, `get_shared_plugin_manager()`, and the `get_sources()` / `get_sinks()` enumeration. Broad is the honest width because the surface is pluggy loading arbitrary third-party entry points - `ImportError` for a missing optional dependency, `ModuleNotFoundError`, and whatever a third-party plugin's own import side effects raise are all the same answer to the question being asked, which is whether the `aws_s3` source and sink are registered in THIS deployment. Narrowing to `ImportError` would let a misbehaving third-party plugin crash the whole doctor run. The result is explicit: `ContractCheck('aws_s3_plugin', False, sanitize_error('AWS S3 plugin discovery failed', exc))`, which reads differently from the `ok=False` returned when discovery SUCCEEDS but the plugins are absent ('aws_s3 source and sink must both be registered'), so an operator can distinguish a broken plugin environment from a merely incomplete one. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_aws_textract_plugin_check:ast=body[26]/body[1]/handlers[0]": "The broad `except Exception as exc` in `_aws_textract_plugin_check` wraps the same deferred-import plus `get_shared_plugin_manager().get_transforms()` discovery as its S3 sibling, asking a different question: is the `aws_textract_document_analysis` transform registered. The width is set by pluggy loading third-party entry points, where an optional AWS dependency missing from the image and a third-party plugin raising during import are both real, both reportable, and neither is worth aborting the other twenty doctor checks for. The handler returns `ContractCheck('aws_textract_plugin', False, sanitize_error('AWS Textract transform discovery failed', exc))` - distinct wording from the discovery-succeeded-but-absent result ('aws_textract_document_analysis transform must be registered') so the report separates a broken plugin environment from an incomplete one. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_bedrock_guardrail_plugins_check:ast=body[29]/body[1]/handlers[0]": "The broad `except Exception as exc` in `_bedrock_guardrail_plugins_check` covers plugin discovery, two membership reads (`transforms['aws_bedrock_prompt_shield']` and `transforms['aws_bedrock_content_safety']`) and the `policy_capabilities` declaration walk. The width is justified by the discovery surface it shares with its S3 and Textract siblings - pluggy loading third-party entry points, plus `cast(Any, ...)` reads of `policy_capabilities` on plugin objects registered by third parties - and by the requirement that a doctor check never crash the surrounding report. The result is the explicit `ContractCheck('bedrock_guardrail_plugins', False, sanitize_error('Bedrock Guardrail plugin discovery failed', exc))`. Recorded limitation, not a claimed control: the handler also ABSORBS the `KeyError` that a missing guardrail plugin produces at those two indexed reads, and the resulting message does not distinguish an absent required plugin from a broken plugin environment - unlike `_aws_s3_plugin_check` and `_aws_textract_plugin_check`, which test with `in` and so keep the 'must be registered' wording separate from 'discovery failed'. That reporting gap is filed as a separate observation against the indexed reads; it is an argument about the message text, not about the breadth of this handler, which would still be needed for the import and third-party-declaration surface. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_bedrock_provider_check:ast=body[27]/body[1]/handlers[0]": "The broad `except Exception as exc` in `_bedrock_provider_check` covers `importlib.import_module('elspeth.plugins.transforms.llm.transform')` followed by a deliberately narrow private-registry read (`provider_module._PROVIDERS`, annotated as such in-source). Both halves fail in open-ended ways that mean the same thing - `ImportError`/`ModuleNotFoundError` when the optional LLM dependency stack is absent from the image, and `AttributeError` if the private registry is ever renamed - and in both cases the truthful report is that the bedrock provider could not be shown to be registered. Catching `AttributeError` here is a deliberate part of the design rather than an oversight: this check reads a private symbol it does not own, so it must degrade to a failed CHECK rather than crash the doctor run if that symbol moves. The result is the explicit `ContractCheck('bedrock_provider', False, sanitize_error('Bedrock provider discovery failed', exc))`. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_dependency_check:ast=body[30]/body[0]/handlers[0]": "The broad `except Exception as exc` in `_dependency_check` wraps a single `importlib.import_module(module_name)` whose only purpose is to answer whether an optional dependency is importable in this deployment image. `ImportError` is the expected failure, but importing a third-party module executes ARBITRARY module-level code, so the honest width is broad - a dependency that is installed but broken (a bad C extension, a misconfigured client raising at import) is exactly as un-usable as one that is absent, and both must be reported rather than crash the doctor run. The handler returns the explicit `ContractCheck(check_name, False, sanitize_error(f'{module_name} dependency import failed', exc))`, pairing with the `ok=True` '{module_name} dependency is importable' on success. `sanitize_error` matters particularly here because third-party import errors routinely embed absolute site-packages paths. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_initialize_database:ast=body[41]/body[1]/handlers[0]": "The broad `except Exception as exc` wraps exactly one call - `_build_engine(label, raw_url)` - in `_initialize_database`, the same operator-supplied-URL parse as its `_inspect_database` twin and with the same open-ended failure set (`ArgumentError`, `NoSuchModuleError`, `ModuleNotFoundError`, `ValueError`). All of them mean the schema could not be initialized because the target could not be addressed, which is returned explicitly as `ContractCheck(label, False, sanitize_error(f'{_human_schema_label(label)} initialization failed', exc))`. This handler returns a single `ContractCheck` rather than the inspection triple because initialization reports no TLS row of its own. Re-raising would abort the doctor run mid-way through an `--init-schema` invocation, leaving an operator unable to see which of the session and landscape targets was at fault. `sanitize_error` is load-bearing because SQLAlchemy URL errors echo the DSN and its password. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_initialize_database:ast=body[41]/body[3]/finalbody[0]/handlers[0]": "The broad `except Exception as exc` guards `engine.dispose()` in `_initialize_database`'s `finally` block. As in `_inspect_database`, it does not discard anything - it REBINDS `result` to `ContractCheck(label, False, sanitize_error(f'{...} engine disposal failed', exc))`, so a disposal fault downgrades an otherwise-successful initialization to a reported failure rather than being ignored. That downgrade is the honest verdict: after DDL against a live external database, a pool that will not dispose means connections may be leaked, and an operator must not read the run as cleanly complete. Broad because disposal reaches driver-level socket teardown. `Exception` rather than `BaseException` is precisely what makes the `assert result is not None` after the `finally` sound - its in-source comment records that a `BaseException` propagates out of the `finally` rather than leaving `result` unset, so the assertion documents an invariant instead of masking one. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_initialize_via_engine:ast=body[40]/body[1]/handlers[3]": "The broad `except Exception as exc` is the FOURTH and last clause of `_initialize_via_engine`'s handler chain, and the three above it are what make it narrow in practice: `SchemaInitBusyError` maps to a 'another schema initialization is in progress' result, `SchemaLockCleanupError` to the specific 'initialization may have completed but lock cleanup was not verified; investigate the database connection and rerun' result, and `(SessionSchemaError, SchemaCompatibilityError)` to `schema_check(label, SchemaState.STALE)`. Only unanticipated DDL, connection, or verification faults reach this clause, and the surface - `init_fn` executing DDL against a live external PostgreSQL, then reconnecting and re-probing - is genuinely unbounded. The function's docstring states the contract this handler completes: 'every outcome is an explicit return', and this one returns `ContractCheck(label, False, sanitize_error(f'{...} initialization failed', exc))`. Redaction is essential here because DDL errors echo SQL text and connection detail. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_inspect_database:ast=body[39]/body[1]/handlers[0]": "The broad `except Exception as exc` wraps exactly one call - `_build_engine(label, raw_url)` - in `_inspect_database`. Engine construction parses an operator-supplied database URL and resolves a driver, so it raises an open-ended set (`ArgumentError` and `NoSuchModuleError` from SQLAlchemy, `ModuleNotFoundError` for an absent driver, `ValueError` for a malformed URL), and all of them mean the same reportable thing: this database target could not be addressed at all. The handler is explicitly evidence-aware, as its in-source comment records - because no engine was ever constructed there is no connection and no TLS evidence, so it synthesises a static failed `session_tls`/`landscape_tls` check when `require_authenticated_tls` is set, rather than omitting the row, and returns the explicit `(None, ContractCheck(label, False, sanitize_error(...)), tls_result)` triple. Redaction is load-bearing at this particular site: SQLAlchemy URL errors routinely echo the DSN, which carries the database password. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_inspect_database:ast=body[39]/body[3]/finalbody[0]/handlers[0]": "The broad `except Exception as exc` guards `engine.dispose()` in `_inspect_database`'s `finally` block. It does not discard the inspection: it REBINDS the `result` triple to a failed one, deliberately preserving the TLS evidence already collected via `result[2] if result is not None else None`, so a disposal fault downgrades the schema verdict without erasing what the connection proved about TLS - the in-source comment states exactly that. Reporting it at all is correct rather than pedantic: a pool that will not dispose means connections may be leaked against a live external database, which an operator running doctor needs to see. Broad because `dispose()` reaches driver-level socket teardown whose failure surface is whatever psycopg or the dialect raises. `Exception` rather than `BaseException` is what makes the `assert result is not None` on the line after the `finally` sound - the assertion's own comment records the reasoning, that a `BaseException` would have propagated out of the `finally` instead of leaving `result` unset. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:_inspect_via_engine:ast=body[38]/body[2]/handlers[1]": "The broad `except Exception as exc` is the SECOND clause of `_inspect_via_engine`'s handler chain - the one above it, `except (SessionSchemaError, SchemaCompatibilityError)`, already maps the two known schema faults to a precise `SchemaState.STALE` result - so only unanticipated connection, TLS, or probe failures reach here. The width is set by the surface: `engine.connect()` and `probe_fn` reach SQLAlchemy dialects, DBAPI drivers, and a TLS stack against a live external PostgreSQL, so the exception type is genuinely unbounded. The handler does more than report - it REPAIRS the evidence: when `require_authenticated_tls` is set and the exception preceded TLS capture (`tls_result is None`), it synthesises an explicit failed `session_tls`/`landscape_tls` `ContractCheck` rather than silently omitting the TLS row from the report, which the in-source comment states and which is the difference between 'TLS was not active' and 'TLS was never checked'. It then returns the explicit `(None, ContractCheck(label, False, sanitize_error(...)), tls_result)` triple, so the caller receives no schema state, a failed schema check, and whatever TLS evidence exists. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:probe_directory_writable:ast=body[23]/body[3]/handlers[0]": "The broad `except Exception as exc` wraps `probe_directory_writable`'s stat/symlink/mode/`resolve(strict=True)` validation sequence. Broad is correct because every arm of that sequence fails in an open-ended way - `lstat` and `resolve(strict=True)` raise the whole `OSError` family (ENOENT, EACCES, ELOOP, ENOTDIR) and `resolve` additionally raises `RuntimeError` on a symlink loop - and all of them answer the one question asked: this directory did not satisfy the private-directory contract. The handler returns `ContractCheck(f'{label}_writable', False, sanitize_error(f'{label} directory validation failed', exc))`, the same shape as the three explicit `ok=False` returns immediately above it for a symlink, a non-directory, and a group/world-writable mode. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:probe_directory_writable:ast=body[23]/body[9]/finalbody[0]/body[0]/handlers[0]": "The broad `except Exception as exc: cleanup_error = exc` guards `os.close(fd)` in `probe_directory_writable`'s `finally` block - the descriptor-leak compensation that runs only when `mkstemp` succeeded but ownership was never transferred to the `os.fdopen` context manager (`fd = None` inside the `with` is what records the transfer). Letting a close() failure propagate out of `finally` would REPLACE the already-captured `probe_error` with a less informative secondary fault, so the secondary is captured into `cleanup_error` instead. The reporting order after the try is the control that decides which the operator sees: `if cleanup_error is not None` is tested BEFORE `if probe_error is not None`, because a descriptor this process cannot reclaim is the more serious operational fact and is reported as 'directory probe cleanup failed'. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:probe_directory_writable:ast=body[23]/body[9]/finalbody[1]/body[0]/handlers[0]": "The broad `except Exception as exc: cleanup_error = cleanup_error or exc` guards the compensating `Path(probe_name).unlink(missing_ok=True)` in `probe_directory_writable`'s `finally` block, which runs only when `unlinked` is still False - i.e. `mkstemp` created a file but the immediate `os.unlink` never ran. `missing_ok=True` already absorbs the benign already-gone case without an exception, so everything reaching this handler is a genuine failure to remove a file this probe created in an operator's deployment directory, and reporting it is correct whatever the errno (EPERM, EROFS, EIO, a sticky-bit parent). The `cleanup_error or exc` idiom is the ordering control: a close() failure captured earlier in the same `finally` block WINS, so this handler can only fill an otherwise-empty slot and can never mask the more proximate fault. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/doctor.py:R4:probe_directory_writable:ast=body[23]/body[9]/handlers[0]": "The broad `except Exception as exc: probe_error = exc` covers `probe_directory_writable`'s actual write probe - `mkstemp`, immediate `os.unlink`, then write/flush/fsync/seek/readback through the surviving descriptor. It must be broad because this is the check that proves a deployment directory is genuinely writable: ENOSPC, EROFS, EDQUOT, EPERM, a full container layer, and the module's own `OSError('doctor probe readback did not match')` for silent corruption are all truthful NOs. The exception is CAPTURED into `probe_error` rather than handled inline, which is what allows the `finally` block below to close the leaked descriptor and remove the probe file before any reporting decision is made; the explicit `if probe_error is not None: return ContractCheck(name, False, sanitize_error(f'{label} directory probe failed', probe_error))` after the try is where the failure becomes a reported result. Re-raising is the behaviour this function exists to prevent: `elspeth-web doctor` must return a COMPLETE report, and `_collect_deployment_checks` runs this alongside roughly twenty other `ContractCheck`s, so one raising probe would abort every remaining check and leave an operator with less information than they started with. The failure is recorded, not lost - the handler returns an explicit `ContractCheck(name, False, ...)`, a first-class failed result that `doctor`'s report renders and exits non-zero on. `sanitize_error` is the named redaction control: it is defined in this module as `f'{context} ({type(exc).__name__})'`, so only a static context string and the exception CLASS reach the report - never the exception's message, which for these probes carries import paths, credentials-bearing URLs, and third-party detail.",
  "web/operator_telemetry.py:R4:record_operator_pipeline_event:ast=body[45]/body[3]/handlers[0]": "The broad `except Exception as exc` wraps exactly one call - `runtime.pipeline_metrics.record(event)` - and the in-source comment states the invariant it enforces: operational metrics are SUBORDINATE to Landscape, so a metric-SDK or recorder failure must never replace an already-audited run or external-call outcome. This function is wired as a telemetry observer and runs AFTER the audit write, so re-raising would convert a best-effort CloudWatch projection into a pipeline-visible failure - inverting the audit>telemetry>logger primacy order. Broad is the honest width because `record` drives OpenTelemetry SDK instruments whose failure surface is third-party and unbounded (`ValueError` from an instrument, `RuntimeError` from a shut-down provider, arbitrary exporter-side faults). The failure is recorded, not lost: `_log.error('operator_pipeline_metric_projection_failed', event_type=type(event).__name__, error_type=type(exc).__name__)` emits a structured error carrying CLASS names only - deliberately not the exception message and not the event's contents, so a raising recorder cannot become an exfiltration channel for row-derived data out of an audited event.",
  "web/operator_telemetry.py:R5:_OperatorPipelineMetrics.record:ast=body[30]/body[6]/body[1]/test": "`isinstance(event, RunFinished)` is a NOMINAL check against a concrete class ELSPETH defines (`elspeth.contracts.events.RunFinished`), on a Tier-1 event the engine constructed and Landscape has already audited - the ADR-032 Decision(1) sanctioned form, not a Tier-3 shape guard. It is the first arm of a discrimination over the owned `TelemetryEvent` union that decides which bounded aggregates this CloudWatch projection records. `isinstance` rather than `type(event) is RunFinished` is load-bearing and fail-safe in the direction that matters here: a future `RunFinished` subclass still carries `duration_ms` and `status`, so `isinstance` keeps recording its run duration and failure count, whereas an exact-type check would silently DIVERT it past this arm, past the `ExternalCallCompleted` arm below, and out of the function - producing a metrics gap that no error surfaces, because operator telemetry is best-effort by design. The `return` at the end of the arm is what makes the two arms exclusive. Only bounded aggregate facts are read (`duration_ms`, `status` against a closed `RunStatus` set); no row content or identifier is projected.",
  "web/operator_telemetry.py:R5:_OperatorPipelineMetrics.record:ast=body[30]/body[6]/body[2]/test/operand": "`if not isinstance(event, ExternalCallCompleted): return` is the second arm of the same nominal discrimination over the owned `TelemetryEvent` union, written as an early-return guard so every event kind that is neither `RunFinished` nor `ExternalCallCompleted` is dropped from the metrics projection rather than mis-recorded. The check against a concrete ELSPETH-defined class on a Tier-1, already-audited event is ADR-032 Decision(1)'s prescribed mechanism. `isinstance` is the correct form for the same fail-safe reason as the arm above: an `ExternalCallCompleted` subclass still carries `latency_ms`, `status`, `call_type` and `token_usage`, so it should continue to be measured, while `type(event) is ...` would route it to the `return` and silently stop recording external-call latency and LLM token counts. It is also what TYPE-NARROWS `event` for the four attribute reads below, none of which exist on the `TelemetryEvent` base - so this guard is the reason those reads are statically sound rather than defensive.",
  "web/operator_telemetry.py:R6:OperatorTelemetryRuntime.shutdown:ast=body[37]/body[12]/body[2]/body[0]/handlers[0]": "`except TELEMETRY_TRANSPORT_ERRORS: _log.warning('operator_otlp_shutdown_unavailable', destination='task-local')` wraps `self.provider.shutdown(timeout_millis=_EXPORT_TIMEOUT_MILLIS)` in `OperatorTelemetryRuntime.shutdown`. The surrounding structure is the control that makes this safe: the call is inside a `try/finally` whose `finally` runs `self._finish_shutdown()` unconditionally, so the once-only shutdown state machine (`_begin_shutdown` / `_shutdown_started` / `_shutdown_complete` under `_shutdown_state_lock`) always reaches a terminal state and a failed teardown can neither be retried into a double-shutdown nor leave the runtime wedged as 'in progress'. Raising is the behaviour that must be prevented: the in-source comment records that this SDK call is run INLINE precisely to avoid a timed-out `to_thread()` leaving a worker alive and keeping the interpreter running, and letting an unreachable task-local collector abort web-service shutdown would trade a telemetry fault for a hung process. The loss is surfaced on the structured logger with its destination named. `TELEMETRY_TRANSPORT_ERRORS` is a NARROW, module-declared tuple of transport failure types, not a broad clause: a programming error inside the SDK still propagates. The failure is not silent - it is accounted.",
  "web/operator_telemetry.py:R6:_HealthTrackingMetricExporter.force_flush:ast=body[36]/body[4]/body[0]/handlers[0]": "`except TELEMETRY_TRANSPORT_ERRORS: self._record_transport_failure(); return False` in `_HealthTrackingMetricExporter.force_flush`. This does not swallow the failure - `_record_transport_failure()` is the control, feeding the `_ExportHealth` surface that `_wire_health_instruments` publishes as the `operator.telemetry.last_success_age_seconds` observable gauge, so a persistently failing OTLP transport becomes visible to the operator as a rising metric rather than as an exception. `return False` is additionally the CORRECT and explicit value here, not a silent default: `force_flush` is an OpenTelemetry SDK interface method whose documented contract is to return a bool meaning 'did the flush succeed', so False is the honest answer and the SDK acts on it. Raising instead would violate the audit>telemetry>logger primacy order - a task-local OTLP collector being unreachable must never break the already-audited pipeline that is emitting the metrics. `TELEMETRY_TRANSPORT_ERRORS` is a NARROW, module-declared tuple of transport failure types, not a broad clause: a programming error inside the SDK still propagates. The failure is not silent - it is accounted.",
  "web/operator_telemetry.py:R6:_HealthTrackingMetricExporter.shutdown:ast=body[36]/body[5]/body[0]/handlers[0]": "`except TELEMETRY_TRANSPORT_ERRORS: self._record_transport_failure()` in `_HealthTrackingMetricExporter.shutdown` - the same accounting control as `force_flush`, on the teardown path. A transport error during exporter shutdown means one final batch of operator metrics did not reach the collector; that is recorded on the `_ExportHealth` surface published as an observable gauge, so the loss is counted rather than discarded. There is no return value to produce because the SDK's `shutdown` contract is `-> None`, which is why the handler body is the accounting call alone. Raising is specifically wrong on this path: `shutdown` runs during process teardown, and an exception escaping it would abort the remaining shutdown sequence over an unreachable telemetry collector - telemetry is subordinate to the pipeline and to Landscape, never the reverse. `TELEMETRY_TRANSPORT_ERRORS` is a NARROW, module-declared tuple of transport failure types, not a broad clause: a programming error inside the SDK still propagates. The failure is not silent - it is accounted.",
  "web/operator_telemetry.py:R6:reset_operator_telemetry_for_tests:ast=body[48]/body[2]/body[4]/body[0]/handlers[0]": "`except TimeoutError: _log.warning('operator_telemetry_test_reset_timeout')` in `reset_operator_telemetry_for_tests`, a NARROW clause around `runtime.provider.shutdown(timeout_millis=_EXPORT_TIMEOUT_MILLIS)`. `TimeoutError` here is the expected, self-inflicted outcome of the bounded deadline this very call passes in - it is the deadline firing, not an unexpected fault - and the function's contract is to return the module to a clean state so the next test can bootstrap telemetry. The control that makes the swallow safe is the `finally` block below, which runs `runtime._finish_shutdown()` and `_runtime = None` unconditionally: the state machine reaches a terminal state and the module global is cleared whether or not the SDK shut down in time, so a timed-out reset cannot leave a stale runtime visible to the next test. Raising instead would make an unreachable or slow collector fail unrelated tests in teardown. Note this handler is not the guard against misuse - the `raise RuntimeError('operator telemetry shutdown is already in progress')` above it is, and it is deliberately left to propagate.",
  "web/operator_telemetry.py:R6:reset_operator_telemetry_for_tests:ast=body[48]/body[2]/body[4]/body[0]/handlers[1]": "`except TELEMETRY_TRANSPORT_ERRORS: _log.warning('operator_telemetry_test_reset_unavailable')` is the second, narrow clause on the same `provider.shutdown` call in `reset_operator_telemetry_for_tests`, distinguishing an unreachable OTLP collector from the `TimeoutError` deadline handled above it - the two log events are separate (`_test_reset_unavailable` vs `_test_reset_timeout`) so the cause is not conflated. In a test process there is frequently no collector listening at all, which is exactly what `TELEMETRY_TRANSPORT_ERRORS` names, so treating it as fatal would make the reset helper fail for the ordinary reason it exists. The control is the same `finally` block: `runtime._finish_shutdown()` and `_runtime = None` run unconditionally, so the once-only shutdown state machine terminates and the module global is cleared regardless of transport outcome. `TELEMETRY_TRANSPORT_ERRORS` is a NARROW, module-declared tuple of transport failure types, not a broad clause: a programming error inside the SDK still propagates. The failure is not silent - it is accounted.",
  "web/paths.py:R6:_is_uuid_path_segment:ast=body[10]/body[1]/handlers[0]": "`except ValueError: return False` is the NEGATIVE ANSWER of a predicate, not a swallowed error: `_is_uuid_path_segment` asks whether one path segment parses as a UUID, and `ValueError` is precisely how `uuid.UUID` reports 'this string is not a UUID'. `return False` is therefore the function's contracted result for that input, and there is no other outcome it could report - the signature is `-> bool`. The clause is deliberately narrow and was narrowed in this change: the previously-caught `AttributeError` was removed because the sole call site passes `raw.parts[1]`, and `Path.parts` is `tuple[str, ...]`, so a non-string cannot reach here and a wrong-typed caller must now crash rather than be silently reported as 'not a UUID'. The security direction is deliberate and is why `uuid.UUID` is kept in preference to a narrower regex: in `resolve_sink_data_path` a True result keeps an explicitly session-scoped FOREIGN path foreign so the caller's allowlist rejects it, while False adopts the path under the caller's own output directory - so accepting every spelling `UUID` accepts (braced, urn, dashless) is the conservative choice, and a narrower parser would silently adopt foreign-looking segments it failed to recognise.",
  "web/readiness.py:R1:ReadinessProbeRunner.run.source_done:ast=body[50]/body[2]/body[2]/body[0]/body[0]/test/left": "`if self._futures.get(label) is completed:` in the `source_done` completion callback is an IDENTITY compare-and-delete under `self._lock`, and the `.get` is the load-bearing half of it. The callback fires when one specific worker future finishes, and it may only remove the registry entry if that entry is still THIS future - a later `run()` for the same label may already have replaced it, and deleting the successor's entry would let a third concurrent probe be admitted for a label that is still in flight, defeating the single-flight invariant. Both branches are handled: `is completed` deletes, anything else (a successor future, or `None` because the entry was already reaped) correctly leaves the registry alone. A membership read is not available - the docstring above `add_done_callback` records that a future which completed synchronously invokes this callback INLINE, so the entry may legitimately be absent, and `KeyError` here would escape into a `concurrent.futures` callback with no caller to receive it.",
  "web/readiness.py:R1:ReadinessProbeRunner.run.wrapped_done:ast=body[50]/body[2]/body[3]/body[0]/body[0]/value": "`registration = self._wrappers.get(label)` in the `wrapped_done` callback is the asyncio-side twin of `source_done`'s compare-and-delete, and it must be even more careful: the guard on the next line checks BOTH stored futures (`registration[0] is source and registration[1] is completed`) before deleting, so a registration belonging to a later `run()` for the same label can never be evicted by an earlier probe's callback. `None` is an explicitly handled branch (`if registration is not None and ...`) meaning the entry was already reaped, and it is genuinely reachable: `ReadinessProbeRunner.close()` snapshots and clears the wrappers while callbacks may still be pending. Raising `KeyError` inside an asyncio done-callback would land in the event loop's exception handler with no caller, so a membership read would convert a benign race into an unhandled loop error. The registry is deliberately self-pruning - `source_done` and `wrapped_done` delete the label's entry as soon as the future completes - so 'absent' is the steady state and the dominant case, not an anomaly. A membership read would raise `KeyError` on the ordinary completed-probe path.",
  "web/readiness.py:R1:ReadinessProbeRunner.run:ast=body[50]/body[2]/body[1]/body[1]/value": "`existing = self._futures.get(label)` in `ReadinessProbeRunner.run` reads the in-flight registry to enforce the class's stated admission rule: at most one unresolved worker future per closed label. Absence is the expected and most common answer - it means no probe for this label is running, so this call may submit one. The value is used immediately and totally on the next line (`if existing is not None and not existing.done(): return _static_failures(check_names, 'probe already in flight')`), so `None` is a first-class branch, not a default that papers over a missing key. `label` is the closed `ReadinessProbeLabel` Literal, so this is not Tier-3 key parsing; the read happens under `self._lock` alongside the `self._futures[label] = source` write that pairs with it. The registry is deliberately self-pruning - `source_done` and `wrapped_done` delete the label's entry as soon as the future completes - so 'absent' is the steady state and the dominant case, not an anomaly. A membership read would raise `KeyError` on the ordinary completed-probe path.",
  "web/readiness.py:R1:readiness_report:ast=body[49]/body[2]/body[3]/value/args[0]/elt": "`by_name.get(name, ReadinessCheck(name, False, 'check result missing'))` in `readiness_report` is a FAIL-CLOSED completeness gate, not a silent default. `READINESS_CHECK_NAMES` is the module's closed tuple of the eight checks a report must contain, while `by_name` is assembled from whatever the concurrent `asyncio.gather` actually returned; the `.get` is what guarantees the response has one entry per contracted name even if a probe returned a short tuple. The default is not a benign filler - `ok=False` with detail 'check result missing' - so a missing probe result makes `_finalize` compute `ready=False`, logs a `readiness_check_not_ready` warning naming the check, and fails the readiness response. Replacing this with a membership read plus `KeyError` would be strictly worse: it would raise inside the `try` and be caught by the `except BaseException` handler below, collapsing all eight checks into one generic 'readiness evaluation failed' line and destroying the per-check evidence this expression exists to preserve. Absence is a genuine first-class value here, and it is reported as a failure.",
  "web/readiness.py:R4:ReadinessCache._harvest_locked:ast=body[51]/body[4]/body[0]/handlers[0]": "The broad `except BaseException` wraps exactly one call - `task.result()` on an already-`done()` readiness task - and `_harvest_locked` is a CACHE-SLOT reaper, not the code that decides the request outcome. Its contract is 'publish a successful report into the cache, otherwise leave the cache empty'; a failed or cancelled task must clear `self._task`/`self._task_completed_at` so the next caller starts a fresh probe instead of awaiting a dead one, which is precisely what the handler does before returning `None`. Re-raising here would be a defect, not a fix: the exception is NOT lost, because the caller `ReadinessCache.get_or_compute` awaits the same task through `asyncio.shield(task)` under its own `except BaseException: ... raise`, so the failure reaches the request handler by that path; re-raising out of the reaper as well would surface one task failure twice and, worse, would abandon the cache slot still pointing at the dead task, permanently wedging readiness for the process. The control that guarantees the failure is surfaced is therefore `ReadinessCache.get_or_compute`'s `await asyncio.shield(task)` / bare `raise` pair, not this handler. `BaseException` rather than `Exception` is deliberate and required: `asyncio.CancelledError` derives from `BaseException`, and a cancelled probe task must clear the slot exactly like a failed one - under `except Exception` a cancelled task would escape the reaper and leave `self._task` permanently non-None. Nothing about the exception is read, so no external content can leak into the readiness surface. Pinned by tests/unit/web/test_readiness.py.",
  "web/readiness.py:R4:ReadinessProbeRunner.run:ast=body[50]/body[2]/body[7]/body[1]/handlers[1]": "The broad `except BaseException as exc: return _exception_failures(check_names, exc)` wraps exactly one expression - `wrapped.result()` - and is the seam that converts a worker-thread probe failure into probe DATA. The probe functions it re-raises for (`_check_session_database`, `_check_landscape_database`, `_check_data_dir`, `_check_payload_store`, `_check_blob_dir`) reach arbitrary third-party depth - SQLAlchemy dialects, DBAPI drivers, TLS stacks, the filesystem - so the exception type is genuinely unbounded and no narrowing is honest. It is not a swallow-all: the clause DIRECTLY ABOVE it is `except asyncio.CancelledError: raise`, so cancellation propagates and only real probe faults land here, and the outer `except asyncio.CancelledError` on the same `try` re-raises after cancelling and draining both futures. `BaseException` is required rather than `Exception` precisely because `CancelledError` is a `BaseException` subclass already claimed by the clause above. The failure is recorded, not lost: `_exception_failures(check_names, exc)` builds one `ReadinessCheck(ok=False)` per contracted name for this label, projecting `type(exc).__name__` only, and marking the dependent `<kind>_schema` check with 'not checked: connectivity probe failed' so an operator can tell a schema mismatch from a database that was never reached.",
  "web/readiness.py:R4:_check_blob_dir:ast=body[45]/body[0]/handlers[0]": "The broad `except BaseException as exc` wraps exactly one call - `managed_blob_directory(str(settings.data_dir))` - which resolves the deployment-owned blob root by `Path(data_dir).resolve() / 'blobs'`. Resolution of an operator-supplied `data_dir` can fail in an open-ended way (`OSError` for ENAMETOOLONG or a broken mount, `RuntimeError` for a symlink loop, `ValueError` for an embedded NUL byte), and every one of them means the same reportable thing: the blob directory could not even be named, so it certainly is not ready. The handler returns the explicit `(ReadinessCheck('blob_dir', False, f'directory resolution failed ({type(exc).__name__})'),)` rather than raising, which keeps the remaining seven checks in the report; the distinct 'directory resolution failed' wording separates this pre-validation failure from the 'directory validation failed' that `_validate_directory` produces on the following line, so an operator can tell a malformed `data_dir` from a missing blob subtree. Class name only, for the same unauthenticated-endpoint redaction reason.",
  "web/readiness.py:R4:_check_payload_store:ast=body[44]/body[2]/handlers[0]": "The broad `except BaseException as exc` in `_check_payload_store` guards the same stat/symlink/mode/`resolve(strict=True)` contract as `_validate_directory`, applied to the payload store specifically because this check first has to select the path per deployment state mode (`settings.payload_store_path` under external-postgresql, `settings.get_payload_store_path()` otherwise). The open-ended `OSError` family plus `resolve`'s `RuntimeError` all mean one reportable thing - the configured payload store did not validate - and the handler returns that as an explicit `(ReadinessCheck('payload_store', False, f'directory validation failed ({type(exc).__name__})'),)`, matching the three explicit `ok=False` returns directly above it. Raising instead would collapse the whole eight-check readiness report over one misconfigured directory, which is the opposite of what a readiness probe is for. `BaseException` covers cancellation inside the `ReadinessProbeRunner` worker thread, and the class-name-only detail keeps the operator-configured absolute path out of an unauthenticated response.",
  "web/readiness.py:R4:_probe_directory:ast=body[41]/body[5]/finalbody[0]/body[0]/handlers[0]": "The broad `except BaseException as exc: cleanup_error = exc` guards `os.close(fd)` in `_probe_directory`'s `finally` block - the descriptor-leak compensation that runs only when `mkstemp` succeeded but ownership was never transferred to the `os.fdopen` context manager. This is the classic cleanup-must-not-mask-the-original rule: the block is already executing because the primary probe failed, and letting a close() error propagate out of `finally` would REPLACE the original `probe_error` with a less informative secondary fault. Instead the secondary is captured into `cleanup_error`, and the explicit reporting order after the try - `if cleanup_error is not None` checked BEFORE `if probe_error is not None` - is the control that decides which failure the operator sees, reporting 'directory probe cleanup failed' because an unreclaimable descriptor is the more serious operational fact. Broad rather than `OSError` because a descriptor that cannot be closed is worth reporting whatever the reason, and because a cancellation arriving mid-teardown in the worker thread must not escape a diagnostic probe. Nothing is discarded: the handler stores the exception into the local `probe_error` / `cleanup_error` variable, and the explicit reporting statements after the `try` convert it into a `ReadinessCheck(name, False, ...)` carrying only `type(exc).__name__`. That class-name-only projection is the redaction control - a readiness endpoint is reachable by an unauthenticated load balancer, so probe exception messages (which carry absolute filesystem paths and OS detail) must never reach the response body.",
  "web/readiness.py:R4:_probe_directory:ast=body[41]/body[5]/finalbody[1]/body[0]/handlers[1]": "The broad `except BaseException as exc: cleanup_error = cleanup_error or exc` is the second arm of the compensating-unlink handler in `_probe_directory`'s `finally` block, sitting after the narrow `except FileNotFoundError` that absorbs the benign already-gone case. It is broad for the cleanup reason: a probe file this function created and could not remove is a real operational fact - it leaves litter in a deployment directory - and is worth reporting whatever raised. The `cleanup_error or exc` idiom is the control that preserves ordering: a close() failure recorded earlier in the same `finally` block WINS, so the first and most proximate cleanup fault is the one reported and this handler can never overwrite it. As with the other arms, `BaseException` covers cancellation of the worker thread mid-teardown, and the captured exception is projected as a class name only. Nothing is discarded: the handler stores the exception into the local `probe_error` / `cleanup_error` variable, and the explicit reporting statements after the `try` convert it into a `ReadinessCheck(name, False, ...)` carrying only `type(exc).__name__`. That class-name-only projection is the redaction control - a readiness endpoint is reachable by an unauthenticated load balancer, so probe exception messages (which carry absolute filesystem paths and OS detail) must never reach the response body.",
  "web/readiness.py:R4:_probe_directory:ast=body[41]/body[5]/handlers[0]": "The broad `except BaseException as exc: probe_error = exc` covers the unlinked-tempfile write/fsync/readback probe in `_probe_directory`. It must be broad because the failure modes it is diagnosing are open-ended - `OSError` families for ENOSPC/EROFS/EDQUOT/EPERM, the module's own `OSError('readiness probe readback did not match')` for silent corruption, and anything a container filesystem or FUSE layer raises. That is the point of the check: the probe exists to answer 'can this deployment actually write here', and any failure is a truthful NO. Re-raising is the behaviour this function exists to prevent - `readiness_report` gathers this probe concurrently with seven other checks, and a raise would abort the whole report so an operator would learn nothing about the database or the other directories. `BaseException` rather than `Exception` is deliberate: this body runs inside a worker thread submitted to `ReadinessProbeRunner`'s `ThreadPoolExecutor`, where a cancellation must still run the `finally` block that closes the leaked file descriptor and unlinks the probe file. Nothing is discarded: the handler stores the exception into the local `probe_error` / `cleanup_error` variable, and the explicit reporting statements after the `try` convert it into a `ReadinessCheck(name, False, ...)` carrying only `type(exc).__name__`. That class-name-only projection is the redaction control - a readiness endpoint is reachable by an unauthenticated load balancer, so probe exception messages (which carry absolute filesystem paths and OS detail) must never reach the response body.",
  "web/readiness.py:R4:_validate_directory:ast=body[42]/body[0]/handlers[0]": "The broad `except BaseException as exc` wraps the whole stat/symlink/mode/`resolve(strict=True)` sequence in `_validate_directory`, whose job is to answer whether a configured directory satisfies the deployment contract before anything writes to it. Every arm of that sequence can fail in an open-ended way - `lstat` and `resolve(strict=True)` raise the `OSError` family (ENOENT, ELOOP, EACCES, ENOTDIR, ENAMETOOLONG) and `resolve` can additionally raise `RuntimeError` on a symlink loop - and the honest report for all of them is identical: this path did not validate. The handler is not silent; it returns the explicit `(ReadinessCheck(name, False, f'directory validation failed ({type(exc).__name__})'),)` tuple, which is the same first-class failure value the three checks above it return for a symlink, a non-directory, and a group/world-writable mode. Re-raising would abort `readiness_report`'s whole `asyncio.gather` and hide the database and other directory results. Only the exception CLASS name is projected, never its message, because the readiness endpoint is unauthenticated and `lstat`/`resolve` messages embed absolute paths.",
  "web/readiness.py:R4:readiness_report:ast=body[49]/body[2]/handlers[2]": "The broad `except BaseException as exc` is the outermost arm of `readiness_report`'s handler chain and is a deliberate total function over an unauthenticated liveness surface: the endpoint must always answer with a `ReadinessReport`, never with a 500 whose body is an unredacted traceback. It is the THIRD clause, and the two above it are what keep it from being a swallow-all - `except asyncio.CancelledError` cancels the child tasks and RE-RAISES so client disconnect and server shutdown propagate normally, and `except TimeoutError` returns the dedicated `overall_timeout_report()`. Only genuinely unanticipated faults reach here. The handler is not silent in any sense: it cancels every task in `tasks`, awaits them with `return_exceptions=True` so no child failure is left unretrieved, and returns `_finalize(_static_failures(READINESS_CHECK_NAMES, f'readiness evaluation failed ({type(exc).__name__})'))` - a complete eight-check report, every check `ok=False`, `ready=False`, and `_finalize` emits a `readiness_check_not_ready` structured warning per check. `BaseException` is required rather than `Exception` because the two narrower clauses above it already claim `CancelledError` and `TimeoutError`, both of which are `BaseException` subclasses; narrowing this clause would let a `BaseException`-derived fault escape past task cancellation and leak the child tasks. Only the exception class name is projected.",
  "web/readiness.py:R6:_probe_directory:ast=body[41]/body[5]/finalbody[1]/body[0]/handlers[0]": "`except FileNotFoundError: pass` around `os.unlink(probe_name)` in `_probe_directory`'s `finally` block. The absent file IS the desired post-condition, not a swallowed error: this compensating unlink runs only when `unlinked` is still False, and `FileNotFoundError` means the probe file is already gone - the exact state the cleanup is trying to reach. Recording it as a cleanup failure would be a false positive that reports a healthy directory as not ready. It is deliberately the NARROW clause and is ordered FIRST, ahead of the `except BaseException as exc: cleanup_error = cleanup_error or exc` immediately below it, so every other unlink failure - EPERM, EROFS, EIO, a sticky-bit parent - still reaches `cleanup_error` and is reported. `pass` here is therefore scoped to one provably benign errno, not a swallow of the class.",
  "web/readiness.py:R7:_cancel_wrapped_on_loop:ast=body[33]/body[3]": "`contextlib.suppress(RuntimeError)` around `loop.call_soon_threadsafe(cancel_and_drain)` - already the NARROW form (not `BaseException`), catching the one documented failure of that API: scheduling onto a loop that closed after the check. `_cancel_wrapped_on_loop` is called from `ReadinessProbeRunner.close()`, which runs on an arbitrary thread while the probe's owning loop may be shutting down concurrently, so `loop.is_closed()` on the line above is a necessary but not sufficient test - the loop can close between that test and the scheduling call. The suppressed `RuntimeError` therefore reports a condition under which the work is already unnecessary rather than a fault: a closed loop has no pending callbacks to leak, the wrapper future is unreachable, and the source future is cancelled unconditionally by the `for future in futures: future.cancel()` loop in `close()` immediately afterwards - that loop, plus `self._executor.shutdown(wait=False, cancel_futures=True)`, is the control that guarantees teardown completes whether or not the loop-side cancel was ever scheduled. Raising here would make closing the runner fail on a benign shutdown ordering.",
  "web/readiness.py:R7:_drain_source:ast=body[31]/body[1]": "`contextlib.suppress(BaseException)` around `future.exception()` in `_drain_source`. The call is made for its SIDE EFFECT only - marking a `concurrent.futures.Future`'s exception as retrieved so the interpreter does not emit a 'Future exception was never retrieved' warning at collection - and its return value is discarded on the very same line. There is no result to lose here: `ReadinessProbeRunner.run` has already converted the worker's outcome into a `_ProbeResult` (via `wrapped.result()` or `_exception_failures`) before any drain runs, so the probe failure is already carried in the returned `ReadinessCheck` tuple with its exception class name. What is suppressed is only what `Future.exception()` itself can raise on an unfinished/cancelled future, and the `if future.cancelled(): return` guard immediately above is part of that control - the suppress covers the residual race in which the future is cancelled between that guard and the call. Narrowing to `CancelledError` would be wrong: this drain also runs on the `ReadinessProbeRunner.close()` teardown path where the executor is shutting down, and the requirement is that teardown of a diagnostic endpoint can never itself raise into a request or an event-loop callback.",
  "web/readiness.py:R7:_drain_wrapped:ast=body[32]/body[1]": "`contextlib.suppress(BaseException)` around `future.exception()` in `_drain_wrapped` - the asyncio-side twin of `_drain_source`, and the same discipline for a different future type. `asyncio.Future.exception()` RAISES `CancelledError` (rather than returning it) when the future was cancelled, which is exactly the normal outcome on the timeout and cancellation paths in `ReadinessProbeRunner.run` and in `_cancel_wrapped_on_loop`'s `cancel_and_drain`; the suppress is what makes retrieval-marking safe on a future whose most common terminal state is cancellation. The return value is discarded, so nothing is swallowed that anyone reads: the caller has already produced the `_ProbeResult` for this label - `_static_failures(check_names, 'probe timed out')` on the deadline path, `_exception_failures` on the failure path - before draining. Because this also runs inside a `loop.call_soon_threadsafe` callback during `close()`, an escaping exception would land in the event loop's exception handler with no caller to receive it; suppressing at the drain is what keeps a shutdown race out of the loop's error path.",
  "web/schema_probe.py:R1:postgres_logical_target_key:ast=body[39]/body[4]/value": "`options_value = parsed.query.get('options')` reads an OPTIONAL element of a SQLAlchemy `URL.query` mapping parsed from an operator-supplied database URL. Absence is the overwhelmingly normal case - most PostgreSQL URLs carry no `options=` parameter at all - and it is handled as a first-class value: `if options_value is not None:` gates the entire search_path validation below, and `explicit_schema` stays `None`, meaning 'this target does not pin a schema'. A membership read would be wrong twice over: it would raise `KeyError` on the common well-formed URL, and it would erase the distinction this function is built to preserve, since `postgres_logical_target_key` exists to compute a target identity that `require_distinct_postgres_targets` compares in order to refuse pointing the session and landscape databases at the same logical target. Note that this `.get` is NOT the permissive part of the parse - every other unexpected condition in this function raises `_target_error()`, including any query key in `_POSTGRES_TARGET_QUERY_OVERRIDES`, so an unrecognised target-altering parameter is rejected rather than defaulted.",
  "web/schema_probe.py:R2:_sqlstate:ast=body[43]/body[1]/value": "`getattr(original, 'sqlstate', None)` where `original` is `exc.orig` - the wrapped DBAPI exception object from a THIRD-PARTY driver, which SQLAlchemy types as `BaseException | None`. This is the ADR-032 Tier-3 parse of an optional capability on an object ELSPETH does not own and cannot nominally type: psycopg (v3) exposes the five-character SQLSTATE as `.sqlstate`, psycopg2 exposes it as `.pgcode`, and other DBAPI drivers expose neither. Absence is a legitimate answer that the caller handles - `_sqlstate` returns `None` and `_run_locked`'s `if _sqlstate(exc) == '55P03'` simply does not take the lock-timeout branch, so an unrecognised driver degrades to the generic `SchemaInitError` rather than misreporting. The banned alternative is a `runtime_checkable` Protocol `isinstance`, which ADR-032 forbids as a dispatch control and which since Python 3.12 resolves members through `inspect.getattr_static`, so a driver exposing `sqlstate` via `__getattr__` would be silently mis-detected. The `getattr` absorbs only `AttributeError`: a driver whose `sqlstate` property RAISES something else propagates rather than being downgraded to 'absent'.",
  "web/schema_probe.py:R2:_sqlstate:ast=body[43]/body[2]/body[0]/value": "`getattr(original, 'pgcode', None)` is the psycopg2 fallback arm of the same Tier-3 optional-capability parse, reached only when `.sqlstate` was absent or `None`. The two spellings are not interchangeable aliases invented here - they are the two names the PostgreSQL driver ecosystem actually uses for the same SQLSTATE value (psycopg v3 `.sqlstate`, psycopg2 `.pgcode`) - so probing both is what lets one code path recognise a `55P03` lock_not_available under either driver ELSPETH may be deployed against. Absence remains a first-class answer: with neither attribute present the function returns `None` and `_run_locked` raises the generic `SchemaInitError` ('lock could not be acquired; investigate database connectivity and retry') instead of the specific `SchemaInitBusyError`, which is a truthful degradation rather than a hidden one. Same `AttributeError`-only absorption as the arm above, and the same reason a Protocol check is not available.",
  "web/schema_probe.py:R4:_invalidate_uncertain:ast=body[44]/body[1]/handlers[0]": "The broad `except BaseException as invalidation_error` wraps exactly one call - `conn.invalidate()` - and the function's whole purpose, stated in its docstring, is that a secondary cleanup fault must never replace the original. `_invalidate_uncertain` is only ever called from an exception path (`_run_locked`'s `OperationalError` and `BaseException` lock-acquisition handlers, and `_finish_lock_cleanup`), each of which immediately re-raises the ORIGINAL exception or raises `SchemaInitBusyError` / `SchemaInitError` / `SchemaLockCleanupError` derived from it. Letting an invalidate() failure propagate would destroy that diagnosis and replace a precise 'another schema initialization is in progress' with a pooling internal error. It must be broad because `conn.invalidate()` reaches DBAPI driver teardown on a connection already known to be in an uncertain state - the failure surface is whatever psycopg/psycopg2/SQLAlchemy raises on a half-dead socket. The failure is recorded, not lost: `_slog.error('schema_connection_invalidation_failed', original_exc_class=..., invalidation_exc_class=...)` emits both exception class names, so an operator sees the cleanup fault alongside the cause; class names only, because this runs against a connection whose exception text carries DSN and host detail.",
  "web/schema_probe.py:R4:_run_locked:ast=body[46]/body[1]/body[3]/finalbody[0]/body[1]/handlers[0]": "The broad `except BaseException as exc: cleanup_error = exc` guards the pre-unlock `if conn.in_transaction(): conn.rollback()` inside `_run_locked`'s `finally` block, which runs while a PostgreSQL advisory lock is still held. It does not swallow: the exception is CAPTURED into `cleanup_error`, and the control that decides its fate is `_finish_lock_cleanup`, called at the end of the same block - when no earlier exception exists it RAISES `SchemaLockCleanupError` from the captured error ('initialization may have completed but lock cleanup was not verified'), and when `earlier` is set it logs `schema_lock_cleanup_unverified` with both exception classes so the primary failure survives. Capturing rather than raising in place is what makes the remaining cleanup run at all: the advisory-unlock statement below this line is what releases `pg_advisory_lock`, and a rollback error escaping here would skip it and strand the lock until the connection dies, blocking every future schema initialization against that target. Broad because a rollback on a connection whose DDL just failed can raise anything from the driver, and `BaseException` so a cancellation cannot strand the lock either.",
  "web/schema_probe.py:R4:_run_locked:ast=body[46]/body[1]/body[3]/finalbody[0]/body[2]/body[0]/handlers[0]": "The broad `except BaseException as exc: cleanup_error = exc` guards the advisory-unlock statement itself - `SELECT pg_catalog.pg_advisory_unlock(...)` and its `scalar_one()` - inside `_run_locked`'s `finally` block. This is the most safety-critical of the three cleanup handlers, because failing to unlock strands the schema-init lock for the target. Note that the same `cleanup_error` variable is also set on the NON-exception path immediately above, when the unlock query returns anything but `True` (`cleanup_error = RuntimeError('unlock was not confirmed')`): an unconfirmed unlock and a raised unlock are treated as the same reportable condition, which is what makes this handler an explicit error path rather than a swallow. `_finish_lock_cleanup` is the control - it invalidates the connection through `_invalidate_uncertain` so the pool cannot hand out a session still holding the lock, then either raises `SchemaLockCleanupError` from the captured error or, when an earlier exception is already in flight, logs `schema_lock_cleanup_unverified` naming both classes. Broad because the failure surface is a DBAPI round-trip on a connection in an unknown state.",
  "web/schema_probe.py:R4:_run_locked:ast=body[46]/body[1]/body[3]/finalbody[0]/body[3]/handlers[0]": "The broad `except BaseException as exc: if cleanup_error is None: cleanup_error = exc` guards the post-unlock `if conn.in_transaction(): conn.rollback()` in `_run_locked`'s `finally` block. The `if cleanup_error is None` guard is the ordering control and the reason this cannot mask anything: a rollback or unlock fault recorded EARLIER in the same block always wins, so the most proximate and most serious cleanup failure - in particular a stranded advisory lock - is the one `_finish_lock_cleanup` reports, and this last handler can only ever fill an otherwise-empty slot. As with its two siblings the exception is captured, not discarded, and `_finish_lock_cleanup` converts it into a raised `SchemaLockCleanupError` or a `schema_lock_cleanup_unverified` log carrying both exception class names. Broad and `BaseException` for the same reason as the other two arms: this runs on a connection of unknown health while a lock may still be held, and no exception - cancellation included - may be allowed to skip the remaining cleanup accounting.",
  "web/schema_probe.py:R5:_sqlstate:ast=body[43]/body[3]/value/test": "`return value if isinstance(value, str) else None` is the closing type assertion of the Tier-3 parse in `_sqlstate`, applied to whatever a third-party DBAPI exception put in `.sqlstate` / `.pgcode`. It is required because the two `getattr` reads above returned `Any` from an object ELSPETH does not own, and the single consumer compares the result to a string literal (`if _sqlstate(exc) == '55P03'`). Without this assertion a driver storing an int, an enum member, or a bytes SQLSTATE would compare unequal to '55P03' and SILENTLY mis-route a genuine lock-timeout into the generic `SchemaInitError` path, telling an operator to investigate connectivity when the truthful answer is 'another schema initialization is in progress'. Collapsing a non-str to `None` is the honest outcome and matches the absent case exactly: the function's declared return is `str | None`, so an unparseable code and a missing one are reported identically rather than one being dressed up as the other. `isinstance` is the right form because the incoming value is third-party and a `str` subclass carrying the code should be accepted.",
  "web/schema_probe.py:R5:postgres_logical_target_key:ast=body[39]/body[6]/body[0]/test/operand": "`if not isinstance(options_value, str): raise _target_error()` is an ADMISSION GATE on Tier-3 data, not a defensive shape guard - it raises rather than coercing or defaulting. SQLAlchemy's `URL.query` is typed `Mapping[str, str | tuple[str, ...]]`: a URL repeating the parameter (`?options=a&options=b`) yields a TUPLE, so the non-str case is genuinely reachable from an operator-supplied connection string and is exactly the smuggling shape this function must refuse. The check is load-bearing for what follows: `_SEARCH_PATH.fullmatch(options_value.strip())` raises a raw `AttributeError`/`TypeError` on a tuple, so without this line a duplicated `options=` parameter would surface as an internal error instead of the module's own `DatabaseTargetConflictError`, whose whole point is that the session and landscape databases must be provably distinct logical targets. `isinstance` rather than `type(...) is str` is correct here because the else-branch is the failure path and the goal is to admit exactly what SQLAlchemy's own `str`-typed arm produces, subclasses included."
}
