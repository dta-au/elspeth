{
  "web/composer/_semantic_validator.py:R1:_source_spec_for:ast=body[13]/body[2]/value": "R1 `sources.get(source_name)` in `_source_spec_for`. The function's declared contract is \"Return the SourceSpec a source producer id names, or None\" -- `None` is a documented return value, not a swallowed failure. `source_name` is decoded from a producer id in a draft composition being validated, so a producer naming a source that is not (or is no longer) in `sources` is an ordinary state of an in-progress composition, not a broken ELSPETH invariant. The sole caller `_instantiate_source_producer` reads that `None` as abstention -- `if source_spec is None: return None` -- on exactly the same footing as its own `if producer.plugin_name is None: return None` guard one line earlier, so the semantic probe reports \"no facts\" rather than fabricating any. Crashing here would fail validation of a draft the composer is meant to be able to describe.",
  "web/composer/guided/emitters.py:R1:_build_wire_projection:ast=body[36]/body[16]/body[3]/value/args[0]/values[4]/body": "R1 `contracts.get((from_id, to_id))` in `_build_wire_projection`. `contracts` maps an endpoint pair to the schema contract validation derived for that edge; validation derives a contract only for edges where both ends declare enough schema to compare. An edge with no entry is the ordinary case, and `None` is the value the wire payload is meant to carry for it -- `\"schema_contract\": None` renders as \"no contract claim for this connection\", which is the honest weakest statement. Crashing on a missing pair would make the wire review unavailable for every pipeline containing one under-declared edge, replacing a truthful \"no contract\" with a 500.",
  "web/composer/guided/emitters.py:R1:_merge_inspection_into_prefill:ast=body[20]/body[1]/value": "R1 `facts.redacted_identity.get(\"blob_id\")` in `_merge_inspection_into_prefill`. `SourceInspectionFacts.redacted_identity` is a per-plugin identity mapping whose key set varies by source kind -- a blob-backed upload carries `blob_id`, a database or HTTP source does not. Absence is therefore a first-class value meaning \"this inspected source is not blob-backed\", and the guard on the next line (`if blob_id and _inspection_matches_source_plugin(...)`) reads it as exactly that, leaving the `path` prefill untouched. A membership read would crash source prefill for every non-blob source. The key's presence is not an ELSPETH invariant, so there is nothing here for a crash to protect.",
  "web/composer/guided/emitters.py:R1:_structured_output_fields:ast=body[33]/body[4]/body[0]/value": "R1 `query.get(\"output_fields\")` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking (`entries = [(name, value) for ...]`, then `for query_name, query in entries`), which the intra-procedural walk does not carry. An llm node whose query declares no `output_fields` is the ordinary case, not an anomaly: absence is a first-class value meaning \"this query produces no typed result fields\", and the very next line skips the query. Reading it as a member would crash the wire review on a perfectly legal pipeline.",
  "web/composer/guided/emitters.py:R1:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[0]/test/values[1]/left/args[0]": "R1 `field.get(\"suffix\")` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. A planner-authored output-field spec that omits `suffix` is malformed input to be skipped, not a broken ELSPETH invariant to crash on: the guard `type(field.get(\"suffix\")) is not str` folds \"absent\" and \"present but not a string\" into the same skip, and only the entries that pass it reach `field['suffix']` as a membership read on the next lines.",
  "web/composer/guided/emitters.py:R1:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[0]/test/values[2]/left/args[0]": "R1 `field.get(\"type\")` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. Identical role to the `suffix` probe on the same line: a planner-authored field spec missing `type` is malformed external input, folded into the same skip by `type(field.get(\"type\")) is not str`, and only entries that pass reach `field[\"type\"]` as a membership read.",
  "web/composer/guided/emitters.py:R1:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[1]/value": "R1 `field.get(\"values\")` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. Absence is a first-class value with a specific meaning here: a typed output field that is not an enum simply has no `values` list, which is the common case, and the projection renders `enum_values: []` for it. Requiring the key would make every non-enum field a crash.",
  "web/composer/guided/emitters.py:R1:_wire_schema:names:ast=body[32]/body[6]/body[0]/value": "R1 `schema.get(key)` in `_wire_schema.names`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. `names` is a closure inside `_wire_schema`, which carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; `schema` is the enclosing function's normalised view of `options[\"schema\"]`, and the analyzer does not carry boundary taint from an enclosing scope into a nested function. `guaranteed_fields` and `required_fields` are optional parts of a business schema -- their absence means the author declared none, which the projection renders as an empty list. That is the honest weakest claim, not a hidden failure.",
  "web/composer/guided/emitters.py:R1:build_step_1_schema_form_turn_from_resolved:ast=body[28]/body[4]/value": "R1 `source.options.get(\"blob_ref\")` in `build_step_1_schema_form_turn_from_resolved`. `SourceResolved.options` is free-form composer-authored plugin configuration, stored verbatim and never schema-validated by the composer, so this is a Tier-3 read. `blob_ref` is present only for a blob-backed source; its absence is a first-class value meaning \"an operator typed this path by hand\", and the code depends on that distinction -- the read is the gate that keeps the storage-path masking from rewriting an operator-typed `path` knob. Requiring the key would break every non-blob source form.",
  "web/composer/guided/emitters.py:R1:build_step_1_schema_form_turn_from_resolved:ast=body[28]/body[5]/test/values[1]/args[0]": "R1 `prefilled.get(\"path\")` in `build_step_1_schema_form_turn_from_resolved`. `prefilled` is built one line earlier by spreading `deep_thaw(source.options)`, so its keys are exactly the Tier-3 composer-authored option names -- a source plugin that takes no `path` knob (a database or HTTP source) legitimately has none. Absence means \"there is no path to mask\", which is the same no-op as a non-string path. This read guards the blob masking that keeps a deploy directory and OS username off the wire, so it must not raise on the shapes it is there to tolerate.",
  "web/composer/guided/emitters.py:R4:_node_cardinality:ast=body[34]/body[12]/handlers[0]/body[0]/handlers[0]": "R4 `except BaseException as cleanup_exc:` in `_node_cardinality`. This is a cleanup-suppression handler, not a swallow of the operational error: it wraps only `transform.close()` inside the handler for a primary failure, and the enclosing block ends in a bare `raise`, so the primary exception is always propagated. The control that guarantees the failure still surfaces is that `raise` statement; the cleanup exception is not discarded either -- it is recorded on the primary via `primary_exc.add_note(f\"transform.close failed during cardinality inspection: ...\")`, which carries its type into the traceback the operator sees. `BaseException` rather than `Exception` is deliberate and required: a `KeyboardInterrupt` or `SystemExit` raised inside a plugin's `close()` must not be allowed to replace the original failure being propagated, which is exactly what an un-caught cleanup exception during exception handling would do.",
  "web/composer/guided/emitters.py:R5:_structured_output_fields:ast=body[33]/body[4]/body[1]/test/values[0]/operand": "R5 `isinstance(fields, Sequence)` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. `isinstance` against the `Sequence` ABC is required rather than `type(x) is list`: the value is decoded from planner JSON or from a YAML import, so a tuple or any other registered Sequence must be accepted on the same terms as a list. The negative arm skips the query, so this is a shape guard on external data, not a dispatch control over an ELSPETH-owned closed union.",
  "web/composer/guided/emitters.py:R5:_structured_output_fields:ast=body[33]/body[4]/body[1]/test/values[1]": "R5 `isinstance(fields, str | bytes)` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. This is the companion exclusion to the `Sequence` test on the same line: `str` and `bytes` satisfy `Sequence`, so without it a planner that emitted `output_fields: \"score\"` would be iterated character by character and projected as a list of one-character field specs. `isinstance` is the correct form because `str`/`bytes` subclasses must be excluded too.",
  "web/composer/guided/emitters.py:R5:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[0]/test/values[0]/operand": "R5 `isinstance(field, Mapping)` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. Each `field` is an untrusted entry of the planner-authored `output_fields` list; the `Mapping` ABC (not `type(x) is dict`) is right because the value may arrive as any mapping the JSON/YAML decoders produce. The negative arm skips the entry, which is this boundary's declared observe-only contract.",
  "web/composer/guided/emitters.py:R5:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[2]/value/args[0]/values[3]/test/values[0]": "R5 `isinstance(values, Sequence)` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. Guards the enum-values projection against a planner-authored `values` of any shape; the `Sequence` ABC accepts the tuple/list forms the decoders produce, and the false arm yields `[]` rather than raising, per the boundary's observe-only invariant.",
  "web/composer/guided/emitters.py:R5:_structured_output_fields:ast=body[33]/body[4]/body[2]/body[2]/value/args[0]/values[3]/test/values[1]/operand": "R5 `isinstance(values, str | bytes)` in `_structured_output_fields`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. The enclosing function carries `@observation_boundary(tier=3, source_param=\"options\", suppresses=(\"R1\", \"R5\"))`; the analyzer suppressed the reads taken directly off `options` but not this one, because the derivation runs through a list comprehension and tuple unpacking. Companion exclusion to the `Sequence` test in the same expression: without it a planner that wrote `values: \"high\"` would be projected as the enum `[\"h\", \"i\", \"g\", \"h\"]` on a screen the user confirms. `isinstance` is required so `str`/`bytes` subclasses are excluded as well.",
  "web/composer/guided/emitters.py:R5:_wire_schema:names:ast=body[32]/body[6]/body[1]/test/values[0]/operand": "R5 `isinstance(value, Sequence)` in `_wire_schema.names`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. Same closure/taint situation as the `schema.get(key)` read directly above it. The `Sequence` ABC is the right test because the value is decoded from planner JSON or imported YAML and may legitimately be a tuple; the false arm returns `[]`, honouring the enclosing boundary's declared non-raising contract.",
  "web/composer/guided/emitters.py:R5:_wire_schema:names:ast=body[32]/body[6]/body[1]/test/values[1]": "R5 `isinstance(value, str | bytes)` in `_wire_schema.names`. `options` is a `NodeSpec.options` / `OutputSpec.options` mapping: free-form plugin configuration authored through the composer (LLM planner tool calls, or the guided schema_form) which the composer stores verbatim and never validates against a plugin schema -- the plugin does that at execution time. So this is a Tier-3 boundary read of external data, not a defensive read of ELSPETH-owned state. Same closure/taint situation as the sibling `Sequence` test in the same condition. It is the exclusion that stops a scalar `guaranteed_fields: \"customer_id\"` from being iterated into eleven single-character field names on the wire-review screen the user confirms. `isinstance` rather than an exact-type test so `str`/`bytes` subclasses are excluded too.",
  "web/composer/guided/emitters.py:R5:build_step_1_schema_form_turn_from_resolved:ast=body[28]/body[5]/test/values[1]": "R5 `isinstance(prefilled.get(\"path\"), str)` in `build_step_1_schema_form_turn_from_resolved`. The value is a Tier-3 composer-authored option, so its type is not guaranteed by construction; this is boundary validation before the string interpolation that builds the `blob:<ref>` sentinel. `isinstance` rather than `type(x) is str` is correct here because the check is a permission-to-mask test, and a `str` subclass is still a path that must be masked -- the exact-type form would leak an unmasked absolute storage path for such a value, which is the egress this masking exists to prevent.",
  "web/composer/guided/emitters.py:R6:_wire_schema:ast=body[32]/body[5]/body[0]/body[1]/handlers[0]": "R6 `except ValueError: continue` around `FieldDefinition.parse(field)` in `_wire_schema`. The swallow is safe because it is not the control that reports a malformed schema. That control is `_sweep_schema_syntax` in `web/composer/state.py`, which runs `get_raw_schema_config` over every source, node and output's options during `CompositionState.validate()` and appends a high-severity `contract_config_invalid` error for exactly the specs `FieldDefinition.parse` rejects here. `build_step_4_wire_turn` renders that same validation result alongside this projection -- `\"blockers\": [error.to_dict() for error in validation.errors]` and `\"can_confirm\": validation.is_valid` -- so a field this projector drops still reaches the user as a blocker and still holds Continue closed. Pinned by `tests/unit/web/composer/test_validation_error_codes.py::TestSchemaContractFamilyCodes::test_contract_config_parse_failure_carries_code`. Re-raising here would instead crash the whole wire review over one bad field, replacing a reviewable blocker with a 500. `_wire_schema` is declared `@observation_boundary`, whose contract is precisely to observe what it can read and stay silent about what it cannot.",
  "web/composer/guided/emitters.py:R8:build_step_2_schema_form_turn:ast=body[25]/body[4]/value": "R8 `prefilled.setdefault(\"on_write_failure\", \"discard\")` in `build_step_2_schema_form_turn`. `prefilled` is a dict this function has just built itself, seeded with a literal and then updated from `deep_thaw(prefilled_options)` (staged sink options from a chat resolution, which are Tier-3 and may or may not carry the knob). `setdefault` is the operative semantics, not a missing-key dodge: `on_write_failure` is a required-no-default sink knob, so the form must arrive carrying a value or its Continue stays disabled, while an operator or planner choice already present in the staged options must win over the seed. The absent case is a first-class state meaning \"nothing has chosen a write-failure route yet\", and `\"discard\"` is the composer's declared route for that. Converting to `if \"on_write_failure\" not in prefilled: ...` would be a pure restatement -- this file spells no such idiom anywhere else -- so it would move the finding without improving the code. Note this seeds a form the user confirms; it does not author pipeline structure, so it is outside the server-side-authoring ban.",
  "web/composer/guided/stage_subjects.py:R1:constraint_from_dict:ast=body[47]/body[2]/value": "R1 `value.get(\"kind\")` in `constraint_from_dict`. `value` is a persisted deferred-intent record being decoded on read-back, which is Tier-3 at this boundary; the line above already refuses anything that is not an exact dict (`if type(value) is not dict: raise InvariantError`). The `.get()` cannot hide a missing key here because the function is fail-closed by construction: an absent `kind` yields `None`, `None` matches none of the `if kind == ...` arms, and control reaches the terminal `raise InvariantError(\"Deferred constraint kind is unsupported\")`. A missing key and an unrecognised key therefore land on the same refusal, and every arm that does match immediately re-reads the record through `_require_exact_dict` with an exact key set. Using `value[\"kind\"]` would swap that one clear domain error for a `KeyError` carrying no contract language.",
  "web/composer/guided/stage_subjects.py:R1:subject_from_dict:ast=body[27]/body[2]/value": "R1 `value.get(\"kind\")` in `subject_from_dict`. `value` is a persisted deferred-intent record being decoded on read-back, which is Tier-3 at this boundary; the line above already refuses anything that is not an exact dict (`if type(value) is not dict: raise InvariantError`). The `.get()` cannot hide a missing key here because the function is fail-closed by construction: an absent `kind` yields `None`, `None` matches none of the `if kind == ...` arms, and control reaches the terminal `raise InvariantError(\"Deferred subject kind is unsupported\")`. A missing key and an unrecognised key therefore land on the same refusal, and every arm that does match immediately re-reads the record through `_require_exact_dict` with an exact key set. Using `value[\"kind\"]` would swap that one clear domain error for a `KeyError` carrying no contract language.",
  "web/composer/pipeline_proposal.py:R1:is_owned_composition_state_authority:ast=body[37]/body[1]/value/left": "R1 `value.get(\"authority_kind\")` in `is_owned_composition_state_authority`. This function is a predicate whose entire job is to answer \"does this arbitrary mapping name the closed private owned-state seam?\" for values that are frequently NOT that seam -- it is the discriminator callers use before committing to the owned-state path. A mapping without `authority_kind` is the expected negative case, so absence is a first-class value that must return `False`, not raise. Contrast the sibling site in `restore_owned_composition_state_authority`, where the key IS guaranteed by an immediately preceding exact-field-set assertion and is consequently read as a member `detached[\"authority_kind\"]` in this same change.",
  "web/composer/pipeline_proposal.py:R1:owned_composition_state_review_arguments:ast=body[47]/body[5]/body[2]/test/left": "R1 `options.get(\"mode\")` in `owned_composition_state_review_arguments`. `options` is composer-authored source configuration (Tier-3, stored verbatim), and `mode` is an optional knob only some source plugins define. Absence is a first-class value meaning \"this source does not declare a mode\", which is correctly not equal to `\"bind_source\"` and so correctly leaves the key alone. The comparison is guarding a `del options[\"mode\"]` immediately below, so reading it as a member would crash the review projection for every source that simply has no `mode` knob.",
  "web/composer/pipeline_proposal.py:R9:owned_composition_state_review_arguments:ast=body[47]/body[5]/body[1]/body[0]/value": "R9 `options.pop(key, None)` in `owned_composition_state_review_arguments`. This is a redaction loop, not a lookup: it iterates the fixed tuple `(\"blob_ref\", \"path\", \"file\", SOURCE_AUTHORING_KEY)` and removes each from a per-source copy of composer-authored options so blob identities and resolver-owned paths never enter the manifest-redactable review projection. The `None` default expresses \"delete if present\", which is the operative semantics -- a source that never carried a `blob_ref` has nothing to redact, and that is the common case, not an anomaly. There is no missing key being hidden: the post-condition this protects is that none of those keys REMAINS, and `pop` with a default establishes it for every input. Raising on an absent key would make the redaction fail closed against the very sources that were already safe.",
  "web/composer/tutorial_service.py:R1:_tutorial_launch_blocker:ast=body[49]/body[10]/test/values[1]/left": "R1 `llm_nodes[0].options.get(\"profile\")` in `_tutorial_launch_blocker`. The receiver is a `NodeSpec.options` mapping -- free-form composer-authored plugin configuration stored verbatim and never schema-validated by the composer -- so this is a Tier-3 read of a saved pipeline that may have been edited or may predate the current tutorial profile. The function is a launch BLOCKER check, and the read is fail-closed in the direction that matters: an absent `profile` yields `None`, which is not equal to `tutorial_profile`, so the saved pipeline is refused with `tutorial_profile_unavailable` -- the same refusal as a profile that is present but wrong. Reading it as a member would turn that clean refusal into an unhandled `KeyError` on the launch path. This is server-side validation/rejection of saved state, not a tutorial-special code branch: per ADR-031 the tutorial runs the same backend, and nothing here normalises, repairs, or short-circuits anything for the tutorial's benefit.",
  "web/composer/yaml_importer.py:R1:_collector_nodes_from_runtime_lists:ast=body[35]/body[3]/body[0]/body[4]/value/args[0]/keywords[6]/value/args[0]/args[0]": "R1 `entry.get(\"options\")` in `_collector_nodes_from_runtime_lists`. `entry` is one element of the operator-supplied `collectors` YAML section, reached through `enumerate(_require_sequence(collectors_section, ...))` and `_require_mapping`, so it is Tier-3 import data being normalised at the importer's boundary. A collector that declares no plugin options is ordinary, so absence is a first-class value; the raw value is handed straight to `_optional_mapping(..., f\"{path}.options\")`, which maps `None` to an empty mapping and raises `RuntimeYamlImportError` on a present but non-mapping value, so omission and malformation stay distinct. Decorating this function was tried and measured: it clears no findings, because the walk does not carry taint from `collectors_section` across the sequence-then-mapping helper pair -- so a decorator here would assert a suppression that suppresses nothing while adding a test obligation.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[2]/body[0]/value": "R1 `entry.get(\"routes\")` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. `routes` is a gate-only optional field: most node kinds never carry it, so absence is a first-class value meaning \"this gate declares no route map\". A present value is validated by `_string_mapping`, so omission and malformation remain distinguishable.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[2]/body[1]/value": "R1 `entry.get(\"fork_to\")` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. `fork_to` is present only on fork gates, so absence is a first-class value meaning \"not a fork gate\"; a present value is validated by `_string_tuple`, which refuses a non-sequence or a non-string element.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[3]/body[2]/value": "R1 `entry.get(\"branches\")` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. In this arm `branches` is an optional coalesce field whose absence has a defined meaning -- the importer derives the node's input from the first branch when it is omitted -- so it must be probed rather than required.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[4]/body[2]/value/args[0]": "R1 `entry.get(\"branches\")` passed to `_row_union_branches` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. The probe hands the raw value straight to `_row_union_branches`, itself a declared Tier-3 boundary (`@trust_boundary(tier=3, source_param=\"value\", ...)`), which raises `RuntimeYamlImportError` unless it finds at least two unique non-blank branch names. A missing `branches` therefore reaches the same refusal as a malformed one rather than being silently accepted -- the `.get()` defers the error to the validator that can name it precisely.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[5]/value/args[0]/args[0]": "R1 `entry.get(\"options\")` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. A node that declares no plugin options is ordinary, so absence is a first-class value; the value is immediately routed through `_optional_mapping(..., f\"{path}.options\")`, which maps `None` to an empty mapping and raises `RuntimeYamlImportError` on a present but non-mapping value.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[6]/value": "R1 `entry.get(\"expected_output_count\")` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. This is an aggregation-only optional field; absence is a first-class value meaning \"this aggregation makes no fixed-count claim\", which the composer carries as `None`. When it IS present the next line refuses anything that is not a non-boolean integer.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[8]/value/args[0]/keywords[13]/value/body/args[0]/args[0]": "R1 `entry.get(\"trigger\")` in `_nodes_from_runtime_list` -- the value handed to `_optional_mapping` once that presence test has passed. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. `trigger` is an aggregation-only optional batch-trigger config, so absence is a first-class value the conditional expression turns into `None`; a present value is validated by `_optional_mapping`, which raises `RuntimeYamlImportError` on a non-mapping.",
  "web/composer/yaml_importer.py:R1:_nodes_from_runtime_list:ast=body[34]/body[2]/body[8]/value/args[0]/keywords[13]/value/test/left": "R1 `entry.get(\"trigger\")` in `_nodes_from_runtime_list` -- the presence test that chooses between a validated trigger mapping and `None`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. `trigger` is an aggregation-only optional batch-trigger config, so absence is a first-class value the conditional expression turns into `None`; a present value is validated by `_optional_mapping`, which raises `RuntimeYamlImportError` on a non-mapping.",
  "web/composer/yaml_importer.py:R1:_reject_unimportable_sections:ast=body[19]/body[6]/value/args[0]/elt/values[2]/value": "R1 `_DECLINED_SECTION_REASONS.get(section, 'not supported by the composer')`. The rule misfires here: the receiver is not external data at all. `_DECLINED_SECTION_REASONS` is a module-level constant dict literal that ELSPETH owns, mapping a section name to the ratified prose explaining why the composer declines it; the only external thing on this line is `section`, which is a lookup ARGUMENT, not the mapping being read. The default is the whole point of the call -- a modelled `ElspethSettings` section with no hand-written reason still gets refused BY NAME with the generic explanation, which is exactly the \"declining silently is not legitimate\" contract this function exists to enforce. A membership read would mean adding a new settings section could crash the importer instead of producing its refusal message. No trust-boundary decorator can or should clear this: `@trust_boundary` would assert the constant is external, which is false.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[0]/value/args[0]": "R1 `doc.get(\"transforms\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A pipeline that declares no `transforms` section is entirely legal, so absence is a first-class value, and the receiving helper is written to take it: `_nodes_from_runtime_list` opens with `if section is None: return []`, then `_require_sequence(section, section_name)` refuses a section that IS present but not a list. Omission and malformation are therefore distinguished -- the first imports as empty, the second raises `RuntimeYamlImportError`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[1]/value/args[0]": "R1 `doc.get(\"gates\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A pipeline that declares no `gates` section is entirely legal, so absence is a first-class value, and the receiving helper is written to take it: `_nodes_from_runtime_list` opens with `if section is None: return []`, then `_require_sequence(section, section_name)` refuses a section that IS present but not a list. Omission and malformation are therefore distinguished -- the first imports as empty, the second raises `RuntimeYamlImportError`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[2]/value/args[0]": "R1 `doc.get(\"row_unions\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A pipeline that declares no `row_unions` section is entirely legal, so absence is a first-class value, and the receiving helper is written to take it: `_nodes_from_runtime_list` opens with `if section is None: return []`, then `_require_sequence(section, section_name)` refuses a section that IS present but not a list. Omission and malformation are therefore distinguished -- the first imports as empty, the second raises `RuntimeYamlImportError`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[3]/value/args[0]": "R1 `doc.get(\"aggregations\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A pipeline that declares no `aggregations` section is entirely legal, so absence is a first-class value, and the receiving helper is written to take it: `_nodes_from_runtime_list` opens with `if section is None: return []`, then `_require_sequence(section, section_name)` refuses a section that IS present but not a list. Omission and malformation are therefore distinguished -- the first imports as empty, the second raises `RuntimeYamlImportError`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[4]/value/args[0]": "R1 `doc.get(\"coalesce\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A pipeline that declares no `coalesce` section is entirely legal, so absence is a first-class value, and the receiving helper is written to take it: `_nodes_from_runtime_list` opens with `if section is None: return []`, then `_require_sequence(section, section_name)` refuses a section that IS present but not a list. Omission and malformation are therefore distinguished -- the first imports as empty, the second raises `RuntimeYamlImportError`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[5]/value/args[0]": "R1 `doc.get(\"collectors\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. Collectors are an optional pipeline section, so absence is a first-class value; the receiving `_collector_nodes_from_runtime_lists` accepts a `None` section and validates a present one. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[5]/value/args[1]": "R1 `doc.get(\"scopes\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. `scopes` is the companion optional section to `collectors`, passed to the same `_collector_nodes_from_runtime_lists` boundary, which pairs the two and refuses an inconsistent combination. Absence is legal and means the document declares no collector scopes. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[10]/value/elts[6]/value/args[0]": "R1 `doc.get(\"queues\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. `queues` is optional; `_queues_from_runtime_mapping` is itself a declared Tier-3 boundary that returns `[]` for an absent section and raises `RuntimeYamlImportError` on a present but non-mapping one, so omission and malformation stay distinguishable. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[13]/body[1]/value/keywords[0]/value/args[0]/values[0]": "R1 `raw_metadata.get(\"name\")` in `composition_state_from_runtime_yaml`. `raw_metadata` is the operator-supplied `metadata` section of the pasted YAML, reached inside the `isinstance(..., Mapping)` guard, so it is Tier-3 external data. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A metadata block that sets only one of the two fields is ordinary, so absence is a first-class value: the `or metadata.name` fallback on the same expression keeps the default `PipelineMetadata` value, which is the documented behaviour for an unspecified field. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[13]/body[1]/value/keywords[1]/value/args[0]/values[0]": "R1 `raw_metadata.get(\"description\")` in `composition_state_from_runtime_yaml`. `raw_metadata` is the operator-supplied `metadata` section of the pasted YAML, reached inside the `isinstance(..., Mapping)` guard, so it is Tier-3 external data. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A metadata block that sets only one of the two fields is ordinary, so absence is a first-class value: the `or metadata.description` fallback on the same expression keeps the default `PipelineMetadata` value, which is the documented behaviour for an unspecified field. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[13]/test/args[0]": "R1 `doc.get(\"metadata\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. `metadata` is an optional section; when it is absent the importer keeps the default `PipelineMetadata()` constructed on the line above. Absence is therefore a first-class value with a defined outcome, not a hidden failure. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[14]/value/keywords[3]/value/args[0]": "R1 `doc.get(\"sinks\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. `sinks` is optional at import; `_outputs_from_runtime_sinks` is itself a declared Tier-3 boundary that treats an absent section as an empty output tuple and raises `RuntimeYamlImportError` via `_require_mapping` on a present but non-mapping one, keeping omission and malformation distinct. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[6]/value": "R1 `raw_sources = doc.get(\"sources\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. A document may spell its sources either as the plural `sources` mapping or as the singular `source` shorthand this importer expands, so `sources` being absent is a first-class value the very next line acts on. The value is then routed through `_optional_mapping(raw_sources, \"sources\")`, which refuses a present-but-non-mapping section. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R1:composition_state_from_runtime_yaml:ast=body[38]/body[7]/test/values[1]/left": "R1 `doc.get(\"source\")` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. This is the singular-to-plural normalisation: `source` is a legacy shorthand most documents do not use, so absence is the common case and means \"no singular shorthand to expand\". The guard only rewrites `raw_sources` when the shorthand is actually present and the plural form is actually absent, after which the result goes through `_optional_mapping`. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R5:_nodes_from_runtime_list:ast=body[34]/body[2]/body[3]/body[4]/test": "R5 `isinstance(branches, Mapping)` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. Coalesce branches are authored in YAML either as a name-to-input mapping or as a plain list of inputs, and this test is the discriminator between those two untrusted spellings. The `Mapping` ABC is required rather than `type(x) is dict` because the value comes from `yaml.safe_load` and the else-branch is a live path that indexes the value as a sequence -- an exact-type test would send a non-dict mapping down the sequence arm and mis-import it.",
  "web/composer/yaml_importer.py:R5:_nodes_from_runtime_list:ast=body[34]/body[2]/body[3]/body[6]/test": "R5 `isinstance(branch_spec, Mapping)` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. Same untrusted mapping-or-list discrimination as the `branches` test above, applied to the normalised branch spec when deriving the coalesce node's input. Both arms are live -- the true arm reads `.values()`, the false arm indexes `[0]` -- so the `Mapping` ABC is the correct test and an exact-type check would misroute a non-dict mapping.",
  "web/composer/yaml_importer.py:R5:_nodes_from_runtime_list:ast=body[34]/body[2]/body[4]/body[3]/value/test": "R5 `isinstance(branch_spec, Mapping)` in the `first_branch_input` expression of `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. `branch_spec` is the return of `_row_union_branches`, whose declared return type is the union `dict[str, str] | tuple[str, ...]` -- the two shapes a row_union's branches may take in untrusted YAML. This test picks `next(iter(branch_spec.values()))` versus `branch_spec[0]` accordingly; both arms are live, so the ABC test is required and an exact-type form would misroute.",
  "web/composer/yaml_importer.py:R5:_nodes_from_runtime_list:ast=body[34]/body[2]/body[7]/test/values[1]/values[0]/operand": "R5 `isinstance(expected_output_count, int)` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. The pair forms one boundary validation of an untrusted YAML scalar: the `int` test admits the value and the `bool` test excludes `True`/`False`, which YAML produces for `yes`/`no` and which Python would otherwise accept as `int`. An aggregation claiming an expected output count of `True` is malformed, and the false arm raises `RuntimeYamlImportError` rather than coercing it. `isinstance` is correct on both halves so integer and boolean subclasses are classified the same way.",
  "web/composer/yaml_importer.py:R5:_nodes_from_runtime_list:ast=body[34]/body[2]/body[7]/test/values[1]/values[1]": "R5 `isinstance(expected_output_count, bool)` in `_nodes_from_runtime_list`. `_nodes_from_runtime_list` is decorated `@trust_boundary(tier=3, source=\"web-authored pipeline YAML node section (untrusted import payload)\", source_param=\"section\", suppresses=(\"R1\", \"R5\"))`. `entry` is one element of that same untrusted section, reached via `enumerate(_require_sequence(section, section_name))` and then `entry = _require_mapping(raw_entry, path)`, so it is Tier-3 YAML the importer is validating at its boundary. The analyzer's intra-procedural walk does not carry the taint across that sequence-then-mapping helper pair, which is why the decorator does not cover this site. The pair forms one boundary validation of an untrusted YAML scalar: the `int` test admits the value and the `bool` test excludes `True`/`False`, which YAML produces for `yes`/`no` and which Python would otherwise accept as `int`. An aggregation claiming an expected output count of `True` is malformed, and the false arm raises `RuntimeYamlImportError` rather than coercing it. `isinstance` is correct on both halves so integer and boolean subclasses are classified the same way.",
  "web/composer/yaml_importer.py:R5:_row_union_branches:ast=body[32]/body[0]/orelse[2]/body[0]/test/values[0]/operand": "R5 `isinstance(raw_branch, str)` in `_row_union_branches`. The function is declared `@trust_boundary(tier=3, source=\"web-authored pipeline YAML row_union branches value (untrusted import payload)\", source_param=\"value\", suppresses=(\"R1\", \"R5\"))`; this site sits in the sequence arm, where `raw_branch` is an element of `_require_sequence(value, path)` -- still the same untrusted YAML value, but reached through a helper call the analyzer's intra-procedural walk does not carry. It is genuine boundary validation: YAML happily yields integers, nulls and nested lists as branch entries, and the false arm raises `RuntimeYamlImportError(f\"{path}[{index}] must be a non-empty string\")`. Pinned by `tests/unit/web/composer/test_yaml_importer.py::test_row_union_branches_rejects_non_string_branch_entry`.",
  "web/composer/yaml_importer.py:R5:_row_union_branches:ast=body[32]/body[1]/value/test": "R5 `isinstance(branches, Mapping)` in `_row_union_branches`. Same declared Tier-3 boundary as above (`source_param=\"value\"`). `branches` is the local union this function has just built from the untrusted value -- `dict[str, str]` from the mapping arm, `tuple[str, ...]` from the sequence arm -- and this test picks how to extract the branch names for the uniqueness check (`tuple(branches)` over a mapping's keys, versus the tuple itself). Both arms are live, so the `Mapping` ABC is the correct discriminator; `type(x) is dict` would send the mapping arm's value down the tuple path and skip the duplicate-branch refusal that guards row_union correctness.",
  "web/composer/yaml_importer.py:R5:composition_state_from_runtime_yaml:ast=body[38]/body[13]/test": "R5 `isinstance(doc.get(\"metadata\"), Mapping)` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. This is boundary shape validation of an optional YAML section before it is coerced into `PipelineMetadata`. The `Mapping` ABC rather than `type(x) is dict` is correct because the value comes from `yaml.safe_load` and must be accepted on the same terms as any mapping the loader can produce. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R5:composition_state_from_runtime_yaml:ast=body[38]/body[9]/body[0]/test/values[0]/operand": "R5 `isinstance(source_name, str)` in `composition_state_from_runtime_yaml`. `doc` is the operator/web-pasted pipeline YAML document: `composition_state_from_runtime_yaml` is decorated `@trust_boundary(tier=3, source=\"operator-supplied runtime pipeline YAML import request\", source_param=\"pipeline_yaml\", suppresses=(\"R1\", \"R5\"))`, and `doc` is that parameter after `_reject_yaml_aliases`, `yaml.safe_load`, and `_require_mapping(parsed, \"pipeline YAML\")`. Every read here is boundary normalisation of untrusted YAML. `source_name` is a KEY of the operator-supplied `sources` mapping, and YAML permits non-string mapping keys (`1:`, `true:`, a nested sequence), so this is genuine boundary validation of untrusted input, not a defensive check on owned state -- the false arm raises `RuntimeYamlImportError(\"sources keys must be non-empty strings\")`. `isinstance` rather than an exact-type test is right because a `str` subclass is a perfectly usable source name and should not be refused. The decorator does not suppress it because `parsed = yaml.safe_load(pipeline_yaml)` is assigned inside the `try:` block that maps parser failure to `RuntimeYamlImportError`, and the tier_model dataflow walk does not propagate taint out of a `try:` body. This was pinned by probe, not assumed: hoisting that one assignment above the `try:` (leaving `safe_load` and `_require_mapping` untouched) clears 15 of these sites, while replacing `parsed` with a bare alias of `pipeline_yaml` inside the `try:` clears none. Restructuring the function to satisfy the walk would reshape working code around an analyzer limitation and would drop the parse-error mapping, so the code stays as written.",
  "web/composer/yaml_importer.py:R9:_source_from_runtime_entry:ast=body[33]/body[4]/value": "R9 `options.pop(\"on_validation_failure\", None)` in `_source_from_runtime_entry`. The enclosing function is a declared Tier-3 boundary (`@trust_boundary(tier=3, source=\"web-authored pipeline YAML source entry (untrusted import payload)\", source_param=\"entry\", ...)`), but note R9 is not a rule any decorator can suppress -- this stands on the code. Runtime YAML spells `on_validation_failure` in two places: as a source-entry field and, historically, inside the plugin `options` block. This `pop` is the migration of the second spelling, and its two jobs both require the default: recover the value when it is there, and REMOVE it from `options` so a non-plugin knob does not survive into `SourceSpec.options`. A source using the modern entry-level spelling has no such key, which is the common case, so the absent branch is a first-class value (`None`) meaning \"nothing to migrate\". Nothing is hidden downstream: the recovered value is fed to `canonicalize_source_validation_failure`, the single owner of the missing/empty-to-`discard` fold (elspeth-bcd7051143), so an absent or malformed setting resolves to the explicit `discard` route rather than being dropped. NOTE: I filed an observation that when BOTH spellings are present and DISAGREE, the entry-level value silently wins and the options spelling vanishes -- that is a real defect in this line's neighbourhood, but it is a behaviour change (refuse the conflict) rather than a tier-model fix, so it is tracked rather than made here."
}
