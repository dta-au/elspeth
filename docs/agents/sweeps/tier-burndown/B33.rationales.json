{
  "web/composer/service.py:R2:_capture_composer_llm_completion_fields:ast=body[127]/body[2]/value": "`choices = getattr(response, \"choices\", missing)` reads the LiteLLM/OpenAI-shaped completion object returned by the provider SDK — Tier-3 data whose class ELSPETH does not define and whose attribute set differs across providers, proxies, and SDK versions. The default is not a value: `missing = object()` is a locally created sentinel that can never collide with a legitimate provider payload (including `None`, which is a MEANINGFUL provider value for `content`), so nothing is coerced or defaulted into the composition. The very next statements test the sentinel and convert its presence into `_MalformedLLMResponseError` with only `admit_llm_provider_metadata`'s already-admitted provider facts attached, which `_admit_composer_llm_completion` propagates. A bare `response.choices` would raise `AttributeError` — an untyped crash carrying the provider object's repr into the compose loop's error paths, which is exactly what this function exists to prevent: it is the single seam where the provider object is read and then discarded in favour of the owned `_AdmittedAssistantMessage` / `_AdmittedLLMProviderMetadata` records. A `@trust_boundary` cannot help here: `BoundaryRule` admits only R1 and R5, and the tier_model derivation walk roots an `ast.Call` at `Call.func`, so a `getattr(response, ...)` result is never derived from `response` in any case.",
  "web/composer/service.py:R5:_capture_composer_llm_completion_fields:ast=body[127]/body[3]/value/test/values[0]": "`choices[0] if isinstance(choices, list | tuple) and choices else None` subscripts a Tier-3 provider-supplied value. This read runs BEFORE the raising validation below because `admit_llm_provider_metadata` needs whatever `choice` and `message` the provider actually returned in order to build the metadata that the `_MalformedLLMResponseError` then carries — the error must not be raised without its provider facts. `isinstance` (not `type(x) is list`) is required: SDKs return `list` subclasses and pydantic sequence wrappers for `choices`, and an exact-type test would classify a perfectly good response as malformed and fail the user's composition closed. The guard protects only against subscripting a non-sequence; the authoritative rejection is the `choices is missing or not isinstance(choices, list | tuple)` raise immediately below, so this expression can never admit a malformed batch on its own.",
  "web/composer/service.py:R2:_capture_composer_llm_completion_fields:ast=body[127]/body[4]/value/body": "`message = getattr(choice, \"message\", missing) if choice is not None else missing` reads one element of the Tier-3 provider `choices` sequence, whose element class ELSPETH does not define. As with the `choices` probe, the default is the locally created `missing = object()` sentinel rather than a substitute value, so an absent `message` cannot be mistaken for a real one; `if message is missing: raise _MalformedLLMResponseError(\"LLM response choice carries no message\", ...)` a few statements below is the explicit rejection. Passing the sentinel-free `None if message is missing else message` into `admit_llm_provider_metadata` keeps the sentinel from escaping this function. Reading `choice.message` directly would raise `AttributeError` and leak the provider object's repr into the compose loop instead of the typed, metadata-bearing malformed-response error.",
  "web/composer/service.py:R5:_capture_composer_llm_completion_fields:ast=body[127]/body[6]/test/values[1]/operand": "`if choices is missing or not isinstance(choices, list | tuple)` is the authoritative Tier-3 shape rejection for the provider's `choices` field, and it RAISES `_MalformedLLMResponseError` — it hides no bug, it reports one. `isinstance` is the correct test rather than `type(choices) is list`: provider SDKs return `list` subclasses and pydantic sequence wrappers, and an exact-type test would reject every such well-formed response as malformed. The guard is what lets everything downstream of this function treat the tool batch as a plain `tuple` of admitted calls.",
  "web/composer/service.py:R2:_capture_composer_llm_completion_fields:ast=body[127]/body[9]/value": "`content = getattr(message, \"content\", missing)` probes the Tier-3 provider message object for the assistant text. Absence and `None` must be distinguished here — `content is None` is the NORMAL provider shape for a pure tool-call turn, while a missing attribute means the object is not a completion message at all — so the `missing = object()` sentinel is load-bearing rather than a hidden default. The check on the next statement, `if content is missing or (content is not None and type(content) is not str)`, raises `_MalformedLLMResponseError` for both anomalies and admits only `None` or an exact `str` into the owned `_AdmittedAssistantMessage`. A bare attribute read would raise `AttributeError` and lose that three-way distinction.",
  "web/composer/service.py:R2:_capture_composer_llm_completion_fields:ast=body[127]/body[11]/value": "`tool_calls = getattr(message, \"tool_calls\", missing)` probes the Tier-3 provider message object for the tool batch. As with `content`, absence and `None` are different facts — `None`/empty is the normal shape for a text-only assistant turn, a missing attribute means the object is not a completion message — and `missing = object()` is a locally created sentinel, not a substituted value, so no fabricated tool batch can be produced. The next statement raises `_MalformedLLMResponseError` on the sentinel or on a non-sequence, and only then is `tuple(tool_calls or ())` handed to `_admit_captured_composer_llm_completion` for per-call admission. This function is the composer's sole seam for reading the raw provider object, so an untyped `AttributeError` here would escape into the compose loop instead of the metadata-bearing malformed-response error the phase owner is written to handle.",
  "web/composer/service.py:R5:_capture_composer_llm_completion_fields:ast=body[127]/body[12]/test/values[1]/values[1]/operand": "`if tool_calls is missing or (tool_calls is not None and not isinstance(tool_calls, list | tuple))` is the authoritative Tier-3 shape rejection for the provider's tool batch, and it RAISES `_MalformedLLMResponseError` rather than defaulting. `isinstance` is required rather than `type(tool_calls) is list`: SDKs return `list` subclasses and pydantic sequence wrappers for `tool_calls`, and an exact-type test would reject well-formed batches as malformed and silently strip the LLM's tool calls — a composer-invariant violation, since the planner's tool calls ARE the authoring work. `None` is admitted explicitly as the text-only-turn shape and normalised by `tuple(tool_calls or ())`.",
  "web/composer/service.py:R5:_tool_outcome_is_interpretation_review_handoff:ast=body[145]/body[1]/test/operand": "`if not isinstance(response, ToolResult)` discriminates the documented CLOSED sum type `_ToolOutcomeResponse = ToolResult | Mapping[str, Any] | None` declared on `_ToolOutcome` — union dispatch between three real producer variants (`ToolResult` from `execute_tool`, a serialized `Mapping` envelope from the `request_advisor_hint` interception path, `None` for argument-error and plugin-crash paths), not a defensive shape guard on a guaranteed type. `isinstance` must NOT be narrowed to `type(response) is ToolResult` here: `ToolResult` is deliberately SUBCLASSED by test doubles that ride the real compose loop as `_ToolOutcome.response` — `_StrayToolResult` in `tests/property/web/composer/test_compose_loop_invariants.py` and `tests/integration/pipeline/test_composer_llm_eval_characterization.py`, `_NonCanonicalizableResult` in `tests/unit/web/composer/test_compose_loop_audit_wiring.py` — and an exact-type test would route every such subclass down the \"not a tool result\" arm, silently changing the handoff verdict for a value that IS a tool result.",
  "web/composer/service.py:R5:_tool_outcome_is_interpretation_review_handoff:ast=body[145]/body[3]/test/operand": "`if not isinstance(data, Mapping)` guards `ToolResult.data`, which is declared `Any` because it is the open, per-tool discovery payload — every tool handler writes its own shape there, and `ToolResult.__post_init__` deep-freezes it, so a dict arrives as `MappingProxyType`. `Mapping` is an abstract base class, so `type(data) is dict` is not available as a replacement: it would return False for the frozen `MappingProxyType` this field actually holds, making `_tool_batch_staged_terminal_interpretation_review_handoff` report False for every genuine pending-review batch. Its sole caller in `_compose_loop`'s classifier would then treat a user-action boundary as another model-planning step, looping instead of completing the handoff and letting the model re-surface the same review. The check is union discrimination over an `Any`-typed open payload, and its False arm is a correct \"this tool did not stage a review handoff\" answer, not a swallowed error.",
  "web/composer/service.py:R5:_tool_batch_staged_terminal_interpretation_review_handoff:ast=body[146]/body[2]/body[2]/test/values[0]": "`if isinstance(response, ToolResult) and not response.success` reads the success flag on the `ToolResult` arm of the closed sum type `_ToolOutcomeResponse = ToolResult | Mapping[str, Any] | None`; the `Mapping` and `None` arms carry no `success` attribute, so the `isinstance` IS the union dispatch that makes the attribute read legal, not a shape guard hiding a bug. It must stay `isinstance` rather than `type(response) is ToolResult`: `ToolResult` is deliberately subclassed by test doubles that flow through this exact path (`_StrayToolResult` in `tests/property/web/composer/test_compose_loop_invariants.py` and `tests/integration/pipeline/test_composer_llm_eval_characterization.py`), and an exact-type test would skip the `not response.success` check for them — admitting a FAILED tool result into the \"clean pending-review suffix\" verdict and staging a terminal review handoff on a batch that actually failed.",
  "web/composer/service.py:R1:_freeform_planner_conversation_context:ast=body[162]/body[3]/body[1]/value": "`authorship = history_message.get(COMPOSER_HISTORY_USER_AUTHORED_KEY)` reads a key declared `_elspeth_user_authored: NotRequired[Literal[True]]` on the `ComposerHistoryMessage` TypedDict. Absence is a first-class VALUE here, not a missing-key bug, and it is the DOMINANT case: assistant rows and every history row written before the marker existed carry no marker, and the immediately following `if authorship is None: continue` skips them. A membership read would also be actively harmful as a rejection: only rows that are present AND `True` proceed, and the very next statement raises `InvariantError` on any other value, so a present-but-malformed marker already crashes rather than being silently accepted. The two sibling reads in this loop (`role`, `content`) WERE converted to membership form in this same change because they are REQUIRED TypedDict keys; this one cannot be, because `COMPOSER_HISTORY_USER_AUTHORED_KEY` is declared `Final[str]` rather than a literal type and mypy rejects a TypedDict subscript with a non-literal key. Note the loop's `type(history_message) is not dict` guard above already rejects any entry that is not an exact dict.",
  "web/composer/service.py:R5:ComposerServiceImpl:_serialize_response_via_walker:ast=body[171]/body[5]/body[5]/body[1]/test": "`if isinstance(response, Mapping)` is union dispatch across the closed sum type `_ToolOutcomeResponse = ToolResult | Mapping[str, Any] | None` declared on `_ToolOutcome`, with the `None` arm already excluded by the enclosing `outcome.error_class is None` guard. The two live arms come from DISTINCT producers and are not interchangeable: a `Mapping` is the already-serialized `request_advisor_hint` interception envelope built outside `execute_tool` (it takes `deep_thaw`), while a `ToolResult` is every other path (it takes `to_dict`). `Mapping` is an abstract base class, so no `type(x) is C` form exists for it, and the envelope arrives deep-frozen as `MappingProxyType` — an exact-type test against `dict` would send the advisor envelope down the `cast(ToolResult, response)` branch and crash the redaction walker on a mapping with no `to_dict`. The False arm is a real producer variant, not a fall-through error path.",
  "web/composer/service.py:R4:ComposerServiceImpl:_run_advisor_checkpoint:ast=body[171]/body[62]/body[9]/body[2]/handlers[0]": "The broad `except Exception as exc` wraps one attempt of the bounded advisor retry loop and is convert-to-verdict by design: `_call_advisor_with_audit` re-raises typed LLM errors (timeout, auth, transport, malformed) and an advisory CHECKPOINT must degrade into a verdict rather than crash the user's compose turn. It swallows nothing — the handler records `last_exc` and `last_response_unparseable = False`, and every exit from the loop routes through `completed(...)` with an `AdvisorCheckpointVerdict`. The controls that make the swallow safe, by symbol: (1) the `failure_class` classifier below, which pattern-matches `last_exc` against `_unavailable_types` / `_unavailable_names` and FAILS CLOSED to `\"malformed\"` for any unrecognised exception class, so a goal-pressured model cannot slip the gate by provoking an unusual error; (2) `_AdvisorCheckpointComposeDeadlineExpired`, raised BEFORE the try when the shared compose budget expired with no attempted call, so a compose timeout can never be misreported as an advisor verdict; and (3) the deliberate exclusion of provider text from the outcome — only `_ADVISOR_UNAVAILABLE_USER_DETAIL` / `_ADVISOR_MALFORMED_USER_DETAIL` reach `findings_text`, never `exc`'s class name, message, URL, or credentials, which preserves the route-level provider-error redaction policy. Per the R4/R6 asymmetry in the rule's `_check_exception_handler`, only an `ast.Raise` clears R4, and re-raising here would destroy the degrade contract this checkpoint exists to provide.",
  "web/composer/service.py:R5:ComposerServiceImpl:_run_advisor_checkpoint:ast=body[171]/body[62]/body[14]/test/values[1]/values[0]": "`isinstance(last_exc, _unavailable_types)` classifies a caught exception against `(TimeoutError, ConnectionError)`. `isinstance` is the ONLY correct test for an exception-type check: Python's exception model is a hierarchy, and matching the builtin transport/deadline families requires admitting their subclasses exactly as an `except` clause would. `type(last_exc) is TimeoutError` would classify every real `asyncio` deadline and stdlib transport subclass as \"not unavailable\". The classification is deliberately fail-closed either way — the `else` arm assigns `failure_class = \"malformed\"` for parse/shape failures AND for any unrecognised class — so a miss here can only make the gate STRICTER, never weaker. The companion `type(last_exc).__name__ in _unavailable_names` term is exact-name matching for LiteLLM's typed transport classes, which is intentionally tight and deliberately excludes the ambiguous `BadRequestError` / `APIError` / `InternalServerError`.",
  "web/composer/service.py:R9:_render_schema_for_advisor:ast=body[207]/body[9]/body[4]/orelse[0]/value": "`candidate.pop(\"additional_fields_withheld\", None)` clears the withheld-count marker on the trial projection when the last field fits and nothing is actually withheld — an advisor-evidence honesty requirement, since a stale `additional_fields_withheld` would tell the advisor LLM that fields were suppressed when they were not. The key is in fact guaranteed present: the enclosing loop iterates `(fields or [])[:MAX]`, so it runs only when `fields` is non-empty, and non-empty `fields` is exactly the condition under which `projection[\"additional_fields_withheld\"] = len(fields)` was written above; `candidate = dict(projection)` re-copies it each iteration and `remaining == 0` can only occur on the final one. The default is therefore never taken and hides no missing key. It is retained rather than converted to `del` for two reasons: this function is a declared `@observation_boundary` whose invariant is that malformed schemas yield a fixed marker and the render never raises, so a `KeyError` path here would contradict the boundary contract; and the existing coverage in `tests/unit/web/composer/test_advisor_checkpoint.py` exercises only the `remaining > 0` arm, so no test pins the guarantee that would make `del` safe to rely on. Restructuring the copy-then-fix-up into a filtered rebuild is also ruled out: key INSERTION ORDER feeds `str(candidate)`, which is both the `_ADVISOR_SUMMARY_SCHEMA_VALUE_MAX_CHARS` size arithmetic and the rendered evidence bytes.",
  "web/composer/service.py:R9:_render_schema_for_advisor:ast=body[207]/body[10]/body[1]/body[5]/orelse[0]/value": "`candidate.pop(withheld_key, None)` is the contract-field-list twin of the `additional_fields_withheld` clear above, applied per key in `contract_values` (`guaranteed_fields`, `required_fields`, `audit_fields`). Same guarantee: the loop iterates `values[:_ADVISOR_SUMMARY_SCHEMA_MAX_CONTRACT_FIELDS]`, so it runs only for a non-empty `values`, which is exactly when `projection[f\"additional_{key}_withheld\"] = len(values)` was written; the three keys are distinct so one arm's clear cannot remove another's marker. The default is unreachable and hides nothing. Same reasons for keeping `pop` over `del`: the function is a declared `@observation_boundary` contracted not to raise on malformed schemas, and the advisor-checkpoint tests cover only the `remaining > 0` arm. Clearing the marker is the honesty requirement — the advisor must not be told values were withheld when the whole contract list was rendered — and a filtered rebuild would move the key to the end of the dict, changing both the size arithmetic and the rendered evidence bytes, since both are taken from `str(candidate)`."
}
