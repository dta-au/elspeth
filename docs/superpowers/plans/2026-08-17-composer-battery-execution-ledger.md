# SDD ledger — plan: docs/superpowers/plans/2026-08-17-composer-battery.md

Spec: docs/superpowers/specs/2026-08-13-composer-battery-design.md (rev 4, ab043791b) — reachable.
Plan rev 2 = 68d508965. Branch release/0.7.2, shared checkout (no worktree — project rule: work on the release branch, subagents cannot use worktrees; stage/commit by PATHSPEC only; sibling untracked files present: docs/plans/2026-08-17-state-engine-maintenance-dimension-ruling.md, tests/unit/architecture/test_state_engine_maintenance_dimension.py — never sweep them).
Ruling: execute on release/0.7.2 in the shared checkout, not a worktree — project memory says work on the release branch and subagents can't use worktrees; the plan's commits are pathspec-scoped — cost if wrong: a sibling sweep could catch our staged files (mitigated by `git commit -- <paths>`).

## Pre-flight conflict scan (2026-08-17)

Interfaces were dry-run as a whole in a scratch tree before this session (all 24 fences materialised, 93 tests green), so cross-task type consistency is verified by execution, not only by reading.

| Pair / task | Produces vs consumes | Found |
| --- | --- | --- |
| T1→T2 | `Topology`, `TNode`, `topology_from_pipeline`, `topologies_match(expected, observed, *, option_values, option_assertions)` | consistent (dry-run) |
| T2→T3 | `load_scenario`, `validate_canonical_arguments`, `topology_to_dict/from_dict`, `extractor_cross_check`; fork_coalesce scenario.json authored in T2, counted in T3's roster | consistent |
| T2→T5 | `Scenario`, `Floor` consumed by `judge` | consistent |
| T3→T5/T6/T7 | `load_corpus() -> (version, dict)`, `CORPUS_PATH`, `SCENARIOS_DIR` | consistent |
| T4→T5 | `Capture`, `LlmCall`, `PlannerAttempt`, `ToolRow`, `AssistantTurn`, `tool_outcomes`, `Instrument`, `load_capture` | consistent |
| T4→T7 | `Instrument(**instrument).to_dict()`, `INSTRUMENT_KEYS` | consistent |
| T5→T6 | `Score`, `score_from_disk`, `write_score`, `MEASUREMENT_KINDS`/`INSTRUMENT_KINDS` | consistent |
| T5→T7 | `path_from_disk(run_dir).excluded` | consistent |
| T5→T8 | `score_path` for the loop arm | consistent |
| T7↔T8 | `drive_battery.main()` imports `planner_probe` LOCALLY inside main (plan L4076); T8's `planner_probe` imports `Battery` from drive_battery; T7's tests never call main() | no import cycle at import time; ordering 7→8 holds |
| T3↔T7 | `.gitignore` re-include lists `drive_battery.py` in test_ignore_policy params before T7 creates it | `git check-ignore` works on absent paths — fine |
| T3↔T9 | Task 3 file list names README.md; Step 9 says the 3-line stub is written in 3a; T9 replaces it | consistent (stub in 3a) |
| T3 self | file list says "remaining 18 cases" and includes canary + transform_pipeline (seeds) — 18 + fork_coalesce (T2) = 19 | consistent, wording only |
| T7 self | Global Constraints assign the `pyproject.toml` T20 per-file-ignore to Task 7; T7 commit pathspec includes pyproject.toml | consistent |
| All | Global Constraint: `ruff check --fix` + `ruff format` before every commit; commit by pathspec | binds every dispatch |
| Whole-tree gates | plan code has no `getattr`/`hasattr` (grep); `Score.to_dict` reads `self.__dict__` (not a masquerade probe); advisor test uses `MagicMock(spec=CatalogService)` + `monkeypatch.setattr` like sibling tests | no known trap; Task 10 full suite is the net |
| Rubric vs plan | no plan-mandated assert-nothing tests or verbatim duplicated logic blocks found in the fences | clean |

Model plan: implementers sonnet (plan carries complete code; fidelity over 300-500-line modules matters more than the cheapest tier); reviewers sonnet for T1/T2/T3/T4/T6/T8, opus for T5/T7 (largest, subtlest); final whole-branch review on the most capable model.

## Progress
Task 1: review clean (spec ✅, quality Approved). Reviewer's report did NOT arrive as a notification — recovered from its transcript (e43f637f…jsonl); chase transcripts when a reviewer goes quiet >8 min.
Task 1: minor (deferred): battery_topology.py `if conn == "fork": continue` in routes handling is untested/uncommented.
Task 1: minor (deferred): `_MISSING`/`_sig_key` defined after first use (style).
Task 1: minor (deferred): `observed_option_values` exported but no direct test.
Task 1: complete (commits 68d508965..e1c2f07a1, review clean)
Task 2: review — spec ✅, quality Approved, 2 Important (both plan-mandated).
Task 2: Ruling: (I1) "scenario's own canonical_arguments never run through validate_canonical_arguments in Task 2's tests" — no Task 2 change; Task 3's `test_corpus.py` parametrizes `test_scenario_is_sound` (validate_canonical_arguments + floor audit) and `test_canonical_payload_commits_to_the_expected_topology` (build_set_pipeline_candidate) over EVERY present scenario, so the fork_coalesce payload is oracle-checked in-repo one task later — verify at Task 3 review. Cost if wrong: an invalid seed payload survives one task.
Task 2: Ruling: (I2) nested `surface.required`/`surface.classifier_decision` unchecked → KeyError not ValueError — FIX in round 1 (spec: load errors are loud AND list the missing keys; 3-line change, no interface impact). Cost if wrong: none material.
Task 2: minor (deferred): load_scenario does not require ALL closed-vocab criteria keys be present (only rejects unknown ones).
Task 2: fix round 1/5 (1 addressed, 0 open — surface sub-key guard + test; commits ba3a6f9cd..911f5116b)
Task 2: complete (commits e1c2f07a1..911f5116b, review clean; Task 2 test count is now 8, plan said 7)
Task 3: Ruling: dispatch as two implementer rounds — 3a (scaffold: .gitignore re-include, ignore test, corpus parser+tests, README stub, 3 seed cases; sonnet) then 3b (author 16 prompts + scenarios in 4-case batches; opus — authoring valid payloads across 16 example topologies is judgment work) — each with its own task review; the plan itself names 3a/3b as separate commit points. Cost if wrong: one extra review seat.
Task 3a: review clean (spec ✅, Approved; reviewer independently re-ran the 44 tests and the classifier over all 3 prompts). Sibling commit 302db075b (state-engine, not ours) landed between 911f5116b and 3c9adcfed — review packages must use the true parent, not the last ledgered head.
Task 3a: minor (deferred): `_HEADING` regex lowercase-only constraint uncommented.
Task 3a: complete (commits 302db075b..3c9adcfed, review clean) — Task 2 ruling I1 confirmed discharged: test_corpus.py's soundness + anchor tests ran over fork_coalesce (12 corpus tests over 3 cases green).
Task 3b: dispatched (opus) at 3c9adcfed.
Ruling: run Task 4's implementer CONCURRENTLY with 3b — the plan itself says "Tasks 4–8 may proceed from here while 3b continues"; the file sets are disjoint (3b: corpus.md + 16 new scenario dirs; T4: evals/lib/battery_capture.py, tests/.../threadgen.py, test_battery_capture.py, and the fixture dir), and both commit by pathspec. Cost if wrong: an index.lock collision (retry) or a review package that spans the other's commits (packages are cut on true parents).
Task 4: review clean (spec ✅, Approved; projection verified branch-for-branch against _helpers.py:422-486).
Task 4: minor (deferred): parse_instrument closes the key set but does not type-check `read_integrity`/`http_unrecovered` values.
Task 4: minor (deferred): llm_calls coerces missing started_at/finished_at to the string "None" instead of raising.
Task 4: complete (commits 3c9adcfed..2c2228068, review clean)
Task 5: review (opus) — spec ❌ on one item, Needs fixes: 2 Important (both plan-mandated), 11 Minor.
Task 5: Ruling: (I1) `wire_blob_inline_ref` scored as a pipeline mutation → FIX: spec §3 names four DATA tools (create/update/delete_blob + wire_blob_inline_ref); the bind is the second half of the `data_setup_detour` (charged once, on create_blob) and must not be `first_mut` for the schema-before-mutation criterion nor enter the repair/backtrack path. Cost if wrong: a bind-first run under-reports; the durable pair still decides applied-ness elsewhere.
Task 5: Ruling: (I2) `attempted_any` built from the registry (blob tools included) suppresses passivity/decline → FIX: attempted_any and first_mut consider only non-data mutation tools, consistent with the loop's own treatment. Cost if wrong: a passivity hard-class fires on a run that only touched blobs — red was already true via passivity_hits, so no false green either way.
Task 5: minor (deferred): docstring overclaims per-call attribution ("silence is not an option") — soften; option_assertions_hold green key is a dead switch (assertions always folded into topologies_match); auth exclusion evidence prints post_message status even when auth_failed came from elsewhere; transport gated on `budget is None` not "no terminal reason captured" (plan text says budget — judgment call for final review); cost sum has no unknown_calls counterpart; missing advisor_model ⇒ advisor calls silently land in other_text_calls; path_from_disk catches only CaptureError (malformed messages list ⇒ AttributeError escapes); data_rework fires on any update/delete_blob (spec: same invented data); `pytest.raises(Exception, match="instrument")` should be CaptureError; Deviation.to_dict keys not asserted in JSON; path_from_disk success path untested; advisor test passes recorder=None (currency inferred from no-`tools`, not from the audit row).
Task 3b: implementer DONE at 056d9ba5f (4 commits eadc3ffc5, e76f77bab, 482980deb, 056d9ba5f; 124 battery tests green, 60 corpus). Review dispatched (opus) on a pathspec-scoped package (dbe90c986 = Task 5 sits inside the range).
Task 3b: Ruling (provisional, pending review): threshold_gate `option_assertions: []` — gate thresholds live in `condition`, not `options`, and `observed_option_values` reads options only, so a `["gate", key, value]` assertion would be an unmeetable green criterion; accepted as authored, and the gap (spec §2 wants a threshold-value pin) is a PLAN/ORACLE defect to surface to John — a condition-value projection would need a robust (non-string-exact) matcher. Cost if wrong: threshold_gate cannot detect a wrong threshold VALUE (topology/route count still pinned).
Task 3b: Ruling: `variant: "top_k"` (not "settings_top_k.yaml") — matches `_pick_settings_yaml`, brief literal was wrong.
Task 3b: note: explicit_routing + llm_source classify CONVERSATIONAL (still compose loop, non-EXPLICIT) — spec-compliant; recorded truthfully in scenario surface.classifier_decision.
Task 5: fix round 1/5 (2 addressed pending re-review, 0 open; commits 056d9ba5f..776aa6087; scorer tests now 29). Re-review dispatched.
Task 6: dispatched (sonnet) at 776aa6087 while the T5 re-review and 3b review run — T6 consumes T5's interfaces, which the fix did not change.
Task 5: fix round 1/5 re-review: 2 addressed, 0 open, no new breakage. Out-of-scope note: create_blob→wire_blob_inline_ref adds 2 tool-bearing calls but one deviation (by design; unattributed_excess doesn't fire when any class fired — same as Minor 1).
Task 5: complete (commits 482980deb..dbe90c986 + 056d9ba5f..776aa6087, review clean after 1 round)
Task 3b: review (opus) — Needs fixes: 5 Important, 3 Minor. Rulings:
Task 3b: Ruling: (I1 llm_source passthrough artifact) → FIX route (a): relax `validate_canonical_arguments` (Task 2 file, battery_scenario.py:127) to require ≥1 output but allow zero nodes (server accepts source→sink, verified by reviewer probe), drop the passthrough node from llm_source, regenerate expected_topology; add a Task-2 test that a zero-node payload validates and one that zero OUTPUTS still fails. Cost if wrong: a scenario author who forgets nodes gets a 2-node oracle instead of an error — the anchor test still commits it, so it is not silent.
Task 3b: Ruling: (I2 threshold_gate assertion undischarged) → FIX by extending the oracle (Task 1 file): `observed_option_values` synthesises a pseudo-option `condition_literals` (numeric literals parsed from a node's `condition` string, unioned across nodes sharing a key) and `topologies_match` treats an assertion key `condition_literal` as MEMBERSHIP (numeric equality, 1000 == 1000.0); threshold_gate gets `[["gate","condition_literal",1000]]`, schema_contracts_demo `[["gate","condition_literal",500]]`. Spec §1/§2 mandate a threshold pin and "wrong threshold is wrong_shape"; string-exact condition matching would be brittle, literal membership is not. Cost if wrong: an equivalent-but-different literal (`> 999`) reads wrong_shape — surfaced at calibration.
Task 3b: Ruling: (I3 schema_contracts_demo payload discards the stated schema) → FIX: author the six declared fields (`mode: flexible`, per the example) into canonical_arguments; no schema-value assertion (options are not part of the topology oracle) — the stratum is example-faithful in the payload and prompt, value-level schema measurement stays a v1 limitation. Cost if wrong: none material.
Task 3b: Ruling: (I4 template_lookups ≡ openrouter_sentiment topology after inlining) → ACCEPT as authored; record as a named v1 blind spot in spec §1 during Task 9 (compose surface cannot author repo-relative asset files). Cost if wrong: strata table over-claims until the note lands.
Task 3b: Ruling: (I5 deep_routing + error_routing prompts read as gate-or-transform-on_error) → FIX: reword both prompts so the failure-path reading dominates (a check whose failures are ERRORS set aside, not a decision), re-classify, update classifier_decision in lockstep. Cost if wrong: still ambiguous → calibration finds it (its job).
Task 3b: minor (deferred): deep_routing is 16 nodes vs spec's "≤ 12" (spec correction at freeze); transform_pipeline seed payload has one csv sink vs the example's two (prompt agrees with payload; extractor ⊆ passes) — watch at calibration; three cases require a volunteered passthrough node.
Task 6: implementer DONE at 5697f4262 (9 tests; pyproject T20 line added here instead of Task 7 — authorised in dispatch). Review dispatched (opus).
Task 7: dispatched (sonnet) at 5697f4262 concurrently with the T6 review and the 3b fix round — disjoint files; the pyproject T20 line already exists (T7 must not duplicate it).
Task 6: review (opus) — Approved, 2 Important, 6 Minor.
Task 6: Ruling: (I1) `mde_pp` is a one-sample 95% CI half-width, not an MDE (spec §5 conflates them; plan-mandated) → FIX: rename the JSON key to `ci_half_width_pp` and render "95% CI ±X pp"; nothing downstream reads the key yet (T7 driver never reads reports; T9 README not written). Spec §5 wording to be corrected at freeze (ledger for Task 9). Cost if wrong: none material.
Task 6: Ruling: (I2) compare-delta DIRECTION untested → FIX: add a test whose prior round differs (e.g. prior clean 100% vs current 0%) and pin the sign of pooled_delta and by_case_delta.
Task 6: minor (deferred): 3 of 5 degraded reasons untested (unattributed >15%, provider retries >10%, binding drift within round); _compare's own corpus_version refusal unreachable in tests (late-binding guard fires first); --force-compare cannot force a corpus_version mismatch (help text should say so); floors_sha256 refusal message is two digests (fires on --cases roster change too — name the differing case); unknown case dir ⇒ traceback not exit 65; caveat/compare lines hard-code N=5 / ±44 pp.
Task 3b: fix round 1/5 landed cbf65c6fc (4 findings addressed pending re-review; oracle gained `condition_literal` membership assertions; zero-node payloads allowed). Re-review dispatched (opus).
Task 7: implementer DONE at 98c875998 (16 tests; whole battery dir 158 green; pyproject untouched; `Identity` dataclass in brief prose never defined by the fence — fence followed, nothing imports it). Review dispatched (opus).
Task 8: dispatched (sonnet) at 98c875998 concurrently with the T6 fix round, 3b re-review, T7 review — disjoint files.
Task 3b: fix round 1/5 re-review (opus): 4 addressed, 0 open; no Critical/Important breakage. Low: literal regex reads quoted '500' as numeric (permissive direction); `1e3`/`1_000`/`1000.` unparsed (fail-loud direction); battery_scenario docstring "same two checks the parity fixtures get" now stale (parity still requires ≥1 node).
Task 3b: minor (deferred, RECOMMENDED for final wave): test_scenario_is_sound / anchor test never pass a scenario's own option_assertions — the three assertion-bearing scenarios rest on manual probes; one-line close: `topologies_match(..., option_values=observed_option_values(args), option_assertions=sc.option_assertions)` inside test_scenario_is_sound.
Task 3 (3a+3b): complete (commits 302db075b..3c9adcfed, 2c2228068..056d9ba5f (pathspec), 5697f4262..cbf65c6fc; review clean after 1 round)
Task 6: fix round 1/5 landed e90973dba (2 addressed pending re-review).
Task 8: implementer DONE at 0b68b2058 (10 tests; battery dir 169 green). Review dispatched (sonnet). Sibling session is editing docs/agents/recent-code-hints.md — not ours.
Task 7: review (opus) — Approved, 3 Important (all plan-mandated), 8 Minor; 7 route contracts verified against src.
Task 7: Ruling: (I1) canary verdicts never enter the abort streak → FIX: feed canary run verdicts into the same instrument-kind streak so 3 consecutive canary instrument exclusions abort before the tripwire/corpus (measurement kinds still never abort). Spec intent: the canary exists to prove the instrument alive. Cost if wrong: an unlucky canary streak aborts a round that would have run — resumable.
Task 7: Ruling: (I2) `_contained` → `_write_meta` → `_identity` → `system_status()` can issue a live request and escape containment when the status cache is unprimed → FIX: `_identity` never performs I/O — it reads the cached status or records nulls (null bindings already make the report refuse compare, fail-closed); `fire()` primes the cache itself. Test the unprimed path.
Task 7: Ruling: (I3) config errors exit 1 (collides with "aborted by instrument rules") → FIX: credentials mode/shape errors → exit 64; `system_status()` non-200 → `BatteryIdentityError` (mapped like the null-model case), no bare RuntimeError.
Task 7: minor (deferred): firing.json session_id always null (dead field); pagination error site clobbers a prior first cause (use `or` like the other sites); `_local_skill_hash` bare except → silent null (add a source sentinel); `cleanup()` bypasses the step seam (401 dict body ⇒ AttributeError, no timeout handling); `_settle` reads limit=500 no offset (latent cap coupling, unreachable today); `_verdict` dead `case` param; untested branches (preferences mismatch, 429, auth_failed at post, HttpTransportError in step, RequestsClient exception translation, MAX_PAGES exhaustion, >15% flag); `case_flags` written to firing.json but no reader in the report (plan text claims the report renders it — Task 6/9 seam).
Task 6: fix round 1/5 re-review: 2 addressed, 0 open, no breakage. Out-of-scope: plan/spec prose still say "MDE" — Task 9 freeze notes.
Task 6: complete (commits 776aa6087..5697f4262 + 98c875998..e90973dba, review clean after 1 round)
Task 8: review clean (spec ✅, Approved; every dependency contract verified live). minor (deferred): tripwire "not fired" branch untested (unreachable via wrapper); wrapper docstrings say ~25 lines (now ~60 after formatting).
Task 8: complete (commits e90973dba..0b68b2058, review clean)
Sibling commit 02b2a4291 "docs(agents): record the composer-battery conftest sys.path shim as a whole-tree trap" — landed by another session on our conftest; read before Task 10.
minor (deferred, final wave): switch tests/unit/evals/composer_battery/conftest.py from sys.path.insert(0, …) to append per the sibling's hint 02b2a4291 (drive_battery/planner_probe have no same-name modules; report.py could shadow a bare `import report`).
Task 7: fix round 1/5 landed 0fbeef1e0 (3 addressed pending re-review; driver tests 22 + probe 1; battery dir 175). Re-review dispatched (sonnet).
Task 9: dispatched (sonnet) — Steps 1-3 only (runbook, calibration record, spec errata block); Steps 4-5 (live calibration + freeze) are OPERATOR steps — NOT run.
Ruling: spec receives an errata block (MDE→CI half-width; ≤12→16 nodes; template_lookups inlining blind spot; condition_literal gate pins) rather than body rewrites — the spec is rev 4 at ab043791b; a post-implementation errata block is the honest minimal record. Cost if wrong: none.
Task 7: fix round 1/5 re-review: 3 addressed, 0 open, no breakage (7 targeted tests re-run by the re-reviewer). Out-of-scope: `_prime_status` retries on every run_prompt while unprimed (CLI path primes once; latent).
Task 7: complete (commits cbf65c6fc..98c875998 + 02b2a4291..0fbeef1e0, review clean after 1 round)
Task 10 (early signal at 00211deeb): ruff clean; battery dir 176 green; wardline gate of record exit 1 with 6 active ERROR (PY-WL-102) ALL in src/elspeth/web/{interpretation_state,composer/redaction} — pre-existing/unrelated (no src/ change on this branch; 0 findings under evals/), "evaluated unsuppressed". Not ours; report to John.
Task 9: review — Needs fixes: 1 Important: the "~1.4 s worst-case rejection" figure in the spec errata + README is unsubstantiated in-tree (it came from the 3b reviewer's ad-hoc measurement relayed by the controller). Ruling: FIX by measuring reproducibly and recording the method+numbers (or soften) — an errata block must not carry an unsourced number.
Task 9: fix round 1/5 landed cac244853 (timing measured: match ≈0.00005 s, worst-case rejection ≈1.21 s; method recorded in both docs). Re-review dispatched (sonnet).
Final whole-branch review: dispatching on the most capable model against review-final-68d508965..cac244853.diff (pathspec-scoped, 55 files, +9600) with the deferred-minor/ruling triage list.
Task 9: fix round 1/5 re-review: addressed (re-reviewer reproduced 3.9e-05 s / 1.18 s), no breakage.
Task 9: complete for Steps 1-3 (commits 0fbeef1e0..00211deeb + 00211deeb..cac244853, review clean after 1 round). Steps 4-5 (live calibration, freeze) remain OPERATOR steps — not run.
Task 10: pending the final whole-branch review's fix wave; early signals recorded above (ruff clean, battery dir 176, wardline: 6 pre-existing src/ ERRORs, none under evals/).
Task 10 (gates at cac244853):
  - Full suite (main checkout, -n 12): **41214 passed, 40 skipped, 1 xfailed, 0 failed** in 19m35s.
  - Attribution done properly: per-file collected-count diff base(68d508965 worktree) vs HEAD shows the battery's own 11 files = exactly 176 new tests (60 corpus, 29 score, 22 drive, 14 ignore, 14 topology, 10 scenario, 10 report, 9 planner, 6 capture, 1 advisor, 1 probe). The other apparent deltas (+21 convergence, +18 paths, +13 execution_repository, +9 manifest_inventory, +1 catalog) are WORKTREE ARTEFACTS, not ours: a fresh worktree lacks the git-ignored `evals/composer-rgr`, `composer-harness`, … assets those suites glob (verified by listing evals/ in both trees). TRAP for the hints doc: a worktree A/B of the full suite under-collects every file-globbing gate because gitignored assets don't exist there.
  - ruff check + ruff format --check on evals/lib, evals/composer-battery, tests/unit/evals/composer_battery: clean.
  - wardline gate of record: exit 1, 6 active ERROR (PY-WL-102) all in src/elspeth/web/{interpretation_state.py, composer/redaction.py}; ZERO findings under evals/; no src/ change on this branch ⇒ pre-existing, not ours. 129 boundaries recognised (not inert).
FINAL whole-branch review (fable, 68d508965..cac244853): "Ready to merge: With fixes" — 2 Critical (C1 courteous closing flips clean; C2 capture-step 5xx leaks into the denominator / measurement bucket), 6 Important (gitignore credential mirror, tripwire containment+resume, criteria ledger invisible, load_capture non-dict rows, cleanup pagination+seam, unknown --cases silent), 13 Minor; triage of all 33 deferred minors/rulings recorded in final-review-report.md; ALL 22 controller rulings AGREED by the reviewer.
Final fix wave dispatched (opus) with .../final-fix-brief.md — one dispatch, all findings; scoped re-review to follow. Spec-level items surfaced for John: §3/§5 red-vs-clean wording, zero-row `surface` vs instrument fault, `no_calls` vs `malformed_output` conflict, `abandoned_mutation` absent from the §3 table, and the volunteered-passthrough floor risk.
Fix wave landed d6a189d17 (21 files; battery gate 208 passed; ruff clean). Scoped re-review dispatched (opus).
Full suite AFTER the fix wave (working tree incl. sibling commits + sibling WIP): 2 failed, 41117 passed, 40 skipped, 1 xfailed. BOTH failures are `tests/unit/elspeth_lints/test_masquerade_gate.py` "stale-baseline-entry" findings naming SIBLING files (tests/unit/core/landscape/test_exporter.py, web/composer/guided/test_chat_solver.py, web/auth/test_local_provider.py, … the elspeth-05cdc7f2df attribute-contract migration lane): the baseline still adjudicates getattr sites those sessions removed. ZERO findings mention evals/ or battery (grep count 0). Not ours; the fix is theirs (`python -m elspeth_lints.rules.masquerade.seed_baseline`).
Fix-wave scoped re-review (opus): ALL brief items ADDRESSED (gitignore mirror verified with git check-ignore; option-assertion pin mutation-probed; C2's terminal_missing choice judged sound; conftest append judged whole-tree safe). One residual INSIDE C2's class: `get_state` client timeout not flagged (step() sets http_unrecovered on HttpTransportError but not HttpTimeout) ⇒ the outage still scores as a product finding.
Ruling: treat that as COMPLETION of C2 (a one-line symmetry fix in a finding I dispatched), not a second fix wave — dispatched back to the same fixer with the exact fix + covering test, plus two one-line leftovers (run_ideal meta.json still carries `server_version`, which would print a spurious compare delta; fake_http comment). Everything else the re-reviewer raised is PARKED for John. Cost if wrong: one extra small round.
Parked for John (re-reviewer's out-of-scope): ArmResult built with 16 positional args (keyword form is house style); probe-side assert_pair_routes still inside the arm loop (calibration-only); plan doc still names server_version at 4 sites; a FAILING tripwire row doesn't set degraded (only a raised one does); behaviour change to note — post_message >=500 now excludes as http even when audit rows exist (moves the denominator, stays visible in Exclusions).
Fix round 2 landed 4d984c24c (self-caught: cleanup deleted WHILE paginating — OFFSET shifts under the reads; now two-phase read-all-then-delete, with a fake that actually shrinks its listing) + 42c29887d (get_state timeout ⇒ instrument fault; run_ideal meta.json server_version dropped; fake_http comment). Scoped re-review (sonnet): BOTH ADDRESSED, verified by reverting each guard in an isolated copy and watching the new tests fail for the stated reason; asymmetry audit confirmed accurate. No new breakage. Battery gate 209 passed.
Parked (re-reviewer, round 2): `resolve_review` discards its step() result entirely — no status check, no flag (pre-existing, same C2 doctrine; worth a future look).
Masquerade gate re-run after the sibling's reseed a42f41c8a: still 1 failing (`test_live_tree_has_zero_unbaselined_findings`), findings in tests/conftest.py, tests/fixtures/{catalog_reference,dag_scenario_corpus/harness,factories,telemetry}.py, tests/unit/plugins/transforms/aws/test_textract_result.py — all the sibling migration lane; ZERO in evals/ or battery files.
TRAP (for recent-code-hints): a full-suite run in the SHARED checkout is invalid if siblings commit during it. My post-round-2 run reported 456 failures across engine/pipeline/e2e (0 in evals/battery); four sibling commits landed inside its 18-minute window (b7dd59c83 freeze guards, fe12a9b48, dd69ce213, 1ece41454 fixtures migration). Re-running a representative slice (test_deaggregation + orchestrator/test_execution_loop) immediately after: 22 passed. The failures were a mutating-tree artefact, not a regression. Record HEAD before AND after a long suite run; if they differ, the run is not evidence.
FINAL GATES (Task 10) at aaa1a228c/44321eb6a:
  - `pytest tests/ -n 12`: **41120 passed, 40 skipped, 1 xfailed, 0 failed, exit 0** (HEAD moved 680935460→aaa1a228c mid-run; a green result is unaffected by that, a red one would not have been trusted). The masquerade gate is green again after the siblings' reseeds (a42f41c8a, 680935460).
  - ruff check + ruff format --check on evals/lib, evals/composer-battery, tests/unit/evals/composer_battery: clean (29 files).
  - wardline gate of record: exit 1, SAME 6 active ERROR (PY-WL-102) as before the branch, all in src/elspeth/web/{interpretation_state.py, composer/redaction.py}; ZERO findings under evals/. Pre-existing; not ours.
  - elspeth-lints trust-tier corpus: NOT re-derived (the battery adds nothing under src/, and the corpus is the known fail-closed state; a diff would measure the siblings' in-flight migration, not us).
  - recent-code-hints.md: measurement-trap entry added and committed (44321eb6a).
Task 10: complete. Plan COMPLETE (Tasks 1-9 + 10); Task 9 Steps 4-5 (live calibration + corpus freeze) remain OPERATOR steps.
