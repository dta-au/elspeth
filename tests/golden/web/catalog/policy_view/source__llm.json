{
  "composer_hints": [
    "Use this source only when the prompt needs no incoming row data; use the LLM transform for row-dependent prompts.",
    "The response field and its usage/model diagnostic fields are emitted automatically.",
    "Choose quarantine when an invalid generated row must remain inspectable; choose discard only for intentional dropping."
  ],
  "description": "Issue one authored prompt and emit at most one validated source row.",
  "json_schema": {
    "$defs": {
      "FieldDefinition": {
        "description": "Definition of a single field in a schema.\n\nAttributes:\n    name: Field name (must be valid Python identifier)\n    field_type: One of: str, int, float, bool, any\n    required: If False, field may be absent from the row\n    nullable: If True, field value may be None when present. Used by\n        coalesce merge logic to track whether a branch can produce None\n        values for this field (affecting collision policy semantics).",
        "properties": {
          "field_type": {
            "enum": [
              "str",
              "int",
              "float",
              "bool",
              "any"
            ],
            "title": "Field Type",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "nullable": {
            "default": false,
            "title": "Nullable",
            "type": "boolean"
          },
          "required": {
            "default": true,
            "title": "Required",
            "type": "boolean"
          }
        },
        "required": [
          "name",
          "field_type"
        ],
        "title": "FieldDefinition",
        "type": "object"
      },
      "OutputFieldConfig": {
        "additionalProperties": false,
        "description": "Configuration for a single output field in the LLM response.\n\nAttributes:\n    suffix: Column suffix in output row (e.g., \"score\" -> \"{prefix}_score\")\n    type: Data type for schema enforcement\n    values: Required for enum type - list of allowed values",
        "properties": {
          "schema": {
            "anyOf": [
              {
                "$ref": "#/$defs/SchemaConfig"
              },
              {
                "type": "null"
              }
            ],
            "composer_hidden": true,
            "default": null,
            "description": "Schema configuration for data validation.",
            "title": "Schema"
          },
          "suffix": {
            "description": "Column suffix in output row",
            "title": "Suffix",
            "type": "string"
          },
          "type": {
            "$ref": "#/$defs/OutputFieldType",
            "description": "Data type for schema enforcement",
            "title": "Type"
          },
          "values": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "description": "Allowed values (required for enum type)",
            "title": "Values"
          }
        },
        "required": [
          "suffix",
          "type"
        ],
        "title": "OutputFieldConfig",
        "type": "object"
      },
      "OutputFieldType": {
        "description": "Supported types for structured output fields.",
        "enum": [
          "string",
          "integer",
          "number",
          "boolean",
          "enum"
        ],
        "title": "OutputFieldType",
        "type": "string"
      },
      "ResponseFormat": {
        "description": "LLM response format modes.\n\n- STANDARD: Uses {\"type\": \"json_object\"} - model outputs JSON but no schema enforcement\n- STRUCTURED: Uses {\"type\": \"json_schema\", ...} - API enforces exact schema compliance",
        "enum": [
          "standard",
          "structured"
        ],
        "title": "ResponseFormat",
        "type": "string"
      },
      "SchemaConfig": {
        "description": "Configuration for a plugin's data schema.\n\nA schema can be either observed (accept anything, infer types from data)\nor explicit (validate against specified fields).\n\nSchema Contracts (for DAG validation):\n    - guaranteed_fields: Fields the producer GUARANTEES will exist AND are\n      part of the stable API contract. Downstream can safely depend on these.\n    - required_fields: Fields the consumer REQUIRES in input.\n    - audit_fields: Fields that exist in output but are NOT part of the\n      stability contract. These are for audit trail reconstruction and may\n      change between versions. DAG validation does NOT enforce these.\n\nFor explicit schemas (mode='fixed' or 'flexible'), the declared fields are\nimplicitly guaranteed. Use guaranteed_fields/required_fields to express\ncontracts for observed schemas that still have known field requirements.\n\nExample YAML for an observed schema with explicit contracts:\n    schema:\n      mode: observed\n      guaranteed_fields: [customer_id, timestamp]  # Producer guarantees these\n\n    schema:\n      mode: observed\n      required_fields: [customer_id, amount]  # Consumer requires these\n\n    schema:\n      mode: observed\n      guaranteed_fields: [response, response_usage]  # Stable API\n      audit_fields: [response_template_hash]  # May change between versions\n\nAttributes:\n    mode: \"fixed\" (exact fields), \"flexible\" (at least these), or \"observed\" (infer from data)\n    fields: List of FieldDefinitions, or None if observed\n    is_observed: True if schema types are inferred from data\n    guaranteed_fields: Field names the producer guarantees (for observed schemas)\n    required_fields: Field names the consumer requires (for observed schemas)\n    audit_fields: Field names that exist but are not part of stability contract",
        "properties": {
          "audit_fields": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Audit Fields"
          },
          "fields": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/$defs/FieldDefinition"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Fields"
          },
          "guaranteed_fields": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Guaranteed Fields"
          },
          "mode": {
            "enum": [
              "fixed",
              "flexible",
              "observed"
            ],
            "title": "Mode",
            "type": "string"
          },
          "required_fields": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "default": null,
            "title": "Required Fields"
          }
        },
        "required": [
          "mode"
        ],
        "title": "SchemaConfig",
        "type": "object"
      }
    },
    "additionalProperties": false,
    "properties": {
      "lookup": {
        "anyOf": [
          {
            "additionalProperties": true,
            "type": "object"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Lookup data loaded from YAML file",
        "title": "Lookup"
      },
      "on_validation_failure": {
        "description": "Sink name for non-conformant output, or 'discard' for explicit drop",
        "title": "On Validation Failure",
        "type": "string"
      },
      "output_fields": {
        "anyOf": [
          {
            "items": {
              "$ref": "#/$defs/OutputFieldConfig"
            },
            "minItems": 1,
            "type": "array"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Typed structured-output field definitions (None = unstructured response)",
        "title": "Output Fields"
      },
      "profile": {
        "description": "Operator-approved LLM profile alias",
        "enum": [
          "tutorial"
        ],
        "type": "string"
      },
      "prompt_template": {
        "description": "Static Jinja2 prompt template",
        "title": "Prompt Template",
        "type": "string"
      },
      "response_field": {
        "default": "llm_response",
        "description": "Field name for LLM response in output",
        "title": "Response Field",
        "type": "string"
      },
      "response_format": {
        "$ref": "#/$defs/ResponseFormat",
        "default": "standard",
        "description": "Response format mode (standard JSON object vs. enforced json_schema)",
        "title": "Response Format"
      },
      "schema": {
        "$ref": "#/$defs/SchemaConfig",
        "composer_description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "composer_placeholder": "{\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"note: str?\"]}",
        "description": "Schema configuration for data validation. Use 'schema: {mode: observed}' to infer types from data, or provide explicit field definitions with mode (fixed/flexible).",
        "title": "Schema"
      },
      "system_prompt": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "null"
          }
        ],
        "default": null,
        "description": "Optional system prompt",
        "title": "System Prompt"
      },
      "temperature": {
        "default": 0.0,
        "description": "Sampling temperature",
        "maximum": 2.0,
        "minimum": 0.0,
        "title": "Temperature",
        "type": "number"
      }
    },
    "required": [
      "profile",
      "schema",
      "prompt_template",
      "on_validation_failure"
    ],
    "type": "object"
  },
  "knob_schema": {
    "fields": [
      {
        "description": "Operator-approved LLM profile alias",
        "enum": [
          "tutorial"
        ],
        "kind": "enum",
        "label": "profile",
        "name": "profile",
        "nullable": false,
        "required": true
      },
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "placeholder": "{\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"note: str?\"]}",
        "required": true
      },
      {
        "description": "Static Jinja2 prompt template",
        "kind": "text",
        "label": "Prompt Template",
        "name": "prompt_template",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Optional system prompt",
        "kind": "text",
        "label": "System Prompt",
        "name": "system_prompt",
        "nullable": true,
        "required": false
      },
      {
        "default": 0.0,
        "description": "Sampling temperature",
        "kind": "number-float",
        "label": "Temperature",
        "name": "temperature",
        "nullable": false,
        "required": false
      },
      {
        "default": "llm_response",
        "description": "Field name for LLM response in output",
        "kind": "text",
        "label": "Response Field",
        "name": "response_field",
        "nullable": false,
        "required": false
      },
      {
        "default": "standard",
        "description": "Response format mode (standard JSON object vs. enforced json_schema)",
        "enum": [
          "standard",
          "structured"
        ],
        "kind": "enum",
        "label": "Response Format",
        "name": "response_format",
        "nullable": false,
        "required": false
      },
      {
        "default": null,
        "description": "Typed structured-output field definitions (None = unstructured response)",
        "kind": "json-array",
        "label": "Output Fields",
        "name": "output_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Sink name for non-conformant output, or 'discard' for explicit drop",
        "kind": "text",
        "label": "On Validation Failure",
        "name": "on_validation_failure",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Lookup data loaded from YAML file",
        "kind": "json-object",
        "label": "Lookup",
        "name": "lookup",
        "nullable": true,
        "required": false
      }
    ]
  },
  "name": "llm",
  "plugin_type": "source",
  "policy_capabilities": [
    {
      "blocks_positive_detection": false,
      "capability": "llm",
      "control_role": null
    }
  ],
  "secret_requirements": [],
  "web_config_authority": "operator_profiled"
}
