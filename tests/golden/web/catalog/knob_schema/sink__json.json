{
  "knob_schema": {
    "fields": [
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "placeholder": "{\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"note: str?\"]}",
        "required": true
      },
      {
        "description": "On the web surface this field takes one of exactly two values: the sentinel \"blob:<uuid>\" naming a file uploaded to this session \u2014 normally prefilled for you, so leave it as it is \u2014 or a plain path that resolves inside this session's own subtree (a source reads from <data_dir>/blobs/<session_id>/; an output writes to <data_dir>/outputs/<session_id>/ or <data_dir>/blobs/<session_id>/). Every other path is rejected, including another session's subtree, a shared or deployment-level directory, and any absolute path outside those trees. Hand-authored YAML runs are not confined this way.",
        "kind": "text",
        "label": "Path",
        "name": "path",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Optional local output collision policy. 'fail_if_exists' refuses an existing write target; 'auto_increment' picks a free sibling path; 'append_or_create' is valid with append mode.",
        "enum": [
          "fail_if_exists",
          "auto_increment",
          "append_or_create"
        ],
        "kind": "enum",
        "label": "Collision Policy",
        "name": "collision_policy",
        "nullable": true,
        "required": false
      },
      {
        "default": null,
        "description": "Header output mode: 'normalized', 'original', or {field: header} mapping",
        "kind": "json-value",
        "label": "Headers",
        "name": "headers",
        "nullable": false,
        "required": false
      },
      {
        "default": null,
        "description": "Output JSON format. When omitted, the sink auto-detects JSONL from a .jsonl filename and JSON otherwise.",
        "enum": [
          "json",
          "jsonl"
        ],
        "kind": "enum",
        "label": "Format",
        "name": "format",
        "nullable": true,
        "required": false
      },
      {
        "default": null,
        "description": "Indentation level for JSON array output; null writes compact JSON.",
        "kind": "number-int",
        "label": "Indent",
        "name": "indent",
        "nullable": true,
        "required": false
      },
      {
        "default": "utf-8",
        "description": "Text encoding used when writing JSON output.",
        "kind": "text",
        "label": "Encoding",
        "name": "encoding",
        "nullable": false,
        "required": false
      },
      {
        "default": "write",
        "description": "Whether to create/replace the JSON output file or append JSONL rows.",
        "enum": [
          "write",
          "append"
        ],
        "kind": "enum",
        "label": "Mode",
        "name": "mode",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "sink",
  "plugin_name": "json"
}
