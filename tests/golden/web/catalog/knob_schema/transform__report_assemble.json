{
  "knob_schema": {
    "fields": [
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "Required Input Fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Name of the string field to collate",
        "kind": "text",
        "label": "Text Field",
        "name": "text_field",
        "nullable": false,
        "required": true
      },
      {
        "default": "report_body",
        "description": "Output field for assembled text",
        "kind": "text",
        "label": "Output Field",
        "name": "output_field",
        "nullable": false,
        "required": false
      },
      {
        "default": "plain_text",
        "description": "Output rendering format: 'plain_text' for newline-joined text, 'markdown' for # title prefix, or 'html_fragment' for <h1>/<p> block elements (escaped).",
        "enum": [
          "plain_text",
          "markdown",
          "html_fragment"
        ],
        "kind": "enum",
        "label": "Format",
        "name": "format",
        "nullable": false,
        "required": false
      },
      {
        "default": "\n",
        "description": "Separator used between input rows",
        "kind": "text",
        "label": "Join With",
        "name": "join_with",
        "nullable": false,
        "required": false
      },
      {
        "default": null,
        "description": "Optional report title",
        "kind": "text",
        "label": "Title",
        "name": "title",
        "nullable": true,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "report_assemble"
}
