{
  "knob_schema": {
    "fields": [
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "Required Input Fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Name of the string field to split into lines",
        "kind": "text",
        "label": "Source Field",
        "name": "source_field",
        "nullable": false,
        "required": true
      },
      {
        "default": "line",
        "description": "Name for each emitted line",
        "kind": "text",
        "label": "Output Field",
        "name": "output_field",
        "nullable": false,
        "required": false
      },
      {
        "default": true,
        "description": "Whether to include a line index field",
        "kind": "checkbox",
        "label": "Include Index",
        "name": "include_index",
        "nullable": false,
        "required": false
      },
      {
        "default": "line_index",
        "description": "Name for the emitted line index",
        "kind": "text",
        "label": "Index Field",
        "name": "index_field",
        "nullable": false,
        "required": false
      },
      {
        "default": 10000,
        "description": "Maximum number of output lines this transform may emit for one input row",
        "kind": "number-int",
        "label": "Max Lines",
        "name": "max_lines",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "line_explode"
}
