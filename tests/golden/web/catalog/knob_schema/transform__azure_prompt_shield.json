{
  "knob_schema": {
    "fields": [
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "Required Input Fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "description": "Azure Content Safety endpoint URL",
        "kind": "text",
        "label": "Endpoint",
        "name": "endpoint",
        "nullable": false,
        "required": true
      },
      {
        "description": "Azure Content Safety API key",
        "kind": "text",
        "label": "API Key",
        "name": "api_key",
        "nullable": false,
        "required": true
      },
      {
        "description": "Field name(s) to analyze, or 'all' for all string fields",
        "kind": "json-value",
        "label": "Fields",
        "name": "fields",
        "nullable": false,
        "required": true
      },
      {
        "default": 3600,
        "description": "Max seconds to retry capacity errors",
        "kind": "number-int",
        "label": "Max Capacity Retry Seconds",
        "name": "max_capacity_retry_seconds",
        "nullable": false,
        "required": false
      },
      {
        "default": 3600,
        "description": "Max seconds to wait for a batch row result",
        "kind": "number-int",
        "label": "Batch Wait Timeout Seconds",
        "name": "batch_wait_timeout_seconds",
        "nullable": false,
        "required": false
      },
      {
        "default": "both",
        "description": "Which analysis to run: 'both', 'user_prompt', or 'document'",
        "kind": "text",
        "label": "Analysis Type",
        "name": "analysis_type",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "azure_prompt_shield"
}
