{
  "knob_schema": {
    "fields": [
      {
        "description": "Enter one JSON object. Worked example: {\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"page_count: int\", \"note: str?\"]}. Every entry in \"fields\" is the string \"name: type\", where type is one of str, int, float, bool, any, and a trailing \"?\" marks the field optional (\"note: str?\" is an optional string). Field names must be valid Python identifiers \u2014 write \"doc_id\", not \"doc-id\" or \"doc.id\". Modes: \"fixed\" accepts exactly these fields, \"flexible\" accepts these plus any extras the data carries, and \"observed\" infers every field from the data \u2014 with \"observed\" leave \"fields\" out entirely ({\"mode\": \"observed\"}).",
        "kind": "json-value",
        "label": "Schema",
        "name": "schema",
        "nullable": false,
        "placeholder": "{\"mode\": \"fixed\", \"fields\": [\"doc_id: str\", \"note: str?\"]}",
        "required": true
      },
      {
        "default": null,
        "description": "Fields this transform requires in input. Used for DAG validation to catch missing field errors at config time. For templates, use elspeth.core.templates.extract_jinja2_fields() to discover fields.",
        "item_kind": "text",
        "kind": "string-list",
        "label": "Required Input Fields",
        "name": "required_input_fields",
        "nullable": true,
        "required": false
      },
      {
        "default": "blob_ref",
        "description": "Input field containing a payload-store content hash.",
        "kind": "text",
        "label": "Blob Ref Field",
        "name": "blob_ref_field",
        "nullable": false,
        "required": false
      },
      {
        "default": "line",
        "description": "Output field receiving each emitted line or chunk.",
        "kind": "text",
        "label": "Output Field",
        "name": "output_field",
        "nullable": false,
        "required": false
      },
      {
        "default": true,
        "description": "Whether to emit the chunk's zero-based position within the blob.",
        "kind": "checkbox",
        "label": "Include Index",
        "name": "include_index",
        "nullable": false,
        "required": false
      },
      {
        "default": "line_index",
        "description": "Output field receiving the chunk's zero-based position within the blob.",
        "kind": "text",
        "label": "Index Field",
        "name": "index_field",
        "nullable": false,
        "required": false
      },
      {
        "default": null,
        "description": "Literal separator to split on. Omitted (the default) splits on CRLF, CR, or LF line endings.",
        "kind": "text",
        "label": "Delimiter",
        "name": "delimiter",
        "nullable": true,
        "required": false
      },
      {
        "default": "utf-8",
        "description": "Encoding used to decode the text blob. Decoding is strict.",
        "kind": "text",
        "label": "Encoding",
        "name": "encoding",
        "nullable": false,
        "required": false
      },
      {
        "default": false,
        "description": "Whether to drop empty chunks instead of emitting a row for each.",
        "kind": "checkbox",
        "label": "Skip Blank Lines",
        "name": "skip_blank_lines",
        "nullable": false,
        "required": false
      },
      {
        "default": 100000,
        "description": "Maximum chunks read from one input blob.",
        "kind": "number-int",
        "label": "Max Output Rows",
        "name": "max_output_rows",
        "nullable": false,
        "required": false
      },
      {
        "default": 104857600,
        "description": "Maximum payload size accepted from the blob store.",
        "kind": "number-int",
        "label": "Max Blob Bytes",
        "name": "max_blob_bytes",
        "nullable": false,
        "required": false
      }
    ]
  },
  "plugin_kind": "transform",
  "plugin_name": "blob_text_expand"
}
